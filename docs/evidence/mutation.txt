command: node node_modules/@stryker-mutator/core/bin/stryker.js run
exit code: 0
run: 2026-09-11T16:50:32.2786199-05:00
node: v24.18.0

[32m16:48:48 (22604) INFO ProjectReader[39m Found 54 of 340 file(s) to be mutated.
[32m16:48:49 (22604) INFO Instrumenter[39m Instrumented 54 source file(s) with 2707 mutant(s)
[32m16:48:49 (22604) INFO ConcurrencyTokenProvider[39m Creating 4 test runner process(es).
[32m16:48:50 (22604) INFO BroadcastReporter[39m Detected that current console does not support the "progress" reporter, downgrading to "progress-append-only" reporter
[32m16:48:50 (22604) INFO DryRunExecutor[39m Starting initial test run (vitest test runner with "perTest" coverage analysis). This may take a while.
[32m16:48:57 (22604) INFO DryRunExecutor[39m Initial test run succeeded. Ran 226 tests in 6 seconds (net 3543.403700000003 ms, overhead 2920.596299999997 ms).
Mutation testing 0% (elapsed: <1m, remaining: ~17m) 1945/2691 tested (58 survived, 0 timed out)
Mutation testing 3% (elapsed: <1m, remaining: ~9m) 2163/2691 tested (81 survived, 5 timed out)
Mutation testing 4% (elapsed: <1m, remaining: ~10m) 2283/2691 tested (124 survived, 5 timed out)
Mutation testing 4% (elapsed: <1m, remaining: ~13m) 2405/2691 tested (156 survived, 5 timed out)
Mutation testing 5% (elapsed: <1m, remaining: ~15m) 2529/2691 tested (183 survived, 5 timed out)
Mutation testing 31% (elapsed: ~1m, remaining: ~2m) 2576/2691 tested (203 survived, 5 timed out)
Mutation testing 55% (elapsed: ~1m, remaining: <1m) 2600/2691 tested (211 survived, 5 timed out)
Mutation testing 80% (elapsed: ~1m, remaining: <1m) 2621/2691 tested (218 survived, 5 timed out)
Mutation testing 98% (elapsed: ~1m, remaining: <1m) 2662/2691 tested (218 survived, 5 timed out)

All tests
  adapters
    sqlite-memory-repository.test.ts
      ✓ SqliteMemoryRepository creates and retrieves a memory by id (killed 8)
      ~ SqliteMemoryRepository returns undefined for missing id (covered 2)
      ✓ SqliteMemoryRepository updates weight and access count (killed 3)
      ✓ SqliteMemoryRepository deletes a memory (killed 3)
      ✓ SqliteMemoryRepository recalls by keyword match (killed 26)
      ✓ SqliteMemoryRepository finds memories for decay (non-core, old) (killed 3)
      ~ SqliteMemoryRepository does not include core memories in decay candidates (covered 17)
      ✓ SqliteMemoryRepository finds memories for tier promotion (killed 2)
      ✓ SqliteMemoryRepository counts memories by tier (killed 19)
      ~ SqliteMemoryRepository stores and serialises tags correctly (covered 26)
      ✓ SqliteMemoryRepository round-trips project and category (killed 4)
    sqlite-team-repository-search.test.ts
      ✓ SqliteTeamRepository.searchSharedCache finds a memory by words that are not a contiguous phrase (killed 15)
      ✓ SqliteTeamRepository.searchSharedCache still finds a memory by an exact phrase (killed 1)
      ~ SqliteTeamRepository.searchSharedCache matches on a single word (covered 52)
      ~ SqliteTeamRepository.searchSharedCache matches case-insensitively (covered 52)
      ✓ SqliteTeamRepository.searchSharedCache matches on tags as well as content (killed 1)
      ~ SqliteTeamRepository.searchSharedCache ORs across words: one matching word is enough (covered 52)
      ✓ SqliteTeamRepository.searchSharedCache returns nothing when no word matches (killed 4)
      ✓ SqliteTeamRepository.searchSharedCache returns the whole team pool for an empty query (killed 2)
      ~ SqliteTeamRepository.searchSharedCache never leaks another team’s memories (covered 52)
      ✓ SqliteTeamRepository.searchSharedCache applies the project filter on top of the word match (killed 5)
      ~ SqliteTeamRepository.searchSharedCache honours the limit (covered 52)
      ~ SqliteTeamRepository.searchSharedCache orders by recency, which is the only ranking it has (covered 52)
      ~ SqliteTeamRepository.searchSharedCache round-trips tags through their JSON column (covered 52)
      ~ SqliteTeamRepository.searchSharedCache accepts a Date-shaped timestamp without refusing the bind (covered 37)
    sqlite-team-repository.test.ts
      ~ SqliteTeamRepository shared cache upserts then finds a shared memory by content query (covered 33)
      ~ SqliteTeamRepository shared cache upsert is idempotent on id — replaces, never duplicates (covered 24)
      ~ SqliteTeamRepository shared cache empty query matches every entry for the team (covered 24)
      ~ SqliteTeamRepository shared cache filters by project when provided (covered 26)
      ~ SqliteTeamRepository shared cache does not leak another team's memories (covered 24)
      ~ SqliteTeamRepository shared cache honors the limit boundary (covered 24)
      ✓ SqliteTeamRepository insights cache returns project-scoped and team-wide (null project) insights together (killed 10)
      ~ SqliteTeamRepository insights cache orders by confidence descending (covered 11)
      ✓ SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed (killed 14)
      ✓ SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op (killed 1)
      ✓ SqliteTeamRepository prompt log buffer counts logs by outcome and category for a user (killed 6)
      ✓ SqliteTeamRepository sync cursor returns undefined before any pull, then the set value (killed 7)
      ~ SqliteTeamRepository sync cursor overwrites the watermark on repeated set (covered 7)
  domain
    memory.test.ts
      ✓ createMemory creates a memory with correct defaults (killed 5)
      ✓ createMemory applies confirmed boost to initial weight (killed 1)
      ✓ createMemory confirmed:true overrides decay to 0 and promotes to core regardless of type (killed 1)
      ~ createMemory insight memories default to core tier and zero decay (covered 9)
      ~ createMemory procedural memories default to core tier and zero decay (covered 9)
      ~ createMemory architectural memories default to core tier and zero decay (covered 9)
      ~ createMemory freezes the tags array (covered 8)
      ✓ reinforceMemory increases weight with asymptotic ceiling formula (killed 7)
      ~ reinforceMemory never exceeds 1.0 (covered 16)
      ~ reinforceMemory returns a new object (immutable) (covered 16)
      ✓ decayMemory applies exponential decay formula (killed 7)
      ✓ decayMemory does not decay memories with decayRate 0 (killed 2)
      ✓ promoteMemory changes tier and returns new object (killed 2)
      ✓ attachEmbedding attaches embedding vector (killed 2)
    session.test.ts
      ✓ startSession creates active session with correct defaults (killed 3)
      ~ startSession stores device when provided (covered 6)
      ✓ endSession transitions status to ended and records timestamp (killed 1)
      ~ endSession works without summary (covered 9)
      ✓ markSessionCrashed transitions status to crashed (killed 3)
      ✓ updateSessionContext updates active tasks (killed 4)
      ✓ updateSessionContext preserves unchanged fields (killed 1)
  infrastructure
    database-concurrency.test.ts
      ✓ database concurrency pragmas switches a file database off the default rollback journal and onto WAL (killed 1)
      ~ database concurrency pragmas sets busy_timeout explicitly rather than inheriting the driver default (covered 5)
      ~ database concurrency pragmas relaxes synchronous to NORMAL, which is safe under WAL (covered 5)
      ~ database concurrency pragmas keeps foreign keys enforced on every connection, not just the first (covered 5)
      ~ database concurrency pragmas lets a second connection read a row the first one committed (covered 5)
      ~ database concurrency pragmas lets a second connection write while the first holds an open read (covered 5)
      ~ database concurrency pragmas applies the schema idempotently, so every instance may run it at startup (covered 5)
    fastembed-gateway.test.ts
      ✓ FastEmbedGateway.cosineSimilarity scores identical vectors as 1 (killed 19)
      ~ FastEmbedGateway.cosineSimilarity scores orthogonal vectors as 0 (covered 28)
      ~ FastEmbedGateway.cosineSimilarity scores opposite vectors as -1 (covered 28)
      ✓ FastEmbedGateway.cosineSimilarity returns 0 for mismatched lengths (killed 3)
      ✓ FastEmbedGateway.cosineSimilarity returns 0 when either vector is empty or all-zero (killed 3)
    local-migration.test.ts
      ✓ migrateLocalSchema adds a missing column to a table that already exists (killed 13)
      ~ migrateLocalSchema defaults existing rows rather than leaving them invalid (covered 20)
      ✓ migrateLocalSchema is idempotent — a second run adds nothing and does not throw (killed 5)
      ~ migrateLocalSchema adds nothing to a database already created from the current schema (covered 18)
      ✓ migrateLocalSchema does not fail on an empty database with no tables at all (killed 1)
      ~ migrateLocalSchema leaves the data in the migrated table intact (covered 20)
      ~ migrateLocalSchema startup order: tables, then migrations, then indexes creating the indexes BEFORE the migration is what broke the server (covered 5)
      ~ migrateLocalSchema startup order: tables, then migrations, then indexes the correct order opens an old database cleanly (covered 20)
      ~ migrateLocalSchema startup order: tables, then migrations, then indexes the correct order also works on a brand new database (covered 18)
  services
    coordination-service.test.ts
      ✓ CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks (killed 26)
      ✓ CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path (killed 2)
      ~ CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other (covered 59)
      ✓ CoordinationService priority ranking: count of transitive dependents terminates on a dependency cycle instead of hanging or throwing (killed 1)
      ~ CoordinationService priority ranking: count of transitive dependents gives every package rank 0 when nothing depends on anything (covered 49)
      ✓ CoordinationService decomposition starts a package with dependencies as blocked and one without as pending (killed 6)
      ✓ CoordinationService decomposition resolves dependsOn titles into the generated package ids (killed 1)
      ✓ CoordinationService decomposition keeps the spec section, because unanchored work is what this layer prevents (killed 2)
      ~ CoordinationService decomposition scopes the queue to its project (covered 50)
      ✓ CoordinationService assignment returns null when there is no work, rather than throwing (killed 8)
      ✓ CoordinationService assignment returns null when work exists but no contributor matches the required role (killed 2)
      ✓ CoordinationService assignment assigns the highest-ranked unblocked package first (killed 13)
      ~ CoordinationService assignment never assigns a blocked package (covered 112)
      ✓ CoordinationService assignment marks the package active and the contributor busy together (killed 4)
      ~ CoordinationService assignment assigns to a named contributor when one is given (covered 101)
      ✓ CoordinationService branch name derivation — a contract between assignNext and resolveMerge derives feature/<contributor>/<package> for a builder (killed 15)
      ✓ CoordinationService branch name derivation — a contract between assignNext and resolveMerge derives merge/<package> for a merger, with no contributor segment (killed 5)
      ✓ CoordinationService branch name derivation — a contract between assignNext and resolveMerge derives spec/<package> for a specwright (killed 3)
      ✓ CoordinationService branch name derivation — a contract between assignNext and resolveMerge persists the branch name on the package, so the merge gate can check it (killed 1)
      ✓ CoordinationService completion frees the contributor, leaving nobody busy without an active assignment (killed 6)
      ✓ CoordinationService completion marks the package complete and stamps completedAt (killed 2)
      ~ CoordinationService completion drops the completed package out of the queue — the queue is work remaining (covered 114)
      ✓ CoordinationService completion reports which downstream packages it unblocked (killed 10)
      ~ CoordinationService completion promotes a downstream package once its only dependency completes (covered 137)
      ✓ CoordinationService completion leaves a package blocked while any dependency is still outstanding (killed 4)
      ~ CoordinationService completion promotes only the packages whose dependencies are satisfied, not every blocked one (covered 138)
      ~ CoordinationService completion lets the freed contributor take the newly unblocked package (covered 137)
      ~ CoordinationService roster registers a contributor as available (covered 13)
      ✓ CoordinationService roster updates availability (killed 2)
      ~ CoordinationService roster does not assign work to an offline contributor (covered 73)
      ~ CoordinationService roster scopes the roster to its project (covered 15)
      ✓ CoordinationService roster round-trips skills through their JSON column (killed 1)
      ✓ CoordinationService merge gating records the verdict the caller passes in, and never computes one (killed 16)
      ~ CoordinationService merge gating accepts a merge request with no verdict at all (covered 133)
      ✓ CoordinationService merge gating stamps the verdict onto the work package too, not only the request (killed 8)
      ~ CoordinationService merge gating carries the assigned branch onto the merge request (covered 133)
      ✓ CoordinationService merge gating lists a pending request, and filters by status (killed 1)
      ✓ CoordinationService merge gating records an approval by a merger (killed 10)
      ~ CoordinationService merge gating completes the work package when the merge is approved (covered 153)
      ✓ CoordinationService merge gating records a rejection with its reason (killed 7)
      ✓ CoordinationService merge gating refuses a resolution from anyone whose role is not merger (killed 3)
      ✓ CoordinationService merge gating refuses a resolution from an unknown reviewer (killed 2)
      ~ CoordinationService merge gating refuses to resolve a merge request that does not exist (covered 6)
      ~ CoordinationService status reports an empty project without throwing (covered 7)
      ✓ CoordinationService status reflects work and roster counts (killed 2)
    memory-service.test.ts
      ✓ MemoryService.remember creates and stores a memory, returns it (killed 3)
      ✓ MemoryService.recall returns matching memories (killed 2)
      ~ MemoryService.recall returns empty array when no match (covered 2)
      ✓ MemoryService.forget removes memory from store (killed 1)
      ✓ MemoryService.reinforce boosts weight correctly (killed 3)
      ~ MemoryService.reinforce throws when memory not found (covered 5)
      ✓ MemoryService.applyDecay decays old non-core memories (killed 2)
      ~ MemoryService.applyDecay does not decay core memories (covered 15)
      ✓ MemoryService.evaluateTierPromotions promotes buffer memories with sufficient access count (killed 5)
    pattern-service.test.ts
      ✓ PatternService.myStats computes good rate from buffered prompt logs (killed 15)
      ✓ PatternService.myStats returns a zero good rate when nothing is logged (killed 2)
      ✓ PatternService.teamStats aggregates contributions per member from the shared pool (killed 12)
      ✓ PatternService.teamStats derives top categories from prompt_category patterns (killed 15)
    prompt-log-service.test.ts
      ✓ PromptLogService buffers a logged prompt as pending (killed 6)
      ~ PromptLogService does not persist raw content unless the user opts in (covered 21)
      ✓ PromptLogService throws when teamId is not configured (killed 3)
      ✓ PromptLogService pushBuffer is a no-op (returns 0) without railwayUrl (killed 4)
    session-service.test.ts
      ✓ SessionService starts a session that is immediately the active one for its project (killed 14)
      ✓ SessionService keeps sessions for different projects separate (killed 1)
      ~ SessionService returns null for a project that has never had a session (covered 4)
      ✓ SessionService ends a session by id and records the summary (killed 8)
      ✓ SessionService stops reporting an ended session as the project active one (killed 2)
      ✓ SessionService throws NotFoundError rather than failing silently for an unknown id (killed 2)
      ~ SessionService throws NotFoundError for an empty id — the shape the MCP handler used to pass (covered 13)
      ✓ SessionService recovers an active session by id (killed 2)
      ~ SessionService returns null, not an error, when recovering an unknown id (covered 4)
      ~ SessionService regression — ending by project (the flow the MCP handler documents) resolves the active session from the project, which is how end must work (covered 40)
      ~ SessionService regression — ending by project (the flow the MCP handler documents) leaves nothing to resolve once the project session is ended, so a second end is refused (covered 40)
    sync.test.ts
      ✓ cloud sync unconfigured is a normal state skips syncMemories with no railwayUrl, and does not throw (killed 5)
      ✓ cloud sync unconfigured is a normal state skips syncInsights with no railwayUrl (killed 5)
      ✓ cloud sync unconfigured is a normal state skips syncCoordination with no railwayUrl (killed 3)
      ✓ cloud sync unconfigured is a normal state skips syncCoordination when railwayUrl is set but teamId is not (killed 2)
      ~ cloud sync unconfigured is a normal state sends nothing at all while unconfigured (covered 18)
      ~ cloud sync unconfigured is a normal state leaves local rows untouched while unconfigured (covered 6)
      ✓ cloud sync what is eligible to push pushes working and core memories but never buffer ones (killed 11)
      ✓ cloud sync what is eligible to push marks pushed rows as synced (killed 1)
      ~ cloud sync what is eligible to push reports zero pushed when there is nothing eligible (covered 19)
      ~ cloud sync the sync cursor creates a cursor for the device on the first run (covered 19)
      ~ cloud sync the sync cursor keeps exactly one cursor row per device across repeated runs (covered 19)
      ~ cloud sync the sync cursor advances the version on each pass, so the cursor is observably moving (covered 19)
      ~ cloud sync the sync cursor uses an epoch floor on the first run, so nothing is silently skipped (covered 26)
      ~ cloud sync the SyncResult contract always reports conflicts as 0 — it is not a signal (covered 26)
      ~ cloud sync the SyncResult contract returns every field of SyncResult on both the skipped and the active path (covered 21)
    team-promotion-service.test.ts
      ✓ tokenize lowercases and drops tokens shorter than three characters (killed 9)
      ~ tokenize returns an empty set for punctuation-only input (covered 10)
      ✓ contentSimilarity scores identical content as 1 (killed 11)
      ✓ contentSimilarity scores fully disjoint content as 0 (killed 1)
      ~ contentSimilarity returns 0 when either side has no usable tokens (covered 16)
      ~ contentSimilarity computes partial overlap as Jaccard (covered 27)
      ✓ TeamPromotionService.promote promotes a novel high-value memory to the team pool (killed 18)
      ~ TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates (covered 44)
      ✓ TeamPromotionService.promote skips a memory already present in the team pool by id (killed 13)
      ✓ TeamPromotionService.promote skips a near-duplicate of existing team knowledge (killed 12)
      ✓ TeamPromotionService.promote reports railwaySkipped and pushes nothing when railwayUrl is absent (killed 3)
      ✘ TeamPromotionService.promote exposes a threshold in the documented [0,1] range (covered 0)
      ✓ TeamPromotionService.promote (semantic mode) reports semantic method when an embedding gateway is available (killed 13)
      ✓ TeamPromotionService.promote (semantic mode) skips a semantic near-duplicate even when wording differs (killed 8)
      ✓ TeamPromotionService.promote (semantic mode) promotes a semantically distinct memory (killed 1)
    team-service.test.ts
      ✓ TeamService (offline / no railwayUrl) listMembers returns an empty array (killed 4)
      ✓ TeamService (offline / no railwayUrl) getTeam returns undefined (killed 2)
      ✓ TeamService (offline / no railwayUrl) getMemberRole returns undefined (killed 2)
      ✓ TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error (killed 5)
      ✓ TeamService (offline / no railwayUrl) assignRole throws a clear configuration error (killed 4)
      ✓ TeamService (offline / no railwayUrl) mintToken throws a clear configuration error (killed 4)
      ✓ canCurate permits owners and leads (killed 1)
      ~ canCurate denies plain members (covered 1)
  shared
    repo-identity.test.ts
      ✓ normalizeRemote normalises the https form, dropping the .git suffix (killed 17)
      ✓ normalizeRemote normalises the scp-like ssh form to the same id as https (killed 5)
      ✓ normalizeRemote normalises the ssh:// form (killed 3)
      ~ normalizeRemote lowercases, so a capitalised clone matches its lowercase remote (covered 50)
      ~ normalizeRemote strips credentials embedded in the URL (covered 50)
      ~ normalizeRemote keeps a non-GitHub host, because identity is host-qualified (covered 50)
      ✓ normalizeRemote tolerates a trailing slash (killed 2)
      ✓ normalizeRemote rejects input that is not a remote identity (killed 10)
      ~ normalizeRemote rejects a local filesystem path used as a remote (covered 45)
      ✓ resolveProjectIdentity prefers the normalised remote (killed 15)
      ✓ resolveProjectIdentity falls back to the root commit when there is no remote (killed 13)
      ✓ resolveProjectIdentity falls back to the directory name outside a git repository (killed 8)
      ~ resolveProjectIdentity prefixes the unstable fallbacks, so a reader can see them (covered 44)
      ~ resolveProjectIdentity resolves the same id from a subdirectory of the repository (covered 76)
      ~ resolveProjectIdentity gives two different repositories different ids (covered 76)
      ~ resolveProjectIdentity gives the same id to two clones in differently-named directories (covered 76)
      ✓ resolveProjectIdentity reports the repository root, not the subdirectory it was called from (killed 3)
      ~ resolveProjectIdentity never throws, whatever the directory (covered 43)
      ✓ resolveProject lets an explicit project win over detection (killed 4)
      ✓ resolveProject trims an explicit project (killed 1)
      ✓ resolveProject falls through to detection for an empty or whitespace value (killed 4)
    time.test.ts
      ✓ toIsoString converts the Date a Postgres driver returns into an ISO string (killed 2)
      ✓ toIsoString leaves an already-ISO string at the same instant (killed 5)
      ~ toIsoString normalises a non-ISO date string into the comparable form (covered 19)
      ✓ toIsoString accepts epoch milliseconds (killed 4)
      ~ toIsoString falls back rather than throwing on an invalid Date (covered 4)
      ✓ toIsoString falls back rather than throwing on unparseable text (killed 1)
      ✓ toIsoString falls back for null, undefined and empty string (killed 2)
      ✓ toIsoString defaults the fallback to now, so a row always carries a usable timestamp (killed 1)
      ~ toIsoString produces output that sorts chronologically as a plain string (covered 4)
      ✓ toTagsJson serialises the array a Postgres text[] column returns (killed 2)
      ✓ toTagsJson passes an already-serialised JSON array through unchanged (killed 5)
      ✓ toTagsJson returns an empty array for null, undefined and a non-array value (killed 5)
      ~ toTagsJson coerces non-string array members rather than emitting them raw (covered 3)
      ~ toTagsJson always returns something SQLite can bind (covered 13)

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:34:22
-     if (row.embedding) {
-       const floats = new Float32Array(row.embedding.buffer, row.embedding.byteOffset, row.embedding.byteLength / 4);
-       embedding = Array.from(floats);
-     }
+     if (row.embedding) {}

[NoCoverage] ArithmeticOperator
src/adapters/repositories/sqlite-memory-repository.ts:35:85
-       const floats = new Float32Array(row.embedding.buffer, row.embedding.byteOffset, row.embedding.byteLength / 4);
+       const floats = new Float32Array(row.embedding.buffer, row.embedding.byteOffset, row.embedding.byteLength * 4);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:89:19
-       } catch (err) {
-         throw new StorageError('Failed to create memory', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:90:30
-         throw new StorageError('Failed to create memory', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:99:19
-       } catch (err) {
-         throw new StorageError('Failed to find memory', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:100:30
-         throw new StorageError('Failed to find memory', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:119:26
-         if (query.project) {
-           conditions.push('project = ?');
-           params.push(query.project);
-         }
+         if (query.project) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:120:25
-           conditions.push('project = ?');
+           conditions.push("");

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:124:27
-         if (query.category) {
-           conditions.push('category = ?');
-           params.push(query.category);
-         }
+         if (query.category) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:125:25
-           conditions.push('category = ?');
+           conditions.push("");

[NoCoverage] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:129:32
-         if (query.memoryTypes && query.memoryTypes.length > 0) {
+         if (query.memoryTypes && true) {

[NoCoverage] EqualityOperator
src/adapters/repositories/sqlite-memory-repository.ts:129:32
-         if (query.memoryTypes && query.memoryTypes.length > 0) {
+         if (query.memoryTypes && query.memoryTypes.length >= 0) {

[NoCoverage] EqualityOperator
src/adapters/repositories/sqlite-memory-repository.ts:129:32
-         if (query.memoryTypes && query.memoryTypes.length > 0) {
+         if (query.memoryTypes && query.memoryTypes.length <= 0) {

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:129:62
-         if (query.memoryTypes && query.memoryTypes.length > 0) {
-           const placeholders = query.memoryTypes.map(() => '?').join(', ');
-           conditions.push(`memory_type IN (${placeholders})`);
-           params.push(...query.memoryTypes);
-         }
+         if (query.memoryTypes && query.memoryTypes.length > 0) {}

[NoCoverage] ArrowFunction
src/adapters/repositories/sqlite-memory-repository.ts:130:52
-           const placeholders = query.memoryTypes.map(() => '?').join(', ');
+           const placeholders = query.memoryTypes.map(() => undefined).join(', ');

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:130:58
-           const placeholders = query.memoryTypes.map(() => '?').join(', ');
+           const placeholders = query.memoryTypes.map(() => "").join(', ');

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:130:68
-           const placeholders = query.memoryTypes.map(() => '?').join(', ');
+           const placeholders = query.memoryTypes.map(() => '?').join("");

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:131:25
-           conditions.push(`memory_type IN (${placeholders})`);
+           conditions.push(``);

[NoCoverage] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:137:27
-         if (query.scopes && query.scopes.length > 0) {
+         if (query.scopes && true) {

[NoCoverage] EqualityOperator
src/adapters/repositories/sqlite-memory-repository.ts:137:27
-         if (query.scopes && query.scopes.length > 0) {
+         if (query.scopes && query.scopes.length >= 0) {

[NoCoverage] EqualityOperator
src/adapters/repositories/sqlite-memory-repository.ts:137:27
-         if (query.scopes && query.scopes.length > 0) {
+         if (query.scopes && query.scopes.length <= 0) {

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:137:52
-         if (query.scopes && query.scopes.length > 0) {
-           const placeholders = query.scopes.map(() => '?').join(', ');
-           conditions.push(`scope IN (${placeholders})`);
-           params.push(...query.scopes);
-         }
+         if (query.scopes && query.scopes.length > 0) {}

[NoCoverage] ArrowFunction
src/adapters/repositories/sqlite-memory-repository.ts:138:47
-           const placeholders = query.scopes.map(() => '?').join(', ');
+           const placeholders = query.scopes.map(() => undefined).join(', ');

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:138:53
-           const placeholders = query.scopes.map(() => '?').join(', ');
+           const placeholders = query.scopes.map(() => "").join(', ');

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:138:63
-           const placeholders = query.scopes.map(() => '?').join(', ');
+           const placeholders = query.scopes.map(() => '?').join("");

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:139:25
-           conditions.push(`scope IN (${placeholders})`);
+           conditions.push(``);

[NoCoverage] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:143:26
-         if (query.tiers && query.tiers.length > 0) {
+         if (query.tiers && true) {

[NoCoverage] EqualityOperator
src/adapters/repositories/sqlite-memory-repository.ts:143:26
-         if (query.tiers && query.tiers.length > 0) {
+         if (query.tiers && query.tiers.length >= 0) {

[NoCoverage] EqualityOperator
src/adapters/repositories/sqlite-memory-repository.ts:143:26
-         if (query.tiers && query.tiers.length > 0) {
+         if (query.tiers && query.tiers.length <= 0) {

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:143:50
-         if (query.tiers && query.tiers.length > 0) {
-           const placeholders = query.tiers.map(() => '?').join(', ');
-           conditions.push(`tier IN (${placeholders})`);
-           params.push(...query.tiers);
-         }
+         if (query.tiers && query.tiers.length > 0) {}

[NoCoverage] ArrowFunction
src/adapters/repositories/sqlite-memory-repository.ts:144:46
-           const placeholders = query.tiers.map(() => '?').join(', ');
+           const placeholders = query.tiers.map(() => undefined).join(', ');

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:144:52
-           const placeholders = query.tiers.map(() => '?').join(', ');
+           const placeholders = query.tiers.map(() => "").join(', ');

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:144:62
-           const placeholders = query.tiers.map(() => '?').join(', ');
+           const placeholders = query.tiers.map(() => '?').join("");

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:145:25
-           conditions.push(`tier IN (${placeholders})`);
+           conditions.push(``);

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:149:83
-         const where = conditions.length > 0 ? `WHERE ${conditions.join(' AND ')}` : '';
+         const where = conditions.length > 0 ? `WHERE ${conditions.join(' AND ')}` : "Stryker was here!";

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:157:19
-       } catch (err) {
-         throw new StorageError('Failed to recall memories', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:158:30
-         throw new StorageError('Failed to recall memories', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:165:29
-         if (memory.embedding) {
-           embeddingBuffer = Buffer.from(new Float32Array(memory.embedding).buffer);
-         }
+         if (memory.embedding) {}

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:192:19
-       } catch (err) {
-         throw new StorageError('Failed to update memory', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:193:30
-         throw new StorageError('Failed to update memory', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:200:19
-       } catch (err) {
-         throw new StorageError('Failed to delete memory', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:201:30
-         throw new StorageError('Failed to delete memory', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:211:19
-       } catch (err) {
-         throw new StorageError('Failed to find memories for decay', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:212:30
-         throw new StorageError('Failed to find memories for decay', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:222:19
-       } catch (err) {
-         throw new StorageError('Failed to find memories for promotion', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:223:30
-         throw new StorageError('Failed to find memories for promotion', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:232:30
-         if (criteria?.project) {
-           conditions.push('project = ?');
-           params.push(criteria.project);
-         }
+         if (criteria?.project) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:233:25
-           conditions.push('project = ?');
+           conditions.push("");

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:236:33
-         if (criteria?.memoryType) {
-           conditions.push('memory_type = ?');
-           params.push(criteria.memoryType);
-         }
+         if (criteria?.memoryType) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:237:25
-           conditions.push('memory_type = ?');
+           conditions.push("");

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:248:19
-       } catch (err) {
-         throw new StorageError('Failed to count memories', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:249:30
-         throw new StorageError('Failed to count memories', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:253:61
-     attachEmbedding(id: MemoryId, embedding: Embedding): void {
-       try {
-         const buffer = Buffer.from(new Float32Array(embedding).buffer);
-         this.db.prepare('UPDATE memories SET embedding = ? WHERE id = ?').run(buffer, id);
-       } catch (err) {
-         throw new StorageError('Failed to attach embedding', err);
-       }
-     }
+     attachEmbedding(id: MemoryId, embedding: Embedding): void {}

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:254:9
-       try {
-         const buffer = Buffer.from(new Float32Array(embedding).buffer);
-         this.db.prepare('UPDATE memories SET embedding = ? WHERE id = ?').run(buffer, id);
-       } catch (err) {
+       try {} catch (err) {

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:256:23
-         this.db.prepare('UPDATE memories SET embedding = ? WHERE id = ?').run(buffer, id);
+         this.db.prepare("").run(buffer, id);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-memory-repository.ts:257:19
-       } catch (err) {
-         throw new StorageError('Failed to attach embedding', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:258:30
-         throw new StorageError('Failed to attach embedding', err);
+         throw new StorageError("", err);

[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:34:7
-     if (row.embedding) {
+     if (false) {
Tests ran:
    SqliteMemoryRepository creates and retrieves a memory by id
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository recalls by keyword match
  and 4 more tests!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-memory-repository.ts:51:12
-       scope: (row.scope as MemoryScope | null) ?? DEFAULT_SCOPE,
+       scope: row.scope as MemoryScope | null && DEFAULT_SCOPE,
Tests ran:
    SqliteMemoryRepository creates and retrieves a memory by id
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository recalls by keyword match
  and 4 more tests!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-memory-repository.ts:54:13
-       source: row.source ?? undefined,
+       source: row.source && undefined,
Tests ran:
    SqliteMemoryRepository creates and retrieves a memory by id
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository recalls by keyword match
  and 4 more tests!


[Survived] BooleanLiteral
src/adapters/repositories/sqlite-memory-repository.ts:56:16
-       confirmed: !!row.confirmed,
+       confirmed: !row.confirmed,
Tests ran:
    SqliteMemoryRepository creates and retrieves a memory by id
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository recalls by keyword match
  and 4 more tests!


[Survived] BooleanLiteral
src/adapters/repositories/sqlite-memory-repository.ts:56:17
-       confirmed: !!row.confirmed,
+       confirmed: !row.confirmed,
Tests ran:
    SqliteMemoryRepository creates and retrieves a memory by id
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository recalls by keyword match
  and 4 more tests!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-memory-repository.ts:85:9
-           memory.source ?? null,
+           memory.source && null,
Tests ran:
    SqliteMemoryRepository creates and retrieves a memory by id
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository deletes a memory
  and 7 more tests!


[Survived] MethodExpression
src/adapters/repositories/sqlite-memory-repository.ts:110:21
-         const words = query.query.trim().split(/\s+/).filter(Boolean);
+         const words = query.query.split(/\s+/).filter(Boolean);
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] MethodExpression
src/adapters/repositories/sqlite-memory-repository.ts:110:21
-         const words = query.query.trim().split(/\s+/).filter(Boolean);
+         const words = query.query.trim().split(/\s+/);
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] Regex
src/adapters/repositories/sqlite-memory-repository.ts:110:46
-         const words = query.query.trim().split(/\s+/).filter(Boolean);
+         const words = query.query.trim().split(/\s/).filter(Boolean);
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:111:11
-         if (words.length > 0) {
+         if (true) {
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] EqualityOperator
src/adapters/repositories/sqlite-memory-repository.ts:111:11
-         if (words.length > 0) {
+         if (words.length >= 0) {
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:113:49
-           conditions.push(`(${textConditions.join(' OR ')})`);
+           conditions.push(`(${textConditions.join("")})`);
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:115:36
-             params.push(`%${word}%`, `%${word}%`);
+             params.push(`%${word}%`, ``);
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:119:11
-         if (query.project) {
+         if (false) {
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:124:11
-         if (query.category) {
+         if (false) {
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:129:11
-         if (query.memoryTypes && query.memoryTypes.length > 0) {
+         if (false) {
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:137:11
-         if (query.scopes && query.scopes.length > 0) {
+         if (false) {
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:149:21
-         const where = conditions.length > 0 ? `WHERE ${conditions.join(' AND ')}` : '';
+         const where = true ? `WHERE ${conditions.join(' AND ')}` : '';
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:143:11
-         if (query.tiers && query.tiers.length > 0) {
+         if (false) {
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] EqualityOperator
src/adapters/repositories/sqlite-memory-repository.ts:149:21
-         const where = conditions.length > 0 ? `WHERE ${conditions.join(' AND ')}` : '';
+         const where = conditions.length >= 0 ? `WHERE ${conditions.join(' AND ')}` : '';
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:149:70
-         const where = conditions.length > 0 ? `WHERE ${conditions.join(' AND ')}` : '';
+         const where = conditions.length > 0 ? `WHERE ${conditions.join("")}` : '';
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] LogicalOperator
src/adapters/repositories/sqlite-memory-repository.ts:150:21
-         const limit = query.limit ?? 20;
+         const limit = query.limit && 20;
Tests ran:
    SqliteMemoryRepository recalls by keyword match


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:165:11
-         if (memory.embedding) {
+         if (true) {
Tests ran:
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository finds memories for decay (non-core, old)
    SqliteMemoryRepository finds memories for tier promotion


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:165:11
-         if (memory.embedding) {
+         if (false) {
Tests ran:
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository finds memories for decay (non-core, old)
    SqliteMemoryRepository finds memories for tier promotion


[Survived] LogicalOperator
src/adapters/repositories/sqlite-memory-repository.ts:183:9
-           memory.project ?? null,
+           memory.project && null,
Tests ran:
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository finds memories for decay (non-core, old)
    SqliteMemoryRepository finds memories for tier promotion


[Survived] LogicalOperator
src/adapters/repositories/sqlite-memory-repository.ts:186:9
-           memory.source ?? null,
+           memory.source && null,
Tests ran:
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository finds memories for decay (non-core, old)
    SqliteMemoryRepository finds memories for tier promotion


[Survived] LogicalOperator
src/adapters/repositories/sqlite-memory-repository.ts:184:9
-           memory.category ?? null,
+           memory.category && null,
Tests ran:
    SqliteMemoryRepository updates weight and access count
    SqliteMemoryRepository finds memories for decay (non-core, old)
    SqliteMemoryRepository finds memories for tier promotion


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:232:11
-         if (criteria?.project) {
+         if (false) {
Tests ran:
    SqliteMemoryRepository counts memories by tier


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-memory-repository.ts:236:11
-         if (criteria?.memoryType) {
+         if (false) {
Tests ran:
    SqliteMemoryRepository counts memories by tier


[Survived] StringLiteral
src/adapters/repositories/sqlite-memory-repository.ts:245:70
-         const where = conditions.length > 0 ? `WHERE ${conditions.join(' AND ')}` : '';
+         const where = conditions.length > 0 ? `WHERE ${conditions.join("")}` : '';
Tests ran:
    SqliteMemoryRepository counts memories by tier


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:25:58
-   function rowToPreference(row: PreferenceRow): Preference {

-     return {

-       id: row.id,

-       key: row.key,

-       value: row.value,

-       context: row.context ?? undefined,

-       strength: row.strength as PreferenceStrength,

-       project: row.project ?? undefined,

-       createdAt: row.updated_at,

-       updatedAt: row.updated_at,

-     };

-   }

+   function rowToPreference(row: PreferenceRow): Preference {}


[NoCoverage] ObjectLiteral
src/adapters/repositories/sqlite-preference-repository.ts:26:10
-     return {

-       id: row.id,

-       key: row.key,

-       value: row.value,

-       context: row.context ?? undefined,

-       strength: row.strength as PreferenceStrength,

-       project: row.project ?? undefined,

-       createdAt: row.updated_at,

-       updatedAt: row.updated_at,

-     };

+     return {};


[NoCoverage] LogicalOperator
src/adapters/repositories/sqlite-preference-repository.ts:30:14
-       context: row.context ?? undefined,

+       context: row.context && undefined,


[NoCoverage] LogicalOperator
src/adapters/repositories/sqlite-preference-repository.ts:32:14
-       project: row.project ?? undefined,

+       project: row.project && undefined,


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:41:60
-     set(id: MemoryId, input: SetPreferenceInput): Preference {

-       const now = new Date().toISOString();

-       try {

-         // Check if a preference with this key+project already exists to preserve id

-         const existing = this.db.prepare(

-           'SELECT id FROM preferences WHERE key = ? AND (project = ? OR (project IS NULL AND ? IS NULL))',

-         ).get(input.key, input.project ?? null, input.project ?? null) as { id: string } | undefined;

-   

-         const useId = existing ? existing.id : id;

-   

-         this.db.prepare(`

-           INSERT INTO preferences (id, key, value, context, strength, project, updated_at)

-           VALUES (?, ?, ?, ?, ?, ?, ?)

-           ON CONFLICT(key, project) DO UPDATE SET

-             value = excluded.value,

-             context = excluded.context,

-             strength = excluded.strength,

-             updated_at = excluded.updated_at

-         `).run(

-           useId,

-           input.key,

-           input.value,

-           input.context ?? null,

-           input.strength ?? 'moderate',

-           input.project ?? null,

-           now,

-         );

-   

-         const row = this.db.prepare('SELECT * FROM preferences WHERE id = ?').get(useId) as PreferenceRow;

-         return rowToPreference(row);

-       } catch (err) {

-         throw new StorageError('Failed to set preference', err);

-       }

-     }

+     set(id: MemoryId, input: SetPreferenceInput): Preference {}


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:43:9
-       try {

-         // Check if a preference with this key+project already exists to preserve id

-         const existing = this.db.prepare(

-           'SELECT id FROM preferences WHERE key = ? AND (project = ? OR (project IS NULL AND ? IS NULL))',

-         ).get(input.key, input.project ?? null, input.project ?? null) as { id: string } | undefined;

-   

-         const useId = existing ? existing.id : id;

-   

-         this.db.prepare(`

-           INSERT INTO preferences (id, key, value, context, strength, project, updated_at)

-           VALUES (?, ?, ?, ?, ?, ?, ?)

-           ON CONFLICT(key, project) DO UPDATE SET

-             value = excluded.value,

-             context = excluded.context,

-             strength = excluded.strength,

-             updated_at = excluded.updated_at

-         `).run(

-           useId,

-           input.key,

-           input.value,

-           input.context ?? null,

-           input.strength ?? 'moderate',

-           input.project ?? null,

-           now,

-         );

-   

-         const row = this.db.prepare('SELECT * FROM preferences WHERE id = ?').get(useId) as PreferenceRow;

-         return rowToPreference(row);

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:51:23
-         this.db.prepare(`

-           INSERT INTO preferences (id, key, value, context, strength, project, updated_at)

-           VALUES (?, ?, ?, ?, ?, ?, ?)

-           ON CONFLICT(key, project) DO UPDATE SET

-             value = excluded.value,

-             context = excluded.context,

-             strength = excluded.strength,

-             updated_at = excluded.updated_at

-         `).run(

+         this.db.prepare(``).run(


[NoCoverage] LogicalOperator
src/adapters/repositories/sqlite-preference-repository.ts:63:9
-           input.context ?? null,

+           input.context && null,


[NoCoverage] LogicalOperator
src/adapters/repositories/sqlite-preference-repository.ts:64:9
-           input.strength ?? 'moderate',

+           input.strength && 'moderate',


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:64:27
-           input.strength ?? 'moderate',

+           input.strength ?? "",


[NoCoverage] LogicalOperator
src/adapters/repositories/sqlite-preference-repository.ts:65:9
-           input.project ?? null,

+           input.project && null,


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:71:19
-       } catch (err) {

-         throw new StorageError('Failed to set preference', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:72:30
-         throw new StorageError('Failed to set preference', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:76:71
-     findByKey(key: string, project?: ProjectId): Preference | undefined {

-       try {

-         const row = this.db.prepare(

-           'SELECT * FROM preferences WHERE key = ? AND (project = ? OR (project IS NULL AND ? IS NULL))',

-         ).get(key, project ?? null, project ?? null) as PreferenceRow | undefined;

-         return row ? rowToPreference(row) : undefined;

-       } catch (err) {

-         throw new StorageError('Failed to find preference', err);

-       }

-     }

+     findByKey(key: string, project?: ProjectId): Preference | undefined {}


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:77:9
-       try {

-         const row = this.db.prepare(

-           'SELECT * FROM preferences WHERE key = ? AND (project = ? OR (project IS NULL AND ? IS NULL))',

-         ).get(key, project ?? null, project ?? null) as PreferenceRow | undefined;

-         return row ? rowToPreference(row) : undefined;

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:82:19
-       } catch (err) {

-         throw new StorageError('Failed to find preference', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:83:30
-         throw new StorageError('Failed to find preference', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:87:70
-     getForContext(context?: string, project?: ProjectId): Preference[] {

-       try {

-         const conditions: string[] = ['(project IS NULL'];

-         const params: unknown[] = [];

-   

-         if (project) {

-           conditions[0] += ` OR project = ?`;

-           params.push(project);

-         }

-         conditions[0] += ')';

-   

-         if (context) {

-           conditions.push('(context IS NULL OR context LIKE ?)');

-           params.push(`%${context}%`);

-         }

-   

-         const where = `WHERE ${conditions.join(' AND ')}`;

-         const rows = this.db.prepare(`SELECT * FROM preferences ${where} ORDER BY key`).all(...params) as PreferenceRow[];

-         return rows.map(rowToPreference);

-       } catch (err) {

-         throw new StorageError('Failed to get preferences for context', err);

-       }

-     }

+     getForContext(context?: string, project?: ProjectId): Preference[] {}


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:88:9
-       try {

-         const conditions: string[] = ['(project IS NULL'];

-         const params: unknown[] = [];

-   

-         if (project) {

-           conditions[0] += ` OR project = ?`;

-           params.push(project);

-         }

-         conditions[0] += ')';

-   

-         if (context) {

-           conditions.push('(context IS NULL OR context LIKE ?)');

-           params.push(`%${context}%`);

-         }

-   

-         const where = `WHERE ${conditions.join(' AND ')}`;

-         const rows = this.db.prepare(`SELECT * FROM preferences ${where} ORDER BY key`).all(...params) as PreferenceRow[];

-         return rows.map(rowToPreference);

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] ArrayDeclaration
src/adapters/repositories/sqlite-preference-repository.ts:89:36
-         const conditions: string[] = ['(project IS NULL'];

+         const conditions: string[] = [];


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:89:37
-         const conditions: string[] = ['(project IS NULL'];

+         const conditions: string[] = [""];


[NoCoverage] ArrayDeclaration
src/adapters/repositories/sqlite-preference-repository.ts:90:33
-         const params: unknown[] = [];

+         const params: unknown[] = ["Stryker was here"];


[NoCoverage] ConditionalExpression
src/adapters/repositories/sqlite-preference-repository.ts:92:11
-         if (project) {

+         if (true) {


[NoCoverage] ConditionalExpression
src/adapters/repositories/sqlite-preference-repository.ts:92:11
-         if (project) {

+         if (false) {


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:92:20
-         if (project) {

-           conditions[0] += ` OR project = ?`;

-           params.push(project);

-         }

+         if (project) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:93:26
-           conditions[0] += ` OR project = ?`;

+           conditions[0] += ``;


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:96:24
-         conditions[0] += ')';

+         conditions[0] += "";


[NoCoverage] ConditionalExpression
src/adapters/repositories/sqlite-preference-repository.ts:98:11
-         if (context) {

+         if (true) {


[NoCoverage] ConditionalExpression
src/adapters/repositories/sqlite-preference-repository.ts:98:11
-         if (context) {

+         if (false) {


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:98:20
-         if (context) {

-           conditions.push('(context IS NULL OR context LIKE ?)');

-           params.push(`%${context}%`);

-         }

+         if (context) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:99:25
-           conditions.push('(context IS NULL OR context LIKE ?)');

+           conditions.push("");


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:100:21
-           params.push(`%${context}%`);

+           params.push(``);


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:103:21
-         const where = `WHERE ${conditions.join(' AND ')}`;

+         const where = ``;


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:103:46
-         const where = `WHERE ${conditions.join(' AND ')}`;

+         const where = `WHERE ${conditions.join("")}`;


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:106:19
-       } catch (err) {

-         throw new StorageError('Failed to get preferences for context', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:107:30
-         throw new StorageError('Failed to get preferences for context', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:111:29
-     getGlobal(): Preference[] {

-       try {

-         const rows = this.db.prepare('SELECT * FROM preferences WHERE project IS NULL ORDER BY key').all() as PreferenceRow[];

-         return rows.map(rowToPreference);

-       } catch (err) {

-         throw new StorageError('Failed to get global preferences', err);

-       }

-     }

+     getGlobal(): Preference[] {}


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:112:9
-       try {

-         const rows = this.db.prepare('SELECT * FROM preferences WHERE project IS NULL ORDER BY key').all() as PreferenceRow[];

-         return rows.map(rowToPreference);

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:115:19
-       } catch (err) {

-         throw new StorageError('Failed to get global preferences', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:116:30
-         throw new StorageError('Failed to get global preferences', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:120:50
-     getByProject(project: ProjectId): Preference[] {

-       try {

-         const rows = this.db.prepare('SELECT * FROM preferences WHERE project = ? ORDER BY key').all(project) as PreferenceRow[];

-         return rows.map(rowToPreference);

-       } catch (err) {

-         throw new StorageError('Failed to get project preferences', err);

-       }

-     }

+     getByProject(project: ProjectId): Preference[] {}


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:121:9
-       try {

-         const rows = this.db.prepare('SELECT * FROM preferences WHERE project = ? ORDER BY key').all(project) as PreferenceRow[];

-         return rows.map(rowToPreference);

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:124:19
-       } catch (err) {

-         throw new StorageError('Failed to get project preferences', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:125:30
-         throw new StorageError('Failed to get project preferences', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:129:50
-     delete(key: string, project?: ProjectId): void {

-       try {

-         this.db.prepare(

-           'DELETE FROM preferences WHERE key = ? AND (project = ? OR (project IS NULL AND ? IS NULL))',

-         ).run(key, project ?? null, project ?? null);

-       } catch (err) {

-         throw new StorageError('Failed to delete preference', err);

-       }

-     }

+     delete(key: string, project?: ProjectId): void {}


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:130:9
-       try {

-         this.db.prepare(

-           'DELETE FROM preferences WHERE key = ? AND (project = ? OR (project IS NULL AND ? IS NULL))',

-         ).run(key, project ?? null, project ?? null);

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:132:9
-           'DELETE FROM preferences WHERE key = ? AND (project = ? OR (project IS NULL AND ? IS NULL))',

+           "",


[NoCoverage] LogicalOperator
src/adapters/repositories/sqlite-preference-repository.ts:133:18
-         ).run(key, project ?? null, project ?? null);

+         ).run(key, project && null, project ?? null);


[NoCoverage] LogicalOperator
src/adapters/repositories/sqlite-preference-repository.ts:133:35
-         ).run(key, project ?? null, project ?? null);

+         ).run(key, project ?? null, project && null);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-preference-repository.ts:134:19
-       } catch (err) {

-         throw new StorageError('Failed to delete preference', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-preference-repository.ts:135:30
-         throw new StorageError('Failed to delete preference', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:64:19
-       } catch (err) {

-         throw new StorageError('Failed to create session', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-session-repository.ts:65:30
-         throw new StorageError('Failed to create session', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:74:19
-       } catch (err) {

-         throw new StorageError('Failed to find session', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-session-repository.ts:75:30
-         throw new StorageError('Failed to find session', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:79:64
-     findLatestByProject(project: ProjectId): Session | undefined {

-       try {

-         const row = this.db.prepare(`

-           SELECT * FROM sessions WHERE project = ? ORDER BY started_at DESC LIMIT 1

-         `).get(project) as SessionRow | undefined;

-         return row ? rowToSession(row) : undefined;

-       } catch (err) {

-         throw new StorageError('Failed to find latest session', err);

-       }

-     }

+     findLatestByProject(project: ProjectId): Session | undefined {}


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:80:9
-       try {

-         const row = this.db.prepare(`

-           SELECT * FROM sessions WHERE project = ? ORDER BY started_at DESC LIMIT 1

-         `).get(project) as SessionRow | undefined;

-         return row ? rowToSession(row) : undefined;

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:85:19
-       } catch (err) {

-         throw new StorageError('Failed to find latest session', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-session-repository.ts:86:30
-         throw new StorageError('Failed to find latest session', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:96:19
-       } catch (err) {

-         throw new StorageError('Failed to find active session', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-session-repository.ts:97:30
-         throw new StorageError('Failed to find active session', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:101:47
-     findCrashed(project?: ProjectId): Session[] {

-       try {

-         if (project) {

-           const rows = this.db.prepare(`

-             SELECT * FROM sessions WHERE status = 'crashed' AND project = ? ORDER BY started_at DESC

-           `).all(project) as SessionRow[];

-           return rows.map(rowToSession);

-         }

-         const rows = this.db.prepare(`

-           SELECT * FROM sessions WHERE status = 'crashed' ORDER BY started_at DESC

-         `).all() as SessionRow[];

-         return rows.map(rowToSession);

-       } catch (err) {

-         throw new StorageError('Failed to find crashed sessions', err);

-       }

-     }

+     findCrashed(project?: ProjectId): Session[] {}


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:102:9
-       try {

-         if (project) {

-           const rows = this.db.prepare(`

-             SELECT * FROM sessions WHERE status = 'crashed' AND project = ? ORDER BY started_at DESC

-           `).all(project) as SessionRow[];

-           return rows.map(rowToSession);

-         }

-         const rows = this.db.prepare(`

-           SELECT * FROM sessions WHERE status = 'crashed' ORDER BY started_at DESC

-         `).all() as SessionRow[];

-         return rows.map(rowToSession);

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] ConditionalExpression
src/adapters/repositories/sqlite-session-repository.ts:103:11
-         if (project) {

+         if (true) {


[NoCoverage] ConditionalExpression
src/adapters/repositories/sqlite-session-repository.ts:103:11
-         if (project) {

+         if (false) {


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:103:20
-         if (project) {

-           const rows = this.db.prepare(`

-             SELECT * FROM sessions WHERE status = 'crashed' AND project = ? ORDER BY started_at DESC

-           `).all(project) as SessionRow[];

-           return rows.map(rowToSession);

-         }

+         if (project) {}


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:113:19
-       } catch (err) {

-         throw new StorageError('Failed to find crashed sessions', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-session-repository.ts:114:30
-         throw new StorageError('Failed to find crashed sessions', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:136:19
-       } catch (err) {

-         throw new StorageError('Failed to update session', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-session-repository.ts:137:30
-         throw new StorageError('Failed to update session', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:141:69
-     findRecentByProject(project: ProjectId, limit: number): Session[] {

-       try {

-         const rows = this.db.prepare(`

-           SELECT * FROM sessions WHERE project = ? ORDER BY started_at DESC LIMIT ?

-         `).all(project, limit) as SessionRow[];

-         return rows.map(rowToSession);

-       } catch (err) {

-         throw new StorageError('Failed to find recent sessions', err);

-       }

-     }

+     findRecentByProject(project: ProjectId, limit: number): Session[] {}


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:142:9
-       try {

-         const rows = this.db.prepare(`

-           SELECT * FROM sessions WHERE project = ? ORDER BY started_at DESC LIMIT ?

-         `).all(project, limit) as SessionRow[];

-         return rows.map(rowToSession);

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-session-repository.ts:147:19
-       } catch (err) {

-         throw new StorageError('Failed to find recent sessions', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-session-repository.ts:148:30
-         throw new StorageError('Failed to find recent sessions', err);

+         throw new StorageError("", err);


[Survived] LogicalOperator
src/adapters/repositories/sqlite-session-repository.ts:33:14
-       endedAt: row.ended_at ?? undefined,

+       endedAt: row.ended_at && undefined,

Tests ran:
    SessionService starts a session that is immediately the active one for its project
    SessionService keeps sessions for different projects separate
    SessionService ends a session by id and records the summary
  and 4 more tests!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-session-repository.ts:30:13
-       device: row.device ?? undefined,

+       device: row.device && undefined,

Tests ran:
    SessionService starts a session that is immediately the active one for its project
    SessionService keeps sessions for different projects separate
    SessionService ends a session by id and records the summary
  and 4 more tests!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-session-repository.ts:37:14
-       summary: row.summary ?? undefined,

+       summary: row.summary && undefined,

Tests ran:
    SessionService starts a session that is immediately the active one for its project
    SessionService keeps sessions for different projects separate
    SessionService ends a session by id and records the summary
  and 4 more tests!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-session-repository.ts:55:9
-           session.device ?? null,

+           session.device && null,

Tests ran:
    SessionService starts a session that is immediately the active one for its project
    SessionService keeps sessions for different projects separate
    SessionService ends a session by id and records the summary
  and 4 more tests!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-session-repository.ts:58:9
-           session.endedAt ?? null,

+           session.endedAt && null,

Tests ran:
    SessionService starts a session that is immediately the active one for its project
    SessionService keeps sessions for different projects separate
    SessionService ends a session by id and records the summary
  and 4 more tests!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-session-repository.ts:62:9
-           session.summary ?? null,

+           session.summary && null,

Tests ran:
    SessionService starts a session that is immediately the active one for its project
    SessionService keeps sessions for different projects separate
    SessionService ends a session by id and records the summary
  and 4 more tests!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-session-repository.ts:127:9
-           session.device ?? null,

+           session.device && null,

Tests ran:
    SessionService ends a session by id and records the summary
    SessionService stops reporting an ended session as the project active one
    SessionService regression — ending by project (the flow the MCP handler documents) resolves the active session from the project, which is how end must work
  and 1 more test!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-session-repository.ts:129:9
-           session.endedAt ?? null,

+           session.endedAt && null,

Tests ran:
    SessionService ends a session by id and records the summary
    SessionService stops reporting an ended session as the project active one
    SessionService regression — ending by project (the flow the MCP handler documents) resolves the active session from the project, which is how end must work
  and 1 more test!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-session-repository.ts:133:9
-           session.summary ?? null,

+           session.summary && null,

Tests ran:
    SessionService ends a session by id and records the summary
    SessionService stops reporting an ended session as the project active one
    SessionService regression — ending by project (the flow the MCP handler documents) resolves the active session from the project, which is how end must work
  and 1 more test!


[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:109:19
-       } catch (err) {
-         throw new StorageError('Failed to upsert shared cache', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:110:30
-         throw new StorageError('Failed to upsert shared cache', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:157:19
-       } catch (err) {
-         throw new StorageError('Failed to search shared cache', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:158:30
-         throw new StorageError('Failed to search shared cache', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:179:19
-       } catch (err) {
-         throw new StorageError('Failed to upsert insight', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:180:30
-         throw new StorageError('Failed to upsert insight', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:201:19
-       } catch (err) {
-         throw new StorageError('Failed to get insights', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:202:30
-         throw new StorageError('Failed to get insights', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:223:19
-       } catch (err) {
-         throw new StorageError('Failed to upsert pattern', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:224:30
-         throw new StorageError('Failed to upsert pattern', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:235:66
-     getUserPatterns(userId: string, teamId: string): TeamPattern[] {
-       try {
-         return (this.db.prepare(
-           'SELECT * FROM team_patterns_cache WHERE user_id = ? AND team_id = ? ORDER BY computed_at DESC'
-         ).all(userId, teamId) as PatternsCacheRow[]).map(rowToPattern);
-       } catch (err) {
-         throw new StorageError('Failed to get user patterns', err);
-       }
-     }
+     getUserPatterns(userId: string, teamId: string): TeamPattern[] {}

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:236:9
-       try {
-         return (this.db.prepare(
-           'SELECT * FROM team_patterns_cache WHERE user_id = ? AND team_id = ? ORDER BY computed_at DESC'
-         ).all(userId, teamId) as PatternsCacheRow[]).map(rowToPattern);
-       } catch (err) {
+       try {} catch (err) {

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:240:19
-       } catch (err) {
-         throw new StorageError('Failed to get user patterns', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:241:30
-         throw new StorageError('Failed to get user patterns', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:256:19
-       } catch (err) {
-         throw new StorageError('Failed to get team patterns', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:257:30
-         throw new StorageError('Failed to get team patterns', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:281:19
-       } catch (err) {
-         throw new StorageError('Failed to buffer prompt log', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:282:30
-         throw new StorageError('Failed to buffer prompt log', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:296:19
-       } catch (err) {
-         throw new StorageError('Failed to get pending prompt logs', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:297:30
-         throw new StorageError('Failed to get pending prompt logs', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:311:19
-       } catch (err) {
-         throw new StorageError('Failed to mark prompt logs pushed', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:312:30
-         throw new StorageError('Failed to mark prompt logs pushed', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:329:19
-       } catch (err) {
-         throw new StorageError('Failed to count prompt logs', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:330:30
-         throw new StorageError('Failed to count prompt logs', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:347:19
-       } catch (err) {
-         throw new StorageError('Failed to count prompt logs by category', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:348:30
-         throw new StorageError('Failed to count prompt logs by category', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:367:19
-       } catch (err) {
-         throw new StorageError('Failed to get team sync cursor', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:368:30
-         throw new StorageError('Failed to get team sync cursor', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:385:19
-       } catch (err) {
-         throw new StorageError('Failed to set team sync cursor', err);
-       }
+       } catch (err) {}

[NoCoverage] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:386:30
-         throw new StorageError('Failed to set team sync cursor', err);
+         throw new StorageError("", err);

[Survived] LogicalOperator
src/adapters/repositories/sqlite-team-repository.ts:57:47
-       id: row.id, teamId: row.team_id, project: row.project ?? undefined,
+       id: row.id, teamId: row.team_id, project: row.project && undefined,
Tests ran:
    SqliteTeamRepository insights cache returns project-scoped and team-wide (null project) insights together
    SqliteTeamRepository insights cache orders by confidence descending


[Survived] LogicalOperator
src/adapters/repositories/sqlite-team-repository.ts:67:14
-       project: row.project ?? undefined,
+       project: row.project && undefined,
Tests ran:
    PatternService.teamStats derives top categories from prompt_category patterns


[Survived] BlockStatement
src/adapters/repositories/sqlite-team-repository.ts:74:55
-   function rowToPromptLog(row: PromptLogRow): PromptLog {
-     return {
-       id: row.id, userId: row.user_id, teamId: row.team_id,
-       project: row.project ?? undefined,
-       pattern: row.pattern, outcome: row.outcome as PromptLog['outcome'],
-       category: row.category,
-       tags: Object.freeze(JSON.parse(row.tags) as string[]),
-       shareContent: !!row.share_content, rawContent: row.raw_content ?? undefined,
-       loggedAt: row.logged_at, status: row.status as PromptLog['status'],
-     };
-   }
+   function rowToPromptLog(row: PromptLogRow): PromptLog {}
Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    PromptLogService buffers a logged prompt as pending
  and 1 more test!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-team-repository.ts:50:15
-       category: row.category ?? undefined,
+       category: row.category && undefined,
Tests ran:
    TeamPromotionService.promote skips a memory already present in the team pool by id
    TeamPromotionService.promote skips a near-duplicate of existing team knowledge
    TeamPromotionService.promote (semantic mode) skips a semantic near-duplicate even when wording differs
  and 21 more tests!


[Survived] ObjectLiteral
src/adapters/repositories/sqlite-team-repository.ts:75:10
-     return {
-       id: row.id, userId: row.user_id, teamId: row.team_id,
-       project: row.project ?? undefined,
-       pattern: row.pattern, outcome: row.outcome as PromptLog['outcome'],
-       category: row.category,
-       tags: Object.freeze(JSON.parse(row.tags) as string[]),
-       shareContent: !!row.share_content, rawContent: row.raw_content ?? undefined,
-       loggedAt: row.logged_at, status: row.status as PromptLog['status'],
-     };
+     return {};
Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    PromptLogService buffers a logged prompt as pending
  and 1 more test!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-team-repository.ts:77:14
-       project: row.project ?? undefined,
+       project: row.project && undefined,
Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    PromptLogService buffers a logged prompt as pending
  and 1 more test!


[Survived] BooleanLiteral
src/adapters/repositories/sqlite-team-repository.ts:81:19
-       shareContent: !!row.share_content, rawContent: row.raw_content ?? undefined,
+       shareContent: !row.share_content, rawContent: row.raw_content ?? undefined,
Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    PromptLogService buffers a logged prompt as pending
  and 1 more test!


[Survived] BooleanLiteral
src/adapters/repositories/sqlite-team-repository.ts:81:20
-       shareContent: !!row.share_content, rawContent: row.raw_content ?? undefined,
+       shareContent: !row.share_content, rawContent: row.raw_content ?? undefined,
Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    PromptLogService buffers a logged prompt as pending
  and 1 more test!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-team-repository.ts:81:52
-       shareContent: !!row.share_content, rawContent: row.raw_content ?? undefined,
+       shareContent: !!row.share_content, rawContent: row.raw_content && undefined,
Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    PromptLogService buffers a logged prompt as pending
  and 1 more test!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-team-repository.ts:107:9
-           row.category ?? null, row.sharedAt, row.updatedAt,
+           row.category && null, row.sharedAt, row.updatedAt,
Tests ran:
    TeamPromotionService.promote skips a memory already present in the team pool by id
    TeamPromotionService.promote skips a near-duplicate of existing team knowledge
    TeamPromotionService.promote (semantic mode) skips a semantic near-duplicate even when wording differs
  and 22 more tests!


[Survived] MethodExpression
src/adapters/repositories/sqlite-team-repository.ts:143:21
-         const words = query.trim().split(/\s+/).filter(Boolean);
+         const words = query.trim().split(/\s+/);
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 29 more tests!


[Survived] MethodExpression
src/adapters/repositories/sqlite-team-repository.ts:143:21
-         const words = query.trim().split(/\s+/).filter(Boolean);
+         const words = query.split(/\s+/).filter(Boolean);
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 29 more tests!


[Survived] Regex
src/adapters/repositories/sqlite-team-repository.ts:143:40
-         const words = query.trim().split(/\s+/).filter(Boolean);
+         const words = query.trim().split(/\s/).filter(Boolean);
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 29 more tests!


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-team-repository.ts:193:11
-         if (project) {
+         if (true) {
Tests ran:
    SqliteTeamRepository insights cache returns project-scoped and team-wide (null project) insights together
    SqliteTeamRepository insights cache orders by confidence descending
    PatternService.teamStats aggregates contributions per member from the shared pool
  and 1 more test!


[Survived] LogicalOperator
src/adapters/repositories/sqlite-team-repository.ts:220:53
-           pattern.id, pattern.userId, pattern.teamId, pattern.project ?? null,
+           pattern.id, pattern.userId, pattern.teamId, pattern.project && null,
Tests ran:
    PatternService.teamStats derives top categories from prompt_category patterns


[Survived] LogicalOperator
src/adapters/repositories/sqlite-team-repository.ts:276:41
-           log.id, log.userId, log.teamId, log.project ?? null,
+           log.id, log.userId, log.teamId, log.project && null,
Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    SqliteTeamRepository prompt log buffer counts logs by outcome and category for a user
  and 4 more tests!


[Survived] ConditionalExpression
src/adapters/repositories/sqlite-team-repository.ts:307:9
-       if (ids.length === 0) return;
+       if (false) return;
Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op


[Survived] LogicalOperator
src/adapters/repositories/sqlite-team-repository.ts:278:35
-           log.shareContent ? 1 : 0, log.rawContent ?? null,
+           log.shareContent ? 1 : 0, log.rawContent && null,
Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    SqliteTeamRepository prompt log buffer counts logs by outcome and category for a user
  and 4 more tests!


[Survived] StringLiteral
src/adapters/repositories/sqlite-team-repository.ts:309:52
-         const placeholders = ids.map(() => '?').join(', ');
+         const placeholders = ids.map(() => '?').join("");
Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed


[NoCoverage] BlockStatement
src/domain/entities/decision.ts:65:84
-   export function recordDecision(id: MemoryId, input: RecordDecisionInput): Decision {
-     return {
-       id,
-       decision: input.decision,
-       context: input.context,
-       alternativesConsidered: Object.freeze(input.alternativesConsidered),
-       consequences: Object.freeze(input.consequences),
-       project: input.project,
-       createdAt: new Date().toISOString(),
-       decidedBy: input.decidedBy,
-     };
-   }
+   export function recordDecision(id: MemoryId, input: RecordDecisionInput): Decision {}

[NoCoverage] ObjectLiteral
src/domain/entities/decision.ts:66:10
-     return {
-       id,
-       decision: input.decision,
-       context: input.context,
-       alternativesConsidered: Object.freeze(input.alternativesConsidered),
-       consequences: Object.freeze(input.consequences),
-       project: input.project,
-       createdAt: new Date().toISOString(),
-       decidedBy: input.decidedBy,
-     };
+     return {};

[NoCoverage] BlockStatement
src/domain/entities/preference.ts:48:84
-   export function setPreference(id: MemoryId, input: SetPreferenceInput): Preference {
-     const now = new Date().toISOString();
-     return {
-       id,
-       key: input.key,
-       value: input.value,
-       context: input.context,
-       strength: input.strength ?? 'moderate',
-       project: input.project,
-       createdAt: now,
-       updatedAt: now,
-     };
-   }
+   export function setPreference(id: MemoryId, input: SetPreferenceInput): Preference {}

[NoCoverage] ObjectLiteral
src/domain/entities/preference.ts:50:10
-     return {
-       id,
-       key: input.key,
-       value: input.value,
-       context: input.context,
-       strength: input.strength ?? 'moderate',
-       project: input.project,
-       createdAt: now,
-       updatedAt: now,
-     };
+     return {};

[NoCoverage] LogicalOperator
src/domain/entities/preference.ts:55:15
-       strength: input.strength ?? 'moderate',
+       strength: input.strength && 'moderate',

[NoCoverage] StringLiteral
src/domain/entities/preference.ts:55:33
-       strength: input.strength ?? 'moderate',
+       strength: input.strength ?? "",

[NoCoverage] BlockStatement
src/domain/entities/preference.ts:72:15
-   ): Preference {
-     return {
-       ...preference,
-       value: updates.value ?? preference.value,
-       strength: updates.strength ?? preference.strength,
-       updatedAt: new Date().toISOString(),
-     };
-   }
+   ): Preference {}

[NoCoverage] ObjectLiteral
src/domain/entities/preference.ts:73:10
-     return {
-       ...preference,
-       value: updates.value ?? preference.value,
-       strength: updates.strength ?? preference.strength,
-       updatedAt: new Date().toISOString(),
-     };
+     return {};

[NoCoverage] LogicalOperator
src/domain/entities/preference.ts:75:12
-       value: updates.value ?? preference.value,
+       value: updates.value && preference.value,

[NoCoverage] LogicalOperator
src/domain/entities/preference.ts:76:15
-       strength: updates.strength ?? preference.strength,
+       strength: updates.strength && preference.strength,

[Survived] StringLiteral
src/domain/entities/prompt-log.ts:66:31
-       outcome: input.outcome ?? 'neutral',

+       outcome: input.outcome ?? "",

Tests ran:
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    PromptLogService does not persist raw content unless the user opts in
    PromptLogService pushBuffer is a no-op (returns 0) without railwayUrl


[Survived] StringLiteral
src/domain/entities/prompt-log.ts:67:33
-       category: input.category ?? 'general',

+       category: input.category ?? "",

Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    PatternService.myStats computes good rate from buffered prompt logs
  and 2 more tests!


[Survived] ArrayDeclaration
src/domain/entities/prompt-log.ts:68:39
-       tags: Object.freeze(input.tags ?? []),

+       tags: Object.freeze(input.tags ?? ["Stryker was here"]),

Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    SqliteTeamRepository prompt log buffer counts logs by outcome and category for a user
  and 4 more tests!


[Survived] BooleanLiteral
src/domain/entities/prompt-log.ts:69:41
-       shareContent: input.shareContent ?? false,

+       shareContent: input.shareContent ?? true,

Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    SqliteTeamRepository prompt log buffer counts logs by outcome and category for a user
  and 4 more tests!


[Survived] LogicalOperator
src/domain/entities/prompt-log.ts:69:19
-       shareContent: input.shareContent ?? false,

+       shareContent: input.shareContent && false,

Tests ran:
    SqliteTeamRepository prompt log buffer buffers a log as pending, then marks it pushed
    SqliteTeamRepository prompt log buffer markPromptLogsPushed with empty list is a no-op
    SqliteTeamRepository prompt log buffer counts logs by outcome and category for a user
  and 4 more tests!


[NoCoverage] BlockStatement
src/domain/entities/solution.ts:49:80
-   export function saveSolution(id: MemoryId, input: SaveSolutionInput): Solution {
-     return {
-       id,
-       problem: input.problem,
-       solution: input.solution,
-       language: input.language,
-       tags: Object.freeze(input.tags ?? []),
-       sourceProject: input.sourceProject,
-       sourceFile: input.sourceFile,
-       createdAt: new Date().toISOString(),
-       embedding: undefined,
-     };
-   }
+   export function saveSolution(id: MemoryId, input: SaveSolutionInput): Solution {}

[NoCoverage] ObjectLiteral
src/domain/entities/solution.ts:50:10
-     return {
-       id,
-       problem: input.problem,
-       solution: input.solution,
-       language: input.language,
-       tags: Object.freeze(input.tags ?? []),
-       sourceProject: input.sourceProject,
-       sourceFile: input.sourceFile,
-       createdAt: new Date().toISOString(),
-       embedding: undefined,
-     };
+     return {};

[NoCoverage] LogicalOperator
src/domain/entities/solution.ts:55:25
-       tags: Object.freeze(input.tags ?? []),
+       tags: Object.freeze(input.tags && []),

[NoCoverage] ArrayDeclaration
src/domain/entities/solution.ts:55:39
-       tags: Object.freeze(input.tags ?? []),
+       tags: Object.freeze(input.tags ?? ["Stryker was here"]),

[NoCoverage] BlockStatement
src/domain/entities/solution.ts:70:93
-   export function attachSolutionEmbedding(solution: Solution, embedding: Embedding): Solution {
-     return {
-       ...solution,
-       embedding,
-     };
-   }
+   export function attachSolutionEmbedding(solution: Solution, embedding: Embedding): Solution {}

[NoCoverage] ObjectLiteral
src/domain/entities/solution.ts:71:10
-     return {
-       ...solution,
-       embedding,
-     };
+     return {};

[NoCoverage] BlockStatement
src/domain/entities/team.ts:51:60
-   export function createTeam(id: TeamId, name: string): Team {

-     return { id, name, createdAt: new Date().toISOString() };

-   }

+   export function createTeam(id: TeamId, name: string): Team {}


[NoCoverage] ObjectLiteral
src/domain/entities/team.ts:52:10
-     return { id, name, createdAt: new Date().toISOString() };

+     return {};


[NoCoverage] StringLiteral
src/domain/entities/team.ts:63:83
-   export function createTeamMember(userId: string, teamId: TeamId, role: TeamRole = 'member'): TeamMember {

+   export function createTeamMember(userId: string, teamId: TeamId, role: TeamRole = ""): TeamMember {


[NoCoverage] BlockStatement
src/domain/entities/team.ts:63:105
-   export function createTeamMember(userId: string, teamId: TeamId, role: TeamRole = 'member'): TeamMember {

-     return { userId, teamId, role, joinedAt: new Date().toISOString() };

-   }

+   export function createTeamMember(userId: string, teamId: TeamId, role: TeamRole = 'member'): TeamMember {}


[NoCoverage] ObjectLiteral
src/domain/entities/team.ts:64:10
-     return { userId, teamId, role, joinedAt: new Date().toISOString() };

+     return {};


[NoCoverage] BlockStatement
src/domain/entities/trigger.ts:43:82
-   export function createTrigger(id: TriggerId, input: CreateTriggerInput): Trigger {
-     return {
-       id,
-       memoryId: input.memoryId,
-       action: input.action,
-       severity: input.severity,
-       createdAt: new Date().toISOString(),
-       active: true,
-     };
-   }
+   export function createTrigger(id: TriggerId, input: CreateTriggerInput): Trigger {}

[NoCoverage] ObjectLiteral
src/domain/entities/trigger.ts:44:10
-     return {
-       id,
-       memoryId: input.memoryId,
-       action: input.action,
-       severity: input.severity,
-       createdAt: new Date().toISOString(),
-       active: true,
-     };
+     return {};

[NoCoverage] BooleanLiteral
src/domain/entities/trigger.ts:50:13
-       active: true,
+       active: false,

[NoCoverage] BlockStatement
src/domain/entities/trigger.ts:60:62
-   export function deactivateTrigger(trigger: Trigger): Trigger {
-     return {
-       ...trigger,
-       active: false,
-     };
-   }
+   export function deactivateTrigger(trigger: Trigger): Trigger {}

[NoCoverage] ObjectLiteral
src/domain/entities/trigger.ts:61:10
-     return {
-       ...trigger,
-       active: false,
-     };
+     return {};

[NoCoverage] BooleanLiteral
src/domain/entities/trigger.ts:63:13
-       active: false,
+       active: true,

[NoCoverage] BlockStatement
src/infrastructure/db/database.ts:59:50
-   export function getDatabase(): Database.Database {
-     if (_db) return _db;
-     try {
-       const config = getConfig();
-       const dbDir = path.dirname(config.dbPath);
-       fs.mkdirSync(dbDir, { recursive: true });
-       _db = new Database(config.dbPath);
-       applyConcurrencyPragmas(_db);
-       // Order matters, and it is not cosmetic: tables, then column migrations, then indexes.
-       // `CREATE TABLE IF NOT EXISTS` does nothing to a table that already exists, so a new column
-       // needs an ALTER — and an index on that column cannot be created before it is there.
-       _db.exec(SCHEMA_TABLES_SQL);
-       migrateLocalSchema(_db);
-       _db.exec(SCHEMA_INDEXES_SQL);
-       return _db;
-     } catch (err) {
-       throw new StorageError('Failed to open database', err);
-     }
-   }
+   export function getDatabase(): Database.Database {}

[NoCoverage] ConditionalExpression
src/infrastructure/db/database.ts:60:7
-     if (_db) return _db;
+     if (true) return _db;

[NoCoverage] ConditionalExpression
src/infrastructure/db/database.ts:60:7
-     if (_db) return _db;
+     if (false) return _db;

[NoCoverage] BlockStatement
src/infrastructure/db/database.ts:62:7
-     try {
-       const config = getConfig();
-       const dbDir = path.dirname(config.dbPath);
-       fs.mkdirSync(dbDir, { recursive: true });
-       _db = new Database(config.dbPath);
-       applyConcurrencyPragmas(_db);
-       // Order matters, and it is not cosmetic: tables, then column migrations, then indexes.
-       // `CREATE TABLE IF NOT EXISTS` does nothing to a table that already exists, so a new column
-       // needs an ALTER — and an index on that column cannot be created before it is there.
-       _db.exec(SCHEMA_TABLES_SQL);
-       migrateLocalSchema(_db);
-       _db.exec(SCHEMA_INDEXES_SQL);
-       return _db;
-     } catch (err) {
+     try {} catch (err) {

[NoCoverage] ObjectLiteral
src/infrastructure/db/database.ts:65:25
-       fs.mkdirSync(dbDir, { recursive: true });
+       fs.mkdirSync(dbDir, {});

[NoCoverage] BooleanLiteral
src/infrastructure/db/database.ts:65:38
-       fs.mkdirSync(dbDir, { recursive: true });
+       fs.mkdirSync(dbDir, { recursive: false });

[NoCoverage] BlockStatement
src/infrastructure/db/database.ts:76:17
-     } catch (err) {
-       throw new StorageError('Failed to open database', err);
-     }
+     } catch (err) {}

[NoCoverage] StringLiteral
src/infrastructure/db/database.ts:77:28
-       throw new StorageError('Failed to open database', err);
+       throw new StorageError("", err);

[NoCoverage] BlockStatement
src/infrastructure/db/database.ts:136:39
-   export function closeDatabase(): void {
-     if (_db) {
-       _db.close();
-       _db = null;
-     }
-   }
+   export function closeDatabase(): void {}

[NoCoverage] ConditionalExpression
src/infrastructure/db/database.ts:137:7
-     if (_db) {
+     if (true) {

[NoCoverage] ConditionalExpression
src/infrastructure/db/database.ts:137:7
-     if (_db) {
+     if (false) {

[NoCoverage] BlockStatement
src/infrastructure/db/database.ts:137:12
-     if (_db) {
-       _db.close();
-       _db = null;
-     }
+     if (_db) {}

[NoCoverage] BlockStatement
src/infrastructure/gateways/fastembed-gateway.ts:45:39
-     async available(): Promise<boolean> {

-       try {

-         await this.model();

-         return true;

-       } catch {

-         return false;

-       }

-     }

+     async available(): Promise<boolean> {}


[NoCoverage] BlockStatement
src/infrastructure/gateways/fastembed-gateway.ts:46:9
-       try {

-         await this.model();

-         return true;

-       } catch {

+       try {} catch {


[NoCoverage] BooleanLiteral
src/infrastructure/gateways/fastembed-gateway.ts:48:14
-         return true;

+         return false;


[NoCoverage] BlockStatement
src/infrastructure/gateways/fastembed-gateway.ts:49:13
-       } catch {

-         return false;

-       }

+       } catch {}


[NoCoverage] BooleanLiteral
src/infrastructure/gateways/fastembed-gateway.ts:50:14
-         return false;

+         return true;


[NoCoverage] BlockStatement
src/infrastructure/gateways/fastembed-gateway.ts:61:52
-     async generate(text: string): Promise<Embedding> {

-       const model = await this.model();

-       return model.queryEmbed(text);

-     }

+     async generate(text: string): Promise<Embedding> {}


[NoCoverage] BlockStatement
src/infrastructure/gateways/fastembed-gateway.ts:73:62
-     async generateBatch(texts: string[]): Promise<Embedding[]> {

-       if (texts.length === 0) return [];

-       const model = await this.model();

-       const out: Embedding[] = [];

-       for await (const batch of model.embed(texts, DEFAULT_BATCH_SIZE)) {

-         for (const vector of batch) out.push(vector);

-       }

-       return out;

-     }

+     async generateBatch(texts: string[]): Promise<Embedding[]> {}


[NoCoverage] ConditionalExpression
src/infrastructure/gateways/fastembed-gateway.ts:74:9
-       if (texts.length === 0) return [];

+       if (true) return [];


[NoCoverage] ConditionalExpression
src/infrastructure/gateways/fastembed-gateway.ts:74:9
-       if (texts.length === 0) return [];

+       if (false) return [];


[NoCoverage] EqualityOperator
src/infrastructure/gateways/fastembed-gateway.ts:74:9
-       if (texts.length === 0) return [];

+       if (texts.length !== 0) return [];


[NoCoverage] ArrayDeclaration
src/infrastructure/gateways/fastembed-gateway.ts:74:36
-       if (texts.length === 0) return [];

+       if (texts.length === 0) return ["Stryker was here"];


[NoCoverage] ArrayDeclaration
src/infrastructure/gateways/fastembed-gateway.ts:76:30
-       const out: Embedding[] = [];

+       const out: Embedding[] = ["Stryker was here"];


[NoCoverage] BlockStatement
src/infrastructure/gateways/fastembed-gateway.ts:77:71
-       for await (const batch of model.embed(texts, DEFAULT_BATCH_SIZE)) {

-         for (const vector of batch) out.push(vector);

-       }

+       for await (const batch of model.embed(texts, DEFAULT_BATCH_SIZE)) {}


[NoCoverage] BlockStatement
src/infrastructure/gateways/fastembed-gateway.ts:107:54
-     private async model(): Promise<FlagEmbeddingModel> {

-       if (this.unavailable) throw new EmbeddingError('fastembed is unavailable');

-       if (!this.modelPromise) {

-         this.modelPromise = this.load().catch((err: unknown) => {

-           this.unavailable = true;

-           this.modelPromise = null;

-           throw new EmbeddingError(`Failed to initialise fastembed: ${String(err)}`);

-         });

-       }

-       return this.modelPromise;

-     }

+     private async model(): Promise<FlagEmbeddingModel> {}


[NoCoverage] ConditionalExpression
src/infrastructure/gateways/fastembed-gateway.ts:108:9
-       if (this.unavailable) throw new EmbeddingError('fastembed is unavailable');

+       if (true) throw new EmbeddingError('fastembed is unavailable');


[NoCoverage] ConditionalExpression
src/infrastructure/gateways/fastembed-gateway.ts:108:9
-       if (this.unavailable) throw new EmbeddingError('fastembed is unavailable');

+       if (false) throw new EmbeddingError('fastembed is unavailable');


[NoCoverage] StringLiteral
src/infrastructure/gateways/fastembed-gateway.ts:108:52
-       if (this.unavailable) throw new EmbeddingError('fastembed is unavailable');

+       if (this.unavailable) throw new EmbeddingError("");


[NoCoverage] BooleanLiteral
src/infrastructure/gateways/fastembed-gateway.ts:109:9
-       if (!this.modelPromise) {

+       if (this.modelPromise) {


[NoCoverage] ConditionalExpression
src/infrastructure/gateways/fastembed-gateway.ts:109:9
-       if (!this.modelPromise) {

+       if (true) {


[NoCoverage] ConditionalExpression
src/infrastructure/gateways/fastembed-gateway.ts:109:9
-       if (!this.modelPromise) {

+       if (false) {


[NoCoverage] BlockStatement
src/infrastructure/gateways/fastembed-gateway.ts:109:29
-       if (!this.modelPromise) {

-         this.modelPromise = this.load().catch((err: unknown) => {

-           this.unavailable = true;

-           this.modelPromise = null;

-           throw new EmbeddingError(`Failed to initialise fastembed: ${String(err)}`);

-         });

-       }

+       if (!this.modelPromise) {}


[NoCoverage] BlockStatement
src/infrastructure/gateways/fastembed-gateway.ts:110:63
-         this.modelPromise = this.load().catch((err: unknown) => {

-           this.unavailable = true;

-           this.modelPromise = null;

-           throw new EmbeddingError(`Failed to initialise fastembed: ${String(err)}`);

-         });

+         this.modelPromise = this.load().catch((err: unknown) => {});


[NoCoverage] BooleanLiteral
src/infrastructure/gateways/fastembed-gateway.ts:111:28
-           this.unavailable = true;

+           this.unavailable = false;


[NoCoverage] StringLiteral
src/infrastructure/gateways/fastembed-gateway.ts:113:34
-           throw new EmbeddingError(`Failed to initialise fastembed: ${String(err)}`);

+           throw new EmbeddingError(``);


[NoCoverage] BlockStatement
src/infrastructure/gateways/fastembed-gateway.ts:120:53
-     private async load(): Promise<FlagEmbeddingModel> {

-       // Non-literal specifier keeps the bundler from hard-linking the optional dep.

-       const mod = (await import('fastembed')) as unknown as {

-         FlagEmbedding: { init(options: Record<string, unknown>): Promise<FlagEmbeddingModel> };

-         EmbeddingModel: Record<string, string>;

-         ExecutionProvider: Record<string, string>;

-       };

-       return mod.FlagEmbedding.init({

-         model: mod.EmbeddingModel['BGESmallENV15'],

-         executionProviders: [mod.ExecutionProvider['CPU']],

-         cacheDir: this.cacheDir,

-         showDownloadProgress: false,

-       });

-     }

+     private async load(): Promise<FlagEmbeddingModel> {}


[NoCoverage] ObjectLiteral
src/infrastructure/gateways/fastembed-gateway.ts:127:35
-       return mod.FlagEmbedding.init({

-         model: mod.EmbeddingModel['BGESmallENV15'],

-         executionProviders: [mod.ExecutionProvider['CPU']],

-         cacheDir: this.cacheDir,

-         showDownloadProgress: false,

-       });

+       return mod.FlagEmbedding.init({});


[NoCoverage] StringLiteral
src/infrastructure/gateways/fastembed-gateway.ts:128:33
-         model: mod.EmbeddingModel['BGESmallENV15'],

+         model: mod.EmbeddingModel[""],


[NoCoverage] ArrayDeclaration
src/infrastructure/gateways/fastembed-gateway.ts:129:27
-         executionProviders: [mod.ExecutionProvider['CPU']],

+         executionProviders: [],


[NoCoverage] StringLiteral
src/infrastructure/gateways/fastembed-gateway.ts:129:50
-         executionProviders: [mod.ExecutionProvider['CPU']],

+         executionProviders: [mod.ExecutionProvider[""]],


[NoCoverage] BooleanLiteral
src/infrastructure/gateways/fastembed-gateway.ts:131:29
-         showDownloadProgress: false,

+         showDownloadProgress: true,


[Survived] ConditionalExpression
src/infrastructure/gateways/fastembed-gateway.ts:91:9
-       if (a.length === 0 || a.length !== b.length) return 0;

+       if (false || a.length !== b.length) return 0;

Tests ran:
    FastEmbedGateway.cosineSimilarity scores identical vectors as 1
    FastEmbedGateway.cosineSimilarity scores orthogonal vectors as 0
    FastEmbedGateway.cosineSimilarity scores opposite vectors as -1
  and 2 more tests!


[Survived] ConditionalExpression
src/infrastructure/gateways/fastembed-gateway.ts:102:24
-       if (normA === 0 || normB === 0) return 0;

+       if (normA === 0 || false) return 0;

Tests ran:
    FastEmbedGateway.cosineSimilarity scores identical vectors as 1
    FastEmbedGateway.cosineSimilarity scores orthogonal vectors as 0
    FastEmbedGateway.cosineSimilarity scores opposite vectors as -1


[NoCoverage] BlockStatement
src/infrastructure/gateways/node-clock.ts:15:19
-     nowDate(): Date {

-       return new Date();

-     }

+     nowDate(): Date {}


[NoCoverage] BlockStatement
src/infrastructure/gateways/node-clock.ts:26:55
-     daysBetween(from: Timestamp, to: Timestamp): number {

-       return (new Date(to).getTime() - new Date(from).getTime()) / (1000 * 60 * 60 * 24);

-     }

+     daysBetween(from: Timestamp, to: Timestamp): number {}


[NoCoverage] ArithmeticOperator
src/infrastructure/gateways/node-clock.ts:27:12
-       return (new Date(to).getTime() - new Date(from).getTime()) / (1000 * 60 * 60 * 24);

+       return (new Date(to).getTime() - new Date(from).getTime()) * (1000 * 60 * 60 * 24);


[NoCoverage] ArithmeticOperator
src/infrastructure/gateways/node-clock.ts:27:13
-       return (new Date(to).getTime() - new Date(from).getTime()) / (1000 * 60 * 60 * 24);

+       return (new Date(to).getTime() + new Date(from).getTime()) / (1000 * 60 * 60 * 24);


[NoCoverage] ArithmeticOperator
src/infrastructure/gateways/node-clock.ts:27:67
-       return (new Date(to).getTime() - new Date(from).getTime()) / (1000 * 60 * 60 * 24);

+       return (new Date(to).getTime() - new Date(from).getTime()) / (1000 * 60 * 60 / 24);


[NoCoverage] ArithmeticOperator
src/infrastructure/gateways/node-clock.ts:27:67
-       return (new Date(to).getTime() - new Date(from).getTime()) / (1000 * 60 * 60 * 24);

+       return (new Date(to).getTime() - new Date(from).getTime()) / (1000 * 60 / 60 * 24);


[NoCoverage] ArithmeticOperator
src/infrastructure/gateways/node-clock.ts:27:67
-       return (new Date(to).getTime() - new Date(from).getTime()) / (1000 * 60 * 60 * 24);

+       return (new Date(to).getTime() - new Date(from).getTime()) / (1000 / 60 * 60 * 24);


[Survived] BlockStatement
src/infrastructure/gateways/node-clock.ts:10:20
-     now(): Timestamp {

-       return new Date().toISOString();

-     }

+     now(): Timestamp {}

Tests ran:
    SessionService starts a session that is immediately the active one for its project
    SessionService keeps sessions for different projects separate
    SessionService ends a session by id and records the summary
  and 4 more tests!


[NoCoverage] BlockStatement
src/infrastructure/gateways/node-id-generator.ts:11:24
-     memoryId(): MemoryId {

-       return `mem_${crypto.randomUUID()}`;

-     }

+     memoryId(): MemoryId {}


[NoCoverage] StringLiteral
src/infrastructure/gateways/node-id-generator.ts:12:12
-       return `mem_${crypto.randomUUID()}`;

+       return ``;


[NoCoverage] BlockStatement
src/infrastructure/gateways/node-id-generator.ts:16:26
-     triggerId(): TriggerId {

-       return `trig_${crypto.randomUUID()}`;

-     }

+     triggerId(): TriggerId {}


[NoCoverage] StringLiteral
src/infrastructure/gateways/node-id-generator.ts:17:12
-       return `trig_${crypto.randomUUID()}`;

+       return ``;


[NoCoverage] BlockStatement
src/mcp/server.ts:37:46
-   export function createMcpServer(): McpServer {
-     const db = getDatabase();
-     const idGen = new NodeIdGenerator();
-     const clock = new NodeClock();
-     const memRepo = new SqliteMemoryRepository(db);
-     const sessRepo = new SqliteSessionRepository(db);
-     const prefRepo = new SqlitePreferenceRepository(db);
-     const memSvc = new MemoryService(memRepo, idGen, clock);
-     const sessSvc = new SessionService(sessRepo, idGen, clock);
-     const trigSvc = new TriggerService(db);
-     const prefSvc = new PreferenceService(prefRepo, idGen);
-     const coordSvc = new CoordinationService(db);
-     const teamRepo = new SqliteTeamRepository(db);
-     const teamSvc = new TeamService();
-     const promptLogSvc = new PromptLogService(teamRepo, idGen);
-     const teamSyncSvc = new TeamSyncService(teamRepo, promptLogSvc);
-     const patternSvc = new PatternService(teamRepo);
-     const promotionSvc = new TeamPromotionService(db, teamRepo, teamSyncSvc, new FastEmbedGateway());
-     const server = new McpServer({ name: 'chronicle', version: '0.4.0' });
-     // ── chronicle ─────────────────────────────────────────────────────────────
-     // Covers: memory CRUD, triggers, preferences, stats, decay.
-     server.tool(
-       'chronicle',
-       [
-         'Persistent memory across Claude Code sessions.',
-         '',
-         'RECALL at the start of any substantive block — a new domain conversation, a decision point, returning to a topic from a prior session. Recall is cheap (~1s) and the value is high: prior context informs current work instead of being re-derived. Skipping recall means losing inherited context.',
-         '',
-         'REMEMBER after any architectural decision — technology choices, pricing decisions, customer-facing positioning, infrastructure setup, methodology pivots, anything you would want the NEXT session to inherit without re-deriving. The cost is ~1s; the value compounds across every future session that touches the same domain.',
-         '',
-         'Six memory types: episodic (it happened), semantic (it is true), procedural (how-to), architectural (why), insight (pattern), coordination (team-state). Set confirmed=true for decisions that should never decay (Core tier).',
-         '',
-         'Tag with project + descriptive keywords so future recall finds the memory. Use stats periodically to confirm the system is healthy; use decay sparingly (it runs automatically on session end).',
-       ].join('\n'),
-       {
-         action: z.enum([
-           'remember', 'recall', 'forget',
-           'trigger', 'check',
-           'pref', 'prefs',
-           'stats', 'decay',
-         ]).describe(
-           'remember=store-decision | recall=load-prior-context | forget=delete | ' +
-           'trigger=set-guard | check=fire-triggers | ' +
-           'pref=set-preference | prefs=get-preferences | ' +
-           'stats=summary | decay=run-decay',
-         ),
-         // Shared
-         id:           z.string().optional().describe('Memory or trigger ID'),
-         project:      z.string().optional()
-                         .describe('Leave UNSET — it is derived from the repository you are in (e.g. github.com/owner/repo). Only pass it to address a different project on purpose.'),
-         scope:        z.enum(['project', 'person', 'team']).optional()
-                         .describe('project=true of this repo (default) | person=true of me anywhere, syncs across my machines | team=shared with the team. Crossing to another person is always deliberate: use the team tool to share.'),
-         category:     z.string().optional(),
-         // remember
-         content:      z.string().optional(),
-         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination']).optional()
-                         .describe('episodic=happened | semantic=is-true | procedural=how-to | architectural=why | insight=pattern | coordination=team-state'),
-         tags:         z.array(z.string()).optional(),
-         confirmed:    z.boolean().optional().describe('Permanent truth — zero decay, Core tier'),
-         // recall / prefs
-         query:        z.string().optional(),
-         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination'])).optional(),
-         tiers:        z.array(z.enum(['buffer', 'working', 'core'])).optional(),
-         limit:        z.number().optional(),
-         context:      z.string().optional(),
-         // trigger
-         trigger_action: z.string().optional().describe('Action keyword for trigger/check ops (e.g. "deploy")'),
-         severity:       z.enum(['critical', 'warning', 'info']).optional(),
-         // pref
-         pref_key:   z.string().optional(),
-         pref_value: z.string().optional(),
-         strength:   z.enum(['strong', 'moderate', 'weak']).optional(),
-       },
-       (args) => {
-         switch (args.action) {
-           case 'remember': {
-             // Project identity is DERIVED from the repository, not taken from whatever label the
-             // agent guessed this session (ADR-018 §2). An explicit argument still wins.
-             const identity = resolveProject(args.project);
-             const scope = (args.scope ?? 'project') as MemoryScope;
-             const memory = memSvc.remember({
-               content: args.content ?? '',
-               memoryType: (args.memory_type ?? 'semantic') as MemoryType,
-               // A person-scoped memory is about the developer, not the repo, so it is not pinned to
-               // one: pinning it would make it invisible from every other project.
-               project: scope === 'person' ? undefined : identity.id,
-               scope,
-               category: args.category,
-               tags: args.tags,
-               confirmed: args.confirmed,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify({
-               id: memory.id, tier: memory.tier, weight: memory.weight,
-               scope: memory.scope, project: memory.project,
-               // Tell the caller how the project was established: anything but `remote` is a fallback
-               // that may not join across machines (ADR-018 §2).
-               projectSource: identity.source,
-             }) }] };
-           }
-           case 'recall': {
-             // Default recall is "what is true about this repository, plus what is true about me".
-             // Person-scoped memories are not pinned to a project, so they must not be filtered out by
-             // the project predicate — hence two queries rather than one OR, which also keeps each
-             // query on an index.
-             const identity = resolveProject(args.project);
-             const scopes = args.scope ? [args.scope as MemoryScope] : undefined;
-             const limit = args.limit ?? 20;
-             const projectHits = (!scopes || scopes.includes('project'))
-               ? memSvc.recall({
-                   query: args.query ?? '',
-                   project: identity.id,
-                   category: args.category,
-                   memoryTypes: args.memory_types as MemoryType[] | undefined,
-                   tiers: args.tiers,
-                   scopes: ['project'],
-                   limit,
-                 })
-               : [];
-             const personHits = (!scopes || scopes.includes('person'))
-               ? memSvc.recall({
-                   query: args.query ?? '',
-                   category: args.category,
-                   memoryTypes: args.memory_types as MemoryType[] | undefined,
-                   tiers: args.tiers,
-                   scopes: ['person'],
-                   limit,
-                 })
-               : [];
-             const memories = [...projectHits, ...personHits]
-               .sort((a, b) => b.weight - a.weight)
-               .slice(0, limit);
-             for (const m of memories) {
-               try { memSvc.reinforce(m.id, 'RECALL_HIT'); } catch { /* non-fatal */ }
-             }
-             return {
-               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({
-                 id: m.id, content: m.content, memoryType: m.memoryType,
-                 tier: m.tier, weight: m.weight, project: m.project, scope: m.scope,
-                 category: m.category, tags: m.tags,
-               }))) }],
-             };
-           }
-           case 'forget': {
-             memSvc.forget(args.id ?? '', args.context);
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Deleted.' }) }] };
-           }
-           case 'trigger': {
-             const id = trigSvc.setTrigger({
-               action: args.trigger_action ?? '',
-               content: args.content ?? '',
-               severity: args.severity,
-               memoryId: args.id,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify({ id, message: 'Trigger set.' }) }] };
-           }
-           case 'check': {
-             const triggers = trigSvc.checkTriggers(args.trigger_action ?? '', args.project);
-             for (const t of triggers) {
-               if (t.memoryId) {
-                 try { memSvc.reinforce(t.memoryId, 'TRIGGER_HIT'); } catch { /* non-fatal */ }
-               }
-             }
-             return { content: [{ type: 'text', text: JSON.stringify(triggers) }] };
-           }
-           case 'pref': {
-             prefSvc.setPreference(args.pref_key ?? '', args.pref_value ?? '', args.context, args.strength, args.project);
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Preference set.' }) }] };
-           }
-           case 'prefs': {
-             const prefs = args.context
-               ? prefSvc.getRelevantPreferences(args.context, args.project)
-               : prefSvc.getPreferences(args.project);
-             return {
-               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({
-                 key: p.key, value: p.value, context: p.context, strength: p.strength, project: p.project,
-               }))) }],
-             };
-           }
-           case 'stats': {
-             const total   = memRepo.count(args.project ? { project: args.project } : undefined);
-             const buffer  = memRepo.count({ tier: 'buffer',  ...(args.project ? { project: args.project } : {}) });
-             const working = memRepo.count({ tier: 'working', ...(args.project ? { project: args.project } : {}) });
-             const core    = memRepo.count({ tier: 'core',    ...(args.project ? { project: args.project } : {}) });
-             return {
-               content: [{ type: 'text', text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],
-             };
-           }
-           case 'decay': {
-             const decayed  = memSvc.applyDecay();
-             const promoted = memSvc.evaluateTierPromotions();
-             return { content: [{ type: 'text', text: JSON.stringify({ memoriesDecayed: decayed, memoriesPromoted: promoted }) }] };
-           }
-           default:
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
-         }
-       },
-     );
-     // ── session ───────────────────────────────────────────────────────────────
-     // Covers: start, end, recover.
-     server.tool(
-       'session',
-       [
-         'Manage Chronicle session boundaries.',
-         '',
-         'Call session START at the beginning of substantive multi-turn work — this automatically injects the project\'s core memories (confirmed=true entries) into context, so the session begins with prior knowledge instead of an empty slate.',
-         '',
-         'Call session END at session close — applies memory decay + tier promotions, keeps the memory store from sprawling, and persists any session summary you pass.',
-         '',
-         'Call session RECOVER to resume an active session by ID or project name — useful when work was interrupted.',
-       ].join('\n'),
-       {
-         action:  z.enum(['start', 'end', 'recover']).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),
-         project: z.string().optional(),
-         device:  z.string().optional(),
-         id:      z.string().optional(),
-         summary: z.string().optional(),
-       },
-       async (args) => {
-         switch (args.action) {
-           case 'start': {
-             // Multi-machine is on whenever a remote is configured — no second flag (ADR-018 §3).
-             // Pull first so the session begins with what another machine learned. Fire-and-forget:
-             // a session must start even with no network, and `skipped: true` is the normal answer for
-             // the single-machine case (ADR-010 §3).
-             const cfgStart = getConfig();
-             if (cfgStart.railwayUrl) {
-               void syncMemories().catch(() => { /* offline — local store is the source of truth */ });
-               void syncInsights().catch(() => { /* offline — non-fatal */ });
-             }
-             const sess = sessSvc.startSession(args.project ?? resolveProject().id, args.device);
-             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: ['core'], limit: 10 });
-             for (const m of coreMemories) {
-               try { memSvc.reinforce(m.id, 'CONTEXT_INJECT'); } catch { /* non-fatal */ }
-             }
-             return {
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 coreMemories: coreMemories.map(m => ({ id: m.id, content: m.content, memoryType: m.memoryType })),
-               }) }],
-             };
-           }
-           case 'end': {
-             // Resolve the same way `recover` does: an id if given, otherwise the project's active
-             // session. Without this fallback the documented F7 flow is impossible — an agent that
-             // called `start` with a project and did not retain the returned id could never end it,
-             // and got `Session not found: ` with an empty id. Found by scripts/smoke-mcp.mjs.
-             // Resolve the project the same way `start` does, or the symmetry breaks: `start` with no
-             // argument opens a session on the derived repository identity, so `end` with no argument
-             // must close that one rather than demand an id the caller never saw (ADR-018 §2).
-             const endProject = args.project ?? resolveProject().id;
-             const sessionId = args.id ?? sessSvc.getActiveSession(endProject)?.id;
-             if (!sessionId) {
-               return { content: [{ type: 'text', text: JSON.stringify({
-                 error: `No active session for project "${endProject}". Pass an id, or call session start first.`,
-                 project: endProject,
-               }) }] };
-             }
-             const sess     = sessSvc.endSession(sessionId, args.summary);
-             const decayed  = memSvc.applyDecay();
-             const promoted = memSvc.evaluateTierPromotions();
-             // Push AFTER decay and promotion, so the other machines receive settled weights and tiers
-             // rather than a mid-maintenance snapshot. Awaited, unlike the pull at session start: this
-             // is the last thing the session does, and losing the push would lose the session's work.
-             let mirrored: { pushed: number; pulled: number; skipped: boolean } | undefined;
-             const cfgEnd = getConfig();
-             if (cfgEnd.railwayUrl) {
-               try {
-                 const [mem] = await Promise.all([
-                   syncMemories(),
-                   syncInsights().catch(() => undefined),
-                   pushSessionSummary(sess).catch(() => undefined),
-                 ]);
-                 mirrored = { pushed: mem.pushed, pulled: mem.pulled, skipped: mem.skipped };
-               } catch { /* offline — the local store keeps everything; the next boundary retries */ }
-             }
-             // Opportunistic team-knowledge sync at session close (non-fatal, offline-safe).
-             const cfg = getConfig();
-             if (cfg.teamToken && cfg.teamId) teamSyncSvc.sync().catch(() => { /* offline — non-fatal */ });
-             return {
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, status: sess.status, endedAt: sess.endedAt,
-                 maintenance: { memoriesDecayed: decayed, memoriesPromoted: promoted },
-                 // Absent when no remote is configured, which is the ordinary single-machine case.
-                 ...(mirrored ? { mirror: mirrored } : {}),
-               }) }],
-             };
-           }
-           case 'recover': {
-             const sess = args.id
-               ? sessSvc.recoverSession(args.id)
-               : args.project ? sessSvc.getActiveSession(args.project) : null;
-             return {
-               content: [{ type: 'text', text: JSON.stringify(sess ? {
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 activeTasks: sess.activeTasks, pendingDecisions: sess.pendingDecisions,
-                 touchedFiles: sess.touchedFiles, summary: sess.summary,
-               } : { message: 'No session found.' }) }],
-             };
-           }
-           default:
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
-         }
-       },
-     );
-     // ── axon ──────────────────────────────────────────────────────────────────
-     // Team coordination: contributors, work decomposition, assignment, status.
-     server.tool(
-       'axon',
-       'Coordinate a development team — register contributors, decompose specs into ranked work packages, assign tasks, track progress.',
-       {
-         action: z.enum([
-           'contributor_add',
-           'spec_sync',
-           'milestone_add',
-           'decompose',
-           'assign',
-           'complete',
-           'request_merge',
-           'resolve_merge',
-           'merges',
-           'status',
-           'queue',
-         ]).describe(
-           'contributor_add=register-dev | spec_sync=read-spec-files | milestone_add=create-milestone | ' +
-           'decompose=break-spec-into-ranked-packages | assign=assign-next-unblocked-package | ' +
-           'complete=mark-work-locally-done | request_merge=submit-for-merger-review | ' +
-           'resolve_merge=approve-or-reject-merge | merges=list-merge-requests | ' +
-           'status=full-team-dashboard | queue=ranked-work-queue',
-         ),
-         project: z.string().optional().describe('Project identifier'),
-         // contributor_add
-         name:         z.string().optional().describe('Contributor full name'),
-         email:        z.string().optional(),
-         skills:       z.array(z.string()).optional().describe('Skill tags, e.g. ["typescript","testing"]'),
-         role:         z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
-                         .describe('specwright=spec/ADR | builder=implement | merger=integrate | verifier=test/score | watcher=monitor'),
-         bandwidth:    z.number().optional().describe('Available hours per week'),
-         // decompose — AI provides the breakdown, Axon ranks and persists it
-         packages: z.array(z.object({
-           title:        z.string().describe('Short unique title'),
-           description:  z.string().describe('What needs to be done'),
-           spec_section: z.string().optional().describe('Spec or ADR reference'),
-           role_required: z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']),
-           depends_on:   z.array(z.string()).optional().describe('Titles of packages this must wait for'),
-         })).optional().describe('Work packages to decompose — AI derives these from spec+state gap'),
-         // spec_sync
-         project_dir: z.string().optional().describe('Absolute path to repo root for spec_sync'),
-         // milestone_add
-         milestone_title:        z.string().optional(),
-         milestone_description:  z.string().optional(),
-         milestone_spec_section: z.string().optional(),
-         // decompose
-         milestone_id: z.string().optional().describe('Parent milestone ID to attach packages to'),
-         // assign / complete / availability
-         id:              z.string().optional().describe('Work package ID, merge request ID, or contributor ID'),
-         contributor_id:  z.string().optional().describe('Force-assign to this contributor'),
-         role_filter:     z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
-                            .describe('Only consider packages requiring this role'),
-         availability:    z.enum(['available', 'busy', 'offline']).optional()
-                            .describe('Update contributor availability state'),
-         // request_merge
-         branch_name:       z.string().optional().describe('Git branch name'),
-         forgecraft_score:  z.number().optional().describe('ForgeCraft verify totalScore (0–14)'),
-         forgecraft_tier:   z.number().optional().describe('ForgeCraft GS maturity tier (1–5)'),
-         forgecraft_pass:   z.boolean().optional().describe('Whether ForgeCraft verify passed'),
-         forgecraft_report: z.string().optional().describe('Full ForgeCraft verify report text'),
-         // resolve_merge
-         approve:          z.boolean().optional().describe('true=approve, false=reject'),
-         rejection_reason: z.string().optional(),
-         // merges filter
-         merge_status: z.enum(['pending', 'approved', 'rejected']).optional(),
-       },
-       async (args) => {
-         const config = getConfig();
-         if (!config.teamToken) {
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
-         }
-         if (!config.teamId) {
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires teamId in ~/.chronicle/config.json.',
-           }) }] };
-         }
-         const tokenOk = await validateTeamToken(config.teamToken, config.railwayUrl, config.teamId);
-         if (!tokenOk) {
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
-         }
-         const project = args.project ?? 'default';
-         // Pull latest team state from Railway before any read or write.
-         // Fire-and-forget on write actions — response is not blocked.
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
-         if (isRead) {
-           await syncCoordination(project).catch(() => { /* offline — use local cache */ });
-         }
-         switch (args.action) {
-           case 'contributor_add': {
-             const contributor = coordSvc.addContributor({
-               project,
-               name: args.name ?? 'Unknown',
-               email: args.email,
-               skills: args.skills ?? [],
-               role: (args.role ?? 'builder') as ContributorRole,
-               bandwidthHoursPerWeek: args.bandwidth ?? 20,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(contributor) }] };
-           }
-           case 'spec_sync': {
-             if (!args.project_dir) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
-             }
-             const sync = coordSvc.syncSpec(args.project_dir);
-             return { content: [{ type: 'text', text: JSON.stringify(sync) }] };
-           }
-           case 'milestone_add': {
-             if (!args.milestone_title) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
-             }
-             const milestone = coordSvc.addMilestone({
-               project,
-               title: args.milestone_title,
-               description: args.milestone_description ?? '',
-               specSection: args.milestone_spec_section,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify(milestone) }] };
-           }
-           case 'decompose': {
-             if (!args.packages?.length) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
-             }
-             const queue = coordSvc.decomposeWork({
-               project,
-               milestoneId: args.milestone_id,
-               packages: args.packages.map(p => ({
-                 title: p.title,
-                 description: p.description,
-                 specSection: p.spec_section,
-                 roleRequired: p.role_required as ContributorRole,
-                 dependsOn: p.depends_on,
-               })),
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
-           }
-           case 'assign': {
-             const result = coordSvc.assignNext({
-               project,
-               contributorId: args.contributor_id,
-               roleFilter: args.role_filter as ContributorRole | undefined,
-             });
-             if (!result) {
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
-             }
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
-           case 'complete': {
-             if (!args.id) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
-             }
-             const result = coordSvc.completeWork(args.id);
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
-           case 'request_merge': {
-             if (!args.id || !args.branch_name || !args.contributor_id) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
-             }
-             const mr = coordSvc.requestMerge({
-               workPackageId: args.id,
-               branchName: args.branch_name,
-               fromContributorId: args.contributor_id,
-               forgecraftScore: args.forgecraft_score,
-               forgecraftTier: args.forgecraft_tier,
-               forgecraftPass: args.forgecraft_pass,
-               forgecraftReport: args.forgecraft_report,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(mr) }] };
-           }
-           case 'resolve_merge': {
-             if (!args.id || !args.contributor_id || args.approve == null) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
-             }
-             const result = coordSvc.resolveMerge({
-               mergeRequestId: args.id,
-               reviewerId: args.contributor_id,
-               approve: args.approve,
-               rejectionReason: args.rejection_reason,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
-           case 'merges': {
-             const mrs = coordSvc.listMergeRequests(
-               project,
-               args.merge_status as 'pending' | 'approved' | 'rejected' | undefined,
-             );
-             return { content: [{ type: 'text', text: JSON.stringify(mrs) }] };
-           }
-           case 'status': {
-             const status = coordSvc.getStatus(project);
-             return { content: [{ type: 'text', text: JSON.stringify(status) }] };
-           }
-           case 'queue': {
-             const queue = coordSvc.getQueue(project);
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
-           }
-           default:
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
-         }
-       },
-     );
-     // ── team ────────────────────────────────────────────────────────────────────
-     // Team knowledge: shared memories, prompt logs, insights, analytics.
-     registerTeamTools(server, { teamSvc, promptLogSvc, teamSyncSvc, patternSvc, promotionSvc, teamRepo, memRepo });
-     return server;
-   }
+   export function createMcpServer(): McpServer {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:56:32
-     const server = new McpServer({ name: 'chronicle', version: '0.4.0' });
+     const server = new McpServer({});

[NoCoverage] StringLiteral
src/mcp/server.ts:56:40
-     const server = new McpServer({ name: 'chronicle', version: '0.4.0' });
+     const server = new McpServer({ name: "", version: '0.4.0' });

[NoCoverage] StringLiteral
src/mcp/server.ts:56:62
-     const server = new McpServer({ name: 'chronicle', version: '0.4.0' });
+     const server = new McpServer({ name: 'chronicle', version: "" });

[NoCoverage] StringLiteral
src/mcp/server.ts:62:5
-       'chronicle',
+       "",

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:63:5
-       [
-         'Persistent memory across Claude Code sessions.',
-         '',
-         'RECALL at the start of any substantive block — a new domain conversation, a decision point, returning to a topic from a prior session. Recall is cheap (~1s) and the value is high: prior context informs current work instead of being re-derived. Skipping recall means losing inherited context.',
-         '',
-         'REMEMBER after any architectural decision — technology choices, pricing decisions, customer-facing positioning, infrastructure setup, methodology pivots, anything you would want the NEXT session to inherit without re-deriving. The cost is ~1s; the value compounds across every future session that touches the same domain.',
-         '',
-         'Six memory types: episodic (it happened), semantic (it is true), procedural (how-to), architectural (why), insight (pattern), coordination (team-state). Set confirmed=true for decisions that should never decay (Core tier).',
-         '',
-         'Tag with project + descriptive keywords so future recall finds the memory. Use stats periodically to confirm the system is healthy; use decay sparingly (it runs automatically on session end).',
-       ].join('\n'),
+       [].join('\n'),

[NoCoverage] StringLiteral
src/mcp/server.ts:64:7
-         'Persistent memory across Claude Code sessions.',
+         "",

[NoCoverage] StringLiteral
src/mcp/server.ts:65:7
-         '',
+         "Stryker was here!",

[NoCoverage] StringLiteral
src/mcp/server.ts:66:7
-         'RECALL at the start of any substantive block — a new domain conversation, a decision point, returning to a topic from a prior session. Recall is cheap (~1s) and the value is high: prior context informs current work instead of being re-derived. Skipping recall means losing inherited context.',
+         "",

[NoCoverage] StringLiteral
src/mcp/server.ts:67:7
-         '',
+         "Stryker was here!",

[NoCoverage] StringLiteral
src/mcp/server.ts:68:7
-         'REMEMBER after any architectural decision — technology choices, pricing decisions, customer-facing positioning, infrastructure setup, methodology pivots, anything you would want the NEXT session to inherit without re-deriving. The cost is ~1s; the value compounds across every future session that touches the same domain.',
+         "",

[NoCoverage] StringLiteral
src/mcp/server.ts:69:7
-         '',
+         "Stryker was here!",

[NoCoverage] StringLiteral
src/mcp/server.ts:70:7
-         'Six memory types: episodic (it happened), semantic (it is true), procedural (how-to), architectural (why), insight (pattern), coordination (team-state). Set confirmed=true for decisions that should never decay (Core tier).',
+         "",

[NoCoverage] StringLiteral
src/mcp/server.ts:71:7
-         '',
+         "Stryker was here!",

[NoCoverage] StringLiteral
src/mcp/server.ts:72:7
-         'Tag with project + descriptive keywords so future recall finds the memory. Use stats periodically to confirm the system is healthy; use decay sparingly (it runs automatically on session end).',
+         "",

[NoCoverage] StringLiteral
src/mcp/server.ts:73:12
-       ].join('\n'),
+       ].join(""),

[NoCoverage] ObjectLiteral
src/mcp/server.ts:74:5
-       {
-         action: z.enum([
-           'remember', 'recall', 'forget',
-           'trigger', 'check',
-           'pref', 'prefs',
-           'stats', 'decay',
-         ]).describe(
-           'remember=store-decision | recall=load-prior-context | forget=delete | ' +
-           'trigger=set-guard | check=fire-triggers | ' +
-           'pref=set-preference | prefs=get-preferences | ' +
-           'stats=summary | decay=run-decay',
-         ),
-         // Shared
-         id:           z.string().optional().describe('Memory or trigger ID'),
-         project:      z.string().optional()
-                         .describe('Leave UNSET — it is derived from the repository you are in (e.g. github.com/owner/repo). Only pass it to address a different project on purpose.'),
-         scope:        z.enum(['project', 'person', 'team']).optional()
-                         .describe('project=true of this repo (default) | person=true of me anywhere, syncs across my machines | team=shared with the team. Crossing to another person is always deliberate: use the team tool to share.'),
-         category:     z.string().optional(),
-         // remember
-         content:      z.string().optional(),
-         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination']).optional()
-                         .describe('episodic=happened | semantic=is-true | procedural=how-to | architectural=why | insight=pattern | coordination=team-state'),
-         tags:         z.array(z.string()).optional(),
-         confirmed:    z.boolean().optional().describe('Permanent truth — zero decay, Core tier'),
-         // recall / prefs
-         query:        z.string().optional(),
-         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination'])).optional(),
-         tiers:        z.array(z.enum(['buffer', 'working', 'core'])).optional(),
-         limit:        z.number().optional(),
-         context:      z.string().optional(),
-         // trigger
-         trigger_action: z.string().optional().describe('Action keyword for trigger/check ops (e.g. "deploy")'),
-         severity:       z.enum(['critical', 'warning', 'info']).optional(),
-         // pref
-         pref_key:   z.string().optional(),
-         pref_value: z.string().optional(),
-         strength:   z.enum(['strong', 'moderate', 'weak']).optional(),
-       },
+       {},

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:75:22
-         action: z.enum([
-           'remember', 'recall', 'forget',
-           'trigger', 'check',
-           'pref', 'prefs',
-           'stats', 'decay',
-         ]).describe(
+         action: z.enum([]).describe(

[NoCoverage] StringLiteral
src/mcp/server.ts:76:9
-           'remember', 'recall', 'forget',
+           "", 'recall', 'forget',

[NoCoverage] StringLiteral
src/mcp/server.ts:76:21
-           'remember', 'recall', 'forget',
+           'remember', "", 'forget',

[NoCoverage] StringLiteral
src/mcp/server.ts:76:31
-           'remember', 'recall', 'forget',
+           'remember', 'recall', "",

[NoCoverage] StringLiteral
src/mcp/server.ts:77:9
-           'trigger', 'check',
+           "", 'check',

[NoCoverage] StringLiteral
src/mcp/server.ts:77:20
-           'trigger', 'check',
+           'trigger', "",

[NoCoverage] StringLiteral
src/mcp/server.ts:78:9
-           'pref', 'prefs',
+           "", 'prefs',

[NoCoverage] StringLiteral
src/mcp/server.ts:78:17
-           'pref', 'prefs',
+           'pref', "",

[NoCoverage] StringLiteral
src/mcp/server.ts:79:9
-           'stats', 'decay',
+           "", 'decay',

[NoCoverage] StringLiteral
src/mcp/server.ts:79:18
-           'stats', 'decay',
+           'stats', "",

[NoCoverage] StringLiteral
src/mcp/server.ts:81:9
-           'remember=store-decision | recall=load-prior-context | forget=delete | ' +
+           "" +

[NoCoverage] StringLiteral
src/mcp/server.ts:82:9
-           'trigger=set-guard | check=fire-triggers | ' +
+           "" +

[NoCoverage] StringLiteral
src/mcp/server.ts:83:9
-           'pref=set-preference | prefs=get-preferences | ' +
+           "" +

[NoCoverage] StringLiteral
src/mcp/server.ts:84:9
-           'stats=summary | decay=run-decay',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:87:52
-         id:           z.string().optional().describe('Memory or trigger ID'),
+         id:           z.string().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:89:33
-                         .describe('Leave UNSET — it is derived from the repository you are in (e.g. github.com/owner/repo). Only pass it to address a different project on purpose.'),
+                         .describe(""),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:90:28
-         scope:        z.enum(['project', 'person', 'team']).optional()
+         scope:        z.enum([]).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:90:29
-         scope:        z.enum(['project', 'person', 'team']).optional()
+         scope:        z.enum(["", 'person', 'team']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:90:40
-         scope:        z.enum(['project', 'person', 'team']).optional()
+         scope:        z.enum(['project', "", 'team']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:90:50
-         scope:        z.enum(['project', 'person', 'team']).optional()
+         scope:        z.enum(['project', 'person', ""]).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:91:33
-                         .describe('project=true of this repo (default) | person=true of me anywhere, syncs across my machines | team=shared with the team. Crossing to another person is always deliberate: use the team tool to share.'),
+                         .describe(""),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:95:28
-         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination']).optional()
+         memory_type:  z.enum([]).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:95:29
-         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination']).optional()
+         memory_type:  z.enum(["", 'semantic', 'procedural', 'architectural', 'insight', 'coordination']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:95:41
-         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination']).optional()
+         memory_type:  z.enum(['episodic', "", 'procedural', 'architectural', 'insight', 'coordination']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:95:53
-         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination']).optional()
+         memory_type:  z.enum(['episodic', 'semantic', "", 'architectural', 'insight', 'coordination']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:95:67
-         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination']).optional()
+         memory_type:  z.enum(['episodic', 'semantic', 'procedural', "", 'insight', 'coordination']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:95:84
-         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination']).optional()
+         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', "", 'coordination']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:95:95
-         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination']).optional()
+         memory_type:  z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', ""]).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:96:33
-                         .describe('episodic=happened | semantic=is-true | procedural=how-to | architectural=why | insight=pattern | coordination=team-state'),
+                         .describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:98:53
-         confirmed:    z.boolean().optional().describe('Permanent truth — zero decay, Core tier'),
+         confirmed:    z.boolean().optional().describe(""),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:101:36
-         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination'])).optional(),
+         memory_types: z.array(z.enum([])).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:101:37
-         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination'])).optional(),
+         memory_types: z.array(z.enum(["", 'semantic', 'procedural', 'architectural', 'insight', 'coordination'])).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:101:49
-         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination'])).optional(),
+         memory_types: z.array(z.enum(['episodic', "", 'procedural', 'architectural', 'insight', 'coordination'])).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:101:61
-         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination'])).optional(),
+         memory_types: z.array(z.enum(['episodic', 'semantic', "", 'architectural', 'insight', 'coordination'])).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:101:75
-         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination'])).optional(),
+         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', "", 'insight', 'coordination'])).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:101:92
-         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination'])).optional(),
+         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', "", 'coordination'])).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:101:103
-         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', 'coordination'])).optional(),
+         memory_types: z.array(z.enum(['episodic', 'semantic', 'procedural', 'architectural', 'insight', ""])).optional(),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:102:36
-         tiers:        z.array(z.enum(['buffer', 'working', 'core'])).optional(),
+         tiers:        z.array(z.enum([])).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:102:37
-         tiers:        z.array(z.enum(['buffer', 'working', 'core'])).optional(),
+         tiers:        z.array(z.enum(["", 'working', 'core'])).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:102:47
-         tiers:        z.array(z.enum(['buffer', 'working', 'core'])).optional(),
+         tiers:        z.array(z.enum(['buffer', "", 'core'])).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:102:58
-         tiers:        z.array(z.enum(['buffer', 'working', 'core'])).optional(),
+         tiers:        z.array(z.enum(['buffer', 'working', ""])).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:106:54
-         trigger_action: z.string().optional().describe('Action keyword for trigger/check ops (e.g. "deploy")'),
+         trigger_action: z.string().optional().describe(""),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:107:30
-         severity:       z.enum(['critical', 'warning', 'info']).optional(),
+         severity:       z.enum([]).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:107:31
-         severity:       z.enum(['critical', 'warning', 'info']).optional(),
+         severity:       z.enum(["", 'warning', 'info']).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:107:43
-         severity:       z.enum(['critical', 'warning', 'info']).optional(),
+         severity:       z.enum(['critical', "", 'info']).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:107:54
-         severity:       z.enum(['critical', 'warning', 'info']).optional(),
+         severity:       z.enum(['critical', 'warning', ""]).optional(),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:111:26
-         strength:   z.enum(['strong', 'moderate', 'weak']).optional(),
+         strength:   z.enum([]).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:111:27
-         strength:   z.enum(['strong', 'moderate', 'weak']).optional(),
+         strength:   z.enum(["", 'moderate', 'weak']).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:111:37
-         strength:   z.enum(['strong', 'moderate', 'weak']).optional(),
+         strength:   z.enum(['strong', "", 'weak']).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:111:49
-         strength:   z.enum(['strong', 'moderate', 'weak']).optional(),
+         strength:   z.enum(['strong', 'moderate', ""]).optional(),

[NoCoverage] BlockStatement
src/mcp/server.ts:113:15
-       (args) => {
-         switch (args.action) {
-           case 'remember': {
-             // Project identity is DERIVED from the repository, not taken from whatever label the
-             // agent guessed this session (ADR-018 §2). An explicit argument still wins.
-             const identity = resolveProject(args.project);
-             const scope = (args.scope ?? 'project') as MemoryScope;
-             const memory = memSvc.remember({
-               content: args.content ?? '',
-               memoryType: (args.memory_type ?? 'semantic') as MemoryType,
-               // A person-scoped memory is about the developer, not the repo, so it is not pinned to
-               // one: pinning it would make it invisible from every other project.
-               project: scope === 'person' ? undefined : identity.id,
-               scope,
-               category: args.category,
-               tags: args.tags,
-               confirmed: args.confirmed,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify({
-               id: memory.id, tier: memory.tier, weight: memory.weight,
-               scope: memory.scope, project: memory.project,
-               // Tell the caller how the project was established: anything but `remote` is a fallback
-               // that may not join across machines (ADR-018 §2).
-               projectSource: identity.source,
-             }) }] };
-           }
-           case 'recall': {
-             // Default recall is "what is true about this repository, plus what is true about me".
-             // Person-scoped memories are not pinned to a project, so they must not be filtered out by
-             // the project predicate — hence two queries rather than one OR, which also keeps each
-             // query on an index.
-             const identity = resolveProject(args.project);
-             const scopes = args.scope ? [args.scope as MemoryScope] : undefined;
-             const limit = args.limit ?? 20;
-             const projectHits = (!scopes || scopes.includes('project'))
-               ? memSvc.recall({
-                   query: args.query ?? '',
-                   project: identity.id,
-                   category: args.category,
-                   memoryTypes: args.memory_types as MemoryType[] | undefined,
-                   tiers: args.tiers,
-                   scopes: ['project'],
-                   limit,
-                 })
-               : [];
-             const personHits = (!scopes || scopes.includes('person'))
-               ? memSvc.recall({
-                   query: args.query ?? '',
-                   category: args.category,
-                   memoryTypes: args.memory_types as MemoryType[] | undefined,
-                   tiers: args.tiers,
-                   scopes: ['person'],
-                   limit,
-                 })
-               : [];
-             const memories = [...projectHits, ...personHits]
-               .sort((a, b) => b.weight - a.weight)
-               .slice(0, limit);
-             for (const m of memories) {
-               try { memSvc.reinforce(m.id, 'RECALL_HIT'); } catch { /* non-fatal */ }
-             }
-             return {
-               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({
-                 id: m.id, content: m.content, memoryType: m.memoryType,
-                 tier: m.tier, weight: m.weight, project: m.project, scope: m.scope,
-                 category: m.category, tags: m.tags,
-               }))) }],
-             };
-           }
-           case 'forget': {
-             memSvc.forget(args.id ?? '', args.context);
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Deleted.' }) }] };
-           }
-           case 'trigger': {
-             const id = trigSvc.setTrigger({
-               action: args.trigger_action ?? '',
-               content: args.content ?? '',
-               severity: args.severity,
-               memoryId: args.id,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify({ id, message: 'Trigger set.' }) }] };
-           }
-           case 'check': {
-             const triggers = trigSvc.checkTriggers(args.trigger_action ?? '', args.project);
-             for (const t of triggers) {
-               if (t.memoryId) {
-                 try { memSvc.reinforce(t.memoryId, 'TRIGGER_HIT'); } catch { /* non-fatal */ }
-               }
-             }
-             return { content: [{ type: 'text', text: JSON.stringify(triggers) }] };
-           }
-           case 'pref': {
-             prefSvc.setPreference(args.pref_key ?? '', args.pref_value ?? '', args.context, args.strength, args.project);
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Preference set.' }) }] };
-           }
-           case 'prefs': {
-             const prefs = args.context
-               ? prefSvc.getRelevantPreferences(args.context, args.project)
-               : prefSvc.getPreferences(args.project);
-             return {
-               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({
-                 key: p.key, value: p.value, context: p.context, strength: p.strength, project: p.project,
-               }))) }],
-             };
-           }
-           case 'stats': {
-             const total   = memRepo.count(args.project ? { project: args.project } : undefined);
-             const buffer  = memRepo.count({ tier: 'buffer',  ...(args.project ? { project: args.project } : {}) });
-             const working = memRepo.count({ tier: 'working', ...(args.project ? { project: args.project } : {}) });
-             const core    = memRepo.count({ tier: 'core',    ...(args.project ? { project: args.project } : {}) });
-             return {
-               content: [{ type: 'text', text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],
-             };
-           }
-           case 'decay': {
-             const decayed  = memSvc.applyDecay();
-             const promoted = memSvc.evaluateTierPromotions();
-             return { content: [{ type: 'text', text: JSON.stringify({ memoriesDecayed: decayed, memoriesPromoted: promoted }) }] };
-           }
-           default:
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
-         }
-       },
+       (args) => {},

[NoCoverage] ConditionalExpression
src/mcp/server.ts:116:9
-           case 'remember': {
-             // Project identity is DERIVED from the repository, not taken from whatever label the
-             // agent guessed this session (ADR-018 §2). An explicit argument still wins.
-             const identity = resolveProject(args.project);
-             const scope = (args.scope ?? 'project') as MemoryScope;
-             const memory = memSvc.remember({
-               content: args.content ?? '',
-               memoryType: (args.memory_type ?? 'semantic') as MemoryType,
-               // A person-scoped memory is about the developer, not the repo, so it is not pinned to
-               // one: pinning it would make it invisible from every other project.
-               project: scope === 'person' ? undefined : identity.id,
-               scope,
-               category: args.category,
-               tags: args.tags,
-               confirmed: args.confirmed,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify({
-               id: memory.id, tier: memory.tier, weight: memory.weight,
-               scope: memory.scope, project: memory.project,
-               // Tell the caller how the project was established: anything but `remote` is a fallback
-               // that may not join across machines (ADR-018 §2).
-               projectSource: identity.source,
-             }) }] };
-           }
+           case 'remember':

[NoCoverage] StringLiteral
src/mcp/server.ts:116:14
-           case 'remember': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:116:26
-           case 'remember': {
-             // Project identity is DERIVED from the repository, not taken from whatever label the
-             // agent guessed this session (ADR-018 §2). An explicit argument still wins.
-             const identity = resolveProject(args.project);
-             const scope = (args.scope ?? 'project') as MemoryScope;
-             const memory = memSvc.remember({
-               content: args.content ?? '',
-               memoryType: (args.memory_type ?? 'semantic') as MemoryType,
-               // A person-scoped memory is about the developer, not the repo, so it is not pinned to
-               // one: pinning it would make it invisible from every other project.
-               project: scope === 'person' ? undefined : identity.id,
-               scope,
-               category: args.category,
-               tags: args.tags,
-               confirmed: args.confirmed,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify({
-               id: memory.id, tier: memory.tier, weight: memory.weight,
-               scope: memory.scope, project: memory.project,
-               // Tell the caller how the project was established: anything but `remote` is a fallback
-               // that may not join across machines (ADR-018 §2).
-               projectSource: identity.source,
-             }) }] };
-           }
+           case 'remember': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:121:42
-             const memory = memSvc.remember({
-               content: args.content ?? '',
-               memoryType: (args.memory_type ?? 'semantic') as MemoryType,
-               // A person-scoped memory is about the developer, not the repo, so it is not pinned to
-               // one: pinning it would make it invisible from every other project.
-               project: scope === 'person' ? undefined : identity.id,
-               scope,
-               category: args.category,
-               tags: args.tags,
-               confirmed: args.confirmed,
-             });
+             const memory = memSvc.remember({});

[NoCoverage] LogicalOperator
src/mcp/server.ts:122:22
-               content: args.content ?? '',
+               content: args.content && '',

[NoCoverage] StringLiteral
src/mcp/server.ts:122:38
-               content: args.content ?? '',
+               content: args.content ?? "Stryker was here!",

[NoCoverage] ConditionalExpression
src/mcp/server.ts:126:22
-               project: scope === 'person' ? undefined : identity.id,
+               project: true ? undefined : identity.id,

[NoCoverage] ConditionalExpression
src/mcp/server.ts:126:22
-               project: scope === 'person' ? undefined : identity.id,
+               project: false ? undefined : identity.id,

[NoCoverage] EqualityOperator
src/mcp/server.ts:126:22
-               project: scope === 'person' ? undefined : identity.id,
+               project: scope !== 'person' ? undefined : identity.id,

[NoCoverage] StringLiteral
src/mcp/server.ts:126:32
-               project: scope === 'person' ? undefined : identity.id,
+               project: scope === "" ? undefined : identity.id,

[NoCoverage] ObjectLiteral
src/mcp/server.ts:132:18
-             return { content: [{ type: 'text', text: JSON.stringify({
-               id: memory.id, tier: memory.tier, weight: memory.weight,
-               scope: memory.scope, project: memory.project,
-               // Tell the caller how the project was established: anything but `remote` is a fallback
-               // that may not join across machines (ADR-018 §2).
-               projectSource: identity.source,
-             }) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:132:29
-             return { content: [{ type: 'text', text: JSON.stringify({
-               id: memory.id, tier: memory.tier, weight: memory.weight,
-               scope: memory.scope, project: memory.project,
-               // Tell the caller how the project was established: anything but `remote` is a fallback
-               // that may not join across machines (ADR-018 §2).
-               projectSource: identity.source,
-             }) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:132:30
-             return { content: [{ type: 'text', text: JSON.stringify({
-               id: memory.id, tier: memory.tier, weight: memory.weight,
-               scope: memory.scope, project: memory.project,
-               // Tell the caller how the project was established: anything but `remote` is a fallback
-               // that may not join across machines (ADR-018 §2).
-               projectSource: identity.source,
-             }) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:132:38
-             return { content: [{ type: 'text', text: JSON.stringify({
+             return { content: [{ type: "", text: JSON.stringify({

[NoCoverage] ObjectLiteral
src/mcp/server.ts:132:67
-             return { content: [{ type: 'text', text: JSON.stringify({
-               id: memory.id, tier: memory.tier, weight: memory.weight,
-               scope: memory.scope, project: memory.project,
-               // Tell the caller how the project was established: anything but `remote` is a fallback
-               // that may not join across machines (ADR-018 §2).
-               projectSource: identity.source,
-             }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:141:9
-           case 'recall': {
-             // Default recall is "what is true about this repository, plus what is true about me".
-             // Person-scoped memories are not pinned to a project, so they must not be filtered out by
-             // the project predicate — hence two queries rather than one OR, which also keeps each
-             // query on an index.
-             const identity = resolveProject(args.project);
-             const scopes = args.scope ? [args.scope as MemoryScope] : undefined;
-             const limit = args.limit ?? 20;
-             const projectHits = (!scopes || scopes.includes('project'))
-               ? memSvc.recall({
-                   query: args.query ?? '',
-                   project: identity.id,
-                   category: args.category,
-                   memoryTypes: args.memory_types as MemoryType[] | undefined,
-                   tiers: args.tiers,
-                   scopes: ['project'],
-                   limit,
-                 })
-               : [];
-             const personHits = (!scopes || scopes.includes('person'))
-               ? memSvc.recall({
-                   query: args.query ?? '',
-                   category: args.category,
-                   memoryTypes: args.memory_types as MemoryType[] | undefined,
-                   tiers: args.tiers,
-                   scopes: ['person'],
-                   limit,
-                 })
-               : [];
-             const memories = [...projectHits, ...personHits]
-               .sort((a, b) => b.weight - a.weight)
-               .slice(0, limit);
-             for (const m of memories) {
-               try { memSvc.reinforce(m.id, 'RECALL_HIT'); } catch { /* non-fatal */ }
-             }
-             return {
-               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({
-                 id: m.id, content: m.content, memoryType: m.memoryType,
-                 tier: m.tier, weight: m.weight, project: m.project, scope: m.scope,
-                 category: m.category, tags: m.tags,
-               }))) }],
-             };
-           }
+           case 'recall':

[NoCoverage] StringLiteral
src/mcp/server.ts:141:14
-           case 'recall': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:141:24
-           case 'recall': {
-             // Default recall is "what is true about this repository, plus what is true about me".
-             // Person-scoped memories are not pinned to a project, so they must not be filtered out by
-             // the project predicate — hence two queries rather than one OR, which also keeps each
-             // query on an index.
-             const identity = resolveProject(args.project);
-             const scopes = args.scope ? [args.scope as MemoryScope] : undefined;
-             const limit = args.limit ?? 20;
-             const projectHits = (!scopes || scopes.includes('project'))
-               ? memSvc.recall({
-                   query: args.query ?? '',
-                   project: identity.id,
-                   category: args.category,
-                   memoryTypes: args.memory_types as MemoryType[] | undefined,
-                   tiers: args.tiers,
-                   scopes: ['project'],
-                   limit,
-                 })
-               : [];
-             const personHits = (!scopes || scopes.includes('person'))
-               ? memSvc.recall({
-                   query: args.query ?? '',
-                   category: args.category,
-                   memoryTypes: args.memory_types as MemoryType[] | undefined,
-                   tiers: args.tiers,
-                   scopes: ['person'],
-                   limit,
-                 })
-               : [];
-             const memories = [...projectHits, ...personHits]
-               .sort((a, b) => b.weight - a.weight)
-               .slice(0, limit);
-             for (const m of memories) {
-               try { memSvc.reinforce(m.id, 'RECALL_HIT'); } catch { /* non-fatal */ }
-             }
-             return {
-               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({
-                 id: m.id, content: m.content, memoryType: m.memoryType,
-                 tier: m.tier, weight: m.weight, project: m.project, scope: m.scope,
-                 category: m.category, tags: m.tags,
-               }))) }],
-             };
-           }
+           case 'recall': {}

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:147:39
-             const scopes = args.scope ? [args.scope as MemoryScope] : undefined;
+             const scopes = args.scope ? [] : undefined;

[NoCoverage] LogicalOperator
src/mcp/server.ts:148:25
-             const limit = args.limit ?? 20;
+             const limit = args.limit && 20;

[NoCoverage] ConditionalExpression
src/mcp/server.ts:150:32
-             const projectHits = (!scopes || scopes.includes('project'))
+             const projectHits = (true)

[NoCoverage] ConditionalExpression
src/mcp/server.ts:150:32
-             const projectHits = (!scopes || scopes.includes('project'))
+             const projectHits = (false)

[NoCoverage] LogicalOperator
src/mcp/server.ts:150:32
-             const projectHits = (!scopes || scopes.includes('project'))
+             const projectHits = (!scopes && scopes.includes('project'))

[NoCoverage] BooleanLiteral
src/mcp/server.ts:150:32
-             const projectHits = (!scopes || scopes.includes('project'))
+             const projectHits = (scopes || scopes.includes('project'))

[NoCoverage] StringLiteral
src/mcp/server.ts:150:59
-             const projectHits = (!scopes || scopes.includes('project'))
+             const projectHits = (!scopes || scopes.includes(""))

[NoCoverage] ObjectLiteral
src/mcp/server.ts:151:29
-               ? memSvc.recall({
-                   query: args.query ?? '',
-                   project: identity.id,
-                   category: args.category,
-                   memoryTypes: args.memory_types as MemoryType[] | undefined,
-                   tiers: args.tiers,
-                   scopes: ['project'],
-                   limit,
-                 })
+               ? memSvc.recall({})

[NoCoverage] LogicalOperator
src/mcp/server.ts:152:24
-                   query: args.query ?? '',
+                   query: args.query && '',

[NoCoverage] StringLiteral
src/mcp/server.ts:152:38
-                   query: args.query ?? '',
+                   query: args.query ?? "Stryker was here!",

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:157:25
-                   scopes: ['project'],
+                   scopes: [],

[NoCoverage] StringLiteral
src/mcp/server.ts:157:26
-                   scopes: ['project'],
+                   scopes: [""],

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:160:15
-               : [];
+               : ["Stryker was here"];

[NoCoverage] ConditionalExpression
src/mcp/server.ts:162:31
-             const personHits = (!scopes || scopes.includes('person'))
+             const personHits = (true)

[NoCoverage] ConditionalExpression
src/mcp/server.ts:162:31
-             const personHits = (!scopes || scopes.includes('person'))
+             const personHits = (false)

[NoCoverage] LogicalOperator
src/mcp/server.ts:162:31
-             const personHits = (!scopes || scopes.includes('person'))
+             const personHits = (!scopes && scopes.includes('person'))

[NoCoverage] BooleanLiteral
src/mcp/server.ts:162:31
-             const personHits = (!scopes || scopes.includes('person'))
+             const personHits = (scopes || scopes.includes('person'))

[NoCoverage] StringLiteral
src/mcp/server.ts:162:58
-             const personHits = (!scopes || scopes.includes('person'))
+             const personHits = (!scopes || scopes.includes(""))

[NoCoverage] ObjectLiteral
src/mcp/server.ts:163:29
-               ? memSvc.recall({
-                   query: args.query ?? '',
-                   category: args.category,
-                   memoryTypes: args.memory_types as MemoryType[] | undefined,
-                   tiers: args.tiers,
-                   scopes: ['person'],
-                   limit,
-                 })
+               ? memSvc.recall({})

[NoCoverage] LogicalOperator
src/mcp/server.ts:164:24
-                   query: args.query ?? '',
+                   query: args.query && '',

[NoCoverage] StringLiteral
src/mcp/server.ts:164:38
-                   query: args.query ?? '',
+                   query: args.query ?? "Stryker was here!",

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:168:25
-                   scopes: ['person'],
+                   scopes: [],

[NoCoverage] StringLiteral
src/mcp/server.ts:168:26
-                   scopes: ['person'],
+                   scopes: [""],

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:171:15
-               : [];
+               : ["Stryker was here"];

[NoCoverage] MethodExpression
src/mcp/server.ts:173:28
-             const memories = [...projectHits, ...personHits]
-               .sort((a, b) => b.weight - a.weight)
-               .slice(0, limit);
+             const memories = [...projectHits, ...personHits].sort((a, b) => b.weight - a.weight);

[NoCoverage] MethodExpression
src/mcp/server.ts:173:28
-             const memories = [...projectHits, ...personHits]
-               .sort((a, b) => b.weight - a.weight)
+             const memories = [...projectHits, ...personHits]

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:173:28
-             const memories = [...projectHits, ...personHits]
+             const memories = []

[NoCoverage] ArrowFunction
src/mcp/server.ts:174:19
-               .sort((a, b) => b.weight - a.weight)
+               .sort(() => undefined)

[NoCoverage] ArithmeticOperator
src/mcp/server.ts:174:29
-               .sort((a, b) => b.weight - a.weight)
+               .sort((a, b) => b.weight + a.weight)

[NoCoverage] BlockStatement
src/mcp/server.ts:176:37
-             for (const m of memories) {
-               try { memSvc.reinforce(m.id, 'RECALL_HIT'); } catch { /* non-fatal */ }
-             }
+             for (const m of memories) {}

[NoCoverage] BlockStatement
src/mcp/server.ts:177:17
-               try { memSvc.reinforce(m.id, 'RECALL_HIT'); } catch { /* non-fatal */ }
+               try {} catch { /* non-fatal */ }

[NoCoverage] StringLiteral
src/mcp/server.ts:177:42
-               try { memSvc.reinforce(m.id, 'RECALL_HIT'); } catch { /* non-fatal */ }
+               try { memSvc.reinforce(m.id, ""); } catch { /* non-fatal */ }

[NoCoverage] ObjectLiteral
src/mcp/server.ts:179:18
-             return {
-               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({
-                 id: m.id, content: m.content, memoryType: m.memoryType,
-                 tier: m.tier, weight: m.weight, project: m.project, scope: m.scope,
-                 category: m.category, tags: m.tags,
-               }))) }],
-             };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:180:22
-               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({
-                 id: m.id, content: m.content, memoryType: m.memoryType,
-                 tier: m.tier, weight: m.weight, project: m.project, scope: m.scope,
-                 category: m.category, tags: m.tags,
-               }))) }],
+               content: [],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:180:23
-               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({
-                 id: m.id, content: m.content, memoryType: m.memoryType,
-                 tier: m.tier, weight: m.weight, project: m.project, scope: m.scope,
-                 category: m.category, tags: m.tags,
-               }))) }],
+               content: [{}],

[NoCoverage] StringLiteral
src/mcp/server.ts:180:31
-               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({
+               content: [{ type: "", text: JSON.stringify(memories.map(m => ({

[NoCoverage] ArrowFunction
src/mcp/server.ts:180:73
-               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({
-                 id: m.id, content: m.content, memoryType: m.memoryType,
-                 tier: m.tier, weight: m.weight, project: m.project, scope: m.scope,
-                 category: m.category, tags: m.tags,
-               }))) }],
+               content: [{ type: 'text', text: JSON.stringify(memories.map(() => undefined)) }],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:180:79
-               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({
-                 id: m.id, content: m.content, memoryType: m.memoryType,
-                 tier: m.tier, weight: m.weight, project: m.project, scope: m.scope,
-                 category: m.category, tags: m.tags,
-               }))) }],
+               content: [{ type: 'text', text: JSON.stringify(memories.map(m => ({}))) }],

[NoCoverage] ConditionalExpression
src/mcp/server.ts:188:9
-           case 'forget': {
-             memSvc.forget(args.id ?? '', args.context);
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Deleted.' }) }] };
-           }
+           case 'forget':

[NoCoverage] StringLiteral
src/mcp/server.ts:188:14
-           case 'forget': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:188:24
-           case 'forget': {
-             memSvc.forget(args.id ?? '', args.context);
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Deleted.' }) }] };
-           }
+           case 'forget': {}

[NoCoverage] LogicalOperator
src/mcp/server.ts:189:25
-             memSvc.forget(args.id ?? '', args.context);
+             memSvc.forget(args.id && '', args.context);

[NoCoverage] StringLiteral
src/mcp/server.ts:189:36
-             memSvc.forget(args.id ?? '', args.context);
+             memSvc.forget(args.id ?? "Stryker was here!", args.context);

[NoCoverage] ObjectLiteral
src/mcp/server.ts:190:18
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Deleted.' }) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:190:29
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Deleted.' }) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:190:30
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Deleted.' }) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:190:38
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Deleted.' }) }] };
+             return { content: [{ type: "", text: JSON.stringify({ message: 'Deleted.' }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:190:67
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Deleted.' }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:190:78
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Deleted.' }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({ message: "" }) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:193:9
-           case 'trigger': {
-             const id = trigSvc.setTrigger({
-               action: args.trigger_action ?? '',
-               content: args.content ?? '',
-               severity: args.severity,
-               memoryId: args.id,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify({ id, message: 'Trigger set.' }) }] };
-           }
+           case 'trigger':

[NoCoverage] StringLiteral
src/mcp/server.ts:193:14
-           case 'trigger': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:193:25
-           case 'trigger': {
-             const id = trigSvc.setTrigger({
-               action: args.trigger_action ?? '',
-               content: args.content ?? '',
-               severity: args.severity,
-               memoryId: args.id,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify({ id, message: 'Trigger set.' }) }] };
-           }
+           case 'trigger': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:194:41
-             const id = trigSvc.setTrigger({
-               action: args.trigger_action ?? '',
-               content: args.content ?? '',
-               severity: args.severity,
-               memoryId: args.id,
-             });
+             const id = trigSvc.setTrigger({});

[NoCoverage] LogicalOperator
src/mcp/server.ts:195:21
-               action: args.trigger_action ?? '',
+               action: args.trigger_action && '',

[NoCoverage] StringLiteral
src/mcp/server.ts:195:44
-               action: args.trigger_action ?? '',
+               action: args.trigger_action ?? "Stryker was here!",

[NoCoverage] LogicalOperator
src/mcp/server.ts:196:22
-               content: args.content ?? '',
+               content: args.content && '',

[NoCoverage] StringLiteral
src/mcp/server.ts:196:38
-               content: args.content ?? '',
+               content: args.content ?? "Stryker was here!",

[NoCoverage] ObjectLiteral
src/mcp/server.ts:200:18
-             return { content: [{ type: 'text', text: JSON.stringify({ id, message: 'Trigger set.' }) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:200:29
-             return { content: [{ type: 'text', text: JSON.stringify({ id, message: 'Trigger set.' }) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:200:30
-             return { content: [{ type: 'text', text: JSON.stringify({ id, message: 'Trigger set.' }) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:200:38
-             return { content: [{ type: 'text', text: JSON.stringify({ id, message: 'Trigger set.' }) }] };
+             return { content: [{ type: "", text: JSON.stringify({ id, message: 'Trigger set.' }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:200:67
-             return { content: [{ type: 'text', text: JSON.stringify({ id, message: 'Trigger set.' }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:200:82
-             return { content: [{ type: 'text', text: JSON.stringify({ id, message: 'Trigger set.' }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({ id, message: "" }) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:203:9
-           case 'check': {
-             const triggers = trigSvc.checkTriggers(args.trigger_action ?? '', args.project);
-             for (const t of triggers) {
-               if (t.memoryId) {
-                 try { memSvc.reinforce(t.memoryId, 'TRIGGER_HIT'); } catch { /* non-fatal */ }
-               }
-             }
-             return { content: [{ type: 'text', text: JSON.stringify(triggers) }] };
-           }
+           case 'check':

[NoCoverage] StringLiteral
src/mcp/server.ts:203:14
-           case 'check': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:203:23
-           case 'check': {
-             const triggers = trigSvc.checkTriggers(args.trigger_action ?? '', args.project);
-             for (const t of triggers) {
-               if (t.memoryId) {
-                 try { memSvc.reinforce(t.memoryId, 'TRIGGER_HIT'); } catch { /* non-fatal */ }
-               }
-             }
-             return { content: [{ type: 'text', text: JSON.stringify(triggers) }] };
-           }
+           case 'check': {}

[NoCoverage] LogicalOperator
src/mcp/server.ts:204:50
-             const triggers = trigSvc.checkTriggers(args.trigger_action ?? '', args.project);
+             const triggers = trigSvc.checkTriggers(args.trigger_action && '', args.project);

[NoCoverage] StringLiteral
src/mcp/server.ts:204:73
-             const triggers = trigSvc.checkTriggers(args.trigger_action ?? '', args.project);
+             const triggers = trigSvc.checkTriggers(args.trigger_action ?? "Stryker was here!", args.project);

[NoCoverage] BlockStatement
src/mcp/server.ts:205:37
-             for (const t of triggers) {
-               if (t.memoryId) {
-                 try { memSvc.reinforce(t.memoryId, 'TRIGGER_HIT'); } catch { /* non-fatal */ }
-               }
-             }
+             for (const t of triggers) {}

[NoCoverage] ConditionalExpression
src/mcp/server.ts:206:17
-               if (t.memoryId) {
+               if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:206:17
-               if (t.memoryId) {
+               if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:206:29
-               if (t.memoryId) {
-                 try { memSvc.reinforce(t.memoryId, 'TRIGGER_HIT'); } catch { /* non-fatal */ }
-               }
+               if (t.memoryId) {}

[NoCoverage] BlockStatement
src/mcp/server.ts:207:19
-                 try { memSvc.reinforce(t.memoryId, 'TRIGGER_HIT'); } catch { /* non-fatal */ }
+                 try {} catch { /* non-fatal */ }

[NoCoverage] StringLiteral
src/mcp/server.ts:207:50
-                 try { memSvc.reinforce(t.memoryId, 'TRIGGER_HIT'); } catch { /* non-fatal */ }
+                 try { memSvc.reinforce(t.memoryId, ""); } catch { /* non-fatal */ }

[NoCoverage] ObjectLiteral
src/mcp/server.ts:210:18
-             return { content: [{ type: 'text', text: JSON.stringify(triggers) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:210:29
-             return { content: [{ type: 'text', text: JSON.stringify(triggers) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:210:30
-             return { content: [{ type: 'text', text: JSON.stringify(triggers) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:210:38
-             return { content: [{ type: 'text', text: JSON.stringify(triggers) }] };
+             return { content: [{ type: "", text: JSON.stringify(triggers) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:213:9
-           case 'pref': {
-             prefSvc.setPreference(args.pref_key ?? '', args.pref_value ?? '', args.context, args.strength, args.project);
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Preference set.' }) }] };
-           }
+           case 'pref':

[NoCoverage] StringLiteral
src/mcp/server.ts:213:14
-           case 'pref': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:213:22
-           case 'pref': {
-             prefSvc.setPreference(args.pref_key ?? '', args.pref_value ?? '', args.context, args.strength, args.project);
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Preference set.' }) }] };
-           }
+           case 'pref': {}

[NoCoverage] LogicalOperator
src/mcp/server.ts:214:33
-             prefSvc.setPreference(args.pref_key ?? '', args.pref_value ?? '', args.context, args.strength, args.project);
+             prefSvc.setPreference(args.pref_key && '', args.pref_value ?? '', args.context, args.strength, args.project);

[NoCoverage] StringLiteral
src/mcp/server.ts:214:50
-             prefSvc.setPreference(args.pref_key ?? '', args.pref_value ?? '', args.context, args.strength, args.project);
+             prefSvc.setPreference(args.pref_key ?? "Stryker was here!", args.pref_value ?? '', args.context, args.strength, args.project);

[NoCoverage] LogicalOperator
src/mcp/server.ts:214:54
-             prefSvc.setPreference(args.pref_key ?? '', args.pref_value ?? '', args.context, args.strength, args.project);
+             prefSvc.setPreference(args.pref_key ?? '', args.pref_value && '', args.context, args.strength, args.project);

[NoCoverage] StringLiteral
src/mcp/server.ts:214:73
-             prefSvc.setPreference(args.pref_key ?? '', args.pref_value ?? '', args.context, args.strength, args.project);
+             prefSvc.setPreference(args.pref_key ?? '', args.pref_value ?? "Stryker was here!", args.context, args.strength, args.project);

[NoCoverage] ObjectLiteral
src/mcp/server.ts:215:18
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Preference set.' }) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:215:29
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Preference set.' }) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:215:30
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Preference set.' }) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:215:38
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Preference set.' }) }] };
+             return { content: [{ type: "", text: JSON.stringify({ message: 'Preference set.' }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:215:67
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Preference set.' }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:215:78
-             return { content: [{ type: 'text', text: JSON.stringify({ message: 'Preference set.' }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({ message: "" }) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:218:9
-           case 'prefs': {
-             const prefs = args.context
-               ? prefSvc.getRelevantPreferences(args.context, args.project)
-               : prefSvc.getPreferences(args.project);
-             return {
-               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({
-                 key: p.key, value: p.value, context: p.context, strength: p.strength, project: p.project,
-               }))) }],
-             };
-           }
+           case 'prefs':

[NoCoverage] StringLiteral
src/mcp/server.ts:218:14
-           case 'prefs': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:218:23
-           case 'prefs': {
-             const prefs = args.context
-               ? prefSvc.getRelevantPreferences(args.context, args.project)
-               : prefSvc.getPreferences(args.project);
-             return {
-               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({
-                 key: p.key, value: p.value, context: p.context, strength: p.strength, project: p.project,
-               }))) }],
-             };
-           }
+           case 'prefs': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:222:18
-             return {
-               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({
-                 key: p.key, value: p.value, context: p.context, strength: p.strength, project: p.project,
-               }))) }],
-             };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:223:22
-               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({
-                 key: p.key, value: p.value, context: p.context, strength: p.strength, project: p.project,
-               }))) }],
+               content: [],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:223:23
-               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({
-                 key: p.key, value: p.value, context: p.context, strength: p.strength, project: p.project,
-               }))) }],
+               content: [{}],

[NoCoverage] StringLiteral
src/mcp/server.ts:223:31
-               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({
+               content: [{ type: "", text: JSON.stringify(prefs.map(p => ({

[NoCoverage] ArrowFunction
src/mcp/server.ts:223:70
-               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({
-                 key: p.key, value: p.value, context: p.context, strength: p.strength, project: p.project,
-               }))) }],
+               content: [{ type: 'text', text: JSON.stringify(prefs.map(() => undefined)) }],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:223:76
-               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({
-                 key: p.key, value: p.value, context: p.context, strength: p.strength, project: p.project,
-               }))) }],
+               content: [{ type: 'text', text: JSON.stringify(prefs.map(p => ({}))) }],

[NoCoverage] ConditionalExpression
src/mcp/server.ts:229:9
-           case 'stats': {
-             const total   = memRepo.count(args.project ? { project: args.project } : undefined);
-             const buffer  = memRepo.count({ tier: 'buffer',  ...(args.project ? { project: args.project } : {}) });
-             const working = memRepo.count({ tier: 'working', ...(args.project ? { project: args.project } : {}) });
-             const core    = memRepo.count({ tier: 'core',    ...(args.project ? { project: args.project } : {}) });
-             return {
-               content: [{ type: 'text', text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],
-             };
-           }
+           case 'stats':

[NoCoverage] StringLiteral
src/mcp/server.ts:229:14
-           case 'stats': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:229:23
-           case 'stats': {
-             const total   = memRepo.count(args.project ? { project: args.project } : undefined);
-             const buffer  = memRepo.count({ tier: 'buffer',  ...(args.project ? { project: args.project } : {}) });
-             const working = memRepo.count({ tier: 'working', ...(args.project ? { project: args.project } : {}) });
-             const core    = memRepo.count({ tier: 'core',    ...(args.project ? { project: args.project } : {}) });
-             return {
-               content: [{ type: 'text', text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],
-             };
-           }
+           case 'stats': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:230:56
-             const total   = memRepo.count(args.project ? { project: args.project } : undefined);
+             const total   = memRepo.count(args.project ? {} : undefined);

[NoCoverage] ObjectLiteral
src/mcp/server.ts:231:41
-             const buffer  = memRepo.count({ tier: 'buffer',  ...(args.project ? { project: args.project } : {}) });
+             const buffer  = memRepo.count({});

[NoCoverage] StringLiteral
src/mcp/server.ts:231:49
-             const buffer  = memRepo.count({ tier: 'buffer',  ...(args.project ? { project: args.project } : {}) });
+             const buffer  = memRepo.count({ tier: "",  ...(args.project ? { project: args.project } : {}) });

[NoCoverage] ObjectLiteral
src/mcp/server.ts:231:79
-             const buffer  = memRepo.count({ tier: 'buffer',  ...(args.project ? { project: args.project } : {}) });
+             const buffer  = memRepo.count({ tier: 'buffer',  ...(args.project ? {} : {}) });

[NoCoverage] ObjectLiteral
src/mcp/server.ts:232:41
-             const working = memRepo.count({ tier: 'working', ...(args.project ? { project: args.project } : {}) });
+             const working = memRepo.count({});

[NoCoverage] StringLiteral
src/mcp/server.ts:232:49
-             const working = memRepo.count({ tier: 'working', ...(args.project ? { project: args.project } : {}) });
+             const working = memRepo.count({ tier: "", ...(args.project ? { project: args.project } : {}) });

[NoCoverage] ObjectLiteral
src/mcp/server.ts:232:79
-             const working = memRepo.count({ tier: 'working', ...(args.project ? { project: args.project } : {}) });
+             const working = memRepo.count({ tier: 'working', ...(args.project ? {} : {}) });

[NoCoverage] ObjectLiteral
src/mcp/server.ts:233:41
-             const core    = memRepo.count({ tier: 'core',    ...(args.project ? { project: args.project } : {}) });
+             const core    = memRepo.count({});

[NoCoverage] StringLiteral
src/mcp/server.ts:233:49
-             const core    = memRepo.count({ tier: 'core',    ...(args.project ? { project: args.project } : {}) });
+             const core    = memRepo.count({ tier: "",    ...(args.project ? { project: args.project } : {}) });

[NoCoverage] ObjectLiteral
src/mcp/server.ts:233:79
-             const core    = memRepo.count({ tier: 'core',    ...(args.project ? { project: args.project } : {}) });
+             const core    = memRepo.count({ tier: 'core',    ...(args.project ? {} : {}) });

[NoCoverage] ObjectLiteral
src/mcp/server.ts:234:18
-             return {
-               content: [{ type: 'text', text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],
-             };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:235:22
-               content: [{ type: 'text', text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],
+               content: [],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:235:23
-               content: [{ type: 'text', text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],
+               content: [{}],

[NoCoverage] StringLiteral
src/mcp/server.ts:235:31
-               content: [{ type: 'text', text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],
+               content: [{ type: "", text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:235:60
-               content: [{ type: 'text', text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],
+               content: [{ type: 'text', text: JSON.stringify({}) }],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:235:77
-               content: [{ type: 'text', text: JSON.stringify({ total, byTier: { buffer, working, core }, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],
+               content: [{ type: 'text', text: JSON.stringify({ total, byTier: {}, project: args.project, boosts: REINFORCEMENT_BOOSTS }) }],

[NoCoverage] ConditionalExpression
src/mcp/server.ts:239:9
-           case 'decay': {
-             const decayed  = memSvc.applyDecay();
-             const promoted = memSvc.evaluateTierPromotions();
-             return { content: [{ type: 'text', text: JSON.stringify({ memoriesDecayed: decayed, memoriesPromoted: promoted }) }] };
-           }
+           case 'decay':

[NoCoverage] StringLiteral
src/mcp/server.ts:239:14
-           case 'decay': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:239:23
-           case 'decay': {
-             const decayed  = memSvc.applyDecay();
-             const promoted = memSvc.evaluateTierPromotions();
-             return { content: [{ type: 'text', text: JSON.stringify({ memoriesDecayed: decayed, memoriesPromoted: promoted }) }] };
-           }
+           case 'decay': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:242:18
-             return { content: [{ type: 'text', text: JSON.stringify({ memoriesDecayed: decayed, memoriesPromoted: promoted }) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:242:29
-             return { content: [{ type: 'text', text: JSON.stringify({ memoriesDecayed: decayed, memoriesPromoted: promoted }) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:242:30
-             return { content: [{ type: 'text', text: JSON.stringify({ memoriesDecayed: decayed, memoriesPromoted: promoted }) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:242:38
-             return { content: [{ type: 'text', text: JSON.stringify({ memoriesDecayed: decayed, memoriesPromoted: promoted }) }] };
+             return { content: [{ type: "", text: JSON.stringify({ memoriesDecayed: decayed, memoriesPromoted: promoted }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:242:67
-             return { content: [{ type: 'text', text: JSON.stringify({ memoriesDecayed: decayed, memoriesPromoted: promoted }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:245:9
-           default:
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+           default:

[NoCoverage] ObjectLiteral
src/mcp/server.ts:246:18
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:246:29
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:246:30
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:246:38
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{ type: "", text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:246:67
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:246:76
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({ error: `` }) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:255:5
-       'session',
+       "",

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:256:5
-       [
-         'Manage Chronicle session boundaries.',
-         '',
-         'Call session START at the beginning of substantive multi-turn work — this automatically injects the project\'s core memories (confirmed=true entries) into context, so the session begins with prior knowledge instead of an empty slate.',
-         '',
-         'Call session END at session close — applies memory decay + tier promotions, keeps the memory store from sprawling, and persists any session summary you pass.',
-         '',
-         'Call session RECOVER to resume an active session by ID or project name — useful when work was interrupted.',
-       ].join('\n'),
+       [].join('\n'),

[NoCoverage] StringLiteral
src/mcp/server.ts:257:7
-         'Manage Chronicle session boundaries.',
+         "",

[NoCoverage] StringLiteral
src/mcp/server.ts:258:7
-         '',
+         "Stryker was here!",

[NoCoverage] StringLiteral
src/mcp/server.ts:259:7
-         'Call session START at the beginning of substantive multi-turn work — this automatically injects the project\'s core memories (confirmed=true entries) into context, so the session begins with prior knowledge instead of an empty slate.',
+         "",

[NoCoverage] StringLiteral
src/mcp/server.ts:260:7
-         '',
+         "Stryker was here!",

[NoCoverage] StringLiteral
src/mcp/server.ts:261:7
-         'Call session END at session close — applies memory decay + tier promotions, keeps the memory store from sprawling, and persists any session summary you pass.',
+         "",

[NoCoverage] StringLiteral
src/mcp/server.ts:262:7
-         '',
+         "Stryker was here!",

[NoCoverage] StringLiteral
src/mcp/server.ts:263:7
-         'Call session RECOVER to resume an active session by ID or project name — useful when work was interrupted.',
+         "",

[NoCoverage] StringLiteral
src/mcp/server.ts:264:12
-       ].join('\n'),
+       ].join(""),

[NoCoverage] ObjectLiteral
src/mcp/server.ts:265:5
-       {
-         action:  z.enum(['start', 'end', 'recover']).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),
-         project: z.string().optional(),
-         device:  z.string().optional(),
-         id:      z.string().optional(),
-         summary: z.string().optional(),
-       },
+       {},

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:266:23
-         action:  z.enum(['start', 'end', 'recover']).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),
+         action:  z.enum([]).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),

[NoCoverage] StringLiteral
src/mcp/server.ts:266:24
-         action:  z.enum(['start', 'end', 'recover']).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),
+         action:  z.enum(["", 'end', 'recover']).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),

[NoCoverage] StringLiteral
src/mcp/server.ts:266:33
-         action:  z.enum(['start', 'end', 'recover']).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),
+         action:  z.enum(['start', "", 'recover']).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),

[NoCoverage] StringLiteral
src/mcp/server.ts:266:40
-         action:  z.enum(['start', 'end', 'recover']).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),
+         action:  z.enum(['start', 'end', ""]).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),

[NoCoverage] StringLiteral
src/mcp/server.ts:266:61
-         action:  z.enum(['start', 'end', 'recover']).describe('start=load-core-memories | end=decay-and-promote | recover=resume-interrupted-session'),
+         action:  z.enum(['start', 'end', 'recover']).describe(""),

[NoCoverage] BlockStatement
src/mcp/server.ts:272:21
-       async (args) => {
-         switch (args.action) {
-           case 'start': {
-             // Multi-machine is on whenever a remote is configured — no second flag (ADR-018 §3).
-             // Pull first so the session begins with what another machine learned. Fire-and-forget:
-             // a session must start even with no network, and `skipped: true` is the normal answer for
-             // the single-machine case (ADR-010 §3).
-             const cfgStart = getConfig();
-             if (cfgStart.railwayUrl) {
-               void syncMemories().catch(() => { /* offline — local store is the source of truth */ });
-               void syncInsights().catch(() => { /* offline — non-fatal */ });
-             }
-             const sess = sessSvc.startSession(args.project ?? resolveProject().id, args.device);
-             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: ['core'], limit: 10 });
-             for (const m of coreMemories) {
-               try { memSvc.reinforce(m.id, 'CONTEXT_INJECT'); } catch { /* non-fatal */ }
-             }
-             return {
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 coreMemories: coreMemories.map(m => ({ id: m.id, content: m.content, memoryType: m.memoryType })),
-               }) }],
-             };
-           }
-           case 'end': {
-             // Resolve the same way `recover` does: an id if given, otherwise the project's active
-             // session. Without this fallback the documented F7 flow is impossible — an agent that
-             // called `start` with a project and did not retain the returned id could never end it,
-             // and got `Session not found: ` with an empty id. Found by scripts/smoke-mcp.mjs.
-             // Resolve the project the same way `start` does, or the symmetry breaks: `start` with no
-             // argument opens a session on the derived repository identity, so `end` with no argument
-             // must close that one rather than demand an id the caller never saw (ADR-018 §2).
-             const endProject = args.project ?? resolveProject().id;
-             const sessionId = args.id ?? sessSvc.getActiveSession(endProject)?.id;
-             if (!sessionId) {
-               return { content: [{ type: 'text', text: JSON.stringify({
-                 error: `No active session for project "${endProject}". Pass an id, or call session start first.`,
-                 project: endProject,
-               }) }] };
-             }
-             const sess     = sessSvc.endSession(sessionId, args.summary);
-             const decayed  = memSvc.applyDecay();
-             const promoted = memSvc.evaluateTierPromotions();
-             // Push AFTER decay and promotion, so the other machines receive settled weights and tiers
-             // rather than a mid-maintenance snapshot. Awaited, unlike the pull at session start: this
-             // is the last thing the session does, and losing the push would lose the session's work.
-             let mirrored: { pushed: number; pulled: number; skipped: boolean } | undefined;
-             const cfgEnd = getConfig();
-             if (cfgEnd.railwayUrl) {
-               try {
-                 const [mem] = await Promise.all([
-                   syncMemories(),
-                   syncInsights().catch(() => undefined),
-                   pushSessionSummary(sess).catch(() => undefined),
-                 ]);
-                 mirrored = { pushed: mem.pushed, pulled: mem.pulled, skipped: mem.skipped };
-               } catch { /* offline — the local store keeps everything; the next boundary retries */ }
-             }
-             // Opportunistic team-knowledge sync at session close (non-fatal, offline-safe).
-             const cfg = getConfig();
-             if (cfg.teamToken && cfg.teamId) teamSyncSvc.sync().catch(() => { /* offline — non-fatal */ });
-             return {
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, status: sess.status, endedAt: sess.endedAt,
-                 maintenance: { memoriesDecayed: decayed, memoriesPromoted: promoted },
-                 // Absent when no remote is configured, which is the ordinary single-machine case.
-                 ...(mirrored ? { mirror: mirrored } : {}),
-               }) }],
-             };
-           }
-           case 'recover': {
-             const sess = args.id
-               ? sessSvc.recoverSession(args.id)
-               : args.project ? sessSvc.getActiveSession(args.project) : null;
-             return {
-               content: [{ type: 'text', text: JSON.stringify(sess ? {
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 activeTasks: sess.activeTasks, pendingDecisions: sess.pendingDecisions,
-                 touchedFiles: sess.touchedFiles, summary: sess.summary,
-               } : { message: 'No session found.' }) }],
-             };
-           }
-           default:
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
-         }
-       },
+       async (args) => {},

[NoCoverage] ConditionalExpression
src/mcp/server.ts:275:9
-           case 'start': {
-             // Multi-machine is on whenever a remote is configured — no second flag (ADR-018 §3).
-             // Pull first so the session begins with what another machine learned. Fire-and-forget:
-             // a session must start even with no network, and `skipped: true` is the normal answer for
-             // the single-machine case (ADR-010 §3).
-             const cfgStart = getConfig();
-             if (cfgStart.railwayUrl) {
-               void syncMemories().catch(() => { /* offline — local store is the source of truth */ });
-               void syncInsights().catch(() => { /* offline — non-fatal */ });
-             }
-             const sess = sessSvc.startSession(args.project ?? resolveProject().id, args.device);
-             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: ['core'], limit: 10 });
-             for (const m of coreMemories) {
-               try { memSvc.reinforce(m.id, 'CONTEXT_INJECT'); } catch { /* non-fatal */ }
-             }
-             return {
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 coreMemories: coreMemories.map(m => ({ id: m.id, content: m.content, memoryType: m.memoryType })),
-               }) }],
-             };
-           }
+           case 'start':

[NoCoverage] StringLiteral
src/mcp/server.ts:275:14
-           case 'start': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:275:23
-           case 'start': {
-             // Multi-machine is on whenever a remote is configured — no second flag (ADR-018 §3).
-             // Pull first so the session begins with what another machine learned. Fire-and-forget:
-             // a session must start even with no network, and `skipped: true` is the normal answer for
-             // the single-machine case (ADR-010 §3).
-             const cfgStart = getConfig();
-             if (cfgStart.railwayUrl) {
-               void syncMemories().catch(() => { /* offline — local store is the source of truth */ });
-               void syncInsights().catch(() => { /* offline — non-fatal */ });
-             }
-             const sess = sessSvc.startSession(args.project ?? resolveProject().id, args.device);
-             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: ['core'], limit: 10 });
-             for (const m of coreMemories) {
-               try { memSvc.reinforce(m.id, 'CONTEXT_INJECT'); } catch { /* non-fatal */ }
-             }
-             return {
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 coreMemories: coreMemories.map(m => ({ id: m.id, content: m.content, memoryType: m.memoryType })),
-               }) }],
-             };
-           }
+           case 'start': {}

[NoCoverage] ConditionalExpression
src/mcp/server.ts:281:15
-             if (cfgStart.railwayUrl) {
+             if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:281:15
-             if (cfgStart.railwayUrl) {
+             if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:281:36
-             if (cfgStart.railwayUrl) {
-               void syncMemories().catch(() => { /* offline — local store is the source of truth */ });
-               void syncInsights().catch(() => { /* offline — non-fatal */ });
-             }
+             if (cfgStart.railwayUrl) {}

[NoCoverage] LogicalOperator
src/mcp/server.ts:285:45
-             const sess = sessSvc.startSession(args.project ?? resolveProject().id, args.device);
+             const sess = sessSvc.startSession(args.project && resolveProject().id, args.device);

[NoCoverage] ObjectLiteral
src/mcp/server.ts:286:46
-             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: ['core'], limit: 10 });
+             const coreMemories = memSvc.recall({});

[NoCoverage] LogicalOperator
src/mcp/server.ts:286:55
-             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: ['core'], limit: 10 });
+             const coreMemories = memSvc.recall({ query: args.project && '', project: args.project, tiers: ['core'], limit: 10 });

[NoCoverage] StringLiteral
src/mcp/server.ts:286:71
-             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: ['core'], limit: 10 });
+             const coreMemories = memSvc.recall({ query: args.project ?? "Stryker was here!", project: args.project, tiers: ['core'], limit: 10 });

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:286:105
-             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: ['core'], limit: 10 });
+             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: [], limit: 10 });

[NoCoverage] StringLiteral
src/mcp/server.ts:286:106
-             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: ['core'], limit: 10 });
+             const coreMemories = memSvc.recall({ query: args.project ?? '', project: args.project, tiers: [""], limit: 10 });

[NoCoverage] BlockStatement
src/mcp/server.ts:287:41
-             for (const m of coreMemories) {
-               try { memSvc.reinforce(m.id, 'CONTEXT_INJECT'); } catch { /* non-fatal */ }
-             }
+             for (const m of coreMemories) {}

[NoCoverage] BlockStatement
src/mcp/server.ts:288:17
-               try { memSvc.reinforce(m.id, 'CONTEXT_INJECT'); } catch { /* non-fatal */ }
+               try {} catch { /* non-fatal */ }

[NoCoverage] StringLiteral
src/mcp/server.ts:288:42
-               try { memSvc.reinforce(m.id, 'CONTEXT_INJECT'); } catch { /* non-fatal */ }
+               try { memSvc.reinforce(m.id, ""); } catch { /* non-fatal */ }

[NoCoverage] ObjectLiteral
src/mcp/server.ts:290:18
-             return {
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 coreMemories: coreMemories.map(m => ({ id: m.id, content: m.content, memoryType: m.memoryType })),
-               }) }],
-             };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:291:22
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 coreMemories: coreMemories.map(m => ({ id: m.id, content: m.content, memoryType: m.memoryType })),
-               }) }],
+               content: [],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:291:23
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 coreMemories: coreMemories.map(m => ({ id: m.id, content: m.content, memoryType: m.memoryType })),
-               }) }],
+               content: [{}],

[NoCoverage] StringLiteral
src/mcp/server.ts:291:31
-               content: [{ type: 'text', text: JSON.stringify({
+               content: [{ type: "", text: JSON.stringify({

[NoCoverage] ObjectLiteral
src/mcp/server.ts:291:60
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 coreMemories: coreMemories.map(m => ({ id: m.id, content: m.content, memoryType: m.memoryType })),
-               }) }],
+               content: [{ type: 'text', text: JSON.stringify({}) }],

[NoCoverage] ArrowFunction
src/mcp/server.ts:293:46
-                 coreMemories: coreMemories.map(m => ({ id: m.id, content: m.content, memoryType: m.memoryType })),
+                 coreMemories: coreMemories.map(() => undefined),

[NoCoverage] ObjectLiteral
src/mcp/server.ts:293:52
-                 coreMemories: coreMemories.map(m => ({ id: m.id, content: m.content, memoryType: m.memoryType })),
+                 coreMemories: coreMemories.map(m => ({})),

[NoCoverage] ConditionalExpression
src/mcp/server.ts:298:9
-           case 'end': {
-             // Resolve the same way `recover` does: an id if given, otherwise the project's active
-             // session. Without this fallback the documented F7 flow is impossible — an agent that
-             // called `start` with a project and did not retain the returned id could never end it,
-             // and got `Session not found: ` with an empty id. Found by scripts/smoke-mcp.mjs.
-             // Resolve the project the same way `start` does, or the symmetry breaks: `start` with no
-             // argument opens a session on the derived repository identity, so `end` with no argument
-             // must close that one rather than demand an id the caller never saw (ADR-018 §2).
-             const endProject = args.project ?? resolveProject().id;
-             const sessionId = args.id ?? sessSvc.getActiveSession(endProject)?.id;
-             if (!sessionId) {
-               return { content: [{ type: 'text', text: JSON.stringify({
-                 error: `No active session for project "${endProject}". Pass an id, or call session start first.`,
-                 project: endProject,
-               }) }] };
-             }
-             const sess     = sessSvc.endSession(sessionId, args.summary);
-             const decayed  = memSvc.applyDecay();
-             const promoted = memSvc.evaluateTierPromotions();
-             // Push AFTER decay and promotion, so the other machines receive settled weights and tiers
-             // rather than a mid-maintenance snapshot. Awaited, unlike the pull at session start: this
-             // is the last thing the session does, and losing the push would lose the session's work.
-             let mirrored: { pushed: number; pulled: number; skipped: boolean } | undefined;
-             const cfgEnd = getConfig();
-             if (cfgEnd.railwayUrl) {
-               try {
-                 const [mem] = await Promise.all([
-                   syncMemories(),
-                   syncInsights().catch(() => undefined),
-                   pushSessionSummary(sess).catch(() => undefined),
-                 ]);
-                 mirrored = { pushed: mem.pushed, pulled: mem.pulled, skipped: mem.skipped };
-               } catch { /* offline — the local store keeps everything; the next boundary retries */ }
-             }
-             // Opportunistic team-knowledge sync at session close (non-fatal, offline-safe).
-             const cfg = getConfig();
-             if (cfg.teamToken && cfg.teamId) teamSyncSvc.sync().catch(() => { /* offline — non-fatal */ });
-             return {
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, status: sess.status, endedAt: sess.endedAt,
-                 maintenance: { memoriesDecayed: decayed, memoriesPromoted: promoted },
-                 // Absent when no remote is configured, which is the ordinary single-machine case.
-                 ...(mirrored ? { mirror: mirrored } : {}),
-               }) }],
-             };
-           }
+           case 'end':

[NoCoverage] StringLiteral
src/mcp/server.ts:298:14
-           case 'end': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:298:21
-           case 'end': {
-             // Resolve the same way `recover` does: an id if given, otherwise the project's active
-             // session. Without this fallback the documented F7 flow is impossible — an agent that
-             // called `start` with a project and did not retain the returned id could never end it,
-             // and got `Session not found: ` with an empty id. Found by scripts/smoke-mcp.mjs.
-             // Resolve the project the same way `start` does, or the symmetry breaks: `start` with no
-             // argument opens a session on the derived repository identity, so `end` with no argument
-             // must close that one rather than demand an id the caller never saw (ADR-018 §2).
-             const endProject = args.project ?? resolveProject().id;
-             const sessionId = args.id ?? sessSvc.getActiveSession(endProject)?.id;
-             if (!sessionId) {
-               return { content: [{ type: 'text', text: JSON.stringify({
-                 error: `No active session for project "${endProject}". Pass an id, or call session start first.`,
-                 project: endProject,
-               }) }] };
-             }
-             const sess     = sessSvc.endSession(sessionId, args.summary);
-             const decayed  = memSvc.applyDecay();
-             const promoted = memSvc.evaluateTierPromotions();
-             // Push AFTER decay and promotion, so the other machines receive settled weights and tiers
-             // rather than a mid-maintenance snapshot. Awaited, unlike the pull at session start: this
-             // is the last thing the session does, and losing the push would lose the session's work.
-             let mirrored: { pushed: number; pulled: number; skipped: boolean } | undefined;
-             const cfgEnd = getConfig();
-             if (cfgEnd.railwayUrl) {
-               try {
-                 const [mem] = await Promise.all([
-                   syncMemories(),
-                   syncInsights().catch(() => undefined),
-                   pushSessionSummary(sess).catch(() => undefined),
-                 ]);
-                 mirrored = { pushed: mem.pushed, pulled: mem.pulled, skipped: mem.skipped };
-               } catch { /* offline — the local store keeps everything; the next boundary retries */ }
-             }
-             // Opportunistic team-knowledge sync at session close (non-fatal, offline-safe).
-             const cfg = getConfig();
-             if (cfg.teamToken && cfg.teamId) teamSyncSvc.sync().catch(() => { /* offline — non-fatal */ });
-             return {
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, status: sess.status, endedAt: sess.endedAt,
-                 maintenance: { memoriesDecayed: decayed, memoriesPromoted: promoted },
-                 // Absent when no remote is configured, which is the ordinary single-machine case.
-                 ...(mirrored ? { mirror: mirrored } : {}),
-               }) }],
-             };
-           }
+           case 'end': {}

[NoCoverage] LogicalOperator
src/mcp/server.ts:306:30
-             const endProject = args.project ?? resolveProject().id;
+             const endProject = args.project && resolveProject().id;

[NoCoverage] LogicalOperator
src/mcp/server.ts:307:29
-             const sessionId = args.id ?? sessSvc.getActiveSession(endProject)?.id;
+             const sessionId = args.id && sessSvc.getActiveSession(endProject)?.id;

[NoCoverage] OptionalChaining
src/mcp/server.ts:307:40
-             const sessionId = args.id ?? sessSvc.getActiveSession(endProject)?.id;
+             const sessionId = args.id ?? sessSvc.getActiveSession(endProject).id;

[NoCoverage] BooleanLiteral
src/mcp/server.ts:308:15
-             if (!sessionId) {
+             if (sessionId) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:308:15
-             if (!sessionId) {
+             if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:308:15
-             if (!sessionId) {
+             if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:308:27
-             if (!sessionId) {
-               return { content: [{ type: 'text', text: JSON.stringify({
-                 error: `No active session for project "${endProject}". Pass an id, or call session start first.`,
-                 project: endProject,
-               }) }] };
-             }
+             if (!sessionId) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:309:20
-               return { content: [{ type: 'text', text: JSON.stringify({
-                 error: `No active session for project "${endProject}". Pass an id, or call session start first.`,
-                 project: endProject,
-               }) }] };
+               return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:309:31
-               return { content: [{ type: 'text', text: JSON.stringify({
-                 error: `No active session for project "${endProject}". Pass an id, or call session start first.`,
-                 project: endProject,
-               }) }] };
+               return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:309:32
-               return { content: [{ type: 'text', text: JSON.stringify({
-                 error: `No active session for project "${endProject}". Pass an id, or call session start first.`,
-                 project: endProject,
-               }) }] };
+               return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:309:40
-               return { content: [{ type: 'text', text: JSON.stringify({
+               return { content: [{ type: "", text: JSON.stringify({

[NoCoverage] ObjectLiteral
src/mcp/server.ts:309:69
-               return { content: [{ type: 'text', text: JSON.stringify({
-                 error: `No active session for project "${endProject}". Pass an id, or call session start first.`,
-                 project: endProject,
-               }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:310:22
-                 error: `No active session for project "${endProject}". Pass an id, or call session start first.`,
+                 error: ``,

[NoCoverage] ConditionalExpression
src/mcp/server.ts:323:15
-             if (cfgEnd.railwayUrl) {
+             if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:323:15
-             if (cfgEnd.railwayUrl) {
+             if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:323:34
-             if (cfgEnd.railwayUrl) {
-               try {
-                 const [mem] = await Promise.all([
-                   syncMemories(),
-                   syncInsights().catch(() => undefined),
-                   pushSessionSummary(sess).catch(() => undefined),
-                 ]);
-                 mirrored = { pushed: mem.pushed, pulled: mem.pulled, skipped: mem.skipped };
-               } catch { /* offline — the local store keeps everything; the next boundary retries */ }
-             }
+             if (cfgEnd.railwayUrl) {}

[NoCoverage] BlockStatement
src/mcp/server.ts:324:17
-               try {
-                 const [mem] = await Promise.all([
-                   syncMemories(),
-                   syncInsights().catch(() => undefined),
-                   pushSessionSummary(sess).catch(() => undefined),
-                 ]);
-                 mirrored = { pushed: mem.pushed, pulled: mem.pulled, skipped: mem.skipped };
-               } catch { /* offline — the local store keeps everything; the next boundary retries */ }
+               try {} catch { /* offline — the local store keeps everything; the next boundary retries */ }

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:325:47
-                 const [mem] = await Promise.all([
-                   syncMemories(),
-                   syncInsights().catch(() => undefined),
-                   pushSessionSummary(sess).catch(() => undefined),
-                 ]);
+                 const [mem] = await Promise.all([]);

[NoCoverage] ObjectLiteral
src/mcp/server.ts:330:26
-                 mirrored = { pushed: mem.pushed, pulled: mem.pulled, skipped: mem.skipped };
+                 mirrored = {};

[NoCoverage] ConditionalExpression
src/mcp/server.ts:335:15
-             if (cfg.teamToken && cfg.teamId) teamSyncSvc.sync().catch(() => { /* offline — non-fatal */ });
+             if (true) teamSyncSvc.sync().catch(() => { /* offline — non-fatal */ });

[NoCoverage] ConditionalExpression
src/mcp/server.ts:335:15
-             if (cfg.teamToken && cfg.teamId) teamSyncSvc.sync().catch(() => { /* offline — non-fatal */ });
+             if (false) teamSyncSvc.sync().catch(() => { /* offline — non-fatal */ });

[NoCoverage] LogicalOperator
src/mcp/server.ts:335:15
-             if (cfg.teamToken && cfg.teamId) teamSyncSvc.sync().catch(() => { /* offline — non-fatal */ });
+             if (cfg.teamToken || cfg.teamId) teamSyncSvc.sync().catch(() => { /* offline — non-fatal */ });

[NoCoverage] ObjectLiteral
src/mcp/server.ts:336:18
-             return {
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, status: sess.status, endedAt: sess.endedAt,
-                 maintenance: { memoriesDecayed: decayed, memoriesPromoted: promoted },
-                 // Absent when no remote is configured, which is the ordinary single-machine case.
-                 ...(mirrored ? { mirror: mirrored } : {}),
-               }) }],
-             };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:337:22
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, status: sess.status, endedAt: sess.endedAt,
-                 maintenance: { memoriesDecayed: decayed, memoriesPromoted: promoted },
-                 // Absent when no remote is configured, which is the ordinary single-machine case.
-                 ...(mirrored ? { mirror: mirrored } : {}),
-               }) }],
+               content: [],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:337:23
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, status: sess.status, endedAt: sess.endedAt,
-                 maintenance: { memoriesDecayed: decayed, memoriesPromoted: promoted },
-                 // Absent when no remote is configured, which is the ordinary single-machine case.
-                 ...(mirrored ? { mirror: mirrored } : {}),
-               }) }],
+               content: [{}],

[NoCoverage] StringLiteral
src/mcp/server.ts:337:31
-               content: [{ type: 'text', text: JSON.stringify({
+               content: [{ type: "", text: JSON.stringify({

[NoCoverage] ObjectLiteral
src/mcp/server.ts:337:60
-               content: [{ type: 'text', text: JSON.stringify({
-                 id: sess.id, status: sess.status, endedAt: sess.endedAt,
-                 maintenance: { memoriesDecayed: decayed, memoriesPromoted: promoted },
-                 // Absent when no remote is configured, which is the ordinary single-machine case.
-                 ...(mirrored ? { mirror: mirrored } : {}),
-               }) }],
+               content: [{ type: 'text', text: JSON.stringify({}) }],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:339:28
-                 maintenance: { memoriesDecayed: decayed, memoriesPromoted: promoted },
+                 maintenance: {},

[NoCoverage] ObjectLiteral
src/mcp/server.ts:341:30
-                 ...(mirrored ? { mirror: mirrored } : {}),
+                 ...(mirrored ? {} : {}),

[NoCoverage] ConditionalExpression
src/mcp/server.ts:346:9
-           case 'recover': {
-             const sess = args.id
-               ? sessSvc.recoverSession(args.id)
-               : args.project ? sessSvc.getActiveSession(args.project) : null;
-             return {
-               content: [{ type: 'text', text: JSON.stringify(sess ? {
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 activeTasks: sess.activeTasks, pendingDecisions: sess.pendingDecisions,
-                 touchedFiles: sess.touchedFiles, summary: sess.summary,
-               } : { message: 'No session found.' }) }],
-             };
-           }
+           case 'recover':

[NoCoverage] StringLiteral
src/mcp/server.ts:346:14
-           case 'recover': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:346:25
-           case 'recover': {
-             const sess = args.id
-               ? sessSvc.recoverSession(args.id)
-               : args.project ? sessSvc.getActiveSession(args.project) : null;
-             return {
-               content: [{ type: 'text', text: JSON.stringify(sess ? {
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 activeTasks: sess.activeTasks, pendingDecisions: sess.pendingDecisions,
-                 touchedFiles: sess.touchedFiles, summary: sess.summary,
-               } : { message: 'No session found.' }) }],
-             };
-           }
+           case 'recover': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:350:18
-             return {
-               content: [{ type: 'text', text: JSON.stringify(sess ? {
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 activeTasks: sess.activeTasks, pendingDecisions: sess.pendingDecisions,
-                 touchedFiles: sess.touchedFiles, summary: sess.summary,
-               } : { message: 'No session found.' }) }],
-             };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:351:22
-               content: [{ type: 'text', text: JSON.stringify(sess ? {
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 activeTasks: sess.activeTasks, pendingDecisions: sess.pendingDecisions,
-                 touchedFiles: sess.touchedFiles, summary: sess.summary,
-               } : { message: 'No session found.' }) }],
+               content: [],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:351:23
-               content: [{ type: 'text', text: JSON.stringify(sess ? {
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 activeTasks: sess.activeTasks, pendingDecisions: sess.pendingDecisions,
-                 touchedFiles: sess.touchedFiles, summary: sess.summary,
-               } : { message: 'No session found.' }) }],
+               content: [{}],

[NoCoverage] StringLiteral
src/mcp/server.ts:351:31
-               content: [{ type: 'text', text: JSON.stringify(sess ? {
+               content: [{ type: "", text: JSON.stringify(sess ? {

[NoCoverage] ObjectLiteral
src/mcp/server.ts:351:67
-               content: [{ type: 'text', text: JSON.stringify(sess ? {
-                 id: sess.id, project: sess.project, status: sess.status, startedAt: sess.startedAt,
-                 activeTasks: sess.activeTasks, pendingDecisions: sess.pendingDecisions,
-                 touchedFiles: sess.touchedFiles, summary: sess.summary,
-               } : { message: 'No session found.' }) }],
+               content: [{ type: 'text', text: JSON.stringify(sess ? {} : { message: 'No session found.' }) }],

[NoCoverage] ObjectLiteral
src/mcp/server.ts:355:17
-               } : { message: 'No session found.' }) }],
+               } : {}) }],

[NoCoverage] StringLiteral
src/mcp/server.ts:355:28
-               } : { message: 'No session found.' }) }],
+               } : { message: "" }) }],

[NoCoverage] ConditionalExpression
src/mcp/server.ts:359:9
-           default:
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+           default:

[NoCoverage] ObjectLiteral
src/mcp/server.ts:360:18
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:360:29
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:360:30
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:360:38
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{ type: "", text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:360:67
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:360:76
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({ error: `` }) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:369:5
-       'axon',
+       "",

[NoCoverage] StringLiteral
src/mcp/server.ts:370:5
-       'Coordinate a development team — register contributors, decompose specs into ranked work packages, assign tasks, track progress.',
+       "",

[NoCoverage] ObjectLiteral
src/mcp/server.ts:371:5
-       {
-         action: z.enum([
-           'contributor_add',
-           'spec_sync',
-           'milestone_add',
-           'decompose',
-           'assign',
-           'complete',
-           'request_merge',
-           'resolve_merge',
-           'merges',
-           'status',
-           'queue',
-         ]).describe(
-           'contributor_add=register-dev | spec_sync=read-spec-files | milestone_add=create-milestone | ' +
-           'decompose=break-spec-into-ranked-packages | assign=assign-next-unblocked-package | ' +
-           'complete=mark-work-locally-done | request_merge=submit-for-merger-review | ' +
-           'resolve_merge=approve-or-reject-merge | merges=list-merge-requests | ' +
-           'status=full-team-dashboard | queue=ranked-work-queue',
-         ),
-         project: z.string().optional().describe('Project identifier'),
-         // contributor_add
-         name:         z.string().optional().describe('Contributor full name'),
-         email:        z.string().optional(),
-         skills:       z.array(z.string()).optional().describe('Skill tags, e.g. ["typescript","testing"]'),
-         role:         z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
-                         .describe('specwright=spec/ADR | builder=implement | merger=integrate | verifier=test/score | watcher=monitor'),
-         bandwidth:    z.number().optional().describe('Available hours per week'),
-         // decompose — AI provides the breakdown, Axon ranks and persists it
-         packages: z.array(z.object({
-           title:        z.string().describe('Short unique title'),
-           description:  z.string().describe('What needs to be done'),
-           spec_section: z.string().optional().describe('Spec or ADR reference'),
-           role_required: z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']),
-           depends_on:   z.array(z.string()).optional().describe('Titles of packages this must wait for'),
-         })).optional().describe('Work packages to decompose — AI derives these from spec+state gap'),
-         // spec_sync
-         project_dir: z.string().optional().describe('Absolute path to repo root for spec_sync'),
-         // milestone_add
-         milestone_title:        z.string().optional(),
-         milestone_description:  z.string().optional(),
-         milestone_spec_section: z.string().optional(),
-         // decompose
-         milestone_id: z.string().optional().describe('Parent milestone ID to attach packages to'),
-         // assign / complete / availability
-         id:              z.string().optional().describe('Work package ID, merge request ID, or contributor ID'),
-         contributor_id:  z.string().optional().describe('Force-assign to this contributor'),
-         role_filter:     z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
-                            .describe('Only consider packages requiring this role'),
-         availability:    z.enum(['available', 'busy', 'offline']).optional()
-                            .describe('Update contributor availability state'),
-         // request_merge
-         branch_name:       z.string().optional().describe('Git branch name'),
-         forgecraft_score:  z.number().optional().describe('ForgeCraft verify totalScore (0–14)'),
-         forgecraft_tier:   z.number().optional().describe('ForgeCraft GS maturity tier (1–5)'),
-         forgecraft_pass:   z.boolean().optional().describe('Whether ForgeCraft verify passed'),
-         forgecraft_report: z.string().optional().describe('Full ForgeCraft verify report text'),
-         // resolve_merge
-         approve:          z.boolean().optional().describe('true=approve, false=reject'),
-         rejection_reason: z.string().optional(),
-         // merges filter
-         merge_status: z.enum(['pending', 'approved', 'rejected']).optional(),
-       },
+       {},

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:372:22
-         action: z.enum([
-           'contributor_add',
-           'spec_sync',
-           'milestone_add',
-           'decompose',
-           'assign',
-           'complete',
-           'request_merge',
-           'resolve_merge',
-           'merges',
-           'status',
-           'queue',
-         ]).describe(
+         action: z.enum([]).describe(

[NoCoverage] StringLiteral
src/mcp/server.ts:373:9
-           'contributor_add',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:374:9
-           'spec_sync',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:375:9
-           'milestone_add',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:376:9
-           'decompose',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:377:9
-           'assign',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:378:9
-           'complete',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:379:9
-           'request_merge',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:380:9
-           'resolve_merge',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:381:9
-           'merges',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:382:9
-           'status',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:383:9
-           'queue',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:385:9
-           'contributor_add=register-dev | spec_sync=read-spec-files | milestone_add=create-milestone | ' +
+           "" +

[NoCoverage] StringLiteral
src/mcp/server.ts:386:9
-           'decompose=break-spec-into-ranked-packages | assign=assign-next-unblocked-package | ' +
+           "" +

[NoCoverage] StringLiteral
src/mcp/server.ts:387:9
-           'complete=mark-work-locally-done | request_merge=submit-for-merger-review | ' +
+           "" +

[NoCoverage] StringLiteral
src/mcp/server.ts:388:9
-           'resolve_merge=approve-or-reject-merge | merges=list-merge-requests | ' +
+           "" +

[NoCoverage] StringLiteral
src/mcp/server.ts:389:9
-           'status=full-team-dashboard | queue=ranked-work-queue',
+           "",

[NoCoverage] StringLiteral
src/mcp/server.ts:391:47
-         project: z.string().optional().describe('Project identifier'),
+         project: z.string().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:394:52
-         name:         z.string().optional().describe('Contributor full name'),
+         name:         z.string().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:396:61
-         skills:       z.array(z.string()).optional().describe('Skill tags, e.g. ["typescript","testing"]'),
+         skills:       z.array(z.string()).optional().describe(""),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:397:28
-         role:         z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role:         z.enum([]).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:397:29
-         role:         z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role:         z.enum(["", 'builder', 'merger', 'verifier', 'watcher']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:397:43
-         role:         z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role:         z.enum(['specwright', "", 'merger', 'verifier', 'watcher']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:397:54
-         role:         z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role:         z.enum(['specwright', 'builder', "", 'verifier', 'watcher']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:397:64
-         role:         z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role:         z.enum(['specwright', 'builder', 'merger', "", 'watcher']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:397:76
-         role:         z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role:         z.enum(['specwright', 'builder', 'merger', 'verifier', ""]).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:398:33
-                         .describe('specwright=spec/ADR | builder=implement | merger=integrate | verifier=test/score | watcher=monitor'),
+                         .describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:399:52
-         bandwidth:    z.number().optional().describe('Available hours per week'),
+         bandwidth:    z.number().optional().describe(""),

[NoCoverage] ObjectLiteral
src/mcp/server.ts:402:34
-         packages: z.array(z.object({
-           title:        z.string().describe('Short unique title'),
-           description:  z.string().describe('What needs to be done'),
-           spec_section: z.string().optional().describe('Spec or ADR reference'),
-           role_required: z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']),
-           depends_on:   z.array(z.string()).optional().describe('Titles of packages this must wait for'),
-         })).optional().describe('Work packages to decompose — AI derives these from spec+state gap'),
+         packages: z.array(z.object({})).optional().describe('Work packages to decompose — AI derives these from spec+state gap'),

[NoCoverage] StringLiteral
src/mcp/server.ts:403:43
-           title:        z.string().describe('Short unique title'),
+           title:        z.string().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:404:43
-           description:  z.string().describe('What needs to be done'),
+           description:  z.string().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:405:54
-           spec_section: z.string().optional().describe('Spec or ADR reference'),
+           spec_section: z.string().optional().describe(""),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:406:31
-           role_required: z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']),
+           role_required: z.enum([]),

[NoCoverage] StringLiteral
src/mcp/server.ts:406:32
-           role_required: z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']),
+           role_required: z.enum(["", 'builder', 'merger', 'verifier', 'watcher']),

[NoCoverage] StringLiteral
src/mcp/server.ts:406:46
-           role_required: z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']),
+           role_required: z.enum(['specwright', "", 'merger', 'verifier', 'watcher']),

[NoCoverage] StringLiteral
src/mcp/server.ts:406:57
-           role_required: z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']),
+           role_required: z.enum(['specwright', 'builder', "", 'verifier', 'watcher']),

[NoCoverage] StringLiteral
src/mcp/server.ts:406:67
-           role_required: z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']),
+           role_required: z.enum(['specwright', 'builder', 'merger', "", 'watcher']),

[NoCoverage] StringLiteral
src/mcp/server.ts:406:79
-           role_required: z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']),
+           role_required: z.enum(['specwright', 'builder', 'merger', 'verifier', ""]),

[NoCoverage] StringLiteral
src/mcp/server.ts:407:63
-           depends_on:   z.array(z.string()).optional().describe('Titles of packages this must wait for'),
+           depends_on:   z.array(z.string()).optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:408:31
-         })).optional().describe('Work packages to decompose — AI derives these from spec+state gap'),
+         })).optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:411:51
-         project_dir: z.string().optional().describe('Absolute path to repo root for spec_sync'),
+         project_dir: z.string().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:419:52
-         milestone_id: z.string().optional().describe('Parent milestone ID to attach packages to'),
+         milestone_id: z.string().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:422:55
-         id:              z.string().optional().describe('Work package ID, merge request ID, or contributor ID'),
+         id:              z.string().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:423:55
-         contributor_id:  z.string().optional().describe('Force-assign to this contributor'),
+         contributor_id:  z.string().optional().describe(""),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:424:31
-         role_filter:     z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role_filter:     z.enum([]).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:424:32
-         role_filter:     z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role_filter:     z.enum(["", 'builder', 'merger', 'verifier', 'watcher']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:424:46
-         role_filter:     z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role_filter:     z.enum(['specwright', "", 'merger', 'verifier', 'watcher']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:424:57
-         role_filter:     z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role_filter:     z.enum(['specwright', 'builder', "", 'verifier', 'watcher']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:424:67
-         role_filter:     z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role_filter:     z.enum(['specwright', 'builder', 'merger', "", 'watcher']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:424:79
-         role_filter:     z.enum(['specwright', 'builder', 'merger', 'verifier', 'watcher']).optional()
+         role_filter:     z.enum(['specwright', 'builder', 'merger', 'verifier', ""]).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:425:36
-                            .describe('Only consider packages requiring this role'),
+                            .describe(""),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:426:31
-         availability:    z.enum(['available', 'busy', 'offline']).optional()
+         availability:    z.enum([]).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:426:32
-         availability:    z.enum(['available', 'busy', 'offline']).optional()
+         availability:    z.enum(["", 'busy', 'offline']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:426:45
-         availability:    z.enum(['available', 'busy', 'offline']).optional()
+         availability:    z.enum(['available', "", 'offline']).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:426:53
-         availability:    z.enum(['available', 'busy', 'offline']).optional()
+         availability:    z.enum(['available', 'busy', ""]).optional()

[NoCoverage] StringLiteral
src/mcp/server.ts:427:36
-                            .describe('Update contributor availability state'),
+                            .describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:430:57
-         branch_name:       z.string().optional().describe('Git branch name'),
+         branch_name:       z.string().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:431:57
-         forgecraft_score:  z.number().optional().describe('ForgeCraft verify totalScore (0–14)'),
+         forgecraft_score:  z.number().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:432:57
-         forgecraft_tier:   z.number().optional().describe('ForgeCraft GS maturity tier (1–5)'),
+         forgecraft_tier:   z.number().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:433:58
-         forgecraft_pass:   z.boolean().optional().describe('Whether ForgeCraft verify passed'),
+         forgecraft_pass:   z.boolean().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:434:57
-         forgecraft_report: z.string().optional().describe('Full ForgeCraft verify report text'),
+         forgecraft_report: z.string().optional().describe(""),

[NoCoverage] StringLiteral
src/mcp/server.ts:437:57
-         approve:          z.boolean().optional().describe('true=approve, false=reject'),
+         approve:          z.boolean().optional().describe(""),

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:441:28
-         merge_status: z.enum(['pending', 'approved', 'rejected']).optional(),
+         merge_status: z.enum([]).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:441:29
-         merge_status: z.enum(['pending', 'approved', 'rejected']).optional(),
+         merge_status: z.enum(["", 'approved', 'rejected']).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:441:40
-         merge_status: z.enum(['pending', 'approved', 'rejected']).optional(),
+         merge_status: z.enum(['pending', "", 'rejected']).optional(),

[NoCoverage] StringLiteral
src/mcp/server.ts:441:52
-         merge_status: z.enum(['pending', 'approved', 'rejected']).optional(),
+         merge_status: z.enum(['pending', 'approved', ""]).optional(),

[NoCoverage] BlockStatement
src/mcp/server.ts:443:21
-       async (args) => {
-         const config = getConfig();
-         if (!config.teamToken) {
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
-         }
-         if (!config.teamId) {
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires teamId in ~/.chronicle/config.json.',
-           }) }] };
-         }
-         const tokenOk = await validateTeamToken(config.teamToken, config.railwayUrl, config.teamId);
-         if (!tokenOk) {
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
-         }
-         const project = args.project ?? 'default';
-         // Pull latest team state from Railway before any read or write.
-         // Fire-and-forget on write actions — response is not blocked.
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
-         if (isRead) {
-           await syncCoordination(project).catch(() => { /* offline — use local cache */ });
-         }
-         switch (args.action) {
-           case 'contributor_add': {
-             const contributor = coordSvc.addContributor({
-               project,
-               name: args.name ?? 'Unknown',
-               email: args.email,
-               skills: args.skills ?? [],
-               role: (args.role ?? 'builder') as ContributorRole,
-               bandwidthHoursPerWeek: args.bandwidth ?? 20,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(contributor) }] };
-           }
-           case 'spec_sync': {
-             if (!args.project_dir) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
-             }
-             const sync = coordSvc.syncSpec(args.project_dir);
-             return { content: [{ type: 'text', text: JSON.stringify(sync) }] };
-           }
-           case 'milestone_add': {
-             if (!args.milestone_title) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
-             }
-             const milestone = coordSvc.addMilestone({
-               project,
-               title: args.milestone_title,
-               description: args.milestone_description ?? '',
-               specSection: args.milestone_spec_section,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify(milestone) }] };
-           }
-           case 'decompose': {
-             if (!args.packages?.length) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
-             }
-             const queue = coordSvc.decomposeWork({
-               project,
-               milestoneId: args.milestone_id,
-               packages: args.packages.map(p => ({
-                 title: p.title,
-                 description: p.description,
-                 specSection: p.spec_section,
-                 roleRequired: p.role_required as ContributorRole,
-                 dependsOn: p.depends_on,
-               })),
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
-           }
-           case 'assign': {
-             const result = coordSvc.assignNext({
-               project,
-               contributorId: args.contributor_id,
-               roleFilter: args.role_filter as ContributorRole | undefined,
-             });
-             if (!result) {
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
-             }
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
-           case 'complete': {
-             if (!args.id) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
-             }
-             const result = coordSvc.completeWork(args.id);
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
-           case 'request_merge': {
-             if (!args.id || !args.branch_name || !args.contributor_id) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
-             }
-             const mr = coordSvc.requestMerge({
-               workPackageId: args.id,
-               branchName: args.branch_name,
-               fromContributorId: args.contributor_id,
-               forgecraftScore: args.forgecraft_score,
-               forgecraftTier: args.forgecraft_tier,
-               forgecraftPass: args.forgecraft_pass,
-               forgecraftReport: args.forgecraft_report,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(mr) }] };
-           }
-           case 'resolve_merge': {
-             if (!args.id || !args.contributor_id || args.approve == null) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
-             }
-             const result = coordSvc.resolveMerge({
-               mergeRequestId: args.id,
-               reviewerId: args.contributor_id,
-               approve: args.approve,
-               rejectionReason: args.rejection_reason,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
-           case 'merges': {
-             const mrs = coordSvc.listMergeRequests(
-               project,
-               args.merge_status as 'pending' | 'approved' | 'rejected' | undefined,
-             );
-             return { content: [{ type: 'text', text: JSON.stringify(mrs) }] };
-           }
-           case 'status': {
-             const status = coordSvc.getStatus(project);
-             return { content: [{ type: 'text', text: JSON.stringify(status) }] };
-           }
-           case 'queue': {
-             const queue = coordSvc.getQueue(project);
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
-           }
-           default:
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
-         }
-       },
+       async (args) => {},

[NoCoverage] BooleanLiteral
src/mcp/server.ts:445:11
-         if (!config.teamToken) {
+         if (config.teamToken) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:445:11
-         if (!config.teamToken) {
+         if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:445:11
-         if (!config.teamToken) {
+         if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:445:30
-         if (!config.teamToken) {
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
-         }
+         if (!config.teamToken) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:446:16
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
+           return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:446:27
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
+           return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:446:28
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
+           return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:446:36
-           return { content: [{ type: 'text', text: JSON.stringify({
+           return { content: [{ type: "", text: JSON.stringify({

[NoCoverage] ObjectLiteral
src/mcp/server.ts:446:65
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
+           return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:447:18
-             error: 'Axon requires a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>',
+             error: "",

[NoCoverage] BooleanLiteral
src/mcp/server.ts:450:11
-         if (!config.teamId) {
+         if (config.teamId) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:450:11
-         if (!config.teamId) {
+         if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:450:11
-         if (!config.teamId) {
+         if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:450:27
-         if (!config.teamId) {
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires teamId in ~/.chronicle/config.json.',
-           }) }] };
-         }
+         if (!config.teamId) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:451:16
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires teamId in ~/.chronicle/config.json.',
-           }) }] };
+           return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:451:27
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires teamId in ~/.chronicle/config.json.',
-           }) }] };
+           return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:451:28
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires teamId in ~/.chronicle/config.json.',
-           }) }] };
+           return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:451:36
-           return { content: [{ type: 'text', text: JSON.stringify({
+           return { content: [{ type: "", text: JSON.stringify({

[NoCoverage] ObjectLiteral
src/mcp/server.ts:451:65
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Axon requires teamId in ~/.chronicle/config.json.',
-           }) }] };
+           return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:452:18
-             error: 'Axon requires teamId in ~/.chronicle/config.json.',
+             error: "",

[NoCoverage] BooleanLiteral
src/mcp/server.ts:456:11
-         if (!tokenOk) {
+         if (tokenOk) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:456:11
-         if (!tokenOk) {
+         if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:456:11
-         if (!tokenOk) {
+         if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:456:21
-         if (!tokenOk) {
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
-         }
+         if (!tokenOk) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:457:16
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
+           return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:457:27
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
+           return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:457:28
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
+           return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:457:36
-           return { content: [{ type: 'text', text: JSON.stringify({
+           return { content: [{ type: "", text: JSON.stringify({

[NoCoverage] ObjectLiteral
src/mcp/server.ts:457:65
-           return { content: [{ type: 'text', text: JSON.stringify({
-             error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>',
-           }) }] };
+           return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:458:18
-             error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>',
+             error: "",

[NoCoverage] LogicalOperator
src/mcp/server.ts:462:23
-         const project = args.project ?? 'default';
+         const project = args.project && 'default';

[NoCoverage] StringLiteral
src/mcp/server.ts:462:39
-         const project = args.project ?? 'default';
+         const project = args.project ?? "";

[NoCoverage] ConditionalExpression
src/mcp/server.ts:466:22
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = true;

[NoCoverage] ConditionalExpression
src/mcp/server.ts:466:22
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = false;

[NoCoverage] LogicalOperator
src/mcp/server.ts:466:22
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = (args.action === 'status' || args.action === 'queue') && args.action === 'merges';

[NoCoverage] ConditionalExpression
src/mcp/server.ts:466:22
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = false || args.action === 'merges';

[NoCoverage] LogicalOperator
src/mcp/server.ts:466:22
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = args.action === 'status' && args.action === 'queue' || args.action === 'merges';

[NoCoverage] ConditionalExpression
src/mcp/server.ts:466:22
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = false || args.action === 'queue' || args.action === 'merges';

[NoCoverage] EqualityOperator
src/mcp/server.ts:466:22
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = args.action !== 'status' || args.action === 'queue' || args.action === 'merges';

[NoCoverage] StringLiteral
src/mcp/server.ts:466:38
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = args.action === "" || args.action === 'queue' || args.action === 'merges';

[NoCoverage] ConditionalExpression
src/mcp/server.ts:466:50
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = args.action === 'status' || false || args.action === 'merges';

[NoCoverage] EqualityOperator
src/mcp/server.ts:466:50
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = args.action === 'status' || args.action !== 'queue' || args.action === 'merges';

[NoCoverage] StringLiteral
src/mcp/server.ts:466:66
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = args.action === 'status' || args.action === "" || args.action === 'merges';

[NoCoverage] ConditionalExpression
src/mcp/server.ts:466:77
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = args.action === 'status' || args.action === 'queue' || false;

[NoCoverage] EqualityOperator
src/mcp/server.ts:466:77
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = args.action === 'status' || args.action === 'queue' || args.action !== 'merges';

[NoCoverage] StringLiteral
src/mcp/server.ts:466:93
-         const isRead = args.action === 'status' || args.action === 'queue' || args.action === 'merges';
+         const isRead = args.action === 'status' || args.action === 'queue' || args.action === "";

[NoCoverage] ConditionalExpression
src/mcp/server.ts:467:11
-         if (isRead) {
+         if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:467:11
-         if (isRead) {
+         if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:467:19
-         if (isRead) {
-           await syncCoordination(project).catch(() => { /* offline — use local cache */ });
-         }
+         if (isRead) {}

[NoCoverage] ConditionalExpression
src/mcp/server.ts:473:9
-           case 'contributor_add': {
-             const contributor = coordSvc.addContributor({
-               project,
-               name: args.name ?? 'Unknown',
-               email: args.email,
-               skills: args.skills ?? [],
-               role: (args.role ?? 'builder') as ContributorRole,
-               bandwidthHoursPerWeek: args.bandwidth ?? 20,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(contributor) }] };
-           }
+           case 'contributor_add':

[NoCoverage] StringLiteral
src/mcp/server.ts:473:14
-           case 'contributor_add': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:473:33
-           case 'contributor_add': {
-             const contributor = coordSvc.addContributor({
-               project,
-               name: args.name ?? 'Unknown',
-               email: args.email,
-               skills: args.skills ?? [],
-               role: (args.role ?? 'builder') as ContributorRole,
-               bandwidthHoursPerWeek: args.bandwidth ?? 20,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(contributor) }] };
-           }
+           case 'contributor_add': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:474:55
-             const contributor = coordSvc.addContributor({
-               project,
-               name: args.name ?? 'Unknown',
-               email: args.email,
-               skills: args.skills ?? [],
-               role: (args.role ?? 'builder') as ContributorRole,
-               bandwidthHoursPerWeek: args.bandwidth ?? 20,
-             });
+             const contributor = coordSvc.addContributor({});

[NoCoverage] LogicalOperator
src/mcp/server.ts:476:19
-               name: args.name ?? 'Unknown',
+               name: args.name && 'Unknown',

[NoCoverage] StringLiteral
src/mcp/server.ts:476:32
-               name: args.name ?? 'Unknown',
+               name: args.name ?? "",

[NoCoverage] LogicalOperator
src/mcp/server.ts:478:21
-               skills: args.skills ?? [],
+               skills: args.skills && [],

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:478:36
-               skills: args.skills ?? [],
+               skills: args.skills ?? ["Stryker was here"],

[NoCoverage] LogicalOperator
src/mcp/server.ts:480:36
-               bandwidthHoursPerWeek: args.bandwidth ?? 20,
+               bandwidthHoursPerWeek: args.bandwidth && 20,

[NoCoverage] ObjectLiteral
src/mcp/server.ts:483:18
-             return { content: [{ type: 'text', text: JSON.stringify(contributor) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:483:29
-             return { content: [{ type: 'text', text: JSON.stringify(contributor) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:483:30
-             return { content: [{ type: 'text', text: JSON.stringify(contributor) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:483:38
-             return { content: [{ type: 'text', text: JSON.stringify(contributor) }] };
+             return { content: [{ type: "", text: JSON.stringify(contributor) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:486:9
-           case 'spec_sync': {
-             if (!args.project_dir) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
-             }
-             const sync = coordSvc.syncSpec(args.project_dir);
-             return { content: [{ type: 'text', text: JSON.stringify(sync) }] };
-           }
+           case 'spec_sync':

[NoCoverage] StringLiteral
src/mcp/server.ts:486:14
-           case 'spec_sync': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:486:27
-           case 'spec_sync': {
-             if (!args.project_dir) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
-             }
-             const sync = coordSvc.syncSpec(args.project_dir);
-             return { content: [{ type: 'text', text: JSON.stringify(sync) }] };
-           }
+           case 'spec_sync': {}

[NoCoverage] BooleanLiteral
src/mcp/server.ts:487:15
-             if (!args.project_dir) {
+             if (args.project_dir) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:487:15
-             if (!args.project_dir) {
+             if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:487:15
-             if (!args.project_dir) {
+             if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:487:34
-             if (!args.project_dir) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
-             }
+             if (!args.project_dir) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:488:20
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
+               return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:488:31
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
+               return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:488:32
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
+               return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:488:40
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
+               return { content: [{ type: "", text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:488:69
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:488:78
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'project_dir required for spec_sync' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({ error: "" }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:491:18
-             return { content: [{ type: 'text', text: JSON.stringify(sync) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:491:29
-             return { content: [{ type: 'text', text: JSON.stringify(sync) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:491:30
-             return { content: [{ type: 'text', text: JSON.stringify(sync) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:491:38
-             return { content: [{ type: 'text', text: JSON.stringify(sync) }] };
+             return { content: [{ type: "", text: JSON.stringify(sync) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:494:9
-           case 'milestone_add': {
-             if (!args.milestone_title) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
-             }
-             const milestone = coordSvc.addMilestone({
-               project,
-               title: args.milestone_title,
-               description: args.milestone_description ?? '',
-               specSection: args.milestone_spec_section,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify(milestone) }] };
-           }
+           case 'milestone_add':

[NoCoverage] StringLiteral
src/mcp/server.ts:494:14
-           case 'milestone_add': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:494:31
-           case 'milestone_add': {
-             if (!args.milestone_title) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
-             }
-             const milestone = coordSvc.addMilestone({
-               project,
-               title: args.milestone_title,
-               description: args.milestone_description ?? '',
-               specSection: args.milestone_spec_section,
-             });
-             return { content: [{ type: 'text', text: JSON.stringify(milestone) }] };
-           }
+           case 'milestone_add': {}

[NoCoverage] BooleanLiteral
src/mcp/server.ts:495:15
-             if (!args.milestone_title) {
+             if (args.milestone_title) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:495:15
-             if (!args.milestone_title) {
+             if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:495:15
-             if (!args.milestone_title) {
+             if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:495:38
-             if (!args.milestone_title) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
-             }
+             if (!args.milestone_title) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:496:20
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
+               return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:496:31
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
+               return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:496:32
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
+               return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:496:40
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
+               return { content: [{ type: "", text: JSON.stringify({ error: 'milestone_title required' }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:496:69
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:496:78
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'milestone_title required' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({ error: "" }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:498:51
-             const milestone = coordSvc.addMilestone({
-               project,
-               title: args.milestone_title,
-               description: args.milestone_description ?? '',
-               specSection: args.milestone_spec_section,
-             });
+             const milestone = coordSvc.addMilestone({});

[NoCoverage] LogicalOperator
src/mcp/server.ts:501:26
-               description: args.milestone_description ?? '',
+               description: args.milestone_description && '',

[NoCoverage] StringLiteral
src/mcp/server.ts:501:56
-               description: args.milestone_description ?? '',
+               description: args.milestone_description ?? "Stryker was here!",

[NoCoverage] ObjectLiteral
src/mcp/server.ts:504:18
-             return { content: [{ type: 'text', text: JSON.stringify(milestone) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:504:29
-             return { content: [{ type: 'text', text: JSON.stringify(milestone) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:504:30
-             return { content: [{ type: 'text', text: JSON.stringify(milestone) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:504:38
-             return { content: [{ type: 'text', text: JSON.stringify(milestone) }] };
+             return { content: [{ type: "", text: JSON.stringify(milestone) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:507:9
-           case 'decompose': {
-             if (!args.packages?.length) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
-             }
-             const queue = coordSvc.decomposeWork({
-               project,
-               milestoneId: args.milestone_id,
-               packages: args.packages.map(p => ({
-                 title: p.title,
-                 description: p.description,
-                 specSection: p.spec_section,
-                 roleRequired: p.role_required as ContributorRole,
-                 dependsOn: p.depends_on,
-               })),
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
-           }
+           case 'decompose':

[NoCoverage] StringLiteral
src/mcp/server.ts:507:14
-           case 'decompose': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:507:27
-           case 'decompose': {
-             if (!args.packages?.length) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
-             }
-             const queue = coordSvc.decomposeWork({
-               project,
-               milestoneId: args.milestone_id,
-               packages: args.packages.map(p => ({
-                 title: p.title,
-                 description: p.description,
-                 specSection: p.spec_section,
-                 roleRequired: p.role_required as ContributorRole,
-                 dependsOn: p.depends_on,
-               })),
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
-           }
+           case 'decompose': {}

[NoCoverage] BooleanLiteral
src/mcp/server.ts:508:15
-             if (!args.packages?.length) {
+             if (args.packages?.length) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:508:15
-             if (!args.packages?.length) {
+             if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:508:15
-             if (!args.packages?.length) {
+             if (false) {

[NoCoverage] OptionalChaining
src/mcp/server.ts:508:16
-             if (!args.packages?.length) {
+             if (!args.packages.length) {

[NoCoverage] BlockStatement
src/mcp/server.ts:508:39
-             if (!args.packages?.length) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
-             }
+             if (!args.packages?.length) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:509:20
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
+               return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:509:31
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
+               return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:509:32
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
+               return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:509:40
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
+               return { content: [{ type: "", text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:509:69
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:509:78
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'packages[] required for decompose' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({ error: "" }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:511:48
-             const queue = coordSvc.decomposeWork({
-               project,
-               milestoneId: args.milestone_id,
-               packages: args.packages.map(p => ({
-                 title: p.title,
-                 description: p.description,
-                 specSection: p.spec_section,
-                 roleRequired: p.role_required as ContributorRole,
-                 dependsOn: p.depends_on,
-               })),
-             });
+             const queue = coordSvc.decomposeWork({});

[NoCoverage] ArrowFunction
src/mcp/server.ts:514:41
-               packages: args.packages.map(p => ({
-                 title: p.title,
-                 description: p.description,
-                 specSection: p.spec_section,
-                 roleRequired: p.role_required as ContributorRole,
-                 dependsOn: p.depends_on,
-               })),
+               packages: args.packages.map(() => undefined),

[NoCoverage] ObjectLiteral
src/mcp/server.ts:514:47
-               packages: args.packages.map(p => ({
-                 title: p.title,
-                 description: p.description,
-                 specSection: p.spec_section,
-                 roleRequired: p.role_required as ContributorRole,
-                 dependsOn: p.depends_on,
-               })),
+               packages: args.packages.map(p => ({})),

[NoCoverage] ObjectLiteral
src/mcp/server.ts:523:18
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:523:29
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:523:30
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:523:38
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
+             return { content: [{ type: "", text: JSON.stringify(queue) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:526:9
-           case 'assign': {
-             const result = coordSvc.assignNext({
-               project,
-               contributorId: args.contributor_id,
-               roleFilter: args.role_filter as ContributorRole | undefined,
-             });
-             if (!result) {
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
-             }
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
+           case 'assign':

[NoCoverage] StringLiteral
src/mcp/server.ts:526:14
-           case 'assign': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:526:24
-           case 'assign': {
-             const result = coordSvc.assignNext({
-               project,
-               contributorId: args.contributor_id,
-               roleFilter: args.role_filter as ContributorRole | undefined,
-             });
-             if (!result) {
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
-             }
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
+           case 'assign': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:527:46
-             const result = coordSvc.assignNext({
-               project,
-               contributorId: args.contributor_id,
-               roleFilter: args.role_filter as ContributorRole | undefined,
-             });
+             const result = coordSvc.assignNext({});

[NoCoverage] BooleanLiteral
src/mcp/server.ts:532:15
-             if (!result) {
+             if (result) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:532:15
-             if (!result) {
+             if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:532:15
-             if (!result) {
+             if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:532:24
-             if (!result) {
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
-             }
+             if (!result) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:533:20
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
+               return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:533:31
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
+               return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:533:32
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
+               return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:533:40
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
+               return { content: [{ type: "", text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:533:69
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:533:80
-               return { content: [{ type: 'text', text: JSON.stringify({ message: 'No unblocked work available or no matching contributor free.' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({ message: "" }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:536:18
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:536:29
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:536:30
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:536:38
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return { content: [{ type: "", text: JSON.stringify(result) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:539:9
-           case 'complete': {
-             if (!args.id) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
-             }
-             const result = coordSvc.completeWork(args.id);
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
+           case 'complete':

[NoCoverage] StringLiteral
src/mcp/server.ts:539:14
-           case 'complete': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:539:26
-           case 'complete': {
-             if (!args.id) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
-             }
-             const result = coordSvc.completeWork(args.id);
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
+           case 'complete': {}

[NoCoverage] BooleanLiteral
src/mcp/server.ts:540:15
-             if (!args.id) {
+             if (args.id) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:540:15
-             if (!args.id) {
+             if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:540:15
-             if (!args.id) {
+             if (false) {

[NoCoverage] BlockStatement
src/mcp/server.ts:540:25
-             if (!args.id) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
-             }
+             if (!args.id) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:541:20
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
+               return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:541:31
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
+               return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:541:32
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
+               return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:541:40
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
+               return { content: [{ type: "", text: JSON.stringify({ error: 'id (work package ID) required' }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:541:69
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:541:78
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work package ID) required' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({ error: "" }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:545:18
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:545:29
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:545:30
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:545:38
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return { content: [{ type: "", text: JSON.stringify(result) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:548:9
-           case 'request_merge': {
-             if (!args.id || !args.branch_name || !args.contributor_id) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
-             }
-             const mr = coordSvc.requestMerge({
-               workPackageId: args.id,
-               branchName: args.branch_name,
-               fromContributorId: args.contributor_id,
-               forgecraftScore: args.forgecraft_score,
-               forgecraftTier: args.forgecraft_tier,
-               forgecraftPass: args.forgecraft_pass,
-               forgecraftReport: args.forgecraft_report,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(mr) }] };
-           }
+           case 'request_merge':

[NoCoverage] StringLiteral
src/mcp/server.ts:548:14
-           case 'request_merge': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:548:31
-           case 'request_merge': {
-             if (!args.id || !args.branch_name || !args.contributor_id) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
-             }
-             const mr = coordSvc.requestMerge({
-               workPackageId: args.id,
-               branchName: args.branch_name,
-               fromContributorId: args.contributor_id,
-               forgecraftScore: args.forgecraft_score,
-               forgecraftTier: args.forgecraft_tier,
-               forgecraftPass: args.forgecraft_pass,
-               forgecraftReport: args.forgecraft_report,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(mr) }] };
-           }
+           case 'request_merge': {}

[NoCoverage] ConditionalExpression
src/mcp/server.ts:549:15
-             if (!args.id || !args.branch_name || !args.contributor_id) {
+             if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:549:15
-             if (!args.id || !args.branch_name || !args.contributor_id) {
+             if (false) {

[NoCoverage] LogicalOperator
src/mcp/server.ts:549:15
-             if (!args.id || !args.branch_name || !args.contributor_id) {
+             if ((!args.id || !args.branch_name) && !args.contributor_id) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:549:15
-             if (!args.id || !args.branch_name || !args.contributor_id) {
+             if (false || !args.contributor_id) {

[NoCoverage] LogicalOperator
src/mcp/server.ts:549:15
-             if (!args.id || !args.branch_name || !args.contributor_id) {
+             if (!args.id && !args.branch_name || !args.contributor_id) {

[NoCoverage] BooleanLiteral
src/mcp/server.ts:549:15
-             if (!args.id || !args.branch_name || !args.contributor_id) {
+             if (args.id || !args.branch_name || !args.contributor_id) {

[NoCoverage] BooleanLiteral
src/mcp/server.ts:549:27
-             if (!args.id || !args.branch_name || !args.contributor_id) {
+             if (!args.id || args.branch_name || !args.contributor_id) {

[NoCoverage] BooleanLiteral
src/mcp/server.ts:549:48
-             if (!args.id || !args.branch_name || !args.contributor_id) {
+             if (!args.id || !args.branch_name || args.contributor_id) {

[NoCoverage] BlockStatement
src/mcp/server.ts:549:70
-             if (!args.id || !args.branch_name || !args.contributor_id) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
-             }
+             if (!args.id || !args.branch_name || !args.contributor_id) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:550:20
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
+               return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:550:31
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
+               return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:550:32
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
+               return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:550:40
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
+               return { content: [{ type: "", text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:550:69
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:550:78
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (work_package_id), branch_name, and contributor_id required' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({ error: "" }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:552:44
-             const mr = coordSvc.requestMerge({
-               workPackageId: args.id,
-               branchName: args.branch_name,
-               fromContributorId: args.contributor_id,
-               forgecraftScore: args.forgecraft_score,
-               forgecraftTier: args.forgecraft_tier,
-               forgecraftPass: args.forgecraft_pass,
-               forgecraftReport: args.forgecraft_report,
-             });
+             const mr = coordSvc.requestMerge({});

[NoCoverage] ObjectLiteral
src/mcp/server.ts:562:18
-             return { content: [{ type: 'text', text: JSON.stringify(mr) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:562:29
-             return { content: [{ type: 'text', text: JSON.stringify(mr) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:562:30
-             return { content: [{ type: 'text', text: JSON.stringify(mr) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:562:38
-             return { content: [{ type: 'text', text: JSON.stringify(mr) }] };
+             return { content: [{ type: "", text: JSON.stringify(mr) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:565:9
-           case 'resolve_merge': {
-             if (!args.id || !args.contributor_id || args.approve == null) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
-             }
-             const result = coordSvc.resolveMerge({
-               mergeRequestId: args.id,
-               reviewerId: args.contributor_id,
-               approve: args.approve,
-               rejectionReason: args.rejection_reason,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
+           case 'resolve_merge':

[NoCoverage] StringLiteral
src/mcp/server.ts:565:14
-           case 'resolve_merge': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:565:31
-           case 'resolve_merge': {
-             if (!args.id || !args.contributor_id || args.approve == null) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
-             }
-             const result = coordSvc.resolveMerge({
-               mergeRequestId: args.id,
-               reviewerId: args.contributor_id,
-               approve: args.approve,
-               rejectionReason: args.rejection_reason,
-             });
-             syncCoordination(project).catch(() => {});
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
-           }
+           case 'resolve_merge': {}

[NoCoverage] ConditionalExpression
src/mcp/server.ts:566:15
-             if (!args.id || !args.contributor_id || args.approve == null) {
+             if (true) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:566:15
-             if (!args.id || !args.contributor_id || args.approve == null) {
+             if (false) {

[NoCoverage] LogicalOperator
src/mcp/server.ts:566:15
-             if (!args.id || !args.contributor_id || args.approve == null) {
+             if ((!args.id || !args.contributor_id) && args.approve == null) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:566:15
-             if (!args.id || !args.contributor_id || args.approve == null) {
+             if (false || args.approve == null) {

[NoCoverage] LogicalOperator
src/mcp/server.ts:566:15
-             if (!args.id || !args.contributor_id || args.approve == null) {
+             if (!args.id && !args.contributor_id || args.approve == null) {

[NoCoverage] BooleanLiteral
src/mcp/server.ts:566:15
-             if (!args.id || !args.contributor_id || args.approve == null) {
+             if (args.id || !args.contributor_id || args.approve == null) {

[NoCoverage] BooleanLiteral
src/mcp/server.ts:566:27
-             if (!args.id || !args.contributor_id || args.approve == null) {
+             if (!args.id || args.contributor_id || args.approve == null) {

[NoCoverage] ConditionalExpression
src/mcp/server.ts:566:51
-             if (!args.id || !args.contributor_id || args.approve == null) {
+             if (!args.id || !args.contributor_id || false) {

[NoCoverage] EqualityOperator
src/mcp/server.ts:566:51
-             if (!args.id || !args.contributor_id || args.approve == null) {
+             if (!args.id || !args.contributor_id || args.approve != null) {

[NoCoverage] BlockStatement
src/mcp/server.ts:566:73
-             if (!args.id || !args.contributor_id || args.approve == null) {
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
-             }
+             if (!args.id || !args.contributor_id || args.approve == null) {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:567:20
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
+               return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:567:31
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
+               return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:567:32
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
+               return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:567:40
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
+               return { content: [{ type: "", text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:567:69
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:567:78
-               return { content: [{ type: 'text', text: JSON.stringify({ error: 'id (merge_request_id), contributor_id, and approve (bool) required' }) }] };
+               return { content: [{ type: 'text', text: JSON.stringify({ error: "" }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:569:48
-             const result = coordSvc.resolveMerge({
-               mergeRequestId: args.id,
-               reviewerId: args.contributor_id,
-               approve: args.approve,
-               rejectionReason: args.rejection_reason,
-             });
+             const result = coordSvc.resolveMerge({});

[NoCoverage] ObjectLiteral
src/mcp/server.ts:576:18
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:576:29
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:576:30
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:576:38
-             return { content: [{ type: 'text', text: JSON.stringify(result) }] };
+             return { content: [{ type: "", text: JSON.stringify(result) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:579:9
-           case 'merges': {
-             const mrs = coordSvc.listMergeRequests(
-               project,
-               args.merge_status as 'pending' | 'approved' | 'rejected' | undefined,
-             );
-             return { content: [{ type: 'text', text: JSON.stringify(mrs) }] };
-           }
+           case 'merges':

[NoCoverage] StringLiteral
src/mcp/server.ts:579:14
-           case 'merges': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:579:24
-           case 'merges': {
-             const mrs = coordSvc.listMergeRequests(
-               project,
-               args.merge_status as 'pending' | 'approved' | 'rejected' | undefined,
-             );
-             return { content: [{ type: 'text', text: JSON.stringify(mrs) }] };
-           }
+           case 'merges': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:584:18
-             return { content: [{ type: 'text', text: JSON.stringify(mrs) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:584:29
-             return { content: [{ type: 'text', text: JSON.stringify(mrs) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:584:30
-             return { content: [{ type: 'text', text: JSON.stringify(mrs) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:584:38
-             return { content: [{ type: 'text', text: JSON.stringify(mrs) }] };
+             return { content: [{ type: "", text: JSON.stringify(mrs) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:587:9
-           case 'status': {
-             const status = coordSvc.getStatus(project);
-             return { content: [{ type: 'text', text: JSON.stringify(status) }] };
-           }
+           case 'status':

[NoCoverage] StringLiteral
src/mcp/server.ts:587:14
-           case 'status': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:587:24
-           case 'status': {
-             const status = coordSvc.getStatus(project);
-             return { content: [{ type: 'text', text: JSON.stringify(status) }] };
-           }
+           case 'status': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:589:18
-             return { content: [{ type: 'text', text: JSON.stringify(status) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:589:29
-             return { content: [{ type: 'text', text: JSON.stringify(status) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:589:30
-             return { content: [{ type: 'text', text: JSON.stringify(status) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:589:38
-             return { content: [{ type: 'text', text: JSON.stringify(status) }] };
+             return { content: [{ type: "", text: JSON.stringify(status) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:592:9
-           case 'queue': {
-             const queue = coordSvc.getQueue(project);
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
-           }
+           case 'queue':

[NoCoverage] StringLiteral
src/mcp/server.ts:592:14
-           case 'queue': {
+           case "": {

[NoCoverage] BlockStatement
src/mcp/server.ts:592:23
-           case 'queue': {
-             const queue = coordSvc.getQueue(project);
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
-           }
+           case 'queue': {}

[NoCoverage] ObjectLiteral
src/mcp/server.ts:594:18
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:594:29
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:594:30
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:594:38
-             return { content: [{ type: 'text', text: JSON.stringify(queue) }] };
+             return { content: [{ type: "", text: JSON.stringify(queue) }] };

[NoCoverage] ConditionalExpression
src/mcp/server.ts:597:9
-           default:
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+           default:

[NoCoverage] ObjectLiteral
src/mcp/server.ts:598:18
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return {};

[NoCoverage] ArrayDeclaration
src/mcp/server.ts:598:29
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:598:30
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{}] };

[NoCoverage] StringLiteral
src/mcp/server.ts:598:38
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{ type: "", text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:598:67
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({}) }] };

[NoCoverage] StringLiteral
src/mcp/server.ts:598:76
-             return { content: [{ type: 'text', text: JSON.stringify({ error: `Unknown action: ${args.action}` }) }] };
+             return { content: [{ type: 'text', text: JSON.stringify({ error: `` }) }] };

[NoCoverage] ObjectLiteral
src/mcp/server.ts:605:29
-     registerTeamTools(server, { teamSvc, promptLogSvc, teamSyncSvc, patternSvc, promotionSvc, teamRepo, memRepo });
+     registerTeamTools(server, {});

[NoCoverage] BlockStatement
src/mcp/team-gate.ts:26:21
-   ): Promise<boolean> {

-     if (_tokenValid !== null) return _tokenValid;

-     if (!railwayUrl) { _tokenValid = true; return true; } // local-only mode — trust presence

-     try {

-       const { default: postgres } = await import('postgres');

-       const sql = postgres(railwayUrl, { ssl: 'require', max: 1 });

-       await sql.unsafe(TEAM_SCHEMA_SQL);

-       const rows = await sql<{ token: string }[]>`

-         SELECT token FROM team_licenses

-         WHERE token = ${token} AND team_id = ${teamId}

-           AND revoked = FALSE

-           AND (expires_at IS NULL OR expires_at > NOW())

-       `;

-       await sql.end();

-       _tokenValid = rows.length > 0;

-     } catch {

-       _tokenValid = true; // Railway down — fail open so offline work is not blocked

-     }

-     return _tokenValid;

-   }

+   ): Promise<boolean> {}


[NoCoverage] ConditionalExpression
src/mcp/team-gate.ts:27:7
-     if (_tokenValid !== null) return _tokenValid;

+     if (true) return _tokenValid;


[NoCoverage] ConditionalExpression
src/mcp/team-gate.ts:27:7
-     if (_tokenValid !== null) return _tokenValid;

+     if (false) return _tokenValid;


[NoCoverage] EqualityOperator
src/mcp/team-gate.ts:27:7
-     if (_tokenValid !== null) return _tokenValid;

+     if (_tokenValid === null) return _tokenValid;


[NoCoverage] BooleanLiteral
src/mcp/team-gate.ts:28:7
-     if (!railwayUrl) { _tokenValid = true; return true; } // local-only mode — trust presence

+     if (railwayUrl) { _tokenValid = true; return true; } // local-only mode — trust presence


[NoCoverage] ConditionalExpression
src/mcp/team-gate.ts:28:7
-     if (!railwayUrl) { _tokenValid = true; return true; } // local-only mode — trust presence

+     if (true) { _tokenValid = true; return true; } // local-only mode — trust presence


[NoCoverage] ConditionalExpression
src/mcp/team-gate.ts:28:7
-     if (!railwayUrl) { _tokenValid = true; return true; } // local-only mode — trust presence

+     if (false) { _tokenValid = true; return true; } // local-only mode — trust presence


[NoCoverage] BlockStatement
src/mcp/team-gate.ts:28:20
-     if (!railwayUrl) { _tokenValid = true; return true; } // local-only mode — trust presence

+     if (!railwayUrl) {} // local-only mode — trust presence


[NoCoverage] BooleanLiteral
src/mcp/team-gate.ts:28:36
-     if (!railwayUrl) { _tokenValid = true; return true; } // local-only mode — trust presence

+     if (!railwayUrl) { _tokenValid = false; return true; } // local-only mode — trust presence


[NoCoverage] BooleanLiteral
src/mcp/team-gate.ts:28:49
-     if (!railwayUrl) { _tokenValid = true; return true; } // local-only mode — trust presence

+     if (!railwayUrl) { _tokenValid = true; return false; } // local-only mode — trust presence


[NoCoverage] BlockStatement
src/mcp/team-gate.ts:29:7
-     try {

-       const { default: postgres } = await import('postgres');

-       const sql = postgres(railwayUrl, { ssl: 'require', max: 1 });

-       await sql.unsafe(TEAM_SCHEMA_SQL);

-       const rows = await sql<{ token: string }[]>`

-         SELECT token FROM team_licenses

-         WHERE token = ${token} AND team_id = ${teamId}

-           AND revoked = FALSE

-           AND (expires_at IS NULL OR expires_at > NOW())

-       `;

-       await sql.end();

-       _tokenValid = rows.length > 0;

-     } catch {

+     try {} catch {


[NoCoverage] StringLiteral
src/mcp/team-gate.ts:30:48
-       const { default: postgres } = await import('postgres');

+       const { default: postgres } = await import("");


[NoCoverage] ObjectLiteral
src/mcp/team-gate.ts:31:38
-       const sql = postgres(railwayUrl, { ssl: 'require', max: 1 });

+       const sql = postgres(railwayUrl, {});


[NoCoverage] StringLiteral
src/mcp/team-gate.ts:31:45
-       const sql = postgres(railwayUrl, { ssl: 'require', max: 1 });

+       const sql = postgres(railwayUrl, { ssl: "", max: 1 });


[NoCoverage] StringLiteral
src/mcp/team-gate.ts:33:48
-       const rows = await sql<{ token: string }[]>`

-         SELECT token FROM team_licenses

-         WHERE token = ${token} AND team_id = ${teamId}

-           AND revoked = FALSE

-           AND (expires_at IS NULL OR expires_at > NOW())

-       `;

+       const rows = await sql<{ token: string }[]>``;


[NoCoverage] ConditionalExpression
src/mcp/team-gate.ts:40:19
-       _tokenValid = rows.length > 0;

+       _tokenValid = true;


[NoCoverage] ConditionalExpression
src/mcp/team-gate.ts:40:19
-       _tokenValid = rows.length > 0;

+       _tokenValid = false;


[NoCoverage] EqualityOperator
src/mcp/team-gate.ts:40:19
-       _tokenValid = rows.length > 0;

+       _tokenValid = rows.length >= 0;


[NoCoverage] EqualityOperator
src/mcp/team-gate.ts:40:19
-       _tokenValid = rows.length > 0;

+       _tokenValid = rows.length <= 0;


[NoCoverage] BlockStatement
src/mcp/team-gate.ts:41:11
-     } catch {

-       _tokenValid = true; // Railway down — fail open so offline work is not blocked

-     }

+     } catch {}


[NoCoverage] BooleanLiteral
src/mcp/team-gate.ts:42:19
-       _tokenValid = true; // Railway down — fail open so offline work is not blocked

+       _tokenValid = false; // Railway down — fail open so offline work is not blocked


[NoCoverage] BlockStatement
src/mcp/team-gate.ts:48:42
-   export function _resetTokenCache(): void {

-     _tokenValid = null;

-   }

+   export function _resetTokenCache(): void {}


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:24:46
-   function reply(payload: unknown): ToolResult {

-     return { content: [{ type: 'text', text: JSON.stringify(payload) }] };

-   }

+   function reply(payload: unknown): ToolResult {}


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:25:10
-     return { content: [{ type: 'text', text: JSON.stringify(payload) }] };

+     return {};


[NoCoverage] ArrayDeclaration
src/mcp/team-tools.ts:25:21
-     return { content: [{ type: 'text', text: JSON.stringify(payload) }] };

+     return { content: [] };


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:25:22
-     return { content: [{ type: 'text', text: JSON.stringify(payload) }] };

+     return { content: [{}] };


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:25:30
-     return { content: [{ type: 'text', text: JSON.stringify(payload) }] };

+     return { content: [{ type: "", text: JSON.stringify(payload) }] };


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:45:9
-   ): void {

-     const { teamSvc, promptLogSvc, teamSyncSvc, patternSvc, promotionSvc, teamRepo, memRepo } = deps;

-   

-     server.tool(

-       'team',

-       [

-         'Shared team knowledge built on Chronicle. Requires a Chronicle Team license (teamToken + teamId).',

-         '',

-         'SHARE a memory the rest of the team should inherit; RECALL pulls the team pool plus synthesized insights; LOG records what a prompt was trying to do (pattern only — raw text stays local unless you opt in).',

-         '',

-         'Memories in the shared pool stay attributed to their author; INSIGHTS are team-level syntheses curated by owners/leads.',

-       ].join('\n'),

-       {

-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

-           .describe('join=register-membership | share=push-memory-by-id | promote=auto-share-worthy-memories(deduped) | recall=search-team-pool | log=record-prompt-pattern | insights=team-practices | stats=usage(scope:me|team) | sync=push-pull | members=list | assign_role=set-member-role(owner/lead) | curate_insight=create/reinforce-team-insight(owner/lead) | mint_token=issue-a-new-team-token(owner)'),

-         // shared

-         id:      z.string().optional().describe('Memory ID — used with share'),

-         project: z.string().optional(),

-         // join

-         team_name: z.string().optional().describe('Display name when first creating the team'),

-         // recall / insights / curate_insight

-         query:        z.string().optional(),

-         limit:        z.number().optional(),

-         insight_type: z.enum(['practice', 'antipattern', 'profile', 'lesson']).optional(),

-         content:      z.string().optional().describe('Insight text — used with curate_insight'),

-         // assign_role (owner/lead only)

-         target_user_id: z.string().optional().describe('User whose role to set'),

-         role:            z.enum(['owner', 'lead', 'member']).optional().describe('Role to assign'),

-         // log

-         pattern:       z.string().optional().describe('What the prompt was trying to achieve'),

-         outcome:       z.enum(['good', 'bad', 'neutral']).optional(),

-         category:      z.string().optional(),

-         share_content: z.boolean().optional().describe('Also share raw prompt text — explicit opt-in only'),

-         raw_content:   z.string().optional(),

-         // stats

-         scope: z.enum(['me', 'team']).optional().describe('me=personal | team=aggregate (default: team)'),

-       },

-       async (args): Promise<ToolResult> => {

-         const config = getConfig();

-   

-         // ── License gate (shared with axon) ────────────────────────────────────

-         if (!config.teamToken) {

-           return reply({ error: 'Team features require a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>' });

-         }

-         if (!config.teamId) {

-           return reply({ error: 'Team features require teamId in ~/.chronicle/config.json.' });

-         }

-         const tokenOk = await validateTeamToken(config.teamToken, config.railwayUrl, config.teamId);

-         if (!tokenOk) {

-           return reply({ error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>' });

-         }

-   

-         const teamId = config.teamId;

-   

-         switch (args.action) {

-   

-           case 'join': {

-             const { team, member } = await teamSvc.joinTeam(teamId, args.team_name ?? teamId);

-             return reply({ message: `Joined team "${team.name}".`, teamId: team.id, role: member.role });

-           }

-   

-           case 'share': {

-             const memory = memRepo.findById(args.id ?? '');

-             if (!memory) return reply({ error: `Memory ${args.id} not found.` });

-             await teamSyncSvc.pushSharedMemory({

-               id: memory.id,

-               content: memory.content,

-               memoryType: memory.memoryType,

-               project: args.project ?? memory.project,

-               category: memory.category,

-               tags: [...memory.tags],

-             });

-             return reply({ message: 'Memory shared with team.', id: memory.id });

-           }

-   

-           case 'promote': {

-             const result = await promotionSvc.promote(args.project, args.limit);

-             return reply({

-               ...result,

-               message: result.railwaySkipped

-                 ? 'Skipped — railwayUrl not configured.'

-                 : `Promoted ${result.promoted.length} of ${result.scanned} candidate(s); skipped ${result.skipped.length}.`,

-             });

-           }

-   

-           case 'recall': {

-             const memories = teamRepo.searchSharedCache(teamId, args.query ?? '', args.project, args.limit ?? 20);

-             const insights = teamRepo.getInsights(teamId, args.project);

-             return reply({

-               sharedMemories: memories,

-               teamInsights: insights.map(i => ({ insightType: i.insightType, content: i.content, confidence: i.confidence, project: i.project })),

-             });

-           }

-   

-           case 'log': {

-             const log = promptLogSvc.log({

-               pattern: args.pattern ?? '',

-               outcome: args.outcome,

-               category: args.category,

-               project: args.project,

-               shareContent: args.share_content,

-               rawContent: args.raw_content,

-             });

-             return reply({ id: log.id, message: 'Prompt pattern logged.', category: log.category, outcome: log.outcome });

-           }

-   

-           case 'insights': {

-             let insights = teamRepo.getInsights(teamId, args.project);

-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

-             return reply(insights.map(i => ({ id: i.id, insightType: i.insightType, content: i.content, confidence: i.confidence, sourceCount: i.sourceCount, project: i.project })));

-           }

-   

-           case 'stats': {

-             const stats = (args.scope ?? 'team') === 'me'

-               ? patternSvc.myStats(args.project)

-               : patternSvc.teamStats(args.project);

-             return reply(stats);

-           }

-   

-           case 'sync': {

-             const result = await teamSyncSvc.sync();

-             return reply({ ...result, message: result.skipped ? 'Skipped — railwayUrl not configured.' : 'Team sync complete.' });

-           }

-   

-           case 'members': {

-             const members = await teamSvc.listMembers(teamId);

-             return reply(members.map(m => ({ userId: m.userId, role: m.role, joinedAt: m.joinedAt })));

-           }

-   

-           case 'assign_role': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (!role || !canCurate(role)) {

-               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role ?? 'not a member'}.` });

-             }

-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

-             const member = await teamSvc.assignRole(teamId, args.target_user_id, args.role);

-             return reply({ message: `Assigned ${args.role} to ${args.target_user_id}.`, member });

-           }

-   

-           case 'curate_insight': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (!role || !canCurate(role)) {

-               return reply({ error: `Curating insights requires owner or lead. Your role: ${role ?? 'not a member'}.` });

-             }

-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

-             const insight = await teamSyncSvc.pushInsight({ insightType: args.insight_type, content: args.content, project: args.project });

-             return reply({ message: 'Team insight curated.', insight });

-           }

-   

-           case 'mint_token': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (role !== 'owner') {

-               return reply({ error: `Minting tokens requires the owner role. Your role: ${role ?? 'not a member'}.` });

-             }

-             const minted = await teamSvc.mintToken(teamId);

-             return reply({

-               message: `New token minted for team ${teamId}. Share railwayUrl + teamId + this token with a teammate; they join as a member.`,

-               teamId: minted.teamId,

-               token: minted.token,

-             });

-           }

-   

-           default:

-             return reply({ error: `Unknown action: ${args.action as string}` });

-         }

-       },

-     );

-   }

+   ): void {}


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:49:5
-       'team',

+       "",


[NoCoverage] ArrayDeclaration
src/mcp/team-tools.ts:50:5
-       [

-         'Shared team knowledge built on Chronicle. Requires a Chronicle Team license (teamToken + teamId).',

-         '',

-         'SHARE a memory the rest of the team should inherit; RECALL pulls the team pool plus synthesized insights; LOG records what a prompt was trying to do (pattern only — raw text stays local unless you opt in).',

-         '',

-         'Memories in the shared pool stay attributed to their author; INSIGHTS are team-level syntheses curated by owners/leads.',

-       ].join('\n'),

+       [].join('\n'),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:51:7
-         'Shared team knowledge built on Chronicle. Requires a Chronicle Team license (teamToken + teamId).',

+         "",


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:52:7
-         '',

+         "Stryker was here!",


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:53:7
-         'SHARE a memory the rest of the team should inherit; RECALL pulls the team pool plus synthesized insights; LOG records what a prompt was trying to do (pattern only — raw text stays local unless you opt in).',

+         "",


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:54:7
-         '',

+         "Stryker was here!",


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:55:7
-         'Memories in the shared pool stay attributed to their author; INSIGHTS are team-level syntheses curated by owners/leads.',

+         "",


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:56:12
-       ].join('\n'),

+       ].join(""),


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:57:5
-       {

-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

-           .describe('join=register-membership | share=push-memory-by-id | promote=auto-share-worthy-memories(deduped) | recall=search-team-pool | log=record-prompt-pattern | insights=team-practices | stats=usage(scope:me|team) | sync=push-pull | members=list | assign_role=set-member-role(owner/lead) | curate_insight=create/reinforce-team-insight(owner/lead) | mint_token=issue-a-new-team-token(owner)'),

-         // shared

-         id:      z.string().optional().describe('Memory ID — used with share'),

-         project: z.string().optional(),

-         // join

-         team_name: z.string().optional().describe('Display name when first creating the team'),

-         // recall / insights / curate_insight

-         query:        z.string().optional(),

-         limit:        z.number().optional(),

-         insight_type: z.enum(['practice', 'antipattern', 'profile', 'lesson']).optional(),

-         content:      z.string().optional().describe('Insight text — used with curate_insight'),

-         // assign_role (owner/lead only)

-         target_user_id: z.string().optional().describe('User whose role to set'),

-         role:            z.enum(['owner', 'lead', 'member']).optional().describe('Role to assign'),

-         // log

-         pattern:       z.string().optional().describe('What the prompt was trying to achieve'),

-         outcome:       z.enum(['good', 'bad', 'neutral']).optional(),

-         category:      z.string().optional(),

-         share_content: z.boolean().optional().describe('Also share raw prompt text — explicit opt-in only'),

-         raw_content:   z.string().optional(),

-         // stats

-         scope: z.enum(['me', 'team']).optional().describe('me=personal | team=aggregate (default: team)'),

-       },

+       {},


[NoCoverage] ArrayDeclaration
src/mcp/team-tools.ts:58:22
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum([])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:23
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(["", 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:31
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', "", 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:40
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', 'share', "", 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:51
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', 'share', 'promote', "", 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:61
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', 'share', 'promote', 'recall', "", 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:68
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', 'share', 'promote', 'recall', 'log', "", 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:80
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', "", 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:89
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', "", 'members', 'assign_role', 'curate_insight', 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:97
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', "", 'assign_role', 'curate_insight', 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:108
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', "", 'curate_insight', 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:123
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', "", 'mint_token'])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:58:141
-         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', 'mint_token'])

+         action: z.enum(['join', 'share', 'promote', 'recall', 'log', 'insights', 'stats', 'sync', 'members', 'assign_role', 'curate_insight', ""])


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:59:19
-           .describe('join=register-membership | share=push-memory-by-id | promote=auto-share-worthy-memories(deduped) | recall=search-team-pool | log=record-prompt-pattern | insights=team-practices | stats=usage(scope:me|team) | sync=push-pull | members=list | assign_role=set-member-role(owner/lead) | curate_insight=create/reinforce-team-insight(owner/lead) | mint_token=issue-a-new-team-token(owner)'),

+           .describe(""),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:61:47
-         id:      z.string().optional().describe('Memory ID — used with share'),

+         id:      z.string().optional().describe(""),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:64:49
-         team_name: z.string().optional().describe('Display name when first creating the team'),

+         team_name: z.string().optional().describe(""),


[NoCoverage] ArrayDeclaration
src/mcp/team-tools.ts:68:28
-         insight_type: z.enum(['practice', 'antipattern', 'profile', 'lesson']).optional(),

+         insight_type: z.enum([]).optional(),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:68:29
-         insight_type: z.enum(['practice', 'antipattern', 'profile', 'lesson']).optional(),

+         insight_type: z.enum(["", 'antipattern', 'profile', 'lesson']).optional(),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:68:41
-         insight_type: z.enum(['practice', 'antipattern', 'profile', 'lesson']).optional(),

+         insight_type: z.enum(['practice', "", 'profile', 'lesson']).optional(),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:68:56
-         insight_type: z.enum(['practice', 'antipattern', 'profile', 'lesson']).optional(),

+         insight_type: z.enum(['practice', 'antipattern', "", 'lesson']).optional(),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:68:67
-         insight_type: z.enum(['practice', 'antipattern', 'profile', 'lesson']).optional(),

+         insight_type: z.enum(['practice', 'antipattern', 'profile', ""]).optional(),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:69:52
-         content:      z.string().optional().describe('Insight text — used with curate_insight'),

+         content:      z.string().optional().describe(""),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:71:54
-         target_user_id: z.string().optional().describe('User whose role to set'),

+         target_user_id: z.string().optional().describe(""),


[NoCoverage] ArrayDeclaration
src/mcp/team-tools.ts:72:31
-         role:            z.enum(['owner', 'lead', 'member']).optional().describe('Role to assign'),

+         role:            z.enum([]).optional().describe('Role to assign'),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:72:32
-         role:            z.enum(['owner', 'lead', 'member']).optional().describe('Role to assign'),

+         role:            z.enum(["", 'lead', 'member']).optional().describe('Role to assign'),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:72:41
-         role:            z.enum(['owner', 'lead', 'member']).optional().describe('Role to assign'),

+         role:            z.enum(['owner', "", 'member']).optional().describe('Role to assign'),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:72:49
-         role:            z.enum(['owner', 'lead', 'member']).optional().describe('Role to assign'),

+         role:            z.enum(['owner', 'lead', ""]).optional().describe('Role to assign'),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:72:80
-         role:            z.enum(['owner', 'lead', 'member']).optional().describe('Role to assign'),

+         role:            z.enum(['owner', 'lead', 'member']).optional().describe(""),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:74:53
-         pattern:       z.string().optional().describe('What the prompt was trying to achieve'),

+         pattern:       z.string().optional().describe(""),


[NoCoverage] ArrayDeclaration
src/mcp/team-tools.ts:75:29
-         outcome:       z.enum(['good', 'bad', 'neutral']).optional(),

+         outcome:       z.enum([]).optional(),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:75:30
-         outcome:       z.enum(['good', 'bad', 'neutral']).optional(),

+         outcome:       z.enum(["", 'bad', 'neutral']).optional(),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:75:38
-         outcome:       z.enum(['good', 'bad', 'neutral']).optional(),

+         outcome:       z.enum(['good', "", 'neutral']).optional(),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:75:45
-         outcome:       z.enum(['good', 'bad', 'neutral']).optional(),

+         outcome:       z.enum(['good', 'bad', ""]).optional(),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:77:54
-         share_content: z.boolean().optional().describe('Also share raw prompt text — explicit opt-in only'),

+         share_content: z.boolean().optional().describe(""),


[NoCoverage] ArrayDeclaration
src/mcp/team-tools.ts:80:21
-         scope: z.enum(['me', 'team']).optional().describe('me=personal | team=aggregate (default: team)'),

+         scope: z.enum([]).optional().describe('me=personal | team=aggregate (default: team)'),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:80:22
-         scope: z.enum(['me', 'team']).optional().describe('me=personal | team=aggregate (default: team)'),

+         scope: z.enum(["", 'team']).optional().describe('me=personal | team=aggregate (default: team)'),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:80:28
-         scope: z.enum(['me', 'team']).optional().describe('me=personal | team=aggregate (default: team)'),

+         scope: z.enum(['me', ""]).optional().describe('me=personal | team=aggregate (default: team)'),


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:80:57
-         scope: z.enum(['me', 'team']).optional().describe('me=personal | team=aggregate (default: team)'),

+         scope: z.enum(['me', 'team']).optional().describe(""),


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:82:42
-       async (args): Promise<ToolResult> => {

-         const config = getConfig();

-   

-         // ── License gate (shared with axon) ────────────────────────────────────

-         if (!config.teamToken) {

-           return reply({ error: 'Team features require a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>' });

-         }

-         if (!config.teamId) {

-           return reply({ error: 'Team features require teamId in ~/.chronicle/config.json.' });

-         }

-         const tokenOk = await validateTeamToken(config.teamToken, config.railwayUrl, config.teamId);

-         if (!tokenOk) {

-           return reply({ error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>' });

-         }

-   

-         const teamId = config.teamId;

-   

-         switch (args.action) {

-   

-           case 'join': {

-             const { team, member } = await teamSvc.joinTeam(teamId, args.team_name ?? teamId);

-             return reply({ message: `Joined team "${team.name}".`, teamId: team.id, role: member.role });

-           }

-   

-           case 'share': {

-             const memory = memRepo.findById(args.id ?? '');

-             if (!memory) return reply({ error: `Memory ${args.id} not found.` });

-             await teamSyncSvc.pushSharedMemory({

-               id: memory.id,

-               content: memory.content,

-               memoryType: memory.memoryType,

-               project: args.project ?? memory.project,

-               category: memory.category,

-               tags: [...memory.tags],

-             });

-             return reply({ message: 'Memory shared with team.', id: memory.id });

-           }

-   

-           case 'promote': {

-             const result = await promotionSvc.promote(args.project, args.limit);

-             return reply({

-               ...result,

-               message: result.railwaySkipped

-                 ? 'Skipped — railwayUrl not configured.'

-                 : `Promoted ${result.promoted.length} of ${result.scanned} candidate(s); skipped ${result.skipped.length}.`,

-             });

-           }

-   

-           case 'recall': {

-             const memories = teamRepo.searchSharedCache(teamId, args.query ?? '', args.project, args.limit ?? 20);

-             const insights = teamRepo.getInsights(teamId, args.project);

-             return reply({

-               sharedMemories: memories,

-               teamInsights: insights.map(i => ({ insightType: i.insightType, content: i.content, confidence: i.confidence, project: i.project })),

-             });

-           }

-   

-           case 'log': {

-             const log = promptLogSvc.log({

-               pattern: args.pattern ?? '',

-               outcome: args.outcome,

-               category: args.category,

-               project: args.project,

-               shareContent: args.share_content,

-               rawContent: args.raw_content,

-             });

-             return reply({ id: log.id, message: 'Prompt pattern logged.', category: log.category, outcome: log.outcome });

-           }

-   

-           case 'insights': {

-             let insights = teamRepo.getInsights(teamId, args.project);

-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

-             return reply(insights.map(i => ({ id: i.id, insightType: i.insightType, content: i.content, confidence: i.confidence, sourceCount: i.sourceCount, project: i.project })));

-           }

-   

-           case 'stats': {

-             const stats = (args.scope ?? 'team') === 'me'

-               ? patternSvc.myStats(args.project)

-               : patternSvc.teamStats(args.project);

-             return reply(stats);

-           }

-   

-           case 'sync': {

-             const result = await teamSyncSvc.sync();

-             return reply({ ...result, message: result.skipped ? 'Skipped — railwayUrl not configured.' : 'Team sync complete.' });

-           }

-   

-           case 'members': {

-             const members = await teamSvc.listMembers(teamId);

-             return reply(members.map(m => ({ userId: m.userId, role: m.role, joinedAt: m.joinedAt })));

-           }

-   

-           case 'assign_role': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (!role || !canCurate(role)) {

-               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role ?? 'not a member'}.` });

-             }

-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

-             const member = await teamSvc.assignRole(teamId, args.target_user_id, args.role);

-             return reply({ message: `Assigned ${args.role} to ${args.target_user_id}.`, member });

-           }

-   

-           case 'curate_insight': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (!role || !canCurate(role)) {

-               return reply({ error: `Curating insights requires owner or lead. Your role: ${role ?? 'not a member'}.` });

-             }

-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

-             const insight = await teamSyncSvc.pushInsight({ insightType: args.insight_type, content: args.content, project: args.project });

-             return reply({ message: 'Team insight curated.', insight });

-           }

-   

-           case 'mint_token': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (role !== 'owner') {

-               return reply({ error: `Minting tokens requires the owner role. Your role: ${role ?? 'not a member'}.` });

-             }

-             const minted = await teamSvc.mintToken(teamId);

-             return reply({

-               message: `New token minted for team ${teamId}. Share railwayUrl + teamId + this token with a teammate; they join as a member.`,

-               teamId: minted.teamId,

-               token: minted.token,

-             });

-           }

-   

-           default:

-             return reply({ error: `Unknown action: ${args.action as string}` });

-         }

-       },

+       async (args): Promise<ToolResult> => {},


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:86:11
-         if (!config.teamToken) {

+         if (config.teamToken) {


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:86:11
-         if (!config.teamToken) {

+         if (true) {


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:86:11
-         if (!config.teamToken) {

+         if (false) {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:86:30
-         if (!config.teamToken) {

-           return reply({ error: 'Team features require a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>' });

-         }

+         if (!config.teamToken) {}


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:87:22
-           return reply({ error: 'Team features require a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>' });

+           return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:87:31
-           return reply({ error: 'Team features require a Chronicle Team license. Add teamToken to ~/.chronicle/config.json. Generate one with: chronicle-mcp generate-token --team <slug>' });

+           return reply({ error: "" });


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:89:11
-         if (!config.teamId) {

+         if (config.teamId) {


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:89:11
-         if (!config.teamId) {

+         if (true) {


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:89:11
-         if (!config.teamId) {

+         if (false) {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:89:27
-         if (!config.teamId) {

-           return reply({ error: 'Team features require teamId in ~/.chronicle/config.json.' });

-         }

+         if (!config.teamId) {}


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:90:22
-           return reply({ error: 'Team features require teamId in ~/.chronicle/config.json.' });

+           return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:90:31
-           return reply({ error: 'Team features require teamId in ~/.chronicle/config.json.' });

+           return reply({ error: "" });


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:93:11
-         if (!tokenOk) {

+         if (tokenOk) {


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:93:11
-         if (!tokenOk) {

+         if (true) {


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:93:11
-         if (!tokenOk) {

+         if (false) {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:93:21
-         if (!tokenOk) {

-           return reply({ error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>' });

-         }

+         if (!tokenOk) {}


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:94:22
-           return reply({ error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>' });

+           return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:94:31
-           return reply({ error: 'Invalid or revoked teamToken. Generate a new one with: chronicle-mcp generate-token --team <slug>' });

+           return reply({ error: "" });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:101:9
-           case 'join': {

-             const { team, member } = await teamSvc.joinTeam(teamId, args.team_name ?? teamId);

-             return reply({ message: `Joined team "${team.name}".`, teamId: team.id, role: member.role });

-           }

+           case 'join':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:101:14
-           case 'join': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:101:22
-           case 'join': {

-             const { team, member } = await teamSvc.joinTeam(teamId, args.team_name ?? teamId);

-             return reply({ message: `Joined team "${team.name}".`, teamId: team.id, role: member.role });

-           }

+           case 'join': {}


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:102:67
-             const { team, member } = await teamSvc.joinTeam(teamId, args.team_name ?? teamId);

+             const { team, member } = await teamSvc.joinTeam(teamId, args.team_name && teamId);


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:103:24
-             return reply({ message: `Joined team "${team.name}".`, teamId: team.id, role: member.role });

+             return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:103:35
-             return reply({ message: `Joined team "${team.name}".`, teamId: team.id, role: member.role });

+             return reply({ message: ``, teamId: team.id, role: member.role });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:106:9
-           case 'share': {

-             const memory = memRepo.findById(args.id ?? '');

-             if (!memory) return reply({ error: `Memory ${args.id} not found.` });

-             await teamSyncSvc.pushSharedMemory({

-               id: memory.id,

-               content: memory.content,

-               memoryType: memory.memoryType,

-               project: args.project ?? memory.project,

-               category: memory.category,

-               tags: [...memory.tags],

-             });

-             return reply({ message: 'Memory shared with team.', id: memory.id });

-           }

+           case 'share':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:106:14
-           case 'share': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:106:23
-           case 'share': {

-             const memory = memRepo.findById(args.id ?? '');

-             if (!memory) return reply({ error: `Memory ${args.id} not found.` });

-             await teamSyncSvc.pushSharedMemory({

-               id: memory.id,

-               content: memory.content,

-               memoryType: memory.memoryType,

-               project: args.project ?? memory.project,

-               category: memory.category,

-               tags: [...memory.tags],

-             });

-             return reply({ message: 'Memory shared with team.', id: memory.id });

-           }

+           case 'share': {}


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:107:43
-             const memory = memRepo.findById(args.id ?? '');

+             const memory = memRepo.findById(args.id && '');


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:107:54
-             const memory = memRepo.findById(args.id ?? '');

+             const memory = memRepo.findById(args.id ?? "Stryker was here!");


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:108:15
-             if (!memory) return reply({ error: `Memory ${args.id} not found.` });

+             if (memory) return reply({ error: `Memory ${args.id} not found.` });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:108:15
-             if (!memory) return reply({ error: `Memory ${args.id} not found.` });

+             if (true) return reply({ error: `Memory ${args.id} not found.` });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:108:15
-             if (!memory) return reply({ error: `Memory ${args.id} not found.` });

+             if (false) return reply({ error: `Memory ${args.id} not found.` });


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:108:37
-             if (!memory) return reply({ error: `Memory ${args.id} not found.` });

+             if (!memory) return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:108:46
-             if (!memory) return reply({ error: `Memory ${args.id} not found.` });

+             if (!memory) return reply({ error: `` });


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:109:46
-             await teamSyncSvc.pushSharedMemory({

-               id: memory.id,

-               content: memory.content,

-               memoryType: memory.memoryType,

-               project: args.project ?? memory.project,

-               category: memory.category,

-               tags: [...memory.tags],

-             });

+             await teamSyncSvc.pushSharedMemory({});


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:113:22
-               project: args.project ?? memory.project,

+               project: args.project && memory.project,


[NoCoverage] ArrayDeclaration
src/mcp/team-tools.ts:115:19
-               tags: [...memory.tags],

+               tags: [],


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:117:24
-             return reply({ message: 'Memory shared with team.', id: memory.id });

+             return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:117:35
-             return reply({ message: 'Memory shared with team.', id: memory.id });

+             return reply({ message: "", id: memory.id });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:120:9
-           case 'promote': {

-             const result = await promotionSvc.promote(args.project, args.limit);

-             return reply({

-               ...result,

-               message: result.railwaySkipped

-                 ? 'Skipped — railwayUrl not configured.'

-                 : `Promoted ${result.promoted.length} of ${result.scanned} candidate(s); skipped ${result.skipped.length}.`,

-             });

-           }

+           case 'promote':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:120:14
-           case 'promote': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:120:25
-           case 'promote': {

-             const result = await promotionSvc.promote(args.project, args.limit);

-             return reply({

-               ...result,

-               message: result.railwaySkipped

-                 ? 'Skipped — railwayUrl not configured.'

-                 : `Promoted ${result.promoted.length} of ${result.scanned} candidate(s); skipped ${result.skipped.length}.`,

-             });

-           }

+           case 'promote': {}


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:122:24
-             return reply({

-               ...result,

-               message: result.railwaySkipped

-                 ? 'Skipped — railwayUrl not configured.'

-                 : `Promoted ${result.promoted.length} of ${result.scanned} candidate(s); skipped ${result.skipped.length}.`,

-             });

+             return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:125:17
-                 ? 'Skipped — railwayUrl not configured.'

+                 ? ""


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:126:17
-                 : `Promoted ${result.promoted.length} of ${result.scanned} candidate(s); skipped ${result.skipped.length}.`,

+                 : ``,


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:130:9
-           case 'recall': {

-             const memories = teamRepo.searchSharedCache(teamId, args.query ?? '', args.project, args.limit ?? 20);

-             const insights = teamRepo.getInsights(teamId, args.project);

-             return reply({

-               sharedMemories: memories,

-               teamInsights: insights.map(i => ({ insightType: i.insightType, content: i.content, confidence: i.confidence, project: i.project })),

-             });

-           }

+           case 'recall':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:130:14
-           case 'recall': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:130:24
-           case 'recall': {

-             const memories = teamRepo.searchSharedCache(teamId, args.query ?? '', args.project, args.limit ?? 20);

-             const insights = teamRepo.getInsights(teamId, args.project);

-             return reply({

-               sharedMemories: memories,

-               teamInsights: insights.map(i => ({ insightType: i.insightType, content: i.content, confidence: i.confidence, project: i.project })),

-             });

-           }

+           case 'recall': {}


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:131:63
-             const memories = teamRepo.searchSharedCache(teamId, args.query ?? '', args.project, args.limit ?? 20);

+             const memories = teamRepo.searchSharedCache(teamId, args.query && '', args.project, args.limit ?? 20);


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:131:77
-             const memories = teamRepo.searchSharedCache(teamId, args.query ?? '', args.project, args.limit ?? 20);

+             const memories = teamRepo.searchSharedCache(teamId, args.query ?? "Stryker was here!", args.project, args.limit ?? 20);


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:131:95
-             const memories = teamRepo.searchSharedCache(teamId, args.query ?? '', args.project, args.limit ?? 20);

+             const memories = teamRepo.searchSharedCache(teamId, args.query ?? '', args.project, args.limit && 20);


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:133:24
-             return reply({

-               sharedMemories: memories,

-               teamInsights: insights.map(i => ({ insightType: i.insightType, content: i.content, confidence: i.confidence, project: i.project })),

-             });

+             return reply({});


[NoCoverage] ArrowFunction
src/mcp/team-tools.ts:135:40
-               teamInsights: insights.map(i => ({ insightType: i.insightType, content: i.content, confidence: i.confidence, project: i.project })),

+               teamInsights: insights.map(() => undefined),


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:135:46
-               teamInsights: insights.map(i => ({ insightType: i.insightType, content: i.content, confidence: i.confidence, project: i.project })),

+               teamInsights: insights.map(i => ({})),


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:139:9
-           case 'log': {

-             const log = promptLogSvc.log({

-               pattern: args.pattern ?? '',

-               outcome: args.outcome,

-               category: args.category,

-               project: args.project,

-               shareContent: args.share_content,

-               rawContent: args.raw_content,

-             });

-             return reply({ id: log.id, message: 'Prompt pattern logged.', category: log.category, outcome: log.outcome });

-           }

+           case 'log':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:139:14
-           case 'log': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:139:21
-           case 'log': {

-             const log = promptLogSvc.log({

-               pattern: args.pattern ?? '',

-               outcome: args.outcome,

-               category: args.category,

-               project: args.project,

-               shareContent: args.share_content,

-               rawContent: args.raw_content,

-             });

-             return reply({ id: log.id, message: 'Prompt pattern logged.', category: log.category, outcome: log.outcome });

-           }

+           case 'log': {}


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:140:40
-             const log = promptLogSvc.log({

-               pattern: args.pattern ?? '',

-               outcome: args.outcome,

-               category: args.category,

-               project: args.project,

-               shareContent: args.share_content,

-               rawContent: args.raw_content,

-             });

+             const log = promptLogSvc.log({});


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:141:22
-               pattern: args.pattern ?? '',

+               pattern: args.pattern && '',


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:141:38
-               pattern: args.pattern ?? '',

+               pattern: args.pattern ?? "Stryker was here!",


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:148:24
-             return reply({ id: log.id, message: 'Prompt pattern logged.', category: log.category, outcome: log.outcome });

+             return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:148:47
-             return reply({ id: log.id, message: 'Prompt pattern logged.', category: log.category, outcome: log.outcome });

+             return reply({ id: log.id, message: "", category: log.category, outcome: log.outcome });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:151:9
-           case 'insights': {

-             let insights = teamRepo.getInsights(teamId, args.project);

-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

-             return reply(insights.map(i => ({ id: i.id, insightType: i.insightType, content: i.content, confidence: i.confidence, sourceCount: i.sourceCount, project: i.project })));

-           }

+           case 'insights':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:151:14
-           case 'insights': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:151:26
-           case 'insights': {

-             let insights = teamRepo.getInsights(teamId, args.project);

-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

-             return reply(insights.map(i => ({ id: i.id, insightType: i.insightType, content: i.content, confidence: i.confidence, sourceCount: i.sourceCount, project: i.project })));

-           }

+           case 'insights': {}


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:153:15
-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

+             if (true) insights = insights.filter(i => i.insightType === args.insight_type);


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:153:15
-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

+             if (false) insights = insights.filter(i => i.insightType === args.insight_type);


[NoCoverage] MethodExpression
src/mcp/team-tools.ts:153:45
-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

+             if (args.insight_type) insights = insights;


[NoCoverage] ArrowFunction
src/mcp/team-tools.ts:153:61
-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

+             if (args.insight_type) insights = insights.filter(() => undefined);


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:153:66
-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

+             if (args.insight_type) insights = insights.filter(i => true);


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:153:66
-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

+             if (args.insight_type) insights = insights.filter(i => false);


[NoCoverage] EqualityOperator
src/mcp/team-tools.ts:153:66
-             if (args.insight_type) insights = insights.filter(i => i.insightType === args.insight_type);

+             if (args.insight_type) insights = insights.filter(i => i.insightType !== args.insight_type);


[NoCoverage] ArrowFunction
src/mcp/team-tools.ts:154:37
-             return reply(insights.map(i => ({ id: i.id, insightType: i.insightType, content: i.content, confidence: i.confidence, sourceCount: i.sourceCount, project: i.project })));

+             return reply(insights.map(() => undefined));


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:154:43
-             return reply(insights.map(i => ({ id: i.id, insightType: i.insightType, content: i.content, confidence: i.confidence, sourceCount: i.sourceCount, project: i.project })));

+             return reply(insights.map(i => ({})));


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:157:9
-           case 'stats': {

-             const stats = (args.scope ?? 'team') === 'me'

-               ? patternSvc.myStats(args.project)

-               : patternSvc.teamStats(args.project);

-             return reply(stats);

-           }

+           case 'stats':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:157:14
-           case 'stats': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:157:23
-           case 'stats': {

-             const stats = (args.scope ?? 'team') === 'me'

-               ? patternSvc.myStats(args.project)

-               : patternSvc.teamStats(args.project);

-             return reply(stats);

-           }

+           case 'stats': {}


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:158:25
-             const stats = (args.scope ?? 'team') === 'me'

+             const stats = true


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:158:25
-             const stats = (args.scope ?? 'team') === 'me'

+             const stats = false


[NoCoverage] EqualityOperator
src/mcp/team-tools.ts:158:25
-             const stats = (args.scope ?? 'team') === 'me'

+             const stats = (args.scope ?? 'team') !== 'me'


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:158:26
-             const stats = (args.scope ?? 'team') === 'me'

+             const stats = (args.scope && 'team') === 'me'


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:158:40
-             const stats = (args.scope ?? 'team') === 'me'

+             const stats = (args.scope ?? "") === 'me'


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:158:52
-             const stats = (args.scope ?? 'team') === 'me'

+             const stats = (args.scope ?? 'team') === ""


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:164:9
-           case 'sync': {

-             const result = await teamSyncSvc.sync();

-             return reply({ ...result, message: result.skipped ? 'Skipped — railwayUrl not configured.' : 'Team sync complete.' });

-           }

+           case 'sync':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:164:14
-           case 'sync': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:164:22
-           case 'sync': {

-             const result = await teamSyncSvc.sync();

-             return reply({ ...result, message: result.skipped ? 'Skipped — railwayUrl not configured.' : 'Team sync complete.' });

-           }

+           case 'sync': {}


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:166:24
-             return reply({ ...result, message: result.skipped ? 'Skipped — railwayUrl not configured.' : 'Team sync complete.' });

+             return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:166:63
-             return reply({ ...result, message: result.skipped ? 'Skipped — railwayUrl not configured.' : 'Team sync complete.' });

+             return reply({ ...result, message: result.skipped ? "" : 'Team sync complete.' });


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:166:104
-             return reply({ ...result, message: result.skipped ? 'Skipped — railwayUrl not configured.' : 'Team sync complete.' });

+             return reply({ ...result, message: result.skipped ? 'Skipped — railwayUrl not configured.' : "" });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:169:9
-           case 'members': {

-             const members = await teamSvc.listMembers(teamId);

-             return reply(members.map(m => ({ userId: m.userId, role: m.role, joinedAt: m.joinedAt })));

-           }

+           case 'members':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:169:14
-           case 'members': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:169:25
-           case 'members': {

-             const members = await teamSvc.listMembers(teamId);

-             return reply(members.map(m => ({ userId: m.userId, role: m.role, joinedAt: m.joinedAt })));

-           }

+           case 'members': {}


[NoCoverage] ArrowFunction
src/mcp/team-tools.ts:171:36
-             return reply(members.map(m => ({ userId: m.userId, role: m.role, joinedAt: m.joinedAt })));

+             return reply(members.map(() => undefined));


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:171:42
-             return reply(members.map(m => ({ userId: m.userId, role: m.role, joinedAt: m.joinedAt })));

+             return reply(members.map(m => ({})));


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:174:9
-           case 'assign_role': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (!role || !canCurate(role)) {

-               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role ?? 'not a member'}.` });

-             }

-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

-             const member = await teamSvc.assignRole(teamId, args.target_user_id, args.role);

-             return reply({ message: `Assigned ${args.role} to ${args.target_user_id}.`, member });

-           }

+           case 'assign_role':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:174:14
-           case 'assign_role': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:174:29
-           case 'assign_role': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (!role || !canCurate(role)) {

-               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role ?? 'not a member'}.` });

-             }

-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

-             const member = await teamSvc.assignRole(teamId, args.target_user_id, args.role);

-             return reply({ message: `Assigned ${args.role} to ${args.target_user_id}.`, member });

-           }

+           case 'assign_role': {}


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:176:15
-             if (!role || !canCurate(role)) {

+             if (true) {


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:176:15
-             if (!role || !canCurate(role)) {

+             if (false) {


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:176:15
-             if (!role || !canCurate(role)) {

+             if (!role && !canCurate(role)) {


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:176:15
-             if (!role || !canCurate(role)) {

+             if (role || !canCurate(role)) {


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:176:24
-             if (!role || !canCurate(role)) {

+             if (!role || canCurate(role)) {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:176:42
-             if (!role || !canCurate(role)) {

-               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role ?? 'not a member'}.` });

-             }

+             if (!role || !canCurate(role)) {}


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:177:26
-               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role ?? 'not a member'}.` });

+               return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:177:35
-               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role ?? 'not a member'}.` });

+               return reply({ error: `` });


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:177:89
-               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role ?? 'not a member'}.` });

+               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role && 'not a member'}.` });


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:177:97
-               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role ?? 'not a member'}.` });

+               return reply({ error: `Assigning roles requires owner or lead. Your role: ${role ?? ""}.` });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:179:15
-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

+             if (true) return reply({ error: 'target_user_id and role are required.' });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:179:15
-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

+             if (false) return reply({ error: 'target_user_id and role are required.' });


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:179:15
-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

+             if (!args.target_user_id && !args.role) return reply({ error: 'target_user_id and role are required.' });


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:179:15
-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

+             if (args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:179:39
-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

+             if (!args.target_user_id || args.role) return reply({ error: 'target_user_id and role are required.' });


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:179:64
-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

+             if (!args.target_user_id || !args.role) return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:179:73
-             if (!args.target_user_id || !args.role) return reply({ error: 'target_user_id and role are required.' });

+             if (!args.target_user_id || !args.role) return reply({ error: "" });


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:181:24
-             return reply({ message: `Assigned ${args.role} to ${args.target_user_id}.`, member });

+             return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:181:35
-             return reply({ message: `Assigned ${args.role} to ${args.target_user_id}.`, member });

+             return reply({ message: ``, member });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:184:9
-           case 'curate_insight': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (!role || !canCurate(role)) {

-               return reply({ error: `Curating insights requires owner or lead. Your role: ${role ?? 'not a member'}.` });

-             }

-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

-             const insight = await teamSyncSvc.pushInsight({ insightType: args.insight_type, content: args.content, project: args.project });

-             return reply({ message: 'Team insight curated.', insight });

-           }

+           case 'curate_insight':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:184:14
-           case 'curate_insight': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:184:32
-           case 'curate_insight': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (!role || !canCurate(role)) {

-               return reply({ error: `Curating insights requires owner or lead. Your role: ${role ?? 'not a member'}.` });

-             }

-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

-             const insight = await teamSyncSvc.pushInsight({ insightType: args.insight_type, content: args.content, project: args.project });

-             return reply({ message: 'Team insight curated.', insight });

-           }

+           case 'curate_insight': {}


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:186:15
-             if (!role || !canCurate(role)) {

+             if (true) {


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:186:15
-             if (!role || !canCurate(role)) {

+             if (false) {


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:186:15
-             if (!role || !canCurate(role)) {

+             if (!role && !canCurate(role)) {


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:186:15
-             if (!role || !canCurate(role)) {

+             if (role || !canCurate(role)) {


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:186:24
-             if (!role || !canCurate(role)) {

+             if (!role || canCurate(role)) {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:186:42
-             if (!role || !canCurate(role)) {

-               return reply({ error: `Curating insights requires owner or lead. Your role: ${role ?? 'not a member'}.` });

-             }

+             if (!role || !canCurate(role)) {}


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:187:26
-               return reply({ error: `Curating insights requires owner or lead. Your role: ${role ?? 'not a member'}.` });

+               return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:187:35
-               return reply({ error: `Curating insights requires owner or lead. Your role: ${role ?? 'not a member'}.` });

+               return reply({ error: `` });


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:187:91
-               return reply({ error: `Curating insights requires owner or lead. Your role: ${role ?? 'not a member'}.` });

+               return reply({ error: `Curating insights requires owner or lead. Your role: ${role && 'not a member'}.` });


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:187:99
-               return reply({ error: `Curating insights requires owner or lead. Your role: ${role ?? 'not a member'}.` });

+               return reply({ error: `Curating insights requires owner or lead. Your role: ${role ?? ""}.` });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:189:15
-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

+             if (true) return reply({ error: 'insight_type and content are required.' });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:189:15
-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

+             if (false) return reply({ error: 'insight_type and content are required.' });


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:189:15
-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

+             if (!args.insight_type && !args.content) return reply({ error: 'insight_type and content are required.' });


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:189:15
-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

+             if (args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });


[NoCoverage] BooleanLiteral
src/mcp/team-tools.ts:189:37
-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

+             if (!args.insight_type || args.content) return reply({ error: 'insight_type and content are required.' });


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:189:65
-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

+             if (!args.insight_type || !args.content) return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:189:74
-             if (!args.insight_type || !args.content) return reply({ error: 'insight_type and content are required.' });

+             if (!args.insight_type || !args.content) return reply({ error: "" });


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:190:57
-             const insight = await teamSyncSvc.pushInsight({ insightType: args.insight_type, content: args.content, project: args.project });

+             const insight = await teamSyncSvc.pushInsight({});


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:191:24
-             return reply({ message: 'Team insight curated.', insight });

+             return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:191:35
-             return reply({ message: 'Team insight curated.', insight });

+             return reply({ message: "", insight });


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:194:9
-           case 'mint_token': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (role !== 'owner') {

-               return reply({ error: `Minting tokens requires the owner role. Your role: ${role ?? 'not a member'}.` });

-             }

-             const minted = await teamSvc.mintToken(teamId);

-             return reply({

-               message: `New token minted for team ${teamId}. Share railwayUrl + teamId + this token with a teammate; they join as a member.`,

-               teamId: minted.teamId,

-               token: minted.token,

-             });

-           }

+           case 'mint_token':


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:194:14
-           case 'mint_token': {

+           case "": {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:194:28
-           case 'mint_token': {

-             const role = await teamSvc.getMemberRole(teamId);

-             if (role !== 'owner') {

-               return reply({ error: `Minting tokens requires the owner role. Your role: ${role ?? 'not a member'}.` });

-             }

-             const minted = await teamSvc.mintToken(teamId);

-             return reply({

-               message: `New token minted for team ${teamId}. Share railwayUrl + teamId + this token with a teammate; they join as a member.`,

-               teamId: minted.teamId,

-               token: minted.token,

-             });

-           }

+           case 'mint_token': {}


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:196:15
-             if (role !== 'owner') {

+             if (true) {


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:196:15
-             if (role !== 'owner') {

+             if (false) {


[NoCoverage] EqualityOperator
src/mcp/team-tools.ts:196:15
-             if (role !== 'owner') {

+             if (role === 'owner') {


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:196:24
-             if (role !== 'owner') {

+             if (role !== "") {


[NoCoverage] BlockStatement
src/mcp/team-tools.ts:196:33
-             if (role !== 'owner') {

-               return reply({ error: `Minting tokens requires the owner role. Your role: ${role ?? 'not a member'}.` });

-             }

+             if (role !== 'owner') {}


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:197:26
-               return reply({ error: `Minting tokens requires the owner role. Your role: ${role ?? 'not a member'}.` });

+               return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:197:35
-               return reply({ error: `Minting tokens requires the owner role. Your role: ${role ?? 'not a member'}.` });

+               return reply({ error: `` });


[NoCoverage] LogicalOperator
src/mcp/team-tools.ts:197:89
-               return reply({ error: `Minting tokens requires the owner role. Your role: ${role ?? 'not a member'}.` });

+               return reply({ error: `Minting tokens requires the owner role. Your role: ${role && 'not a member'}.` });


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:197:97
-               return reply({ error: `Minting tokens requires the owner role. Your role: ${role ?? 'not a member'}.` });

+               return reply({ error: `Minting tokens requires the owner role. Your role: ${role ?? ""}.` });


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:200:24
-             return reply({

-               message: `New token minted for team ${teamId}. Share railwayUrl + teamId + this token with a teammate; they join as a member.`,

-               teamId: minted.teamId,

-               token: minted.token,

-             });

+             return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:201:22
-               message: `New token minted for team ${teamId}. Share railwayUrl + teamId + this token with a teammate; they join as a member.`,

+               message: ``,


[NoCoverage] ConditionalExpression
src/mcp/team-tools.ts:207:9
-           default:

-             return reply({ error: `Unknown action: ${args.action as string}` });

+           default:


[NoCoverage] ObjectLiteral
src/mcp/team-tools.ts:208:24
-             return reply({ error: `Unknown action: ${args.action as string}` });

+             return reply({});


[NoCoverage] StringLiteral
src/mcp/team-tools.ts:208:33
-             return reply({ error: `Unknown action: ${args.action as string}` });

+             return reply({ error: `` });


[NoCoverage] ArrayDeclaration
src/services/coordination-service.ts:247:50
-         const queue = [...(dependents.get(n.id) ?? [])];
+         const queue = [...(dependents.get(n.id) ?? ["Stryker was here"])];

[NoCoverage] ArrayDeclaration
src/services/coordination-service.ts:252:48
-           for (const d of dependents.get(cur) ?? []) queue.push(d);
+           for (const d of dependents.get(cur) ?? ["Stryker was here"]) queue.push(d);

[NoCoverage] StringLiteral
src/services/coordination-service.ts:363:61
-         const deps: string[] = JSON.parse(row.dependencies ?? '[]');
+         const deps: string[] = JSON.parse(row.dependencies ?? "");

[NoCoverage] BlockStatement
src/services/coordination-service.ts:387:48
-     syncSpec(projectDir: string): SpecSyncResult {
-       const specFiles = [
-         'CLAUDE.md', 'use-cases.md', 'milestones.md', 'TODO.md',
-         'STATUS.md', 'README.md',
-       ];
-       const filesFound: string[] = [];
-       const specContent: Record<string, string> = {};
-       for (const f of specFiles) {
-         const p = join(projectDir, f);
-         if (existsSync(p)) {
-           filesFound.push(f);
-           specContent[f] = readFileSync(p, 'utf-8');
-         }
-       }
-       // Scan ADR directory
-       const adrDir = join(projectDir, 'docs', 'adrs');
-       if (existsSync(adrDir)) {
-         try {
-           const adrs = readdirSync(adrDir).filter(f => f.endsWith('.md'));
-           for (const adr of adrs.slice(0, 20)) {
-             const key = `docs/adrs/${adr}`;
-             filesFound.push(key);
-             specContent[key] = readFileSync(join(adrDir, adr), 'utf-8');
-           }
-         } catch { /* non-fatal */ }
-       }
-       // Extract milestone headings from milestones.md or TODO.md
-       const milestones: string[] = [];
-       const todos: string[] = [];
-       for (const [, content] of Object.entries(specContent)) {
-         for (const line of content.split('\n')) {
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
-             milestones.push(line.replace(/^#+\s*/, '').trim());
-           }
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
-           }
-         }
-       }
-       const summary = [
-         `${filesFound.length} spec file(s) found: ${filesFound.join(', ')}`,
-         milestones.length > 0 ? `${milestones.length} milestone(s) detected` : '',
-         todos.length > 0 ? `${todos.length} TODO item(s) detected` : '',
-       ].filter(Boolean).join(' · ');
-       return { projectDir, filesFound, specContent, milestones, todos, summary };
-     }
+     syncSpec(projectDir: string): SpecSyncResult {}

[NoCoverage] ArrayDeclaration
src/services/coordination-service.ts:388:23
-       const specFiles = [
-         'CLAUDE.md', 'use-cases.md', 'milestones.md', 'TODO.md',
-         'STATUS.md', 'README.md',
-       ];
+       const specFiles = [];

[NoCoverage] StringLiteral
src/services/coordination-service.ts:389:7
-         'CLAUDE.md', 'use-cases.md', 'milestones.md', 'TODO.md',
+         "", 'use-cases.md', 'milestones.md', 'TODO.md',

[NoCoverage] StringLiteral
src/services/coordination-service.ts:389:20
-         'CLAUDE.md', 'use-cases.md', 'milestones.md', 'TODO.md',
+         'CLAUDE.md', "", 'milestones.md', 'TODO.md',

[NoCoverage] StringLiteral
src/services/coordination-service.ts:389:36
-         'CLAUDE.md', 'use-cases.md', 'milestones.md', 'TODO.md',
+         'CLAUDE.md', 'use-cases.md', "", 'TODO.md',

[NoCoverage] StringLiteral
src/services/coordination-service.ts:389:53
-         'CLAUDE.md', 'use-cases.md', 'milestones.md', 'TODO.md',
+         'CLAUDE.md', 'use-cases.md', 'milestones.md', "",

[NoCoverage] StringLiteral
src/services/coordination-service.ts:390:7
-         'STATUS.md', 'README.md',
+         "", 'README.md',

[NoCoverage] StringLiteral
src/services/coordination-service.ts:390:20
-         'STATUS.md', 'README.md',
+         'STATUS.md', "",

[NoCoverage] ArrayDeclaration
src/services/coordination-service.ts:392:34
-       const filesFound: string[] = [];
+       const filesFound: string[] = ["Stryker was here"];

[NoCoverage] BlockStatement
src/services/coordination-service.ts:395:32
-       for (const f of specFiles) {
-         const p = join(projectDir, f);
-         if (existsSync(p)) {
-           filesFound.push(f);
-           specContent[f] = readFileSync(p, 'utf-8');
-         }
-       }
+       for (const f of specFiles) {}

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:397:11
-         if (existsSync(p)) {
+         if (true) {

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:397:11
-         if (existsSync(p)) {
+         if (false) {

[NoCoverage] BlockStatement
src/services/coordination-service.ts:397:26
-         if (existsSync(p)) {
-           filesFound.push(f);
-           specContent[f] = readFileSync(p, 'utf-8');
-         }
+         if (existsSync(p)) {}

[NoCoverage] StringLiteral
src/services/coordination-service.ts:399:42
-           specContent[f] = readFileSync(p, 'utf-8');
+           specContent[f] = readFileSync(p, "");

[NoCoverage] StringLiteral
src/services/coordination-service.ts:404:37
-       const adrDir = join(projectDir, 'docs', 'adrs');
+       const adrDir = join(projectDir, "", 'adrs');

[NoCoverage] StringLiteral
src/services/coordination-service.ts:404:45
-       const adrDir = join(projectDir, 'docs', 'adrs');
+       const adrDir = join(projectDir, 'docs', "");

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:405:9
-       if (existsSync(adrDir)) {
+       if (true) {

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:405:9
-       if (existsSync(adrDir)) {
+       if (false) {

[NoCoverage] BlockStatement
src/services/coordination-service.ts:405:29
-       if (existsSync(adrDir)) {
-         try {
-           const adrs = readdirSync(adrDir).filter(f => f.endsWith('.md'));
-           for (const adr of adrs.slice(0, 20)) {
-             const key = `docs/adrs/${adr}`;
-             filesFound.push(key);
-             specContent[key] = readFileSync(join(adrDir, adr), 'utf-8');
-           }
-         } catch { /* non-fatal */ }
-       }
+       if (existsSync(adrDir)) {}

[NoCoverage] BlockStatement
src/services/coordination-service.ts:406:11
-         try {
-           const adrs = readdirSync(adrDir).filter(f => f.endsWith('.md'));
-           for (const adr of adrs.slice(0, 20)) {
-             const key = `docs/adrs/${adr}`;
-             filesFound.push(key);
-             specContent[key] = readFileSync(join(adrDir, adr), 'utf-8');
-           }
-         } catch { /* non-fatal */ }
+         try {} catch { /* non-fatal */ }

[NoCoverage] MethodExpression
src/services/coordination-service.ts:407:22
-           const adrs = readdirSync(adrDir).filter(f => f.endsWith('.md'));
+           const adrs = readdirSync(adrDir);

[NoCoverage] ArrowFunction
src/services/coordination-service.ts:407:49
-           const adrs = readdirSync(adrDir).filter(f => f.endsWith('.md'));
+           const adrs = readdirSync(adrDir).filter(() => undefined);

[NoCoverage] MethodExpression
src/services/coordination-service.ts:407:54
-           const adrs = readdirSync(adrDir).filter(f => f.endsWith('.md'));
+           const adrs = readdirSync(adrDir).filter(f => f.startsWith('.md'));

[NoCoverage] StringLiteral
src/services/coordination-service.ts:407:65
-           const adrs = readdirSync(adrDir).filter(f => f.endsWith('.md'));
+           const adrs = readdirSync(adrDir).filter(f => f.endsWith(""));

[NoCoverage] MethodExpression
src/services/coordination-service.ts:408:27
-           for (const adr of adrs.slice(0, 20)) {
+           for (const adr of adrs) {

[NoCoverage] BlockStatement
src/services/coordination-service.ts:408:46
-           for (const adr of adrs.slice(0, 20)) {
-             const key = `docs/adrs/${adr}`;
-             filesFound.push(key);
-             specContent[key] = readFileSync(join(adrDir, adr), 'utf-8');
-           }
+           for (const adr of adrs.slice(0, 20)) {}

[NoCoverage] StringLiteral
src/services/coordination-service.ts:409:23
-             const key = `docs/adrs/${adr}`;
+             const key = ``;

[NoCoverage] StringLiteral
src/services/coordination-service.ts:411:62
-             specContent[key] = readFileSync(join(adrDir, adr), 'utf-8');
+             specContent[key] = readFileSync(join(adrDir, adr), "");

[NoCoverage] ArrayDeclaration
src/services/coordination-service.ts:417:34
-       const milestones: string[] = [];
+       const milestones: string[] = ["Stryker was here"];

[NoCoverage] ArrayDeclaration
src/services/coordination-service.ts:418:29
-       const todos: string[] = [];
+       const todos: string[] = ["Stryker was here"];

[NoCoverage] BlockStatement
src/services/coordination-service.ts:419:60
-       for (const [, content] of Object.entries(specContent)) {
-         for (const line of content.split('\n')) {
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
-             milestones.push(line.replace(/^#+\s*/, '').trim());
-           }
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
-           }
-         }
-       }
+       for (const [, content] of Object.entries(specContent)) {}

[NoCoverage] StringLiteral
src/services/coordination-service.ts:420:40
-         for (const line of content.split('\n')) {
+         for (const line of content.split("")) {

[NoCoverage] BlockStatement
src/services/coordination-service.ts:420:47
-         for (const line of content.split('\n')) {
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
-             milestones.push(line.replace(/^#+\s*/, '').trim());
-           }
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
-           }
-         }
+         for (const line of content.split('\n')) {}

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:421:13
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (true) {

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:421:13
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (false) {

[NoCoverage] LogicalOperator
src/services/coordination-service.ts:421:13
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (/^#+\s+milestone/i.test(line) && /^#+\s+v\d/i.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:421:13
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (/#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:421:13
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (/^#\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:421:13
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (/^#+\smilestone/i.test(line) || /^#+\s+v\d/i.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:421:13
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (/^#+\S+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:421:46
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (/^#+\s+milestone/i.test(line) || /#+\s+v\d/i.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:421:46
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (/^#+\s+milestone/i.test(line) || /^#\s+v\d/i.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:421:46
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (/^#+\s+milestone/i.test(line) || /^#+\sv\d/i.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:421:46
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (/^#+\s+milestone/i.test(line) || /^#+\S+v\d/i.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:421:46
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
+           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\D/i.test(line)) {

[NoCoverage] BlockStatement
src/services/coordination-service.ts:421:71
-           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {
-             milestones.push(line.replace(/^#+\s*/, '').trim());
-           }
+           if (/^#+\s+milestone/i.test(line) || /^#+\s+v\d/i.test(line)) {}

[NoCoverage] MethodExpression
src/services/coordination-service.ts:422:27
-             milestones.push(line.replace(/^#+\s*/, '').trim());
+             milestones.push(line.replace(/^#+\s*/, ''));

[NoCoverage] Regex
src/services/coordination-service.ts:422:40
-             milestones.push(line.replace(/^#+\s*/, '').trim());
+             milestones.push(line.replace(/#+\s*/, '').trim());

[NoCoverage] Regex
src/services/coordination-service.ts:422:40
-             milestones.push(line.replace(/^#+\s*/, '').trim());
+             milestones.push(line.replace(/^#\s*/, '').trim());

[NoCoverage] Regex
src/services/coordination-service.ts:422:40
-             milestones.push(line.replace(/^#+\s*/, '').trim());
+             milestones.push(line.replace(/^#+\s/, '').trim());

[NoCoverage] Regex
src/services/coordination-service.ts:422:40
-             milestones.push(line.replace(/^#+\s*/, '').trim());
+             milestones.push(line.replace(/^#+\S*/, '').trim());

[NoCoverage] StringLiteral
src/services/coordination-service.ts:422:50
-             milestones.push(line.replace(/^#+\s*/, '').trim());
+             milestones.push(line.replace(/^#+\s*/, "Stryker was here!").trim());

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:424:13
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
+           if (true) {

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:424:13
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
+           if (false) {

[NoCoverage] Regex
src/services/coordination-service.ts:424:13
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
+           if (/[-*]\s+\[[ x]\]/.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:424:13
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
+           if (/^[^-*]\s+\[[ x]\]/.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:424:13
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
+           if (/^[-*]\s\[[ x]\]/.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:424:13
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
+           if (/^[-*]\S+\[[ x]\]/.test(line)) {

[NoCoverage] Regex
src/services/coordination-service.ts:424:13
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
+           if (/^[-*]\s+\[[^ x]\]/.test(line)) {

[NoCoverage] BlockStatement
src/services/coordination-service.ts:424:44
-           if (/^[-*]\s+\[[ x]\]/.test(line)) {
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
-           }
+           if (/^[-*]\s+\[[ x]\]/.test(line)) {}

[NoCoverage] MethodExpression
src/services/coordination-service.ts:425:22
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
+             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, ''));

[NoCoverage] Regex
src/services/coordination-service.ts:425:35
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
+             todos.push(line.replace(/[-*]\s+\[[ x]\]\s*/, '').trim());

[NoCoverage] Regex
src/services/coordination-service.ts:425:35
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
+             todos.push(line.replace(/^[^-*]\s+\[[ x]\]\s*/, '').trim());

[NoCoverage] Regex
src/services/coordination-service.ts:425:35
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
+             todos.push(line.replace(/^[-*]\s\[[ x]\]\s*/, '').trim());

[NoCoverage] Regex
src/services/coordination-service.ts:425:35
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
+             todos.push(line.replace(/^[-*]\S+\[[ x]\]\s*/, '').trim());

[NoCoverage] Regex
src/services/coordination-service.ts:425:35
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
+             todos.push(line.replace(/^[-*]\s+\[[^ x]\]\s*/, '').trim());

[NoCoverage] Regex
src/services/coordination-service.ts:425:35
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
+             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s/, '').trim());

[NoCoverage] Regex
src/services/coordination-service.ts:425:35
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
+             todos.push(line.replace(/^[-*]\s+\[[ x]\]\S*/, '').trim());

[NoCoverage] StringLiteral
src/services/coordination-service.ts:425:58
-             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, '').trim());
+             todos.push(line.replace(/^[-*]\s+\[[ x]\]\s*/, "Stryker was here!").trim());

[NoCoverage] MethodExpression
src/services/coordination-service.ts:430:21
-       const summary = [
-         `${filesFound.length} spec file(s) found: ${filesFound.join(', ')}`,
-         milestones.length > 0 ? `${milestones.length} milestone(s) detected` : '',
-         todos.length > 0 ? `${todos.length} TODO item(s) detected` : '',
-       ].filter(Boolean).join(' · ');
+       const summary = [`${filesFound.length} spec file(s) found: ${filesFound.join(', ')}`, milestones.length > 0 ? `${milestones.length} milestone(s) detected` : '', todos.length > 0 ? `${todos.length} TODO item(s) detected` : ''].join(' · ');

[NoCoverage] ArrayDeclaration
src/services/coordination-service.ts:430:21
-       const summary = [
-         `${filesFound.length} spec file(s) found: ${filesFound.join(', ')}`,
-         milestones.length > 0 ? `${milestones.length} milestone(s) detected` : '',
-         todos.length > 0 ? `${todos.length} TODO item(s) detected` : '',
-       ].filter(Boolean).join(' · ');
+       const summary = [].filter(Boolean).join(' · ');

[NoCoverage] StringLiteral
src/services/coordination-service.ts:431:7
-         `${filesFound.length} spec file(s) found: ${filesFound.join(', ')}`,
+         ``,

[NoCoverage] StringLiteral
src/services/coordination-service.ts:431:67
-         `${filesFound.length} spec file(s) found: ${filesFound.join(', ')}`,
+         `${filesFound.length} spec file(s) found: ${filesFound.join("")}`,

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:432:7
-         milestones.length > 0 ? `${milestones.length} milestone(s) detected` : '',
+         true ? `${milestones.length} milestone(s) detected` : '',

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:432:7
-         milestones.length > 0 ? `${milestones.length} milestone(s) detected` : '',
+         false ? `${milestones.length} milestone(s) detected` : '',

[NoCoverage] EqualityOperator
src/services/coordination-service.ts:432:7
-         milestones.length > 0 ? `${milestones.length} milestone(s) detected` : '',
+         milestones.length >= 0 ? `${milestones.length} milestone(s) detected` : '',

[NoCoverage] EqualityOperator
src/services/coordination-service.ts:432:7
-         milestones.length > 0 ? `${milestones.length} milestone(s) detected` : '',
+         milestones.length <= 0 ? `${milestones.length} milestone(s) detected` : '',

[NoCoverage] StringLiteral
src/services/coordination-service.ts:432:31
-         milestones.length > 0 ? `${milestones.length} milestone(s) detected` : '',
+         milestones.length > 0 ? `` : '',

[NoCoverage] StringLiteral
src/services/coordination-service.ts:432:78
-         milestones.length > 0 ? `${milestones.length} milestone(s) detected` : '',
+         milestones.length > 0 ? `${milestones.length} milestone(s) detected` : "Stryker was here!",

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:433:7
-         todos.length > 0 ? `${todos.length} TODO item(s) detected` : '',
+         true ? `${todos.length} TODO item(s) detected` : '',

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:433:7
-         todos.length > 0 ? `${todos.length} TODO item(s) detected` : '',
+         false ? `${todos.length} TODO item(s) detected` : '',

[NoCoverage] EqualityOperator
src/services/coordination-service.ts:433:7
-         todos.length > 0 ? `${todos.length} TODO item(s) detected` : '',
+         todos.length >= 0 ? `${todos.length} TODO item(s) detected` : '',

[NoCoverage] EqualityOperator
src/services/coordination-service.ts:433:7
-         todos.length > 0 ? `${todos.length} TODO item(s) detected` : '',
+         todos.length <= 0 ? `${todos.length} TODO item(s) detected` : '',

[NoCoverage] StringLiteral
src/services/coordination-service.ts:433:26
-         todos.length > 0 ? `${todos.length} TODO item(s) detected` : '',
+         todos.length > 0 ? `` : '',

[NoCoverage] StringLiteral
src/services/coordination-service.ts:433:68
-         todos.length > 0 ? `${todos.length} TODO item(s) detected` : '',
+         todos.length > 0 ? `${todos.length} TODO item(s) detected` : "Stryker was here!",

[NoCoverage] StringLiteral
src/services/coordination-service.ts:434:28
-       ].filter(Boolean).join(' · ');
+       ].filter(Boolean).join("");

[NoCoverage] ObjectLiteral
src/services/coordination-service.ts:436:12
-       return { projectDir, filesFound, specContent, milestones, todos, summary };
+       return {};

[NoCoverage] BlockStatement
src/services/coordination-service.ts:450:19
-     }): WorkPackage {
-       const id = randomUUID();
-       const now = new Date().toISOString();
-       this.db.prepare(`
-         INSERT INTO work_packages
-           (id, project, title, description, spec_section, status, role_required, dependencies, priority_rank, is_milestone, created_at)
-         VALUES (?, ?, ?, ?, ?, 'pending', 'merger', '[]', 0, 1, ?)
-       `).run(id, input.project, input.title, input.description, input.specSection ?? null, now);
-       return this.requireWorkPackage(id);
-     }
+     }): WorkPackage {}

[NoCoverage] StringLiteral
src/services/coordination-service.ts:453:21
-       this.db.prepare(`
-         INSERT INTO work_packages
-           (id, project, title, description, spec_section, status, role_required, dependencies, priority_rank, is_milestone, created_at)
-         VALUES (?, ?, ?, ?, ?, 'pending', 'merger', '[]', 0, 1, ?)
-       `).run(id, input.project, input.title, input.description, input.specSection ?? null, now);
+       this.db.prepare(``).run(id, input.project, input.title, input.description, input.specSection ?? null, now);

[NoCoverage] LogicalOperator
src/services/coordination-service.ts:457:63
-       `).run(id, input.project, input.title, input.description, input.specSection ?? null, now);
+       `).run(id, input.project, input.title, input.description, input.specSection && null, now);

[NoCoverage] BlockStatement
src/services/coordination-service.ts:471:33
-       for (const m of milestones) {
-         const children = this.db.prepare(
-           `SELECT status FROM work_packages WHERE project = ? AND parent_id = ?`,
-         ).all(project, m.id) as any[];
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
-           const now = new Date().toISOString();
-           this.db.prepare(
-             `UPDATE work_packages SET status = 'complete', completed_at = ? WHERE id = ?`,
-           ).run(now, m.id);
-           completed.push(this.requireWorkPackage(m.id));
-         }
-       }
+       for (const m of milestones) {}

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:475:11
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (true) {

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:475:11
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (false) {

[NoCoverage] LogicalOperator
src/services/coordination-service.ts:475:11
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (children.length > 0 || children.every((c: any) => c.status === 'complete')) {

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:475:11
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (true && children.every((c: any) => c.status === 'complete')) {

[NoCoverage] EqualityOperator
src/services/coordination-service.ts:475:11
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (children.length >= 0 && children.every((c: any) => c.status === 'complete')) {

[NoCoverage] EqualityOperator
src/services/coordination-service.ts:475:11
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (children.length <= 0 && children.every((c: any) => c.status === 'complete')) {

[NoCoverage] MethodExpression
src/services/coordination-service.ts:475:34
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (children.length > 0 && children.some((c: any) => c.status === 'complete')) {

[NoCoverage] ArrowFunction
src/services/coordination-service.ts:475:49
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (children.length > 0 && children.every(() => undefined)) {

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:475:61
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (children.length > 0 && children.every((c: any) => true)) {

[NoCoverage] ConditionalExpression
src/services/coordination-service.ts:475:61
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (children.length > 0 && children.every((c: any) => false)) {

[NoCoverage] EqualityOperator
src/services/coordination-service.ts:475:61
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (children.length > 0 && children.every((c: any) => c.status !== 'complete')) {

[NoCoverage] StringLiteral
src/services/coordination-service.ts:475:74
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
+         if (children.length > 0 && children.every((c: any) => c.status === "")) {

[NoCoverage] BlockStatement
src/services/coordination-service.ts:475:87
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
-           const now = new Date().toISOString();
-           this.db.prepare(
-             `UPDATE work_packages SET status = 'complete', completed_at = ? WHERE id = ?`,
-           ).run(now, m.id);
-           completed.push(this.requireWorkPackage(m.id));
-         }
+         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {}

[NoCoverage] StringLiteral
src/services/coordination-service.ts:478:11
-             `UPDATE work_packages SET status = 'complete', completed_at = ? WHERE id = ?`,
+             ``,

[NoCoverage] StringLiteral
src/services/coordination-service.ts:692:31
-       if (!row) throw new Error(`Work package not found: ${id}`);
+       if (!row) throw new Error(``);

[NoCoverage] StringLiteral
src/services/coordination-service.ts:702:40
-         skills: JSON.parse(row.skills ?? '[]'),
+         skills: JSON.parse(row.skills ?? ""),

[NoCoverage] StringLiteral
src/services/coordination-service.ts:720:52
-         dependencies: JSON.parse(row.dependencies ?? '[]'),
+         dependencies: JSON.parse(row.dependencies ?? ""),

[Survived] LogicalOperator
src/services/coordination-service.ts:146:7
-         input.bandwidthHoursPerWeek ?? 20, now, now,
+         input.bandwidthHoursPerWeek && 20, now, now,
Tests ran:
    CoordinationService assignment returns null when there is no work, rather than throwing
    CoordinationService assignment returns null when work exists but no contributor matches the required role
    CoordinationService assignment assigns the highest-ranked unblocked package first
  and 31 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:144:38
-         id, input.project, input.name, input.email ?? null,
+         id, input.project, input.name, input.email && null,
Tests ran:
    CoordinationService assignment returns null when there is no work, rather than throwing
    CoordinationService assignment returns null when work exists but no contributor matches the required role
    CoordinationService assignment assigns the highest-ranked unblocked package first
  and 31 more tests!


[Survived] ArrayDeclaration
src/services/coordination-service.ts:196:38
-         dependencyIds: (p.dependsOn ?? []).map(t => titleToId.get(t) ?? t),
+         dependencyIds: (p.dependsOn ?? ["Stryker was here"]).map(t => titleToId.get(t) ?? t),
Tests ran:
    CoordinationService decomposition scopes the queue to its project


[Survived] LogicalOperator
src/services/coordination-service.ts:216:9
-           input.milestoneId ?? null, now,
+           input.milestoneId && null, now,
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] BooleanLiteral
src/services/coordination-service.ts:236:11
-         if (!dependents.has(n.id)) dependents.set(n.id, new Set());
+         if (dependents.has(n.id)) dependents.set(n.id, new Set());
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:236:11
-         if (!dependents.has(n.id)) dependents.set(n.id, new Set());
+         if (true) dependents.set(n.id, new Set());
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:236:11
-         if (!dependents.has(n.id)) dependents.set(n.id, new Set());
+         if (false) dependents.set(n.id, new Set());
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:336:9
-       if (wp.assignedTo) {
+       if (true) {
Tests ran:
    CoordinationService completion frees the contributor, leaving nobody busy without an active assignment
    CoordinationService completion marks the package complete and stamps completedAt
    CoordinationService completion drops the completed package out of the queue — the queue is work remaining
  and 5 more tests!


[Survived] OptionalChaining
src/services/coordination-service.ts:368:16
-           return dep?.status === 'complete';
+           return dep.status === 'complete';
Tests ran:
    CoordinationService completion reports which downstream packages it unblocked
    CoordinationService completion promotes a downstream package once its only dependency completes
    CoordinationService completion leaves a package blocked while any dependency is still outstanding
  and 2 more tests!


[Survived] BlockStatement
src/services/coordination-service.ts:465:54
-     evaluateMilestones(project: string): WorkPackage[] {
-       const milestones = this.db.prepare(
-         `SELECT * FROM work_packages WHERE project = ? AND is_milestone = 1 AND status != 'complete'`,
-       ).all(project) as any[];
-       const completed: WorkPackage[] = [];
-       for (const m of milestones) {
-         const children = this.db.prepare(
-           `SELECT status FROM work_packages WHERE project = ? AND parent_id = ?`,
-         ).all(project, m.id) as any[];
-         if (children.length > 0 && children.every((c: any) => c.status === 'complete')) {
-           const now = new Date().toISOString();
-           this.db.prepare(
-             `UPDATE work_packages SET status = 'complete', completed_at = ? WHERE id = ?`,
-           ).run(now, m.id);
-           completed.push(this.requireWorkPackage(m.id));
-         }
-       }
-       return completed;
-     }
+     evaluateMilestones(project: string): WorkPackage[] {}
Tests ran:
    CoordinationService completion frees the contributor, leaving nobody busy without an active assignment
    CoordinationService completion marks the package complete and stamps completedAt
    CoordinationService completion drops the completed package out of the queue — the queue is work remaining
  and 7 more tests!


[Survived] ArrayDeclaration
src/services/coordination-service.ts:470:38
-       const completed: WorkPackage[] = [];
+       const completed: WorkPackage[] = ["Stryker was here"];
Tests ran:
    CoordinationService completion frees the contributor, leaving nobody busy without an active assignment
    CoordinationService completion marks the package complete and stamps completedAt
    CoordinationService completion drops the completed package out of the queue — the queue is work remaining
  and 7 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:516:7
-         input.forgecraftTier ?? null,
+         input.forgecraftTier && null,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:517:7
-         input.forgecraftPass != null ? (input.forgecraftPass ? 1 : 0) : null,
+         true ? (input.forgecraftPass ? 1 : 0) : null,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:528:38
-         input.forgecraftScore ?? null, input.forgecraftTier ?? null,
+         input.forgecraftScore ?? null, input.forgecraftTier && null,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:529:7
-         input.forgecraftPass != null ? (input.forgecraftPass ? 1 : 0) : null,
+         true ? (input.forgecraftPass ? 1 : 0) : null,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:530:7
-         input.forgecraftReport ?? null, now,
+         input.forgecraftReport && null, now,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:571:11
-         if (wp.assignedTo) {
+         if (true) {
Tests ran:
    CoordinationService merge gating records an approval by a merger
    CoordinationService merge gating completes the work package when the merge is approved


[Survived] ConditionalExpression
src/services/coordination-service.ts:571:11
-         if (wp.assignedTo) {
+         if (false) {
Tests ran:
    CoordinationService merge gating records an approval by a merger
    CoordinationService merge gating completes the work package when the merge is approved


[Survived] BlockStatement
src/services/coordination-service.ts:571:26
-         if (wp.assignedTo) {
-           this.db.prepare(
-             `UPDATE contributors SET availability = 'available', last_active_at = ? WHERE id = ?`,
-           ).run(now, wp.assignedTo);
-         }
+         if (wp.assignedTo) {}
Tests ran:
    CoordinationService merge gating records an approval by a merger
    CoordinationService merge gating completes the work package when the merge is approved


[Survived] ObjectLiteral
src/services/coordination-service.ts:583:14
-         return { mergeRequest: this.requireMergeRequest(input.mergeRequestId), milestonesCompleted };
+         return {};
Tests ran:
    CoordinationService merge gating records an approval by a merger
    CoordinationService merge gating completes the work package when the merge is approved


[Survived] ObjectLiteral
src/services/coordination-service.ts:596:14
-         return { mergeRequest: this.requireMergeRequest(input.mergeRequestId), milestonesCompleted: [] };
+         return {};
Tests ran:
    CoordinationService merge gating records a rejection with its reason


[Survived] ArrayDeclaration
src/services/coordination-service.ts:596:99
-         return { mergeRequest: this.requireMergeRequest(input.mergeRequestId), milestonesCompleted: [] };
+         return { mergeRequest: this.requireMergeRequest(input.mergeRequestId), milestonesCompleted: ["Stryker was here"] };
Tests ran:
    CoordinationService merge gating records a rejection with its reason


[Survived] BlockStatement
src/services/coordination-service.ts:621:66
-       const contributors = this.listContributors(project).map(c => {
-         const row = this.db.prepare(
-           `SELECT * FROM work_packages WHERE project = ? AND assigned_to = ? AND status = 'active' LIMIT 1`,
-         ).get(project, c.id) as any;
-         return { ...c, currentWork: row ? this.rowToWorkPackage(row) : undefined };
-       });
+       const contributors = this.listContributors(project).map(c => {});
Tests ran:
    CoordinationService status reflects work and roster counts


[Survived] ObjectLiteral
src/services/coordination-service.ts:625:14
-         return { ...c, currentWork: row ? this.rowToWorkPackage(row) : undefined };
+         return {};
Tests ran:
    CoordinationService status reflects work and roster counts


[Survived] ObjectLiteral
src/services/coordination-service.ts:633:21
-       const summary = { total: 0, pending: 0, active: 0, blocked: 0, complete: 0 };
+       const summary = {};
Tests ran:
    CoordinationService status reports an empty project without throwing
    CoordinationService status reflects work and roster counts


[Survived] BlockStatement
src/services/coordination-service.ts:634:34
-       for (const row of countRows) {
-         const key = row.status as keyof typeof summary;
-         if (key in summary) summary[key] = row.n;
-         summary.total += row.n;
-       }
+       for (const row of countRows) {}
Tests ran:
    CoordinationService status reflects work and roster counts


[Survived] ConditionalExpression
src/services/coordination-service.ts:636:11
-         if (key in summary) summary[key] = row.n;
+         if (true) summary[key] = row.n;
Tests ran:
    CoordinationService status reflects work and roster counts


[Survived] ConditionalExpression
src/services/coordination-service.ts:636:11
-         if (key in summary) summary[key] = row.n;
+         if (false) summary[key] = row.n;
Tests ran:
    CoordinationService status reflects work and roster counts


[Survived] AssignmentOperator
src/services/coordination-service.ts:637:7
-         summary.total += row.n;
+         summary.total -= row.n;
Tests ran:
    CoordinationService status reflects work and roster counts


[Survived] ConditionalExpression
src/services/coordination-service.ts:692:9
-       if (!row) throw new Error(`Work package not found: ${id}`);
+       if (false) throw new Error(`Work package not found: ${id}`);
Tests ran:
    CoordinationService assignment assigns the highest-ranked unblocked package first
    CoordinationService assignment never assigns a blocked package
    CoordinationService assignment marks the package active and the contributor busy together
  and 23 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:701:14
-         email: row.email ?? undefined,
+         email: row.email && undefined,
Tests ran:
    CoordinationService assignment returns null when there is no work, rather than throwing
    CoordinationService assignment returns null when work exists but no contributor matches the required role
    CoordinationService assignment assigns the highest-ranked unblocked package first
  and 31 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:722:17
-         parentId: row.parent_id ?? undefined,
+         parentId: row.parent_id && undefined,
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:723:20
-         isMilestone: row.is_milestone === 1,
+         isMilestone: true,
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:723:20
-         isMilestone: row.is_milestone === 1,
+         isMilestone: false,
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] EqualityOperator
src/services/coordination-service.ts:723:20
-         isMilestone: row.is_milestone === 1,
+         isMilestone: row.is_milestone !== 1,
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:726:23
-         forgecraftTier: row.forgecraft_tier ?? undefined,
+         forgecraftTier: row.forgecraft_tier && undefined,
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:727:23
-         forgecraftPass: row.forgecraft_pass != null ? row.forgecraft_pass === 1 : undefined,
+         forgecraftPass: true ? row.forgecraft_pass === 1 : undefined,
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:727:53
-         forgecraftPass: row.forgecraft_pass != null ? row.forgecraft_pass === 1 : undefined,
+         forgecraftPass: row.forgecraft_pass != null ? true : undefined,
Tests ran:
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request


[Survived] LogicalOperator
src/services/coordination-service.ts:729:19
-         assignedAt: row.assigned_at ?? undefined,
+         assignedAt: row.assigned_at && undefined,
Tests ran:
    CoordinationService priority ranking: count of transitive dependents ranks a linear chain by how much each package unblocks
    CoordinationService priority ranking: count of transitive dependents counts a diamond once per dependent, not once per path
    CoordinationService priority ranking: count of transitive dependents keeps disconnected components from contaminating each other
  and 35 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:737:9
-       if (!row) throw new Error(`Merge request not found: ${id}`);
+       if (false) throw new Error(`Merge request not found: ${id}`);
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 8 more tests!


[Survived] StringLiteral
src/services/coordination-service.ts:737:31
-       if (!row) throw new Error(`Merge request not found: ${id}`);
+       if (!row) throw new Error(``);
Tests ran:
    CoordinationService merge gating refuses to resolve a merge request that does not exist


[Survived] LogicalOperator
src/services/coordination-service.ts:749:23
-         forgecraftTier: row.forgecraft_tier ?? undefined,
+         forgecraftTier: row.forgecraft_tier && undefined,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:750:23
-         forgecraftPass: row.forgecraft_pass != null ? row.forgecraft_pass === 1 : undefined,
+         forgecraftPass: true ? row.forgecraft_pass === 1 : undefined,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] ConditionalExpression
src/services/coordination-service.ts:750:53
-         forgecraftPass: row.forgecraft_pass != null ? row.forgecraft_pass === 1 : undefined,
+         forgecraftPass: row.forgecraft_pass != null ? true : undefined,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request


[Survived] LogicalOperator
src/services/coordination-service.ts:751:25
-         forgecraftReport: row.forgecraft_report ?? undefined,
+         forgecraftReport: row.forgecraft_report && undefined,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:752:19
-         approvedBy: row.approved_by ?? undefined,
+         approvedBy: row.approved_by && undefined,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:753:19
-         approvedAt: row.approved_at ?? undefined,
+         approvedAt: row.approved_at && undefined,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:754:19
-         rejectedBy: row.rejected_by ?? undefined,
+         rejectedBy: row.rejected_by && undefined,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] LogicalOperator
src/services/coordination-service.ts:755:19
-         rejectedAt: row.rejected_at ?? undefined,
+         rejectedAt: row.rejected_at && undefined,
Tests ran:
    CoordinationService merge gating records the verdict the caller passes in, and never computes one
    CoordinationService merge gating accepts a merge request with no verdict at all
    CoordinationService merge gating stamps the verdict onto the work package too, not only the request
  and 7 more tests!


[Survived] MethodExpression
src/services/coordination-service.ts:764:12
-       return text
-         .toLowerCase()
-         .replace(/[^a-z0-9]+/g, '-')
-         .replace(/^-|-$/g, '')
-         .slice(0, 40);
+       return text.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
Tests ran:
    CoordinationService assignment assigns the highest-ranked unblocked package first
    CoordinationService assignment never assigns a blocked package
    CoordinationService assignment marks the package active and the contributor busy together
  and 23 more tests!


[Survived] Regex
src/services/coordination-service.ts:766:16
-         .replace(/[^a-z0-9]+/g, '-')
+         .replace(/[^a-z0-9]/g, '-')
Tests ran:
    CoordinationService assignment assigns the highest-ranked unblocked package first
    CoordinationService assignment never assigns a blocked package
    CoordinationService assignment marks the package active and the contributor busy together
  and 23 more tests!


[Survived] StringLiteral
src/services/coordination-service.ts:767:26
-         .replace(/^-|-$/g, '')
+         .replace(/^-|-$/g, "Stryker was here!")
Tests ran:
    CoordinationService assignment assigns the highest-ranked unblocked package first
    CoordinationService assignment never assigns a blocked package
    CoordinationService assignment marks the package active and the contributor busy together
  and 23 more tests!


[NoCoverage] BlockStatement
src/services/distill.ts:28:11
-   ): string {

-     const memoriesList = newMemories

-       .map(m => `- [${m.memoryType}] ${m.content}${m.project ? ` (project: ${m.project})` : ''}`)

-       .join('\n');

-   

-     const existingProfile  = existingInsights['profile']  ?? '(none yet)';

-     const existingLessons  = existingInsights['lesson']   ?? '(none yet)';

-     const existingPlaybook = existingInsights['playbook'] ?? '(none yet)';

-   

-     return `You are updating a developer's persistent memory profile. Based on NEW MEMORIES from this session, update the intelligence layer.

-   

-   ## New memories captured this session:

-   ${memoriesList}

-   

-   ## Current intelligence layer:

-   ### Developer Profile (preferences, patterns, tendencies):

-   ${existingProfile}

-   

-   ### Lessons Learned (cross-project insights, mistakes, solutions):

-   ${existingLessons}

-   

-   ### Coding Playbook (procedures, workflows, best practices):

-   ${existingPlaybook}

-   

-   ## Instructions:

-   Return a JSON object with these keys (only include keys that have meaningful updates):

-   {

-     "profile":  "<updated developer profile — integrate new preferences/patterns>",

-     "lesson":   "<updated lessons — add any new cross-project insights>",

-     "playbook": "<updated playbook — add any new procedures or solutions>",

-     "bias":     "<any AI behavior patterns observed — optional>"

-   }

-   

-   Rules:

-   - Keep each section concise (max 500 words each)

-   - Merge new information — do not discard existing knowledge

-   - If nothing new warrants updating a section, omit that key

-   - Write in second person ("You prefer...", "When working with...")`;

-   }

+   ): string {}


[NoCoverage] ArrowFunction
src/services/distill.ts:30:10
-       .map(m => `- [${m.memoryType}] ${m.content}${m.project ? ` (project: ${m.project})` : ''}`)

+       .map(() => undefined)


[NoCoverage] StringLiteral
src/services/distill.ts:30:15
-       .map(m => `- [${m.memoryType}] ${m.content}${m.project ? ` (project: ${m.project})` : ''}`)

+       .map(m => ``)


[NoCoverage] StringLiteral
src/services/distill.ts:30:62
-       .map(m => `- [${m.memoryType}] ${m.content}${m.project ? ` (project: ${m.project})` : ''}`)

+       .map(m => `- [${m.memoryType}] ${m.content}${m.project ? `` : ''}`)


[NoCoverage] StringLiteral
src/services/distill.ts:30:91
-       .map(m => `- [${m.memoryType}] ${m.content}${m.project ? ` (project: ${m.project})` : ''}`)

+       .map(m => `- [${m.memoryType}] ${m.content}${m.project ? ` (project: ${m.project})` : "Stryker was here!"}`)


[NoCoverage] StringLiteral
src/services/distill.ts:31:11
-       .join('\n');

+       .join("");


[NoCoverage] LogicalOperator
src/services/distill.ts:33:28
-     const existingProfile  = existingInsights['profile']  ?? '(none yet)';

+     const existingProfile  = existingInsights['profile'] && '(none yet)';


[NoCoverage] StringLiteral
src/services/distill.ts:33:45
-     const existingProfile  = existingInsights['profile']  ?? '(none yet)';

+     const existingProfile  = existingInsights[""]  ?? '(none yet)';


[NoCoverage] StringLiteral
src/services/distill.ts:33:60
-     const existingProfile  = existingInsights['profile']  ?? '(none yet)';

+     const existingProfile  = existingInsights['profile']  ?? "";


[NoCoverage] LogicalOperator
src/services/distill.ts:34:28
-     const existingLessons  = existingInsights['lesson']   ?? '(none yet)';

+     const existingLessons  = existingInsights['lesson'] && '(none yet)';


[NoCoverage] StringLiteral
src/services/distill.ts:34:45
-     const existingLessons  = existingInsights['lesson']   ?? '(none yet)';

+     const existingLessons  = existingInsights[""]   ?? '(none yet)';


[NoCoverage] StringLiteral
src/services/distill.ts:34:60
-     const existingLessons  = existingInsights['lesson']   ?? '(none yet)';

+     const existingLessons  = existingInsights['lesson']   ?? "";


[NoCoverage] LogicalOperator
src/services/distill.ts:35:28
-     const existingPlaybook = existingInsights['playbook'] ?? '(none yet)';

+     const existingPlaybook = existingInsights['playbook'] && '(none yet)';


[NoCoverage] StringLiteral
src/services/distill.ts:35:45
-     const existingPlaybook = existingInsights['playbook'] ?? '(none yet)';

+     const existingPlaybook = existingInsights[""] ?? '(none yet)';


[NoCoverage] StringLiteral
src/services/distill.ts:35:60
-     const existingPlaybook = existingInsights['playbook'] ?? '(none yet)';

+     const existingPlaybook = existingInsights['playbook'] ?? "";


[NoCoverage] StringLiteral
src/services/distill.ts:37:10
-     return `You are updating a developer's persistent memory profile. Based on NEW MEMORIES from this session, update the intelligence layer.

-   

-   ## New memories captured this session:

-   ${memoriesList}

-   

-   ## Current intelligence layer:

-   ### Developer Profile (preferences, patterns, tendencies):

-   ${existingProfile}

-   

-   ### Lessons Learned (cross-project insights, mistakes, solutions):

-   ${existingLessons}

-   

-   ### Coding Playbook (procedures, workflows, best practices):

-   ${existingPlaybook}

-   

-   ## Instructions:

-   Return a JSON object with these keys (only include keys that have meaningful updates):

-   {

-     "profile":  "<updated developer profile — integrate new preferences/patterns>",

-     "lesson":   "<updated lessons — add any new cross-project insights>",

-     "playbook": "<updated playbook — add any new procedures or solutions>",

-     "bias":     "<any AI behavior patterns observed — optional>"

-   }

-   

-   Rules:

-   - Keep each section concise (max 500 words each)

-   - Merge new information — do not discard existing knowledge

-   - If nothing new warrants updating a section, omit that key

-   - Write in second person ("You prefer...", "When working with...")`;

+     return ``;


[NoCoverage] BlockStatement
src/services/distill.ts:75:9
-   ): void {

-     const db = getDatabase();

-     const now = new Date().toISOString();

-   

-     for (const [insightType, content] of Object.entries(updates)) {

-       if (!content || typeof content !== 'string') continue;

-   

-       // Valid types only

-       if (!['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;

-   

-       const existing = db.prepare(

-         `SELECT id, version FROM insights WHERE insight_type = ? AND project IS ?`

-       ).get(insightType, sessionProject ?? null) as { id: string; version: number } | undefined;

-   

-       if (existing) {

-         db.prepare(

-           `UPDATE insights SET content = ?, confidence = 0.8, version = version + 1, updated_at = ? WHERE id = ?`

-         ).run(content, now, existing.id);

-       } else {

-         db.prepare(`

-           INSERT INTO insights (id, insight_type, project, content, confidence, source_count, version, updated_at)

-           VALUES (?, ?, ?, ?, 0.8, 1, 1, ?)

-         `).run(crypto.randomUUID(), insightType, sessionProject ?? null, content, now);

-       }

-     }

-   }

+   ): void {}


[NoCoverage] BlockStatement
src/services/distill.ts:79:65
-     for (const [insightType, content] of Object.entries(updates)) {

-       if (!content || typeof content !== 'string') continue;

-   

-       // Valid types only

-       if (!['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;

-   

-       const existing = db.prepare(

-         `SELECT id, version FROM insights WHERE insight_type = ? AND project IS ?`

-       ).get(insightType, sessionProject ?? null) as { id: string; version: number } | undefined;

-   

-       if (existing) {

-         db.prepare(

-           `UPDATE insights SET content = ?, confidence = 0.8, version = version + 1, updated_at = ? WHERE id = ?`

-         ).run(content, now, existing.id);

-       } else {

-         db.prepare(`

-           INSERT INTO insights (id, insight_type, project, content, confidence, source_count, version, updated_at)

-           VALUES (?, ?, ?, ?, 0.8, 1, 1, ?)

-         `).run(crypto.randomUUID(), insightType, sessionProject ?? null, content, now);

-       }

-     }

+     for (const [insightType, content] of Object.entries(updates)) {}


[NoCoverage] ConditionalExpression
src/services/distill.ts:80:9
-       if (!content || typeof content !== 'string') continue;

+       if (true) continue;


[NoCoverage] ConditionalExpression
src/services/distill.ts:80:9
-       if (!content || typeof content !== 'string') continue;

+       if (false) continue;


[NoCoverage] LogicalOperator
src/services/distill.ts:80:9
-       if (!content || typeof content !== 'string') continue;

+       if (!content && typeof content !== 'string') continue;


[NoCoverage] BooleanLiteral
src/services/distill.ts:80:9
-       if (!content || typeof content !== 'string') continue;

+       if (content || typeof content !== 'string') continue;


[NoCoverage] ConditionalExpression
src/services/distill.ts:80:21
-       if (!content || typeof content !== 'string') continue;

+       if (!content || false) continue;


[NoCoverage] EqualityOperator
src/services/distill.ts:80:21
-       if (!content || typeof content !== 'string') continue;

+       if (!content || typeof content === 'string') continue;


[NoCoverage] StringLiteral
src/services/distill.ts:80:40
-       if (!content || typeof content !== 'string') continue;

+       if (!content || typeof content !== "") continue;


[NoCoverage] BooleanLiteral
src/services/distill.ts:83:9
-       if (!['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;

+       if (['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;


[NoCoverage] ConditionalExpression
src/services/distill.ts:83:9
-       if (!['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;

+       if (true) continue;


[NoCoverage] ConditionalExpression
src/services/distill.ts:83:9
-       if (!['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;

+       if (false) continue;


[NoCoverage] ArrayDeclaration
src/services/distill.ts:83:10
-       if (!['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;

+       if (![].includes(insightType)) continue;


[NoCoverage] StringLiteral
src/services/distill.ts:83:11
-       if (!['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;

+       if (!["", 'lesson', 'playbook', 'bias'].includes(insightType)) continue;


[NoCoverage] StringLiteral
src/services/distill.ts:83:22
-       if (!['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;

+       if (!['profile', "", 'playbook', 'bias'].includes(insightType)) continue;


[NoCoverage] StringLiteral
src/services/distill.ts:83:32
-       if (!['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;

+       if (!['profile', 'lesson', "", 'bias'].includes(insightType)) continue;


[NoCoverage] StringLiteral
src/services/distill.ts:83:44
-       if (!['profile', 'lesson', 'playbook', 'bias'].includes(insightType)) continue;

+       if (!['profile', 'lesson', 'playbook', ""].includes(insightType)) continue;


[NoCoverage] ConditionalExpression
src/services/distill.ts:89:9
-       if (existing) {

+       if (true) {


[NoCoverage] ConditionalExpression
src/services/distill.ts:89:9
-       if (existing) {

+       if (false) {


[NoCoverage] BlockStatement
src/services/distill.ts:89:19
-       if (existing) {

-         db.prepare(

-           `UPDATE insights SET content = ?, confidence = 0.8, version = version + 1, updated_at = ? WHERE id = ?`

-         ).run(content, now, existing.id);

-       } else {

+       if (existing) {} else {


[NoCoverage] StringLiteral
src/services/distill.ts:91:9
-           `UPDATE insights SET content = ?, confidence = 0.8, version = version + 1, updated_at = ? WHERE id = ?`

+           ``


[NoCoverage] BlockStatement
src/services/distill.ts:93:12
-       } else {

-         db.prepare(`

-           INSERT INTO insights (id, insight_type, project, content, confidence, source_count, version, updated_at)

-           VALUES (?, ?, ?, ?, 0.8, 1, 1, ?)

-         `).run(crypto.randomUUID(), insightType, sessionProject ?? null, content, now);

-       }

+       } else {}


[NoCoverage] StringLiteral
src/services/distill.ts:94:18
-         db.prepare(`

-           INSERT INTO insights (id, insight_type, project, content, confidence, source_count, version, updated_at)

-           VALUES (?, ?, ?, ?, 0.8, 1, 1, ?)

-         `).run(crypto.randomUUID(), insightType, sessionProject ?? null, content, now);

+         db.prepare(``).run(crypto.randomUUID(), insightType, sessionProject ?? null, content, now);


[NoCoverage] LogicalOperator
src/services/distill.ts:97:48
-         `).run(crypto.randomUUID(), insightType, sessionProject ?? null, content, now);

+         `).run(crypto.randomUUID(), insightType, sessionProject && null, content, now);


[NoCoverage] BlockStatement
src/services/distill.ts:105:79
-   export function getExistingInsights(project?: string): Record<string, string> {

-     const db = getDatabase();

-     const rows = db.prepare(

-       `SELECT insight_type, content FROM insights WHERE project IS ? OR project IS NULL ORDER BY version DESC`

-     ).all(project ?? null) as { insight_type: string; content: string }[];

-   

-     const map: Record<string, string> = {};

-     for (const row of rows) {

-       if (!map[row.insight_type]) {

-         map[row.insight_type] = row.content;

-       }

-     }

-     return map;

-   }

+   export function getExistingInsights(project?: string): Record<string, string> {}


[NoCoverage] BlockStatement
src/services/distill.ts:112:27
-     for (const row of rows) {

-       if (!map[row.insight_type]) {

-         map[row.insight_type] = row.content;

-       }

-     }

+     for (const row of rows) {}


[NoCoverage] BooleanLiteral
src/services/distill.ts:113:9
-       if (!map[row.insight_type]) {

+       if (map[row.insight_type]) {


[NoCoverage] ConditionalExpression
src/services/distill.ts:113:9
-       if (!map[row.insight_type]) {

+       if (true) {


[NoCoverage] ConditionalExpression
src/services/distill.ts:113:9
-       if (!map[row.insight_type]) {

+       if (false) {


[NoCoverage] BlockStatement
src/services/distill.ts:113:33
-       if (!map[row.insight_type]) {

-         map[row.insight_type] = row.content;

-       }

+       if (!map[row.insight_type]) {}


[NoCoverage] StringLiteral
src/services/memory-service.ts:49:36
-       this.repo.delete(id, reason ?? 'forgotten');

+       this.repo.delete(id, reason ?? "");


[NoCoverage] BlockStatement
src/services/memory-service.ts:98:45
-       for (const memory of workingCandidates) {

-         this.repo.update(promoteMemory(memory, 'core'));

-         count++;

-       }

+       for (const memory of workingCandidates) {}


[NoCoverage] StringLiteral
src/services/memory-service.ts:99:46
-         this.repo.update(promoteMemory(memory, 'core'));

+         this.repo.update(promoteMemory(memory, ""));


[NoCoverage] UpdateOperator
src/services/memory-service.ts:100:7
-         count++;

+         count--;


[Survived] LogicalOperator
src/services/memory-service.ts:49:26
-       this.repo.delete(id, reason ?? 'forgotten');

+       this.repo.delete(id, reason && 'forgotten');

Tests ran:
    MemoryService.forget removes memory from store


[Survived] ConditionalExpression
src/services/memory-service.ts:61:9
-       if (!memory) throw new Error(`Memory not found: ${id}`);

+       if (false) throw new Error(`Memory not found: ${id}`);

Tests ran:
    MemoryService.reinforce boosts weight correctly
    MemoryService.reinforce throws when memory not found


[Survived] StringLiteral
src/services/memory-service.ts:61:34
-       if (!memory) throw new Error(`Memory not found: ${id}`);

+       if (!memory) throw new Error(``);

Tests ran:
    MemoryService.reinforce throws when memory not found


[Survived] ArithmeticOperator
src/services/memory-service.ts:73:29
-       const cutoff = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString();

+       const cutoff = new Date(Date.now() + 7 * 24 * 60 * 60 * 1000).toISOString();

Tests ran:
    MemoryService.applyDecay decays old non-core memories
    MemoryService.applyDecay does not decay core memories


[Survived] ArithmeticOperator
src/services/memory-service.ts:73:42
-       const cutoff = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString();

+       const cutoff = new Date(Date.now() - 7 * 24 * 60 * 60 / 1000).toISOString();

Tests ran:
    MemoryService.applyDecay decays old non-core memories
    MemoryService.applyDecay does not decay core memories


[Survived] ArithmeticOperator
src/services/memory-service.ts:73:42
-       const cutoff = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString();

+       const cutoff = new Date(Date.now() - 7 * 24 * 60 / 60 * 1000).toISOString();

Tests ran:
    MemoryService.applyDecay decays old non-core memories
    MemoryService.applyDecay does not decay core memories


[Survived] ArithmeticOperator
src/services/memory-service.ts:73:42
-       const cutoff = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString();

+       const cutoff = new Date(Date.now() - 7 * 24 / 60 * 60 * 1000).toISOString();

Tests ran:
    MemoryService.applyDecay decays old non-core memories
    MemoryService.applyDecay does not decay core memories


[Survived] ArithmeticOperator
src/services/memory-service.ts:73:42
-       const cutoff = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString();

+       const cutoff = new Date(Date.now() - 7 / 24 * 60 * 60 * 1000).toISOString();

Tests ran:
    MemoryService.applyDecay decays old non-core memories
    MemoryService.applyDecay does not decay core memories


[Survived] StringLiteral
src/services/memory-service.ts:97:58
-       const workingCandidates = this.repo.findForPromotion('working', 10);

+       const workingCandidates = this.repo.findForPromotion("", 10);

Tests ran:
    MemoryService.evaluateTierPromotions promotes buffer memories with sufficient access count


[NoCoverage] StringLiteral
src/services/pattern-service.ts:44:37
-       const teamId = config.teamId ?? '';
+       const teamId = config.teamId ?? "Stryker was here!";

[NoCoverage] ConditionalExpression
src/services/pattern-service.ts:54:43
-       const sharedByMe = shared.filter(m => m.userId === config.userId).length;
+       const sharedByMe = shared.filter(m => true).length;

[NoCoverage] ConditionalExpression
src/services/pattern-service.ts:54:43
-       const sharedByMe = shared.filter(m => m.userId === config.userId).length;
+       const sharedByMe = shared.filter(m => false).length;

[NoCoverage] EqualityOperator
src/services/pattern-service.ts:54:43
-       const sharedByMe = shared.filter(m => m.userId === config.userId).length;
+       const sharedByMe = shared.filter(m => m.userId !== config.userId).length;

[NoCoverage] StringLiteral
src/services/pattern-service.ts:75:37
-       const teamId = config.teamId ?? '';
+       const teamId = config.teamId ?? "Stryker was here!";

[NoCoverage] ObjectLiteral
src/services/pattern-service.ts:109:36
-         insights: insights.map(i => ({
-           insightType: i.insightType,
-           content: i.content,
-           confidence: i.confidence,
-         })),
+         insights: insights.map(i => ({})),

[Survived] StringLiteral
src/services/pattern-service.ts:53:60
-       const shared = this.teamRepo.searchSharedCache(teamId, '', project, 1000);
+       const shared = this.teamRepo.searchSharedCache(teamId, "Stryker was here!", project, 1000);
Tests ran:
    PatternService.myStats computes good rate from buffered prompt logs
    PatternService.myStats returns a zero good rate when nothing is logged


[Survived] MethodExpression
src/services/pattern-service.ts:54:24
-       const sharedByMe = shared.filter(m => m.userId === config.userId).length;
+       const sharedByMe = shared.length;
Tests ran:
    PatternService.myStats computes good rate from buffered prompt logs
    PatternService.myStats returns a zero good rate when nothing is logged


[Survived] ArrowFunction
src/services/pattern-service.ts:54:38
-       const sharedByMe = shared.filter(m => m.userId === config.userId).length;
+       const sharedByMe = shared.filter(() => undefined).length;
Tests ran:
    PatternService.myStats computes good rate from buffered prompt logs
    PatternService.myStats returns a zero good rate when nothing is logged


[Survived] ConditionalExpression
src/services/pattern-service.ts:90:11
-         if (p.patternType === 'prompt_category') {
+         if (true) {
Tests ran:
    PatternService.teamStats derives top categories from prompt_category patterns


[Survived] MethodExpression
src/services/pattern-service.ts:94:27
-       const topCategories = [...categoryMap.entries()]
-         .sort((a, b) => b[1] - a[1])
-         .slice(0, 5)
+       const topCategories = [...categoryMap.entries()].sort((a, b) => b[1] - a[1])
Tests ran:
    PatternService.teamStats aggregates contributions per member from the shared pool
    PatternService.teamStats derives top categories from prompt_category patterns


[Survived] MethodExpression
src/services/pattern-service.ts:94:27
-       const topCategories = [...categoryMap.entries()]
-         .sort((a, b) => b[1] - a[1])
+       const topCategories = [...categoryMap.entries()]
Tests ran:
    PatternService.teamStats aggregates contributions per member from the shared pool
    PatternService.teamStats derives top categories from prompt_category patterns


[Survived] ArithmeticOperator
src/services/pattern-service.ts:95:23
-         .sort((a, b) => b[1] - a[1])
+         .sort((a, b) => b[1] + a[1])
Tests ran:
    PatternService.teamStats derives top categories from prompt_category patterns


[Survived] ArrowFunction
src/services/pattern-service.ts:95:13
-         .sort((a, b) => b[1] - a[1])
+         .sort(() => undefined)
Tests ran:
    PatternService.teamStats aggregates contributions per member from the shared pool
    PatternService.teamStats derives top categories from prompt_category patterns


[Survived] ArrowFunction
src/services/pattern-service.ts:109:30
-         insights: insights.map(i => ({
-           insightType: i.insightType,
-           content: i.content,
-           confidence: i.confidence,
-         })),
+         insights: insights.map(() => undefined),
Tests ran:
    PatternService.teamStats aggregates contributions per member from the shared pool
    PatternService.teamStats derives top categories from prompt_category patterns


[NoCoverage] BlockStatement
src/services/preference-service.ts:31:11
-     ): void {

-       const id = this.idGen.memoryId();

-       this.repo.set(id, {

-         key,

-         value,

-         context,

-         strength: (strength as PreferenceStrength) ?? 'moderate',

-         project,

-       });

-     }

+     ): void {}


[NoCoverage] ObjectLiteral
src/services/preference-service.ts:33:23
-       this.repo.set(id, {

-         key,

-         value,

-         context,

-         strength: (strength as PreferenceStrength) ?? 'moderate',

-         project,

-       });

+       this.repo.set(id, {});


[NoCoverage] LogicalOperator
src/services/preference-service.ts:37:17
-         strength: (strength as PreferenceStrength) ?? 'moderate',

+         strength: strength as PreferenceStrength && 'moderate',


[NoCoverage] StringLiteral
src/services/preference-service.ts:37:53
-         strength: (strength as PreferenceStrength) ?? 'moderate',

+         strength: (strength as PreferenceStrength) ?? "",


[NoCoverage] BlockStatement
src/services/preference-service.ts:48:50
-     getPreferences(project?: string): Preference[] {

-       if (project) return this.repo.getForContext(undefined, project);

-       return this.repo.getGlobal();

-     }

+     getPreferences(project?: string): Preference[] {}


[NoCoverage] ConditionalExpression
src/services/preference-service.ts:49:9
-       if (project) return this.repo.getForContext(undefined, project);

+       if (true) return this.repo.getForContext(undefined, project);


[NoCoverage] ConditionalExpression
src/services/preference-service.ts:49:9
-       if (project) return this.repo.getForContext(undefined, project);

+       if (false) return this.repo.getForContext(undefined, project);


[NoCoverage] BlockStatement
src/services/preference-service.ts:60:80
-     getRelevantPreferences(contextQuery: string, project?: string): Preference[] {

-       return this.repo.getForContext(contextQuery, project);

-     }

+     getRelevantPreferences(contextQuery: string, project?: string): Preference[] {}


[NoCoverage] BooleanLiteral
src/services/prompt-log-service.ts:52:31
-       if (!config.railwayUrl || !config.teamId) return 0;
+       if (!config.railwayUrl || config.teamId) return 0;

[NoCoverage] ConditionalExpression
src/services/prompt-log-service.ts:55:9
-       if (pending.length === 0) return 0;
+       if (true) return 0;

[NoCoverage] ConditionalExpression
src/services/prompt-log-service.ts:55:9
-       if (pending.length === 0) return 0;
+       if (false) return 0;

[NoCoverage] EqualityOperator
src/services/prompt-log-service.ts:55:9
-       if (pending.length === 0) return 0;
+       if (pending.length !== 0) return 0;

[NoCoverage] StringLiteral
src/services/prompt-log-service.ts:57:48
-       const { default: postgres } = await import('postgres');
+       const { default: postgres } = await import("");

[NoCoverage] ObjectLiteral
src/services/prompt-log-service.ts:58:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
+       const sql = postgres(config.railwayUrl, {});

[NoCoverage] StringLiteral
src/services/prompt-log-service.ts:58:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
+       const sql = postgres(config.railwayUrl, { ssl: "", max: 3 });

[NoCoverage] BlockStatement
src/services/prompt-log-service.ts:59:9
-       try {
-         const pushed: string[] = [];
-         for (const log of pending) {
-           await sql`
-             INSERT INTO prompt_logs
-               (id, user_id, team_id, project, pattern, outcome, category, tags,
-                share_content, raw_content, logged_at)
-             VALUES (
-               ${log.id}, ${log.userId}, ${log.teamId}, ${log.project ?? null},
-               ${log.pattern}, ${log.outcome}, ${log.category},
-               ${log.tags as string[]}, ${log.shareContent},
-               ${log.shareContent ? (log.rawContent ?? null) : null},
-               ${log.loggedAt}
-             )
-             ON CONFLICT (id) DO NOTHING
-           `;
-           pushed.push(log.id);
-         }
-         this.teamRepo.markPromptLogsPushed(pushed);
-         return pushed.length;
-       } catch (err) {
+       try {} catch (err) {

[NoCoverage] ArrayDeclaration
src/services/prompt-log-service.ts:60:32
-         const pushed: string[] = [];
+         const pushed: string[] = ["Stryker was here"];

[NoCoverage] BlockStatement
src/services/prompt-log-service.ts:61:34
-         for (const log of pending) {
-           await sql`
-             INSERT INTO prompt_logs
-               (id, user_id, team_id, project, pattern, outcome, category, tags,
-                share_content, raw_content, logged_at)
-             VALUES (
-               ${log.id}, ${log.userId}, ${log.teamId}, ${log.project ?? null},
-               ${log.pattern}, ${log.outcome}, ${log.category},
-               ${log.tags as string[]}, ${log.shareContent},
-               ${log.shareContent ? (log.rawContent ?? null) : null},
-               ${log.loggedAt}
-             )
-             ON CONFLICT (id) DO NOTHING
-           `;
-           pushed.push(log.id);
-         }
+         for (const log of pending) {}

[NoCoverage] StringLiteral
src/services/prompt-log-service.ts:62:18
-           await sql`
-             INSERT INTO prompt_logs
-               (id, user_id, team_id, project, pattern, outcome, category, tags,
-                share_content, raw_content, logged_at)
-             VALUES (
-               ${log.id}, ${log.userId}, ${log.teamId}, ${log.project ?? null},
-               ${log.pattern}, ${log.outcome}, ${log.category},
-               ${log.tags as string[]}, ${log.shareContent},
-               ${log.shareContent ? (log.rawContent ?? null) : null},
-               ${log.loggedAt}
-             )
-             ON CONFLICT (id) DO NOTHING
-           `;
+           await sql``;

[NoCoverage] LogicalOperator
src/services/prompt-log-service.ts:67:56
-               ${log.id}, ${log.userId}, ${log.teamId}, ${log.project ?? null},
+               ${log.id}, ${log.userId}, ${log.teamId}, ${log.project && null},

[NoCoverage] LogicalOperator
src/services/prompt-log-service.ts:70:35
-               ${log.shareContent ? (log.rawContent ?? null) : null},
+               ${log.shareContent ? (log.rawContent && null) : null},

[NoCoverage] BlockStatement
src/services/prompt-log-service.ts:79:19
-       } catch (err) {
-         throw new StorageError('Failed to push prompt log buffer', err);
-       } finally {
+       } catch (err) {} finally {

[NoCoverage] StringLiteral
src/services/prompt-log-service.ts:80:30
-         throw new StorageError('Failed to push prompt log buffer', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/services/prompt-log-service.ts:81:15
-       } finally {
-         await sql.end();
-       }
+       } finally {}

[Survived] ConditionalExpression
src/services/prompt-log-service.ts:52:9
-       if (!config.railwayUrl || !config.teamId) return 0;
+       if (true) return 0;
Tests ran:
    PromptLogService pushBuffer is a no-op (returns 0) without railwayUrl


[Survived] StringLiteral
src/services/session-service.ts:42:43
-       if (!session) throw new NotFoundError('Session', id);

+       if (!session) throw new NotFoundError("", id);

Tests ran:
    SessionService throws NotFoundError rather than failing silently for an unknown id
    SessionService throws NotFoundError for an empty id — the shape the MCP handler used to pass


[NoCoverage] StringLiteral
src/services/sync.ts:88:80
-             ${m['project'] as string | null}, ${(m['scope'] as string | null) ?? 'project'},
+             ${m['project'] as string | null}, ${(m['scope'] as string | null) ?? ""},

[NoCoverage] BlockStatement
src/services/sync.ts:118:30
-       for (const rm of remote) {
-         const existing = db.prepare(`SELECT last_accessed_at FROM memories WHERE id = ?`)
-           .get(rm['id'] as string) as { last_accessed_at: string } | undefined;
-         // Normalise BEFORE comparing: the driver returns a Date, and `'2026-09-10T…' >= Date`
-         // coerces the Date to a locale string, so the conflict policy was comparing two different
-         // formats (EDR-003 invariant 4).
-         const remoteLastAccessed = toIsoString(rm['last_accessed_at']);
-         if (existing && existing.last_accessed_at >= remoteLastAccessed) continue;
-         const tags = toTagsJson(rm['tags']);
-         db.prepare(`
-           INSERT OR REPLACE INTO memories
-             (id, content, memory_type, tier, weight, decay_rate, access_count,
-              created_at, last_accessed_at, project, scope, category, tags, source,
-              confirmed, device_id, synced_at)
-           VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
-         `).run(
-           rm['id'], rm['content'], rm['memory_type'], rm['tier'],
-           rm['weight'], rm['decay_rate'], rm['access_count'],
-           toIsoString(rm['created_at']), remoteLastAccessed, rm['project'],
-           (rm['scope'] as string | null) ?? 'project',
-           rm['category'], tags, rm['source'], rm['confirmed'] ? 1 : 0,
-           rm['source_device'], new Date().toISOString()
-         );
-         pulled++;
-       }
+       for (const rm of remote) {}

[NoCoverage] StringLiteral
src/services/sync.ts:124:49
-         const remoteLastAccessed = toIsoString(rm['last_accessed_at']);
+         const remoteLastAccessed = toIsoString(rm[""]);

[NoCoverage] ConditionalExpression
src/services/sync.ts:125:11
-         if (existing && existing.last_accessed_at >= remoteLastAccessed) continue;
+         if (true) continue;

[NoCoverage] ConditionalExpression
src/services/sync.ts:125:11
-         if (existing && existing.last_accessed_at >= remoteLastAccessed) continue;
+         if (false) continue;

[NoCoverage] LogicalOperator
src/services/sync.ts:125:11
-         if (existing && existing.last_accessed_at >= remoteLastAccessed) continue;
+         if (existing || existing.last_accessed_at >= remoteLastAccessed) continue;

[NoCoverage] ConditionalExpression
src/services/sync.ts:125:23
-         if (existing && existing.last_accessed_at >= remoteLastAccessed) continue;
+         if (existing && true) continue;

[NoCoverage] EqualityOperator
src/services/sync.ts:125:23
-         if (existing && existing.last_accessed_at >= remoteLastAccessed) continue;
+         if (existing && existing.last_accessed_at > remoteLastAccessed) continue;

[NoCoverage] EqualityOperator
src/services/sync.ts:125:23
-         if (existing && existing.last_accessed_at >= remoteLastAccessed) continue;
+         if (existing && existing.last_accessed_at < remoteLastAccessed) continue;

[NoCoverage] StringLiteral
src/services/sync.ts:127:34
-         const tags = toTagsJson(rm['tags']);
+         const tags = toTagsJson(rm[""]);

[NoCoverage] StringLiteral
src/services/sync.ts:128:18
-         db.prepare(`
-           INSERT OR REPLACE INTO memories
-             (id, content, memory_type, tier, weight, decay_rate, access_count,
-              created_at, last_accessed_at, project, scope, category, tags, source,
-              confirmed, device_id, synced_at)
-           VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
-         `).run(
+         db.prepare(``).run(

[NoCoverage] StringLiteral
src/services/sync.ts:135:12
-           rm['id'], rm['content'], rm['memory_type'], rm['tier'],
+           rm[""], rm['content'], rm['memory_type'], rm['tier'],

[NoCoverage] StringLiteral
src/services/sync.ts:135:22
-           rm['id'], rm['content'], rm['memory_type'], rm['tier'],
+           rm['id'], rm[""], rm['memory_type'], rm['tier'],

[NoCoverage] StringLiteral
src/services/sync.ts:135:37
-           rm['id'], rm['content'], rm['memory_type'], rm['tier'],
+           rm['id'], rm['content'], rm[""], rm['tier'],

[NoCoverage] StringLiteral
src/services/sync.ts:135:56
-           rm['id'], rm['content'], rm['memory_type'], rm['tier'],
+           rm['id'], rm['content'], rm['memory_type'], rm[""],

[NoCoverage] StringLiteral
src/services/sync.ts:136:12
-           rm['weight'], rm['decay_rate'], rm['access_count'],
+           rm[""], rm['decay_rate'], rm['access_count'],

[NoCoverage] StringLiteral
src/services/sync.ts:136:26
-           rm['weight'], rm['decay_rate'], rm['access_count'],
+           rm['weight'], rm[""], rm['access_count'],

[NoCoverage] StringLiteral
src/services/sync.ts:136:44
-           rm['weight'], rm['decay_rate'], rm['access_count'],
+           rm['weight'], rm['decay_rate'], rm[""],

[NoCoverage] StringLiteral
src/services/sync.ts:137:24
-           toIsoString(rm['created_at']), remoteLastAccessed, rm['project'],
+           toIsoString(rm[""]), remoteLastAccessed, rm['project'],

[NoCoverage] StringLiteral
src/services/sync.ts:137:63
-           toIsoString(rm['created_at']), remoteLastAccessed, rm['project'],
+           toIsoString(rm['created_at']), remoteLastAccessed, rm[""],

[NoCoverage] LogicalOperator
src/services/sync.ts:138:9
-           (rm['scope'] as string | null) ?? 'project',
+           rm['scope'] as string | null && 'project',

[NoCoverage] StringLiteral
src/services/sync.ts:138:43
-           (rm['scope'] as string | null) ?? 'project',
+           (rm['scope'] as string | null) ?? "",

[NoCoverage] StringLiteral
src/services/sync.ts:139:12
-           rm['category'], tags, rm['source'], rm['confirmed'] ? 1 : 0,
+           rm[""], tags, rm['source'], rm['confirmed'] ? 1 : 0,

[NoCoverage] StringLiteral
src/services/sync.ts:139:34
-           rm['category'], tags, rm['source'], rm['confirmed'] ? 1 : 0,
+           rm['category'], tags, rm[""], rm['confirmed'] ? 1 : 0,

[NoCoverage] StringLiteral
src/services/sync.ts:139:48
-           rm['category'], tags, rm['source'], rm['confirmed'] ? 1 : 0,
+           rm['category'], tags, rm['source'], rm[""] ? 1 : 0,

[NoCoverage] StringLiteral
src/services/sync.ts:140:12
-           rm['source_device'], new Date().toISOString()
+           rm[""], new Date().toISOString()

[NoCoverage] UpdateOperator
src/services/sync.ts:142:7
-         pulled++;
+         pulled--;

[NoCoverage] BlockStatement
src/services/sync.ts:155:17
-     } catch (err) {
-       throw new SyncError(`Memory sync failed: ${String(err)}`, err);
-     }
+     } catch (err) {}

[NoCoverage] StringLiteral
src/services/sync.ts:156:25
-       throw new SyncError(`Memory sync failed: ${String(err)}`, err);
+       throw new SyncError(``, err);

[NoCoverage] BlockStatement
src/services/sync.ts:168:7
-     try {
-       const { default: postgres } = await import('postgres');
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
-       const db  = getDatabase();
-       await sql`INSERT INTO users (id) VALUES (${config.userId}) ON CONFLICT DO NOTHING`;
-       // Push local insights
-       const localInsights = db.prepare(`SELECT * FROM insights`).all() as Record<string, unknown>[];
-       let pushed = 0;
-       for (const ins of localInsights) {
-         await sql`
-           INSERT INTO insights (id, user_id, insight_type, project, content, confidence, source_count, version, updated_at)
-           VALUES (${ins['id'] as string}, ${config.userId}, ${ins['insight_type'] as string},
-                   ${ins['project'] as string | null}, ${ins['content'] as string},
-                   ${ins['confidence'] as number}, ${ins['source_count'] as number},
-                   ${ins['version'] as number}, ${ins['updated_at'] as string})
-           ON CONFLICT (user_id, insight_type, project) DO UPDATE SET
-             content      = EXCLUDED.content,
-             confidence   = EXCLUDED.confidence,
-             source_count = EXCLUDED.source_count,
-             version      = EXCLUDED.version,
-             updated_at   = EXCLUDED.updated_at
-           WHERE EXCLUDED.version > insights.version
-         `;
-         pushed++;
-       }
-       // Pull remote insights
-       const cursor = db.prepare(
-         `SELECT last_pull_at FROM sync_cursor WHERE device_id = ? AND user_id = ?`
-       ).get(config.deviceId, config.userId) as { last_pull_at: string | null } | undefined;
-       const since = cursor?.last_pull_at ?? '1970-01-01T00:00:00Z';
-       const remote = await sql<Record<string, unknown>[]>`
-         SELECT * FROM insights
-         WHERE user_id = ${config.userId} AND updated_at > ${since}
-       `;
-       let pulled = 0;
-       for (const ri of remote) {
-         const local = db.prepare(`SELECT version FROM insights WHERE id = ?`).get(ri['id'] as string) as { version: number } | undefined;
-         if (local && local.version >= (ri['version'] as number)) continue;
-         db.prepare(`
-           INSERT OR REPLACE INTO insights (id, insight_type, project, content, confidence, source_count, version, updated_at)
-           VALUES (?,?,?,?,?,?,?,?)
-         `).run(ri['id'], ri['insight_type'], ri['project'], ri['content'],
-                ri['confidence'], ri['source_count'], ri['version'], toIsoString(ri['updated_at']));
-         pulled++;
-       }
-       await sql.end();
-       return { pushed, pulled, conflicts: 0, skipped: false };
-     } catch (err) {
+     try {} catch (err) {

[NoCoverage] StringLiteral
src/services/sync.ts:169:48
-       const { default: postgres } = await import('postgres');
+       const { default: postgres } = await import("");

[NoCoverage] ObjectLiteral
src/services/sync.ts:170:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
+       const sql = postgres(config.railwayUrl, {});

[NoCoverage] StringLiteral
src/services/sync.ts:170:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
+       const sql = postgres(config.railwayUrl, { ssl: "", max: 3 });

[NoCoverage] StringLiteral
src/services/sync.ts:173:14
-       await sql`INSERT INTO users (id) VALUES (${config.userId}) ON CONFLICT DO NOTHING`;
+       await sql``;

[NoCoverage] BlockStatement
src/services/sync.ts:178:38
-       for (const ins of localInsights) {
-         await sql`
-           INSERT INTO insights (id, user_id, insight_type, project, content, confidence, source_count, version, updated_at)
-           VALUES (${ins['id'] as string}, ${config.userId}, ${ins['insight_type'] as string},
-                   ${ins['project'] as string | null}, ${ins['content'] as string},
-                   ${ins['confidence'] as number}, ${ins['source_count'] as number},
-                   ${ins['version'] as number}, ${ins['updated_at'] as string})
-           ON CONFLICT (user_id, insight_type, project) DO UPDATE SET
-             content      = EXCLUDED.content,
-             confidence   = EXCLUDED.confidence,
-             source_count = EXCLUDED.source_count,
-             version      = EXCLUDED.version,
-             updated_at   = EXCLUDED.updated_at
-           WHERE EXCLUDED.version > insights.version
-         `;
-         pushed++;
-       }
+       for (const ins of localInsights) {}

[NoCoverage] StringLiteral
src/services/sync.ts:179:16
-         await sql`
-           INSERT INTO insights (id, user_id, insight_type, project, content, confidence, source_count, version, updated_at)
-           VALUES (${ins['id'] as string}, ${config.userId}, ${ins['insight_type'] as string},
-                   ${ins['project'] as string | null}, ${ins['content'] as string},
-                   ${ins['confidence'] as number}, ${ins['source_count'] as number},
-                   ${ins['version'] as number}, ${ins['updated_at'] as string})
-           ON CONFLICT (user_id, insight_type, project) DO UPDATE SET
-             content      = EXCLUDED.content,
-             confidence   = EXCLUDED.confidence,
-             source_count = EXCLUDED.source_count,
-             version      = EXCLUDED.version,
-             updated_at   = EXCLUDED.updated_at
-           WHERE EXCLUDED.version > insights.version
-         `;
+         await sql``;

[NoCoverage] UpdateOperator
src/services/sync.ts:193:7
-         pushed++;
+         pushed--;

[NoCoverage] LogicalOperator
src/services/sync.ts:200:19
-       const since = cursor?.last_pull_at ?? '1970-01-01T00:00:00Z';
+       const since = cursor?.last_pull_at && '1970-01-01T00:00:00Z';

[NoCoverage] OptionalChaining
src/services/sync.ts:200:19
-       const since = cursor?.last_pull_at ?? '1970-01-01T00:00:00Z';
+       const since = cursor.last_pull_at ?? '1970-01-01T00:00:00Z';

[NoCoverage] StringLiteral
src/services/sync.ts:200:43
-       const since = cursor?.last_pull_at ?? '1970-01-01T00:00:00Z';
+       const since = cursor?.last_pull_at ?? "";

[NoCoverage] StringLiteral
src/services/sync.ts:202:56
-       const remote = await sql<Record<string, unknown>[]>`
-         SELECT * FROM insights
-         WHERE user_id = ${config.userId} AND updated_at > ${since}
-       `;
+       const remote = await sql<Record<string, unknown>[]>``;

[NoCoverage] BlockStatement
src/services/sync.ts:207:30
-       for (const ri of remote) {
-         const local = db.prepare(`SELECT version FROM insights WHERE id = ?`).get(ri['id'] as string) as { version: number } | undefined;
-         if (local && local.version >= (ri['version'] as number)) continue;
-         db.prepare(`
-           INSERT OR REPLACE INTO insights (id, insight_type, project, content, confidence, source_count, version, updated_at)
-           VALUES (?,?,?,?,?,?,?,?)
-         `).run(ri['id'], ri['insight_type'], ri['project'], ri['content'],
-                ri['confidence'], ri['source_count'], ri['version'], toIsoString(ri['updated_at']));
-         pulled++;
-       }
+       for (const ri of remote) {}

[NoCoverage] ConditionalExpression
src/services/sync.ts:209:11
-         if (local && local.version >= (ri['version'] as number)) continue;
+         if (true) continue;

[NoCoverage] ConditionalExpression
src/services/sync.ts:209:11
-         if (local && local.version >= (ri['version'] as number)) continue;
+         if (false) continue;

[NoCoverage] LogicalOperator
src/services/sync.ts:209:11
-         if (local && local.version >= (ri['version'] as number)) continue;
+         if (local || local.version >= (ri['version'] as number)) continue;

[NoCoverage] ConditionalExpression
src/services/sync.ts:209:20
-         if (local && local.version >= (ri['version'] as number)) continue;
+         if (local && true) continue;

[NoCoverage] EqualityOperator
src/services/sync.ts:209:20
-         if (local && local.version >= (ri['version'] as number)) continue;
+         if (local && local.version > (ri['version'] as number)) continue;

[NoCoverage] EqualityOperator
src/services/sync.ts:209:20
-         if (local && local.version >= (ri['version'] as number)) continue;
+         if (local && local.version < (ri['version'] as number)) continue;

[NoCoverage] StringLiteral
src/services/sync.ts:210:18
-         db.prepare(`
-           INSERT OR REPLACE INTO insights (id, insight_type, project, content, confidence, source_count, version, updated_at)
-           VALUES (?,?,?,?,?,?,?,?)
-         `).run(ri['id'], ri['insight_type'], ri['project'], ri['content'],
+         db.prepare(``).run(ri['id'], ri['insight_type'], ri['project'], ri['content'],

[NoCoverage] StringLiteral
src/services/sync.ts:213:17
-         `).run(ri['id'], ri['insight_type'], ri['project'], ri['content'],
+         `).run(ri[""], ri['insight_type'], ri['project'], ri['content'],

[NoCoverage] StringLiteral
src/services/sync.ts:213:27
-         `).run(ri['id'], ri['insight_type'], ri['project'], ri['content'],
+         `).run(ri['id'], ri[""], ri['project'], ri['content'],

[NoCoverage] StringLiteral
src/services/sync.ts:213:47
-         `).run(ri['id'], ri['insight_type'], ri['project'], ri['content'],
+         `).run(ri['id'], ri['insight_type'], ri[""], ri['content'],

[NoCoverage] StringLiteral
src/services/sync.ts:213:62
-         `).run(ri['id'], ri['insight_type'], ri['project'], ri['content'],
+         `).run(ri['id'], ri['insight_type'], ri['project'], ri[""],

[NoCoverage] StringLiteral
src/services/sync.ts:214:17
-                ri['confidence'], ri['source_count'], ri['version'], toIsoString(ri['updated_at']));
+                ri[""], ri['source_count'], ri['version'], toIsoString(ri['updated_at']));

[NoCoverage] StringLiteral
src/services/sync.ts:214:35
-                ri['confidence'], ri['source_count'], ri['version'], toIsoString(ri['updated_at']));
+                ri['confidence'], ri[""], ri['version'], toIsoString(ri['updated_at']));

[NoCoverage] StringLiteral
src/services/sync.ts:214:55
-                ri['confidence'], ri['source_count'], ri['version'], toIsoString(ri['updated_at']));
+                ri['confidence'], ri['source_count'], ri[""], toIsoString(ri['updated_at']));

[NoCoverage] StringLiteral
src/services/sync.ts:214:82
-                ri['confidence'], ri['source_count'], ri['version'], toIsoString(ri['updated_at']));
+                ri['confidence'], ri['source_count'], ri['version'], toIsoString(ri[""]));

[NoCoverage] UpdateOperator
src/services/sync.ts:215:7
-         pulled++;
+         pulled--;

[NoCoverage] ObjectLiteral
src/services/sync.ts:219:12
-       return { pushed, pulled, conflicts: 0, skipped: false };
+       return {};

[NoCoverage] BooleanLiteral
src/services/sync.ts:219:53
-       return { pushed, pulled, conflicts: 0, skipped: false };
+       return { pushed, pulled, conflicts: 0, skipped: true };

[NoCoverage] BlockStatement
src/services/sync.ts:221:17
-     } catch (err) {
-       throw new SyncError(`Insight sync failed: ${String(err)}`, err);
-     }
+     } catch (err) {}

[NoCoverage] StringLiteral
src/services/sync.ts:222:25
-       throw new SyncError(`Insight sync failed: ${String(err)}`, err);
+       throw new SyncError(``, err);

[NoCoverage] BlockStatement
src/services/sync.ts:229:75
-   export async function pushSessionSummary(session: Session): Promise<void> {
-     const config = getConfig();
-     if (!config.railwayUrl || !session.summary) return;
-     try {
-       const { default: postgres } = await import('postgres');
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });
-       await sql`INSERT INTO users (id) VALUES (${config.userId}) ON CONFLICT DO NOTHING`;
-       await sql`
-         INSERT INTO session_summaries (id, user_id, project, device, summary, active_tasks, touched_files, started_at, ended_at)
-         VALUES (
-           ${session.id}, ${config.userId}, ${session.project}, ${config.deviceId},
-           ${session.summary ?? ''},
-           ${JSON.stringify(session.activeTasks)},
-           ${JSON.stringify(session.touchedFiles)},
-           ${session.startedAt}, ${session.endedAt ?? new Date().toISOString()}
-         )
-         ON CONFLICT (id) DO NOTHING
-       `;
-       await sql.end();
-     } catch (err) {
-       // Non-fatal — local session end should succeed even if sync fails
-       console.warn(`[chronicle] session summary sync failed: ${String(err)}`);
-     }
-   }
+   export async function pushSessionSummary(session: Session): Promise<void> {}

[NoCoverage] ConditionalExpression
src/services/sync.ts:231:7
-     if (!config.railwayUrl || !session.summary) return;
+     if (true) return;

[NoCoverage] ConditionalExpression
src/services/sync.ts:231:7
-     if (!config.railwayUrl || !session.summary) return;
+     if (false) return;

[NoCoverage] LogicalOperator
src/services/sync.ts:231:7
-     if (!config.railwayUrl || !session.summary) return;
+     if (!config.railwayUrl && !session.summary) return;

[NoCoverage] BooleanLiteral
src/services/sync.ts:231:7
-     if (!config.railwayUrl || !session.summary) return;
+     if (config.railwayUrl || !session.summary) return;

[NoCoverage] BooleanLiteral
src/services/sync.ts:231:29
-     if (!config.railwayUrl || !session.summary) return;
+     if (!config.railwayUrl || session.summary) return;

[NoCoverage] BlockStatement
src/services/sync.ts:233:7
-     try {
-       const { default: postgres } = await import('postgres');
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });
-       await sql`INSERT INTO users (id) VALUES (${config.userId}) ON CONFLICT DO NOTHING`;
-       await sql`
-         INSERT INTO session_summaries (id, user_id, project, device, summary, active_tasks, touched_files, started_at, ended_at)
-         VALUES (
-           ${session.id}, ${config.userId}, ${session.project}, ${config.deviceId},
-           ${session.summary ?? ''},
-           ${JSON.stringify(session.activeTasks)},
-           ${JSON.stringify(session.touchedFiles)},
-           ${session.startedAt}, ${session.endedAt ?? new Date().toISOString()}
-         )
-         ON CONFLICT (id) DO NOTHING
-       `;
-       await sql.end();
-     } catch (err) {
+     try {} catch (err) {

[NoCoverage] StringLiteral
src/services/sync.ts:234:48
-       const { default: postgres } = await import('postgres');
+       const { default: postgres } = await import("");

[NoCoverage] ObjectLiteral
src/services/sync.ts:235:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });
+       const sql = postgres(config.railwayUrl, {});

[NoCoverage] StringLiteral
src/services/sync.ts:235:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });
+       const sql = postgres(config.railwayUrl, { ssl: "", max: 1 });

[NoCoverage] StringLiteral
src/services/sync.ts:236:14
-       await sql`INSERT INTO users (id) VALUES (${config.userId}) ON CONFLICT DO NOTHING`;
+       await sql``;

[NoCoverage] StringLiteral
src/services/sync.ts:237:14
-       await sql`
-         INSERT INTO session_summaries (id, user_id, project, device, summary, active_tasks, touched_files, started_at, ended_at)
-         VALUES (
-           ${session.id}, ${config.userId}, ${session.project}, ${config.deviceId},
-           ${session.summary ?? ''},
-           ${JSON.stringify(session.activeTasks)},
-           ${JSON.stringify(session.touchedFiles)},
-           ${session.startedAt}, ${session.endedAt ?? new Date().toISOString()}
-         )
-         ON CONFLICT (id) DO NOTHING
-       `;
+       await sql``;

[NoCoverage] LogicalOperator
src/services/sync.ts:241:11
-           ${session.summary ?? ''},
+           ${session.summary && ''},

[NoCoverage] StringLiteral
src/services/sync.ts:241:30
-           ${session.summary ?? ''},
+           ${session.summary ?? "Stryker was here!"},

[NoCoverage] LogicalOperator
src/services/sync.ts:244:33
-           ${session.startedAt}, ${session.endedAt ?? new Date().toISOString()}
+           ${session.startedAt}, ${session.endedAt && new Date().toISOString()}

[NoCoverage] BlockStatement
src/services/sync.ts:249:17
-     } catch (err) {
-       // Non-fatal — local session end should succeed even if sync fails
-       console.warn(`[chronicle] session summary sync failed: ${String(err)}`);
-     }
+     } catch (err) {}

[NoCoverage] StringLiteral
src/services/sync.ts:251:18
-       console.warn(`[chronicle] session summary sync failed: ${String(err)}`);
+       console.warn(``);

[NoCoverage] BlockStatement
src/services/sync.ts:272:7
-     try {
-       const { default: postgres } = await import('postgres');
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
-       const db = getDatabase();
-       // Ensure schema and team row exist
-       await sql.unsafe(TEAM_SCHEMA_SQL);
-       await sql`INSERT INTO teams (id, name) VALUES (${config.teamId}, ${config.teamId}) ON CONFLICT DO NOTHING`;
-       const cursor = db.prepare(
-         `SELECT last_push_at, last_pull_at FROM sync_cursor WHERE device_id = ? AND user_id = ?`,
-       ).get(config.deviceId, config.userId) as { last_push_at: string | null; last_pull_at: string | null } | undefined;
-       const lastPush = cursor?.last_push_at ?? '1970-01-01T00:00:00Z';
-       const lastPull = cursor?.last_pull_at ?? '1970-01-01T00:00:00Z';
-       const now = new Date().toISOString();
-       let pushed = 0;
-       let pulled = 0;
-       // ── PUSH contributors ────────────────────────────────────────────────────
-       const contributors = (project
-         ? db.prepare(`SELECT * FROM contributors WHERE project = ? AND last_active_at > ?`).all(project, lastPush)
-         : db.prepare(`SELECT * FROM contributors WHERE last_active_at > ?`).all(lastPush)
-       ) as any[];
-       for (const c of contributors) {
-         await sql`
-           INSERT INTO team_contributors
-             (id, team_id, project, name, email, skills, role, bandwidth_hours_per_week,
-              availability, last_active_at, created_at, updated_at, source_device)
-           VALUES (
-             ${c.id}, ${config.teamId}, ${c.project}, ${c.name}, ${c.email ?? null},
-             ${JSON.parse(c.skills ?? '[]')}, ${c.role}, ${c.bandwidth_hours_per_week},
-             ${c.availability}, ${c.last_active_at}, ${c.created_at}, ${now}, ${config.deviceId}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             availability       = EXCLUDED.availability,
-             last_active_at     = EXCLUDED.last_active_at,
-             bandwidth_hours_per_week = EXCLUDED.bandwidth_hours_per_week,
-             updated_at         = EXCLUDED.updated_at,
-             source_device      = EXCLUDED.source_device
-           WHERE EXCLUDED.last_active_at > team_contributors.last_active_at
-         `;
-         pushed++;
-       }
-       // ── PUSH work_packages ───────────────────────────────────────────────────
-       const packages = (project
-         ? db.prepare(`SELECT * FROM work_packages WHERE project = ? AND created_at > ?`).all(project, lastPush)
-         : db.prepare(`SELECT * FROM work_packages WHERE created_at > ?`).all(lastPush)
-       ) as any[];
-       // Also push packages whose status changed (assigned, completed, etc.)
-       const updatedPackages = (project
-         ? db.prepare(`SELECT * FROM work_packages WHERE project = ? AND (assigned_at > ? OR completed_at > ?)`).all(project, lastPush, lastPush)
-         : db.prepare(`SELECT * FROM work_packages WHERE assigned_at > ? OR completed_at > ?`).all(lastPush, lastPush)
-       ) as any[];
-       const allPackages = [...new Map([...packages, ...updatedPackages].map(p => [p.id, p])).values()];
-       for (const p of allPackages) {
-         await sql`
-           INSERT INTO team_work_packages
-             (id, team_id, project, title, description, spec_section, status, role_required,
-              dependencies, priority_rank, parent_id, is_milestone, branch_name,
-              forgecraft_score, forgecraft_tier, forgecraft_pass,
-              assigned_to, assigned_at, completed_at, created_at, updated_at, source_device)
-           VALUES (
-             ${p.id}, ${config.teamId}, ${p.project}, ${p.title}, ${p.description},
-             ${p.spec_section ?? null}, ${p.status}, ${p.role_required},
-             ${JSON.parse(p.dependencies ?? '[]')}, ${p.priority_rank},
-             ${p.parent_id ?? null}, ${p.is_milestone === 1},
-             ${p.branch_name ?? null}, ${p.forgecraft_score ?? null},
-             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},
-             ${p.assigned_to ?? null}, ${p.assigned_at ?? null}, ${p.completed_at ?? null},
-             ${p.created_at}, ${now}, ${config.deviceId}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             status         = EXCLUDED.status,
-             assigned_to    = EXCLUDED.assigned_to,
-             assigned_at    = EXCLUDED.assigned_at,
-             completed_at   = EXCLUDED.completed_at,
-             branch_name    = EXCLUDED.branch_name,
-             forgecraft_score = EXCLUDED.forgecraft_score,
-             forgecraft_tier  = EXCLUDED.forgecraft_tier,
-             forgecraft_pass  = EXCLUDED.forgecraft_pass,
-             updated_at     = EXCLUDED.updated_at,
-             source_device  = EXCLUDED.source_device
-         `;
-         pushed++;
-       }
-       // ── PUSH merge_requests ──────────────────────────────────────────────────
-       const mrs = (project
-         ? db.prepare(`SELECT * FROM merge_requests WHERE project = ? AND created_at > ?`).all(project, lastPush)
-         : db.prepare(`SELECT * FROM merge_requests WHERE created_at > ?`).all(lastPush)
-       ) as any[];
-       const updatedMrs = (project
-         ? db.prepare(`SELECT * FROM merge_requests WHERE project = ? AND (approved_at > ? OR rejected_at > ?)`).all(project, lastPush, lastPush)
-         : db.prepare(`SELECT * FROM merge_requests WHERE approved_at > ? OR rejected_at > ?`).all(lastPush, lastPush)
-       ) as any[];
-       const allMrs = [...new Map([...mrs, ...updatedMrs].map(m => [m.id, m])).values()];
-       for (const m of allMrs) {
-         await sql`
-           INSERT INTO team_merge_requests
-             (id, team_id, project, work_package_id, branch_name, from_contributor_id,
-              forgecraft_score, forgecraft_tier, forgecraft_pass, forgecraft_report,
-              approved_by, approved_at, rejected_by, rejected_at, rejection_reason,
-              status, created_at, updated_at, source_device)
-           VALUES (
-             ${m.id}, ${config.teamId}, ${m.project}, ${m.work_package_id}, ${m.branch_name},
-             ${m.from_contributor_id}, ${m.forgecraft_score ?? null}, ${m.forgecraft_tier ?? null},
-             ${m.forgecraft_pass != null ? m.forgecraft_pass === 1 : null},
-             ${m.forgecraft_report ?? null},
-             ${m.approved_by ?? null}, ${m.approved_at ?? null},
-             ${m.rejected_by ?? null}, ${m.rejected_at ?? null}, ${m.rejection_reason ?? null},
-             ${m.status}, ${m.created_at}, ${now}, ${config.deviceId}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             status          = EXCLUDED.status,
-             approved_by     = EXCLUDED.approved_by,
-             approved_at     = EXCLUDED.approved_at,
-             rejected_by     = EXCLUDED.rejected_by,
-             rejected_at     = EXCLUDED.rejected_at,
-             rejection_reason = EXCLUDED.rejection_reason,
-             updated_at      = EXCLUDED.updated_at,
-             source_device   = EXCLUDED.source_device
-           WHERE EXCLUDED.updated_at > team_merge_requests.updated_at
-         `;
-         pushed++;
-       }
-       // ── PULL all tables from other devices ───────────────────────────────────
-       // Pull contributors
-       const remoteContribs = await sql<any[]>`
-         SELECT * FROM team_contributors
-         WHERE team_id = ${config.teamId}
-           AND updated_at > ${lastPull}
-           AND source_device != ${config.deviceId}
-           ${project ? sql`AND project = ${project}` : sql``}
-       `;
-       for (const rc of remoteContribs) {
-         db.prepare(`
-           INSERT OR REPLACE INTO contributors
-             (id, project, name, email, skills, role, bandwidth_hours_per_week, availability, last_active_at, created_at)
-           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-         `).run(
-           rc.id, rc.project, rc.name, rc.email ?? null,
-           JSON.stringify(rc.skills ?? []), rc.role, rc.bandwidth_hours_per_week,
-           rc.availability, toIsoString(rc.last_active_at), toIsoString(rc.created_at),
-         );
-         pulled++;
-       }
-       // Pull work packages
-       const remotePackages = await sql<any[]>`
-         SELECT * FROM team_work_packages
-         WHERE team_id = ${config.teamId}
-           AND updated_at > ${lastPull}
-           AND source_device != ${config.deviceId}
-           ${project ? sql`AND project = ${project}` : sql``}
-       `;
-       for (const rp of remotePackages) {
-         db.prepare(`
-           INSERT OR REPLACE INTO work_packages
-             (id, project, title, description, spec_section, status, role_required,
-              dependencies, priority_rank, parent_id, is_milestone, branch_name,
-              forgecraft_score, forgecraft_tier, forgecraft_pass,
-              assigned_to, assigned_at, completed_at, created_at)
-           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-         `).run(
-           rp.id, rp.project, rp.title, rp.description, rp.spec_section ?? null,
-           rp.status, rp.role_required, JSON.stringify(rp.dependencies ?? []),
-           rp.priority_rank, rp.parent_id ?? null, rp.is_milestone ? 1 : 0,
-           rp.branch_name ?? null, rp.forgecraft_score ?? null,
-           rp.forgecraft_tier ?? null,
-           rp.forgecraft_pass != null ? (rp.forgecraft_pass ? 1 : 0) : null,
-           rp.assigned_to ?? null, rp.assigned_at ? toIsoString(rp.assigned_at) : null,
-           rp.completed_at ? toIsoString(rp.completed_at) : null, toIsoString(rp.created_at),
-         );
-         pulled++;
-       }
-       // Pull merge requests
-       const remoteMrs = await sql<any[]>`
-         SELECT * FROM team_merge_requests
-         WHERE team_id = ${config.teamId}
-           AND updated_at > ${lastPull}
-           AND source_device != ${config.deviceId}
-           ${project ? sql`AND project = ${project}` : sql``}
-       `;
-       for (const rm of remoteMrs) {
-         db.prepare(`
-           INSERT OR REPLACE INTO merge_requests
-             (id, project, work_package_id, branch_name, from_contributor_id,
-              forgecraft_score, forgecraft_tier, forgecraft_pass, forgecraft_report,
-              approved_by, approved_at, rejected_by, rejected_at, rejection_reason,
-              status, created_at)
-           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-         `).run(
-           rm.id, rm.project, rm.work_package_id, rm.branch_name, rm.from_contributor_id,
-           rm.forgecraft_score ?? null, rm.forgecraft_tier ?? null,
-           rm.forgecraft_pass != null ? (rm.forgecraft_pass ? 1 : 0) : null,
-           rm.forgecraft_report ?? null,
-           rm.approved_by ?? null, rm.approved_at ?? null,
-           rm.rejected_by ?? null, rm.rejected_at ?? null, rm.rejection_reason ?? null,
-           rm.status, rm.created_at,
-         );
-         pulled++;
-       }
-       // Update sync cursor
-       db.prepare(`
-         INSERT OR REPLACE INTO sync_cursor (device_id, user_id, last_push_at, last_pull_at, memories_version)
-         VALUES (?, ?, ?, ?, COALESCE((SELECT memories_version FROM sync_cursor WHERE device_id=? AND user_id=?), 0))
-       `).run(config.deviceId, config.userId, now, now, config.deviceId, config.userId);
-       await sql.end();
-       return { pushed, pulled, conflicts: 0, skipped: false };
-     } catch (err) {
+     try {} catch (err) {

[NoCoverage] StringLiteral
src/services/sync.ts:273:48
-       const { default: postgres } = await import('postgres');
+       const { default: postgres } = await import("");

[NoCoverage] ObjectLiteral
src/services/sync.ts:274:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
+       const sql = postgres(config.railwayUrl, {});

[NoCoverage] StringLiteral
src/services/sync.ts:274:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
+       const sql = postgres(config.railwayUrl, { ssl: "", max: 3 });

[NoCoverage] StringLiteral
src/services/sync.ts:279:14
-       await sql`INSERT INTO teams (id, name) VALUES (${config.teamId}, ${config.teamId}) ON CONFLICT DO NOTHING`;
+       await sql``;

[NoCoverage] LogicalOperator
src/services/sync.ts:284:22
-       const lastPush = cursor?.last_push_at ?? '1970-01-01T00:00:00Z';
+       const lastPush = cursor?.last_push_at && '1970-01-01T00:00:00Z';

[NoCoverage] OptionalChaining
src/services/sync.ts:284:22
-       const lastPush = cursor?.last_push_at ?? '1970-01-01T00:00:00Z';
+       const lastPush = cursor.last_push_at ?? '1970-01-01T00:00:00Z';

[NoCoverage] StringLiteral
src/services/sync.ts:284:46
-       const lastPush = cursor?.last_push_at ?? '1970-01-01T00:00:00Z';
+       const lastPush = cursor?.last_push_at ?? "";

[NoCoverage] LogicalOperator
src/services/sync.ts:285:22
-       const lastPull = cursor?.last_pull_at ?? '1970-01-01T00:00:00Z';
+       const lastPull = cursor?.last_pull_at && '1970-01-01T00:00:00Z';

[NoCoverage] OptionalChaining
src/services/sync.ts:285:22
-       const lastPull = cursor?.last_pull_at ?? '1970-01-01T00:00:00Z';
+       const lastPull = cursor.last_pull_at ?? '1970-01-01T00:00:00Z';

[NoCoverage] StringLiteral
src/services/sync.ts:285:46
-       const lastPull = cursor?.last_pull_at ?? '1970-01-01T00:00:00Z';
+       const lastPull = cursor?.last_pull_at ?? "";

[NoCoverage] BlockStatement
src/services/sync.ts:297:35
-       for (const c of contributors) {
-         await sql`
-           INSERT INTO team_contributors
-             (id, team_id, project, name, email, skills, role, bandwidth_hours_per_week,
-              availability, last_active_at, created_at, updated_at, source_device)
-           VALUES (
-             ${c.id}, ${config.teamId}, ${c.project}, ${c.name}, ${c.email ?? null},
-             ${JSON.parse(c.skills ?? '[]')}, ${c.role}, ${c.bandwidth_hours_per_week},
-             ${c.availability}, ${c.last_active_at}, ${c.created_at}, ${now}, ${config.deviceId}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             availability       = EXCLUDED.availability,
-             last_active_at     = EXCLUDED.last_active_at,
-             bandwidth_hours_per_week = EXCLUDED.bandwidth_hours_per_week,
-             updated_at         = EXCLUDED.updated_at,
-             source_device      = EXCLUDED.source_device
-           WHERE EXCLUDED.last_active_at > team_contributors.last_active_at
-         `;
-         pushed++;
-       }
+       for (const c of contributors) {}

[NoCoverage] StringLiteral
src/services/sync.ts:298:16
-         await sql`
-           INSERT INTO team_contributors
-             (id, team_id, project, name, email, skills, role, bandwidth_hours_per_week,
-              availability, last_active_at, created_at, updated_at, source_device)
-           VALUES (
-             ${c.id}, ${config.teamId}, ${c.project}, ${c.name}, ${c.email ?? null},
-             ${JSON.parse(c.skills ?? '[]')}, ${c.role}, ${c.bandwidth_hours_per_week},
-             ${c.availability}, ${c.last_active_at}, ${c.created_at}, ${now}, ${config.deviceId}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             availability       = EXCLUDED.availability,
-             last_active_at     = EXCLUDED.last_active_at,
-             bandwidth_hours_per_week = EXCLUDED.bandwidth_hours_per_week,
-             updated_at         = EXCLUDED.updated_at,
-             source_device      = EXCLUDED.source_device
-           WHERE EXCLUDED.last_active_at > team_contributors.last_active_at
-         `;
+         await sql``;

[NoCoverage] LogicalOperator
src/services/sync.ts:303:65
-             ${c.id}, ${config.teamId}, ${c.project}, ${c.name}, ${c.email ?? null},
+             ${c.id}, ${config.teamId}, ${c.project}, ${c.name}, ${c.email && null},

[NoCoverage] LogicalOperator
src/services/sync.ts:304:24
-             ${JSON.parse(c.skills ?? '[]')}, ${c.role}, ${c.bandwidth_hours_per_week},
+             ${JSON.parse(c.skills && '[]')}, ${c.role}, ${c.bandwidth_hours_per_week},

[NoCoverage] StringLiteral
src/services/sync.ts:304:36
-             ${JSON.parse(c.skills ?? '[]')}, ${c.role}, ${c.bandwidth_hours_per_week},
+             ${JSON.parse(c.skills ?? "")}, ${c.role}, ${c.bandwidth_hours_per_week},

[NoCoverage] UpdateOperator
src/services/sync.ts:315:7
-         pushed++;
+         pushed--;

[NoCoverage] ArrayDeclaration
src/services/sync.ts:330:25
-       const allPackages = [...new Map([...packages, ...updatedPackages].map(p => [p.id, p])).values()];
+       const allPackages = [];

[NoCoverage] ArrayDeclaration
src/services/sync.ts:330:37
-       const allPackages = [...new Map([...packages, ...updatedPackages].map(p => [p.id, p])).values()];
+       const allPackages = [...new Map([].map(p => [p.id, p])).values()];

[NoCoverage] ArrowFunction
src/services/sync.ts:330:75
-       const allPackages = [...new Map([...packages, ...updatedPackages].map(p => [p.id, p])).values()];
+       const allPackages = [...new Map([...packages, ...updatedPackages].map(() => undefined)).values()];

[NoCoverage] ArrayDeclaration
src/services/sync.ts:330:80
-       const allPackages = [...new Map([...packages, ...updatedPackages].map(p => [p.id, p])).values()];
+       const allPackages = [...new Map([...packages, ...updatedPackages].map(p => [])).values()];

[NoCoverage] BlockStatement
src/services/sync.ts:332:34
-       for (const p of allPackages) {
-         await sql`
-           INSERT INTO team_work_packages
-             (id, team_id, project, title, description, spec_section, status, role_required,
-              dependencies, priority_rank, parent_id, is_milestone, branch_name,
-              forgecraft_score, forgecraft_tier, forgecraft_pass,
-              assigned_to, assigned_at, completed_at, created_at, updated_at, source_device)
-           VALUES (
-             ${p.id}, ${config.teamId}, ${p.project}, ${p.title}, ${p.description},
-             ${p.spec_section ?? null}, ${p.status}, ${p.role_required},
-             ${JSON.parse(p.dependencies ?? '[]')}, ${p.priority_rank},
-             ${p.parent_id ?? null}, ${p.is_milestone === 1},
-             ${p.branch_name ?? null}, ${p.forgecraft_score ?? null},
-             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},
-             ${p.assigned_to ?? null}, ${p.assigned_at ?? null}, ${p.completed_at ?? null},
-             ${p.created_at}, ${now}, ${config.deviceId}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             status         = EXCLUDED.status,
-             assigned_to    = EXCLUDED.assigned_to,
-             assigned_at    = EXCLUDED.assigned_at,
-             completed_at   = EXCLUDED.completed_at,
-             branch_name    = EXCLUDED.branch_name,
-             forgecraft_score = EXCLUDED.forgecraft_score,
-             forgecraft_tier  = EXCLUDED.forgecraft_tier,
-             forgecraft_pass  = EXCLUDED.forgecraft_pass,
-             updated_at     = EXCLUDED.updated_at,
-             source_device  = EXCLUDED.source_device
-         `;
-         pushed++;
-       }
+       for (const p of allPackages) {}

[NoCoverage] StringLiteral
src/services/sync.ts:333:16
-         await sql`
-           INSERT INTO team_work_packages
-             (id, team_id, project, title, description, spec_section, status, role_required,
-              dependencies, priority_rank, parent_id, is_milestone, branch_name,
-              forgecraft_score, forgecraft_tier, forgecraft_pass,
-              assigned_to, assigned_at, completed_at, created_at, updated_at, source_device)
-           VALUES (
-             ${p.id}, ${config.teamId}, ${p.project}, ${p.title}, ${p.description},
-             ${p.spec_section ?? null}, ${p.status}, ${p.role_required},
-             ${JSON.parse(p.dependencies ?? '[]')}, ${p.priority_rank},
-             ${p.parent_id ?? null}, ${p.is_milestone === 1},
-             ${p.branch_name ?? null}, ${p.forgecraft_score ?? null},
-             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},
-             ${p.assigned_to ?? null}, ${p.assigned_at ?? null}, ${p.completed_at ?? null},
-             ${p.created_at}, ${now}, ${config.deviceId}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             status         = EXCLUDED.status,
-             assigned_to    = EXCLUDED.assigned_to,
-             assigned_at    = EXCLUDED.assigned_at,
-             completed_at   = EXCLUDED.completed_at,
-             branch_name    = EXCLUDED.branch_name,
-             forgecraft_score = EXCLUDED.forgecraft_score,
-             forgecraft_tier  = EXCLUDED.forgecraft_tier,
-             forgecraft_pass  = EXCLUDED.forgecraft_pass,
-             updated_at     = EXCLUDED.updated_at,
-             source_device  = EXCLUDED.source_device
-         `;
+         await sql``;

[NoCoverage] LogicalOperator
src/services/sync.ts:341:13
-             ${p.spec_section ?? null}, ${p.status}, ${p.role_required},
+             ${p.spec_section && null}, ${p.status}, ${p.role_required},

[NoCoverage] LogicalOperator
src/services/sync.ts:342:24
-             ${JSON.parse(p.dependencies ?? '[]')}, ${p.priority_rank},
+             ${JSON.parse(p.dependencies && '[]')}, ${p.priority_rank},

[NoCoverage] StringLiteral
src/services/sync.ts:342:42
-             ${JSON.parse(p.dependencies ?? '[]')}, ${p.priority_rank},
+             ${JSON.parse(p.dependencies ?? "")}, ${p.priority_rank},

[NoCoverage] LogicalOperator
src/services/sync.ts:343:13
-             ${p.parent_id ?? null}, ${p.is_milestone === 1},
+             ${p.parent_id && null}, ${p.is_milestone === 1},

[NoCoverage] ConditionalExpression
src/services/sync.ts:343:37
-             ${p.parent_id ?? null}, ${p.is_milestone === 1},
+             ${p.parent_id ?? null}, ${true},

[NoCoverage] ConditionalExpression
src/services/sync.ts:343:37
-             ${p.parent_id ?? null}, ${p.is_milestone === 1},
+             ${p.parent_id ?? null}, ${false},

[NoCoverage] EqualityOperator
src/services/sync.ts:343:37
-             ${p.parent_id ?? null}, ${p.is_milestone === 1},
+             ${p.parent_id ?? null}, ${p.is_milestone !== 1},

[NoCoverage] LogicalOperator
src/services/sync.ts:344:13
-             ${p.branch_name ?? null}, ${p.forgecraft_score ?? null},
+             ${p.branch_name && null}, ${p.forgecraft_score ?? null},

[NoCoverage] LogicalOperator
src/services/sync.ts:344:39
-             ${p.branch_name ?? null}, ${p.forgecraft_score ?? null},
+             ${p.branch_name ?? null}, ${p.forgecraft_score && null},

[NoCoverage] LogicalOperator
src/services/sync.ts:345:13
-             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},
+             ${p.forgecraft_tier && null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},

[NoCoverage] ConditionalExpression
src/services/sync.ts:345:43
-             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},
+             ${p.forgecraft_tier ?? null}, ${true ? p.forgecraft_pass === 1 : null},

[NoCoverage] ConditionalExpression
src/services/sync.ts:345:43
-             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},
+             ${p.forgecraft_tier ?? null}, ${false ? p.forgecraft_pass === 1 : null},

[NoCoverage] EqualityOperator
src/services/sync.ts:345:43
-             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},
+             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass == null ? p.forgecraft_pass === 1 : null},

[NoCoverage] ConditionalExpression
src/services/sync.ts:345:71
-             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},
+             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? true : null},

[NoCoverage] ConditionalExpression
src/services/sync.ts:345:71
-             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},
+             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? false : null},

[NoCoverage] EqualityOperator
src/services/sync.ts:345:71
-             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass === 1 : null},
+             ${p.forgecraft_tier ?? null}, ${p.forgecraft_pass != null ? p.forgecraft_pass !== 1 : null},

[NoCoverage] LogicalOperator
src/services/sync.ts:346:13
-             ${p.assigned_to ?? null}, ${p.assigned_at ?? null}, ${p.completed_at ?? null},
+             ${p.assigned_to && null}, ${p.assigned_at ?? null}, ${p.completed_at ?? null},

[NoCoverage] LogicalOperator
src/services/sync.ts:346:39
-             ${p.assigned_to ?? null}, ${p.assigned_at ?? null}, ${p.completed_at ?? null},
+             ${p.assigned_to ?? null}, ${p.assigned_at && null}, ${p.completed_at ?? null},

[NoCoverage] LogicalOperator
src/services/sync.ts:346:65
-             ${p.assigned_to ?? null}, ${p.assigned_at ?? null}, ${p.completed_at ?? null},
+             ${p.assigned_to ?? null}, ${p.assigned_at ?? null}, ${p.completed_at && null},

[NoCoverage] UpdateOperator
src/services/sync.ts:361:7
-         pushed++;
+         pushed--;

[NoCoverage] ArrayDeclaration
src/services/sync.ts:375:20
-       const allMrs = [...new Map([...mrs, ...updatedMrs].map(m => [m.id, m])).values()];
+       const allMrs = [];

[NoCoverage] ArrayDeclaration
src/services/sync.ts:375:32
-       const allMrs = [...new Map([...mrs, ...updatedMrs].map(m => [m.id, m])).values()];
+       const allMrs = [...new Map([].map(m => [m.id, m])).values()];

[NoCoverage] ArrowFunction
src/services/sync.ts:375:60
-       const allMrs = [...new Map([...mrs, ...updatedMrs].map(m => [m.id, m])).values()];
+       const allMrs = [...new Map([...mrs, ...updatedMrs].map(() => undefined)).values()];

[NoCoverage] ArrayDeclaration
src/services/sync.ts:375:65
-       const allMrs = [...new Map([...mrs, ...updatedMrs].map(m => [m.id, m])).values()];
+       const allMrs = [...new Map([...mrs, ...updatedMrs].map(m => [])).values()];

[NoCoverage] BlockStatement
src/services/sync.ts:377:29
-       for (const m of allMrs) {
-         await sql`
-           INSERT INTO team_merge_requests
-             (id, team_id, project, work_package_id, branch_name, from_contributor_id,
-              forgecraft_score, forgecraft_tier, forgecraft_pass, forgecraft_report,
-              approved_by, approved_at, rejected_by, rejected_at, rejection_reason,
-              status, created_at, updated_at, source_device)
-           VALUES (
-             ${m.id}, ${config.teamId}, ${m.project}, ${m.work_package_id}, ${m.branch_name},
-             ${m.from_contributor_id}, ${m.forgecraft_score ?? null}, ${m.forgecraft_tier ?? null},
-             ${m.forgecraft_pass != null ? m.forgecraft_pass === 1 : null},
-             ${m.forgecraft_report ?? null},
-             ${m.approved_by ?? null}, ${m.approved_at ?? null},
-             ${m.rejected_by ?? null}, ${m.rejected_at ?? null}, ${m.rejection_reason ?? null},
-             ${m.status}, ${m.created_at}, ${now}, ${config.deviceId}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             status          = EXCLUDED.status,
-             approved_by     = EXCLUDED.approved_by,
-             approved_at     = EXCLUDED.approved_at,
-             rejected_by     = EXCLUDED.rejected_by,
-             rejected_at     = EXCLUDED.rejected_at,
-             rejection_reason = EXCLUDED.rejection_reason,
-             updated_at      = EXCLUDED.updated_at,
-             source_device   = EXCLUDED.source_device
-           WHERE EXCLUDED.updated_at > team_merge_requests.updated_at
-         `;
-         pushed++;
-       }
+       for (const m of allMrs) {}

[NoCoverage] StringLiteral
src/services/sync.ts:378:16
-         await sql`
-           INSERT INTO team_merge_requests
-             (id, team_id, project, work_package_id, branch_name, from_contributor_id,
-              forgecraft_score, forgecraft_tier, forgecraft_pass, forgecraft_report,
-              approved_by, approved_at, rejected_by, rejected_at, rejection_reason,
-              status, created_at, updated_at, source_device)
-           VALUES (
-             ${m.id}, ${config.teamId}, ${m.project}, ${m.work_package_id}, ${m.branch_name},
-             ${m.from_contributor_id}, ${m.forgecraft_score ?? null}, ${m.forgecraft_tier ?? null},
-             ${m.forgecraft_pass != null ? m.forgecraft_pass === 1 : null},
-             ${m.forgecraft_report ?? null},
-             ${m.approved_by ?? null}, ${m.approved_at ?? null},
-             ${m.rejected_by ?? null}, ${m.rejected_at ?? null}, ${m.rejection_reason ?? null},
-             ${m.status}, ${m.created_at}, ${now}, ${config.deviceId}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             status          = EXCLUDED.status,
-             approved_by     = EXCLUDED.approved_by,
-             approved_at     = EXCLUDED.approved_at,
-             rejected_by     = EXCLUDED.rejected_by,
-             rejected_at     = EXCLUDED.rejected_at,
-             rejection_reason = EXCLUDED.rejection_reason,
-             updated_at      = EXCLUDED.updated_at,
-             source_device   = EXCLUDED.source_device
-           WHERE EXCLUDED.updated_at > team_merge_requests.updated_at
-         `;
+         await sql``;

[NoCoverage] LogicalOperator
src/services/sync.ts:386:39
-             ${m.from_contributor_id}, ${m.forgecraft_score ?? null}, ${m.forgecraft_tier ?? null},
+             ${m.from_contributor_id}, ${m.forgecraft_score && null}, ${m.forgecraft_tier ?? null},

[NoCoverage] LogicalOperator
src/services/sync.ts:386:70
-             ${m.from_contributor_id}, ${m.forgecraft_score ?? null}, ${m.forgecraft_tier ?? null},
+             ${m.from_contributor_id}, ${m.forgecraft_score ?? null}, ${m.forgecraft_tier && null},

[NoCoverage] ConditionalExpression
src/services/sync.ts:387:13
-             ${m.forgecraft_pass != null ? m.forgecraft_pass === 1 : null},
+             ${true ? m.forgecraft_pass === 1 : null},

[NoCoverage] ConditionalExpression
src/services/sync.ts:387:13
-             ${m.forgecraft_pass != null ? m.forgecraft_pass === 1 : null},
+             ${false ? m.forgecraft_pass === 1 : null},

[NoCoverage] EqualityOperator
src/services/sync.ts:387:13
-             ${m.forgecraft_pass != null ? m.forgecraft_pass === 1 : null},
+             ${m.forgecraft_pass == null ? m.forgecraft_pass === 1 : null},

[NoCoverage] ConditionalExpression
src/services/sync.ts:387:41
-             ${m.forgecraft_pass != null ? m.forgecraft_pass === 1 : null},
+             ${m.forgecraft_pass != null ? true : null},

[NoCoverage] ConditionalExpression
src/services/sync.ts:387:41
-             ${m.forgecraft_pass != null ? m.forgecraft_pass === 1 : null},
+             ${m.forgecraft_pass != null ? false : null},

[NoCoverage] EqualityOperator
src/services/sync.ts:387:41
-             ${m.forgecraft_pass != null ? m.forgecraft_pass === 1 : null},
+             ${m.forgecraft_pass != null ? m.forgecraft_pass !== 1 : null},

[NoCoverage] LogicalOperator
src/services/sync.ts:388:13
-             ${m.forgecraft_report ?? null},
+             ${m.forgecraft_report && null},

[NoCoverage] LogicalOperator
src/services/sync.ts:389:13
-             ${m.approved_by ?? null}, ${m.approved_at ?? null},
+             ${m.approved_by && null}, ${m.approved_at ?? null},

[NoCoverage] LogicalOperator
src/services/sync.ts:389:39
-             ${m.approved_by ?? null}, ${m.approved_at ?? null},
+             ${m.approved_by ?? null}, ${m.approved_at && null},

[NoCoverage] LogicalOperator
src/services/sync.ts:390:13
-             ${m.rejected_by ?? null}, ${m.rejected_at ?? null}, ${m.rejection_reason ?? null},
+             ${m.rejected_by && null}, ${m.rejected_at ?? null}, ${m.rejection_reason ?? null},

[NoCoverage] LogicalOperator
src/services/sync.ts:390:39
-             ${m.rejected_by ?? null}, ${m.rejected_at ?? null}, ${m.rejection_reason ?? null},
+             ${m.rejected_by ?? null}, ${m.rejected_at && null}, ${m.rejection_reason ?? null},

[NoCoverage] LogicalOperator
src/services/sync.ts:390:65
-             ${m.rejected_by ?? null}, ${m.rejected_at ?? null}, ${m.rejection_reason ?? null},
+             ${m.rejected_by ?? null}, ${m.rejected_at ?? null}, ${m.rejection_reason && null},

[NoCoverage] UpdateOperator
src/services/sync.ts:404:7
-         pushed++;
+         pushed--;

[NoCoverage] StringLiteral
src/services/sync.ts:410:44
-       const remoteContribs = await sql<any[]>`
-         SELECT * FROM team_contributors
-         WHERE team_id = ${config.teamId}
-           AND updated_at > ${lastPull}
-           AND source_device != ${config.deviceId}
-           ${project ? sql`AND project = ${project}` : sql``}
-       `;
+       const remoteContribs = await sql<any[]>``;

[NoCoverage] StringLiteral
src/services/sync.ts:415:24
-           ${project ? sql`AND project = ${project}` : sql``}
+           ${project ? sql`` : sql``}

[NoCoverage] StringLiteral
src/services/sync.ts:415:56
-           ${project ? sql`AND project = ${project}` : sql``}
+           ${project ? sql`AND project = ${project}` : sql`Stryker was here!`}

[NoCoverage] BlockStatement
src/services/sync.ts:417:38
-       for (const rc of remoteContribs) {
-         db.prepare(`
-           INSERT OR REPLACE INTO contributors
-             (id, project, name, email, skills, role, bandwidth_hours_per_week, availability, last_active_at, created_at)
-           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-         `).run(
-           rc.id, rc.project, rc.name, rc.email ?? null,
-           JSON.stringify(rc.skills ?? []), rc.role, rc.bandwidth_hours_per_week,
-           rc.availability, toIsoString(rc.last_active_at), toIsoString(rc.created_at),
-         );
-         pulled++;
-       }
+       for (const rc of remoteContribs) {}

[NoCoverage] StringLiteral
src/services/sync.ts:418:18
-         db.prepare(`
-           INSERT OR REPLACE INTO contributors
-             (id, project, name, email, skills, role, bandwidth_hours_per_week, availability, last_active_at, created_at)
-           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-         `).run(
+         db.prepare(``).run(

[NoCoverage] LogicalOperator
src/services/sync.ts:423:37
-           rc.id, rc.project, rc.name, rc.email ?? null,
+           rc.id, rc.project, rc.name, rc.email && null,

[NoCoverage] LogicalOperator
src/services/sync.ts:424:24
-           JSON.stringify(rc.skills ?? []), rc.role, rc.bandwidth_hours_per_week,
+           JSON.stringify(rc.skills && []), rc.role, rc.bandwidth_hours_per_week,

[NoCoverage] ArrayDeclaration
src/services/sync.ts:424:37
-           JSON.stringify(rc.skills ?? []), rc.role, rc.bandwidth_hours_per_week,
+           JSON.stringify(rc.skills ?? ["Stryker was here"]), rc.role, rc.bandwidth_hours_per_week,

[NoCoverage] UpdateOperator
src/services/sync.ts:427:7
-         pulled++;
+         pulled--;

[NoCoverage] StringLiteral
src/services/sync.ts:431:44
-       const remotePackages = await sql<any[]>`
-         SELECT * FROM team_work_packages
-         WHERE team_id = ${config.teamId}
-           AND updated_at > ${lastPull}
-           AND source_device != ${config.deviceId}
-           ${project ? sql`AND project = ${project}` : sql``}
-       `;
+       const remotePackages = await sql<any[]>``;

[NoCoverage] StringLiteral
src/services/sync.ts:436:24
-           ${project ? sql`AND project = ${project}` : sql``}
+           ${project ? sql`` : sql``}

[NoCoverage] StringLiteral
src/services/sync.ts:436:56
-           ${project ? sql`AND project = ${project}` : sql``}
+           ${project ? sql`AND project = ${project}` : sql`Stryker was here!`}

[NoCoverage] BlockStatement
src/services/sync.ts:438:38
-       for (const rp of remotePackages) {
-         db.prepare(`
-           INSERT OR REPLACE INTO work_packages
-             (id, project, title, description, spec_section, status, role_required,
-              dependencies, priority_rank, parent_id, is_milestone, branch_name,
-              forgecraft_score, forgecraft_tier, forgecraft_pass,
-              assigned_to, assigned_at, completed_at, created_at)
-           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-         `).run(
-           rp.id, rp.project, rp.title, rp.description, rp.spec_section ?? null,
-           rp.status, rp.role_required, JSON.stringify(rp.dependencies ?? []),
-           rp.priority_rank, rp.parent_id ?? null, rp.is_milestone ? 1 : 0,
-           rp.branch_name ?? null, rp.forgecraft_score ?? null,
-           rp.forgecraft_tier ?? null,
-           rp.forgecraft_pass != null ? (rp.forgecraft_pass ? 1 : 0) : null,
-           rp.assigned_to ?? null, rp.assigned_at ? toIsoString(rp.assigned_at) : null,
-           rp.completed_at ? toIsoString(rp.completed_at) : null, toIsoString(rp.created_at),
-         );
-         pulled++;
-       }
+       for (const rp of remotePackages) {}

[NoCoverage] StringLiteral
src/services/sync.ts:439:18
-         db.prepare(`
-           INSERT OR REPLACE INTO work_packages
-             (id, project, title, description, spec_section, status, role_required,
-              dependencies, priority_rank, parent_id, is_milestone, branch_name,
-              forgecraft_score, forgecraft_tier, forgecraft_pass,
-              assigned_to, assigned_at, completed_at, created_at)
-           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-         `).run(
+         db.prepare(``).run(

[NoCoverage] LogicalOperator
src/services/sync.ts:447:54
-           rp.id, rp.project, rp.title, rp.description, rp.spec_section ?? null,
+           rp.id, rp.project, rp.title, rp.description, rp.spec_section && null,

[NoCoverage] LogicalOperator
src/services/sync.ts:448:53
-           rp.status, rp.role_required, JSON.stringify(rp.dependencies ?? []),
+           rp.status, rp.role_required, JSON.stringify(rp.dependencies && []),

[NoCoverage] ArrayDeclaration
src/services/sync.ts:448:72
-           rp.status, rp.role_required, JSON.stringify(rp.dependencies ?? []),
+           rp.status, rp.role_required, JSON.stringify(rp.dependencies ?? ["Stryker was here"]),

[NoCoverage] LogicalOperator
src/services/sync.ts:449:27
-           rp.priority_rank, rp.parent_id ?? null, rp.is_milestone ? 1 : 0,
+           rp.priority_rank, rp.parent_id && null, rp.is_milestone ? 1 : 0,

[NoCoverage] LogicalOperator
src/services/sync.ts:450:9
-           rp.branch_name ?? null, rp.forgecraft_score ?? null,
+           rp.branch_name && null, rp.forgecraft_score ?? null,

[NoCoverage] LogicalOperator
src/services/sync.ts:450:33
-           rp.branch_name ?? null, rp.forgecraft_score ?? null,
+           rp.branch_name ?? null, rp.forgecraft_score && null,

[NoCoverage] LogicalOperator
src/services/sync.ts:451:9
-           rp.forgecraft_tier ?? null,
+           rp.forgecraft_tier && null,

[NoCoverage] ConditionalExpression
src/services/sync.ts:452:9
-           rp.forgecraft_pass != null ? (rp.forgecraft_pass ? 1 : 0) : null,
+           true ? (rp.forgecraft_pass ? 1 : 0) : null,

[NoCoverage] ConditionalExpression
src/services/sync.ts:452:9
-           rp.forgecraft_pass != null ? (rp.forgecraft_pass ? 1 : 0) : null,
+           false ? (rp.forgecraft_pass ? 1 : 0) : null,

[NoCoverage] EqualityOperator
src/services/sync.ts:452:9
-           rp.forgecraft_pass != null ? (rp.forgecraft_pass ? 1 : 0) : null,
+           rp.forgecraft_pass == null ? (rp.forgecraft_pass ? 1 : 0) : null,

[NoCoverage] LogicalOperator
src/services/sync.ts:453:9
-           rp.assigned_to ?? null, rp.assigned_at ? toIsoString(rp.assigned_at) : null,
+           rp.assigned_to && null, rp.assigned_at ? toIsoString(rp.assigned_at) : null,

[NoCoverage] UpdateOperator
src/services/sync.ts:456:7
-         pulled++;
+         pulled--;

[NoCoverage] StringLiteral
src/services/sync.ts:460:39
-       const remoteMrs = await sql<any[]>`
-         SELECT * FROM team_merge_requests
-         WHERE team_id = ${config.teamId}
-           AND updated_at > ${lastPull}
-           AND source_device != ${config.deviceId}
-           ${project ? sql`AND project = ${project}` : sql``}
-       `;
+       const remoteMrs = await sql<any[]>``;

[NoCoverage] StringLiteral
src/services/sync.ts:465:24
-           ${project ? sql`AND project = ${project}` : sql``}
+           ${project ? sql`` : sql``}

[NoCoverage] StringLiteral
src/services/sync.ts:465:56
-           ${project ? sql`AND project = ${project}` : sql``}
+           ${project ? sql`AND project = ${project}` : sql`Stryker was here!`}

[NoCoverage] BlockStatement
src/services/sync.ts:467:33
-       for (const rm of remoteMrs) {
-         db.prepare(`
-           INSERT OR REPLACE INTO merge_requests
-             (id, project, work_package_id, branch_name, from_contributor_id,
-              forgecraft_score, forgecraft_tier, forgecraft_pass, forgecraft_report,
-              approved_by, approved_at, rejected_by, rejected_at, rejection_reason,
-              status, created_at)
-           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-         `).run(
-           rm.id, rm.project, rm.work_package_id, rm.branch_name, rm.from_contributor_id,
-           rm.forgecraft_score ?? null, rm.forgecraft_tier ?? null,
-           rm.forgecraft_pass != null ? (rm.forgecraft_pass ? 1 : 0) : null,
-           rm.forgecraft_report ?? null,
-           rm.approved_by ?? null, rm.approved_at ?? null,
-           rm.rejected_by ?? null, rm.rejected_at ?? null, rm.rejection_reason ?? null,
-           rm.status, rm.created_at,
-         );
-         pulled++;
-       }
+       for (const rm of remoteMrs) {}

[NoCoverage] StringLiteral
src/services/sync.ts:468:18
-         db.prepare(`
-           INSERT OR REPLACE INTO merge_requests
-             (id, project, work_package_id, branch_name, from_contributor_id,
-              forgecraft_score, forgecraft_tier, forgecraft_pass, forgecraft_report,
-              approved_by, approved_at, rejected_by, rejected_at, rejection_reason,
-              status, created_at)
-           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-         `).run(
+         db.prepare(``).run(

[NoCoverage] LogicalOperator
src/services/sync.ts:477:9
-           rm.forgecraft_score ?? null, rm.forgecraft_tier ?? null,
+           rm.forgecraft_score && null, rm.forgecraft_tier ?? null,

[NoCoverage] LogicalOperator
src/services/sync.ts:477:38
-           rm.forgecraft_score ?? null, rm.forgecraft_tier ?? null,
+           rm.forgecraft_score ?? null, rm.forgecraft_tier && null,

[NoCoverage] ConditionalExpression
src/services/sync.ts:478:9
-           rm.forgecraft_pass != null ? (rm.forgecraft_pass ? 1 : 0) : null,
+           true ? (rm.forgecraft_pass ? 1 : 0) : null,

[NoCoverage] ConditionalExpression
src/services/sync.ts:478:9
-           rm.forgecraft_pass != null ? (rm.forgecraft_pass ? 1 : 0) : null,
+           false ? (rm.forgecraft_pass ? 1 : 0) : null,

[NoCoverage] EqualityOperator
src/services/sync.ts:478:9
-           rm.forgecraft_pass != null ? (rm.forgecraft_pass ? 1 : 0) : null,
+           rm.forgecraft_pass == null ? (rm.forgecraft_pass ? 1 : 0) : null,

[NoCoverage] LogicalOperator
src/services/sync.ts:479:9
-           rm.forgecraft_report ?? null,
+           rm.forgecraft_report && null,

[NoCoverage] LogicalOperator
src/services/sync.ts:480:9
-           rm.approved_by ?? null, rm.approved_at ?? null,
+           rm.approved_by && null, rm.approved_at ?? null,

[NoCoverage] LogicalOperator
src/services/sync.ts:480:33
-           rm.approved_by ?? null, rm.approved_at ?? null,
+           rm.approved_by ?? null, rm.approved_at && null,

[NoCoverage] LogicalOperator
src/services/sync.ts:481:9
-           rm.rejected_by ?? null, rm.rejected_at ?? null, rm.rejection_reason ?? null,
+           rm.rejected_by && null, rm.rejected_at ?? null, rm.rejection_reason ?? null,

[NoCoverage] LogicalOperator
src/services/sync.ts:481:33
-           rm.rejected_by ?? null, rm.rejected_at ?? null, rm.rejection_reason ?? null,
+           rm.rejected_by ?? null, rm.rejected_at && null, rm.rejection_reason ?? null,

[NoCoverage] LogicalOperator
src/services/sync.ts:481:57
-           rm.rejected_by ?? null, rm.rejected_at ?? null, rm.rejection_reason ?? null,
+           rm.rejected_by ?? null, rm.rejected_at ?? null, rm.rejection_reason && null,

[NoCoverage] UpdateOperator
src/services/sync.ts:484:7
-         pulled++;
+         pulled--;

[NoCoverage] StringLiteral
src/services/sync.ts:488:16
-       db.prepare(`
-         INSERT OR REPLACE INTO sync_cursor (device_id, user_id, last_push_at, last_pull_at, memories_version)
-         VALUES (?, ?, ?, ?, COALESCE((SELECT memories_version FROM sync_cursor WHERE device_id=? AND user_id=?), 0))
-       `).run(config.deviceId, config.userId, now, now, config.deviceId, config.userId);
+       db.prepare(``).run(config.deviceId, config.userId, now, now, config.deviceId, config.userId);

[NoCoverage] ObjectLiteral
src/services/sync.ts:494:12
-       return { pushed, pulled, conflicts: 0, skipped: false };
+       return {};

[NoCoverage] BooleanLiteral
src/services/sync.ts:494:53
-       return { pushed, pulled, conflicts: 0, skipped: false };
+       return { pushed, pulled, conflicts: 0, skipped: true };

[NoCoverage] BlockStatement
src/services/sync.ts:496:17
-     } catch (err) {
-       // Non-fatal — coordination still works locally if Railway is down
-       console.warn(`[chronicle] coordination sync failed: ${String(err)}`);
-       return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
-     }
+     } catch (err) {}

[NoCoverage] StringLiteral
src/services/sync.ts:498:18
-       console.warn(`[chronicle] coordination sync failed: ${String(err)}`);
+       console.warn(``);

[NoCoverage] ObjectLiteral
src/services/sync.ts:499:12
-       return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
+       return {};

[NoCoverage] BooleanLiteral
src/services/sync.ts:499:59
-       return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
+       return { pushed: 0, pulled: 0, conflicts: 0, skipped: false };

[NoCoverage] BlockStatement
src/services/sync.ts:509:6
-   }[]> {
-     const config = getConfig();
-     if (!config.railwayUrl) return [];
-     try {
-       const { default: postgres } = await import('postgres');
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });
-       const rows = await sql<{ summary: string; device: string | null; ended_at: string }[]>`
-         SELECT summary, device, ended_at
-         FROM session_summaries
-         WHERE user_id = ${config.userId}
-           AND project  = ${project}
-           AND device  != ${config.deviceId}
-         ORDER BY ended_at DESC
-         LIMIT ${limit}
-       `;
-       await sql.end();
-       return rows.map(r => ({ summary: r.summary, device: r.device, endedAt: r.ended_at }));
-     } catch {
-       return [];
-     }
-   }
+   }[]> {}

[NoCoverage] BooleanLiteral
src/services/sync.ts:511:7
-     if (!config.railwayUrl) return [];
+     if (config.railwayUrl) return [];

[NoCoverage] ConditionalExpression
src/services/sync.ts:511:7
-     if (!config.railwayUrl) return [];
+     if (true) return [];

[NoCoverage] ConditionalExpression
src/services/sync.ts:511:7
-     if (!config.railwayUrl) return [];
+     if (false) return [];

[NoCoverage] ArrayDeclaration
src/services/sync.ts:511:34
-     if (!config.railwayUrl) return [];
+     if (!config.railwayUrl) return ["Stryker was here"];

[NoCoverage] BlockStatement
src/services/sync.ts:513:7
-     try {
-       const { default: postgres } = await import('postgres');
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });
-       const rows = await sql<{ summary: string; device: string | null; ended_at: string }[]>`
-         SELECT summary, device, ended_at
-         FROM session_summaries
-         WHERE user_id = ${config.userId}
-           AND project  = ${project}
-           AND device  != ${config.deviceId}
-         ORDER BY ended_at DESC
-         LIMIT ${limit}
-       `;
-       await sql.end();
-       return rows.map(r => ({ summary: r.summary, device: r.device, endedAt: r.ended_at }));
-     } catch {
+     try {} catch {

[NoCoverage] StringLiteral
src/services/sync.ts:514:48
-       const { default: postgres } = await import('postgres');
+       const { default: postgres } = await import("");

[NoCoverage] ObjectLiteral
src/services/sync.ts:515:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });
+       const sql = postgres(config.railwayUrl, {});

[NoCoverage] StringLiteral
src/services/sync.ts:515:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });
+       const sql = postgres(config.railwayUrl, { ssl: "", max: 1 });

[NoCoverage] StringLiteral
src/services/sync.ts:516:91
-       const rows = await sql<{ summary: string; device: string | null; ended_at: string }[]>`
-         SELECT summary, device, ended_at
-         FROM session_summaries
-         WHERE user_id = ${config.userId}
-           AND project  = ${project}
-           AND device  != ${config.deviceId}
-         ORDER BY ended_at DESC
-         LIMIT ${limit}
-       `;
+       const rows = await sql<{ summary: string; device: string | null; ended_at: string }[]>``;

[NoCoverage] ArrowFunction
src/services/sync.ts:526:21
-       return rows.map(r => ({ summary: r.summary, device: r.device, endedAt: r.ended_at }));
+       return rows.map(() => undefined);

[NoCoverage] ObjectLiteral
src/services/sync.ts:526:27
-       return rows.map(r => ({ summary: r.summary, device: r.device, endedAt: r.ended_at }));
+       return rows.map(r => ({}));

[NoCoverage] BlockStatement
src/services/sync.ts:527:11
-     } catch {
-       return [];
-     }
+     } catch {}

[NoCoverage] ArrayDeclaration
src/services/sync.ts:528:12
-       return [];
+       return ["Stryker was here"];

[Survived] ObjectLiteral
src/services/sync.ts:56:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
+       const sql = postgres(config.railwayUrl, {});
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync what is eligible to push reports zero pushed when there is nothing eligible
  and 6 more tests!


[Survived] StringLiteral
src/services/sync.ts:56:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
+       const sql = postgres(config.railwayUrl, { ssl: "", max: 3 });
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync what is eligible to push reports zero pushed when there is nothing eligible
  and 6 more tests!


[Survived] StringLiteral
src/services/sync.ts:60:14
-       await sql`INSERT INTO users (id) VALUES (${config.userId}) ON CONFLICT DO NOTHING`;
+       await sql``;
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync what is eligible to push reports zero pushed when there is nothing eligible
  and 6 more tests!


[Survived] LogicalOperator
src/services/sync.ts:67:19
-       const since = cursor?.last_push_at ?? '1970-01-01T00:00:00Z';
+       const since = cursor?.last_push_at && '1970-01-01T00:00:00Z';
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync what is eligible to push reports zero pushed when there is nothing eligible
  and 6 more tests!


[Survived] StringLiteral
src/services/sync.ts:67:43
-       const since = cursor?.last_push_at ?? '1970-01-01T00:00:00Z';
+       const since = cursor?.last_push_at ?? "";
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync what is eligible to push reports zero pushed when there is nothing eligible
  and 6 more tests!


[Survived] StringLiteral
src/services/sync.ts:79:16
-         await sql`
-           INSERT INTO memories (id, user_id, content, memory_type, tier, weight, decay_rate,
-             access_count, created_at, last_accessed_at, project, scope, category, tags, source,
-             confirmed, fact_subject, fact_predicate, source_device, updated_at)
-           VALUES (
-             ${m['id'] as string}, ${config.userId}, ${m['content'] as string},
-             ${m['memory_type'] as string}, ${m['tier'] as string}, ${m['weight'] as number},
-             ${m['decay_rate'] as number}, ${m['access_count'] as number},
-             ${m['created_at'] as string}, ${m['last_accessed_at'] as string},
-             ${m['project'] as string | null}, ${(m['scope'] as string | null) ?? 'project'},
-             ${m['category'] as string | null},
-             ${tags}, ${m['source'] as string | null},
-             ${Boolean(m['confirmed'])}, ${m['fact_subject'] as string | null},
-             ${m['fact_predicate'] as string | null}, ${config.deviceId},
-             NOW()
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             weight           = EXCLUDED.weight,
-             access_count     = EXCLUDED.access_count,
-             last_accessed_at = EXCLUDED.last_accessed_at,
-             tier             = EXCLUDED.tier,
-             updated_at       = NOW()
-           WHERE EXCLUDED.last_accessed_at > memories.last_accessed_at
-         `;
+         await sql``;
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync the sync cursor uses an epoch floor on the first run, so nothing is silently skipped
  and 1 more test!


[Survived] LogicalOperator
src/services/sync.ts:88:47
-             ${m['project'] as string | null}, ${(m['scope'] as string | null) ?? 'project'},
+             ${m['project'] as string | null}, ${m['scope'] as string | null && 'project'},
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync the sync cursor uses an epoch floor on the first run, so nothing is silently skipped
  and 1 more test!


[Survived] StringLiteral
src/services/sync.ts:91:23
-             ${Boolean(m['confirmed'])}, ${m['fact_subject'] as string | null},
+             ${Boolean(m[""])}, ${m['fact_subject'] as string | null},
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync the sync cursor uses an epoch floor on the first run, so nothing is silently skipped
  and 1 more test!


[Survived] LogicalOperator
src/services/sync.ts:109:22
-       const lastPull = cursor?.last_push_at ?? '1970-01-01T00:00:00Z';
+       const lastPull = cursor?.last_push_at && '1970-01-01T00:00:00Z';
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync what is eligible to push reports zero pushed when there is nothing eligible
  and 6 more tests!


[Survived] StringLiteral
src/services/sync.ts:109:46
-       const lastPull = cursor?.last_push_at ?? '1970-01-01T00:00:00Z';
+       const lastPull = cursor?.last_push_at ?? "";
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync what is eligible to push reports zero pushed when there is nothing eligible
  and 6 more tests!


[Survived] StringLiteral
src/services/sync.ts:110:56
-       const remote = await sql<Record<string, unknown>[]>`
-         SELECT * FROM memories
-         WHERE user_id = ${config.userId}
-           AND updated_at > ${lastPull}
-           AND source_device != ${config.deviceId}
-       `;
+       const remote = await sql<Record<string, unknown>[]>``;
Tests ran:
    cloud sync what is eligible to push pushes working and core memories but never buffer ones
    cloud sync what is eligible to push marks pushed rows as synced
    cloud sync what is eligible to push reports zero pushed when there is nothing eligible
  and 6 more tests!


[Survived] ConditionalExpression
src/services/sync.ts:166:7
-     if (!config.railwayUrl) return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
+     if (true) return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
Tests ran:
    cloud sync unconfigured is a normal state skips syncInsights with no railwayUrl
    cloud sync unconfigured is a normal state sends nothing at all while unconfigured


[Survived] BooleanLiteral
src/services/sync.ts:269:7
-     if (!config.railwayUrl) return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
+     if (config.railwayUrl) return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
Tests ran:
    cloud sync unconfigured is a normal state skips syncCoordination with no railwayUrl
    cloud sync unconfigured is a normal state skips syncCoordination when railwayUrl is set but teamId is not
    cloud sync unconfigured is a normal state sends nothing at all while unconfigured


[Survived] ConditionalExpression
src/services/sync.ts:269:7
-     if (!config.railwayUrl) return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
+     if (true) return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
Tests ran:
    cloud sync unconfigured is a normal state skips syncCoordination with no railwayUrl
    cloud sync unconfigured is a normal state skips syncCoordination when railwayUrl is set but teamId is not
    cloud sync unconfigured is a normal state sends nothing at all while unconfigured


[Survived] ConditionalExpression
src/services/sync.ts:269:7
-     if (!config.railwayUrl) return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
+     if (false) return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
Tests ran:
    cloud sync unconfigured is a normal state skips syncCoordination with no railwayUrl
    cloud sync unconfigured is a normal state skips syncCoordination when railwayUrl is set but teamId is not
    cloud sync unconfigured is a normal state sends nothing at all while unconfigured


[Survived] BooleanLiteral
src/services/sync.ts:270:7
-     if (!config.teamId)     return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
+     if (config.teamId)     return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
Tests ran:
    cloud sync unconfigured is a normal state skips syncCoordination when railwayUrl is set but teamId is not


[Survived] ConditionalExpression
src/services/sync.ts:270:7
-     if (!config.teamId)     return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
+     if (true)     return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
Tests ran:
    cloud sync unconfigured is a normal state skips syncCoordination when railwayUrl is set but teamId is not


[Survived] ConditionalExpression
src/services/sync.ts:270:7
-     if (!config.teamId)     return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
+     if (false)     return { pushed: 0, pulled: 0, conflicts: 0, skipped: true };
Tests ran:
    cloud sync unconfigured is a normal state skips syncCoordination when railwayUrl is set but teamId is not


[NoCoverage] StringLiteral
src/services/team-promotion-service.ts:100:37
-       const teamId = config.teamId ?? '';
+       const teamId = config.teamId ?? "Stryker was here!";

[NoCoverage] BlockStatement
src/services/team-promotion-service.ts:180:13
-       } catch {
-         return null;
-       }
+       } catch {}

[NoCoverage] ConditionalExpression
src/services/team-promotion-service.ts:194:20
-         if (!best || score > best.score) best = { id: pool[i]!.id, score };
+         if (!best || false) best = { id: pool[i]!.id, score };

[NoCoverage] EqualityOperator
src/services/team-promotion-service.ts:194:20
-         if (!best || score > best.score) best = { id: pool[i]!.id, score };
+         if (!best || score >= best.score) best = { id: pool[i]!.id, score };

[NoCoverage] EqualityOperator
src/services/team-promotion-service.ts:194:20
-         if (!best || score > best.score) best = { id: pool[i]!.id, score };
+         if (!best || score <= best.score) best = { id: pool[i]!.id, score };

[NoCoverage] ConditionalExpression
src/services/team-promotion-service.ts:207:20
-         if (!best || score > best.score) best = { id: p.id, score };
+         if (!best || false) best = { id: p.id, score };

[NoCoverage] EqualityOperator
src/services/team-promotion-service.ts:207:20
-         if (!best || score > best.score) best = { id: p.id, score };
+         if (!best || score >= best.score) best = { id: p.id, score };

[NoCoverage] EqualityOperator
src/services/team-promotion-service.ts:207:20
-         if (!best || score > best.score) best = { id: p.id, score };
+         if (!best || score <= best.score) best = { id: p.id, score };

[Survived] Regex
src/services/team-promotion-service.ts:59:12
-       .split(/[^a-z0-9]+/)
+       .split(/[^a-z0-9]/)
Tests ran:
    tokenize lowercases and drops tokens shorter than three characters
    tokenize returns an empty set for punctuation-only input
    contentSimilarity scores identical content as 1
  and 4 more tests!


[Survived] LogicalOperator
src/services/team-promotion-service.ts:74:7
-     if (sa.size === 0 || sb.size === 0) return 0;
+     if (sa.size === 0 && sb.size === 0) return 0;
Tests ran:
    contentSimilarity scores identical content as 1
    contentSimilarity scores fully disjoint content as 0
    contentSimilarity returns 0 when either side has no usable tokens
  and 2 more tests!


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:74:7
-     if (sa.size === 0 || sb.size === 0) return 0;
+     if (false) return 0;
Tests ran:
    contentSimilarity scores identical content as 1
    contentSimilarity scores fully disjoint content as 0
    contentSimilarity returns 0 when either side has no usable tokens
  and 2 more tests!


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:74:7
-     if (sa.size === 0 || sb.size === 0) return 0;
+     if (false || sb.size === 0) return 0;
Tests ran:
    contentSimilarity scores identical content as 1
    contentSimilarity scores fully disjoint content as 0
    contentSimilarity returns 0 when either side has no usable tokens
  and 2 more tests!


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:74:24
-     if (sa.size === 0 || sb.size === 0) return 0;
+     if (sa.size === 0 || false) return 0;
Tests ran:
    contentSimilarity scores identical content as 1
    contentSimilarity scores fully disjoint content as 0
    contentSimilarity computes partial overlap as Jaccard
  and 1 more test!


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:78:10
-     return union === 0 ? 0 : intersection / union;
+     return false ? 0 : intersection / union;
Tests ran:
    contentSimilarity scores identical content as 1
    contentSimilarity scores fully disjoint content as 0
    contentSimilarity computes partial overlap as Jaccard
  and 1 more test!


[Survived] BooleanLiteral
src/services/team-promotion-service.ts:105:108
-       const result: PromoteResult = { scanned: candidates.length, promoted: [], skipped: [], railwaySkipped: false, method: 'lexical' };
+       const result: PromoteResult = { scanned: candidates.length, promoted: [], skipped: [], railwaySkipped: true, method: 'lexical' };
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 5 more tests!


[Survived] StringLiteral
src/services/team-promotion-service.ts:105:123
-       const result: PromoteResult = { scanned: candidates.length, promoted: [], skipped: [], railwaySkipped: false, method: 'lexical' };
+       const result: PromoteResult = { scanned: candidates.length, promoted: [], skipped: [], railwaySkipped: false, method: "" };
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 5 more tests!


[Survived] ArrowFunction
src/services/team-promotion-service.ts:113:93
-       const semantic = await this.buildSemanticIndex(candidates.map(c => c.content), pool.map(p => p.content));
+       const semantic = await this.buildSemanticIndex(candidates.map(c => c.content), pool.map(() => undefined));
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 4 more tests!


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:114:9
-       if (semantic) result.method = 'semantic';
+       if (true) result.method = 'semantic';
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 4 more tests!


[Survived] EqualityOperator
src/services/team-promotion-service.ts:128:20
-         if (match && match.score >= threshold) {
+         if (match && match.score > threshold) {
Tests ran:
    TeamPromotionService.promote skips a near-duplicate of existing team knowledge
    TeamPromotionService.promote (semantic mode) skips a semantic near-duplicate even when wording differs
    TeamPromotionService.promote (semantic mode) promotes a semantically distinct memory


[Survived] ObjectLiteral
src/services/team-promotion-service.ts:132:47
-         await this.teamSyncSvc.pushSharedMemory({
-           id: cand.id,
-           content: cand.content,
-           memoryType: cand.memory_type,
-           project: cand.project ?? project,
-           category: cand.category ?? undefined,
-           tags: JSON.parse(cand.tags || '[]') as string[],
-         });
+         await this.teamSyncSvc.pushSharedMemory({});
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote (semantic mode) reports semantic method when an embedding gateway is available
    TeamPromotionService.promote (semantic mode) promotes a semantically distinct memory


[Survived] LogicalOperator
src/services/team-promotion-service.ts:136:18
-           project: cand.project ?? project,
+           project: cand.project && project,
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote (semantic mode) reports semantic method when an embedding gateway is available
    TeamPromotionService.promote (semantic mode) promotes a semantically distinct memory


[Survived] LogicalOperator
src/services/team-promotion-service.ts:137:19
-           category: cand.category ?? undefined,
+           category: cand.category && undefined,
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote (semantic mode) reports semantic method when an embedding gateway is available
    TeamPromotionService.promote (semantic mode) promotes a semantically distinct memory


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:156:9
-       if (project) {
+       if (true) {
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 5 more tests!


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:156:9
-       if (project) {
+       if (false) {
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 5 more tests!


[Survived] BlockStatement
src/services/team-promotion-service.ts:156:18
-       if (project) {
-         return this.db.prepare(`${base} AND project = ? ORDER BY weight DESC, last_accessed_at DESC LIMIT ?`)
-           .all(project, limit) as CandidateRow[];
-       }
+       if (project) {}
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 5 more tests!


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:172:9
-       if (!this.embeddings) return null;
+       if (false) return null;
Tests ran:
    TeamPromotionService.promote promotes a novel high-value memory to the team pool
    TeamPromotionService.promote ignores buffer-tier, unconfirmed memories as candidates
    TeamPromotionService.promote skips a memory already present in the team pool by id
  and 4 more tests!


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:174:11
-         if (!(await this.embeddings.available())) return null;
+         if (false) return null;
Tests ran:
    TeamPromotionService.promote (semantic mode) reports semantic method when an embedding gateway is available
    TeamPromotionService.promote (semantic mode) skips a semantic near-duplicate even when wording differs
    TeamPromotionService.promote (semantic mode) promotes a semantically distinct memory


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:194:11
-         if (!best || score > best.score) best = { id: pool[i]!.id, score };
+         if (true) best = { id: pool[i]!.id, score };
Tests ran:
    TeamPromotionService.promote (semantic mode) skips a semantic near-duplicate even when wording differs
    TeamPromotionService.promote (semantic mode) promotes a semantically distinct memory


[Survived] ConditionalExpression
src/services/team-promotion-service.ts:207:11
-         if (!best || score > best.score) best = { id: p.id, score };
+         if (true) best = { id: p.id, score };
Tests ran:
    TeamPromotionService.promote skips a near-duplicate of existing team knowledge


[NoCoverage] StringLiteral
src/services/team-service.ts:29:48
-       const { default: postgres } = await import('postgres');

+       const { default: postgres } = await import("");


[NoCoverage] ObjectLiteral
src/services/team-service.ts:30:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 2 });

+       const sql = postgres(config.railwayUrl, {});


[NoCoverage] StringLiteral
src/services/team-service.ts:30:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 2 });

+       const sql = postgres(config.railwayUrl, { ssl: "", max: 2 });


[NoCoverage] BlockStatement
src/services/team-service.ts:31:9
-       try {

-         const team = createTeam(teamId, teamName);

-         const member = createTeamMember(config.userId, teamId, role);

-   

-         await sql`

-           INSERT INTO teams (id, name, created_at)

-           VALUES (${team.id}, ${team.name}, ${team.createdAt})

-           ON CONFLICT (id) DO NOTHING

-         `;

-         // Never silently downgrade an existing role on re-join.

-         await sql`

-           INSERT INTO team_members (user_id, team_id, role, joined_at)

-           VALUES (${member.userId}, ${member.teamId}, ${member.role}, ${member.joinedAt})

-           ON CONFLICT (user_id, team_id) DO NOTHING

-         `;

-         const row = await sql<{ role: string }[]>`

-           SELECT role FROM team_members WHERE user_id = ${member.userId} AND team_id = ${member.teamId}

-         `;

-         const effectiveRole = (row[0]?.role as TeamRole) ?? role;

-         return { team, member: { ...member, role: effectiveRole } };

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] StringLiteral
src/services/team-service.ts:35:16
-         await sql`

-           INSERT INTO teams (id, name, created_at)

-           VALUES (${team.id}, ${team.name}, ${team.createdAt})

-           ON CONFLICT (id) DO NOTHING

-         `;

+         await sql``;


[NoCoverage] StringLiteral
src/services/team-service.ts:41:16
-         await sql`

-           INSERT INTO team_members (user_id, team_id, role, joined_at)

-           VALUES (${member.userId}, ${member.teamId}, ${member.role}, ${member.joinedAt})

-           ON CONFLICT (user_id, team_id) DO NOTHING

-         `;

+         await sql``;


[NoCoverage] StringLiteral
src/services/team-service.ts:46:48
-         const row = await sql<{ role: string }[]>`

-           SELECT role FROM team_members WHERE user_id = ${member.userId} AND team_id = ${member.teamId}

-         `;

+         const row = await sql<{ role: string }[]>``;


[NoCoverage] LogicalOperator
src/services/team-service.ts:49:29
-         const effectiveRole = (row[0]?.role as TeamRole) ?? role;

+         const effectiveRole = row[0]?.role as TeamRole && role;


[NoCoverage] ObjectLiteral
src/services/team-service.ts:50:14
-         return { team, member: { ...member, role: effectiveRole } };

+         return {};


[NoCoverage] ObjectLiteral
src/services/team-service.ts:50:30
-         return { team, member: { ...member, role: effectiveRole } };

+         return { team, member: {} };


[NoCoverage] BlockStatement
src/services/team-service.ts:51:19
-       } catch (err) {

-         throw new StorageError('Failed to join team', err);

-       } finally {

+       } catch (err) {} finally {


[NoCoverage] StringLiteral
src/services/team-service.ts:52:30
-         throw new StorageError('Failed to join team', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/services/team-service.ts:53:15
-       } finally {

-         await sql.end();

-       }

+       } finally {}


[NoCoverage] StringLiteral
src/services/team-service.ts:68:48
-       const { default: postgres } = await import('postgres');

+       const { default: postgres } = await import("");


[NoCoverage] ObjectLiteral
src/services/team-service.ts:69:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });

+       const sql = postgres(config.railwayUrl, {});


[NoCoverage] StringLiteral
src/services/team-service.ts:69:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });

+       const sql = postgres(config.railwayUrl, { ssl: "", max: 1 });


[NoCoverage] BlockStatement
src/services/team-service.ts:70:9
-       try {

-         const rows = await sql<{ user_id: string; team_id: string; role: string; joined_at: string }[]>`

-           SELECT user_id, team_id, role, joined_at FROM team_members WHERE team_id = ${teamId}

-           ORDER BY joined_at ASC

-         `;

-         return rows.map(r => ({

-           userId: r.user_id, teamId: r.team_id,

-           role: r.role as TeamRole, joinedAt: r.joined_at,

-         }));

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] StringLiteral
src/services/team-service.ts:71:102
-         const rows = await sql<{ user_id: string; team_id: string; role: string; joined_at: string }[]>`

-           SELECT user_id, team_id, role, joined_at FROM team_members WHERE team_id = ${teamId}

-           ORDER BY joined_at ASC

-         `;

+         const rows = await sql<{ user_id: string; team_id: string; role: string; joined_at: string }[]>``;


[NoCoverage] ArrowFunction
src/services/team-service.ts:75:23
-         return rows.map(r => ({

-           userId: r.user_id, teamId: r.team_id,

-           role: r.role as TeamRole, joinedAt: r.joined_at,

-         }));

+         return rows.map(() => undefined);


[NoCoverage] ObjectLiteral
src/services/team-service.ts:75:29
-         return rows.map(r => ({

-           userId: r.user_id, teamId: r.team_id,

-           role: r.role as TeamRole, joinedAt: r.joined_at,

-         }));

+         return rows.map(r => ({}));


[NoCoverage] BlockStatement
src/services/team-service.ts:79:19
-       } catch (err) {

-         throw new StorageError('Failed to list team members', err);

-       } finally {

+       } catch (err) {} finally {


[NoCoverage] StringLiteral
src/services/team-service.ts:80:30
-         throw new StorageError('Failed to list team members', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/services/team-service.ts:81:15
-       } finally {

-         await sql.end();

-       }

+       } finally {}


[NoCoverage] StringLiteral
src/services/team-service.ts:96:48
-       const { default: postgres } = await import('postgres');

+       const { default: postgres } = await import("");


[NoCoverage] ObjectLiteral
src/services/team-service.ts:97:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });

+       const sql = postgres(config.railwayUrl, {});


[NoCoverage] StringLiteral
src/services/team-service.ts:97:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });

+       const sql = postgres(config.railwayUrl, { ssl: "", max: 1 });


[NoCoverage] BlockStatement
src/services/team-service.ts:98:9
-       try {

-         const rows = await sql<{ role: string }[]>`

-           SELECT role FROM team_members WHERE user_id = ${config.userId} AND team_id = ${teamId}

-         `;

-         return rows[0]?.role as TeamRole | undefined;

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] StringLiteral
src/services/team-service.ts:99:49
-         const rows = await sql<{ role: string }[]>`

-           SELECT role FROM team_members WHERE user_id = ${config.userId} AND team_id = ${teamId}

-         `;

+         const rows = await sql<{ role: string }[]>``;


[NoCoverage] BlockStatement
src/services/team-service.ts:103:19
-       } catch (err) {

-         throw new StorageError('Failed to get member role', err);

-       } finally {

+       } catch (err) {} finally {


[NoCoverage] StringLiteral
src/services/team-service.ts:104:30
-         throw new StorageError('Failed to get member role', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/services/team-service.ts:105:15
-       } finally {

-         await sql.end();

-       }

+       } finally {}


[NoCoverage] StringLiteral
src/services/team-service.ts:123:48
-       const { default: postgres } = await import('postgres');

+       const { default: postgres } = await import("");


[NoCoverage] ObjectLiteral
src/services/team-service.ts:124:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 2 });

+       const sql = postgres(config.railwayUrl, {});


[NoCoverage] StringLiteral
src/services/team-service.ts:124:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 2 });

+       const sql = postgres(config.railwayUrl, { ssl: "", max: 2 });


[NoCoverage] BlockStatement
src/services/team-service.ts:126:9
-       try {

-         await sql`

-           INSERT INTO team_members (user_id, team_id, role, joined_at)

-           VALUES (${targetUserId}, ${teamId}, ${role}, ${joinedAt})

-           ON CONFLICT (user_id, team_id) DO UPDATE SET role = EXCLUDED.role

-         `;

-         return { userId: targetUserId, teamId, role, joinedAt };

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] StringLiteral
src/services/team-service.ts:127:16
-         await sql`

-           INSERT INTO team_members (user_id, team_id, role, joined_at)

-           VALUES (${targetUserId}, ${teamId}, ${role}, ${joinedAt})

-           ON CONFLICT (user_id, team_id) DO UPDATE SET role = EXCLUDED.role

-         `;

+         await sql``;


[NoCoverage] ObjectLiteral
src/services/team-service.ts:132:14
-         return { userId: targetUserId, teamId, role, joinedAt };

+         return {};


[NoCoverage] BlockStatement
src/services/team-service.ts:133:19
-       } catch (err) {

-         throw new StorageError('Failed to assign role', err);

-       } finally {

+       } catch (err) {} finally {


[NoCoverage] StringLiteral
src/services/team-service.ts:134:30
-         throw new StorageError('Failed to assign role', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/services/team-service.ts:135:15
-       } finally {

-         await sql.end();

-       }

+       } finally {}


[NoCoverage] StringLiteral
src/services/team-service.ts:153:48
-       const { default: postgres } = await import('postgres');

+       const { default: postgres } = await import("");


[NoCoverage] ObjectLiteral
src/services/team-service.ts:154:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });

+       const sql = postgres(config.railwayUrl, {});


[NoCoverage] StringLiteral
src/services/team-service.ts:154:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });

+       const sql = postgres(config.railwayUrl, { ssl: "", max: 1 });


[NoCoverage] BlockStatement
src/services/team-service.ts:155:9
-       try {

-         await sql.unsafe(TEAM_SCHEMA_SQL);

-         await sql`INSERT INTO teams (id, name) VALUES (${teamSlug}, ${teamSlug}) ON CONFLICT DO NOTHING`;

-         const token = `chron_${randomBytes(32).toString('hex')}`;

-         await sql`

-           INSERT INTO team_licenses (token, team_id, created_by)

-           VALUES (${token}, ${teamSlug}, ${config.userId})

-         `;

-         return { teamId: teamSlug, token };

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] StringLiteral
src/services/team-service.ts:157:16
-         await sql`INSERT INTO teams (id, name) VALUES (${teamSlug}, ${teamSlug}) ON CONFLICT DO NOTHING`;

+         await sql``;


[NoCoverage] StringLiteral
src/services/team-service.ts:158:21
-         const token = `chron_${randomBytes(32).toString('hex')}`;

+         const token = ``;


[NoCoverage] StringLiteral
src/services/team-service.ts:158:55
-         const token = `chron_${randomBytes(32).toString('hex')}`;

+         const token = `chron_${randomBytes(32).toString("")}`;


[NoCoverage] StringLiteral
src/services/team-service.ts:159:16
-         await sql`

-           INSERT INTO team_licenses (token, team_id, created_by)

-           VALUES (${token}, ${teamSlug}, ${config.userId})

-         `;

+         await sql``;


[NoCoverage] ObjectLiteral
src/services/team-service.ts:163:14
-         return { teamId: teamSlug, token };

+         return {};


[NoCoverage] BlockStatement
src/services/team-service.ts:164:19
-       } catch (err) {

-         throw new StorageError('Failed to mint team token', err);

-       } finally {

+       } catch (err) {} finally {


[NoCoverage] StringLiteral
src/services/team-service.ts:165:30
-         throw new StorageError('Failed to mint team token', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/services/team-service.ts:166:15
-       } finally {

-         await sql.end();

-       }

+       } finally {}


[NoCoverage] StringLiteral
src/services/team-service.ts:181:48
-       const { default: postgres } = await import('postgres');

+       const { default: postgres } = await import("");


[NoCoverage] ObjectLiteral
src/services/team-service.ts:182:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });

+       const sql = postgres(config.railwayUrl, {});


[NoCoverage] StringLiteral
src/services/team-service.ts:182:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });

+       const sql = postgres(config.railwayUrl, { ssl: "", max: 1 });


[NoCoverage] BlockStatement
src/services/team-service.ts:183:9
-       try {

-         const rows = await sql<{ id: string; name: string; created_at: string }[]>`

-           SELECT id, name, created_at FROM teams WHERE id = ${teamId}

-         `;

-         if (rows.length === 0) return undefined;

-         return { id: rows[0]!.id, name: rows[0]!.name, createdAt: rows[0]!.created_at };

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] StringLiteral
src/services/team-service.ts:184:81
-         const rows = await sql<{ id: string; name: string; created_at: string }[]>`

-           SELECT id, name, created_at FROM teams WHERE id = ${teamId}

-         `;

+         const rows = await sql<{ id: string; name: string; created_at: string }[]>``;


[NoCoverage] ConditionalExpression
src/services/team-service.ts:187:11
-         if (rows.length === 0) return undefined;

+         if (true) return undefined;


[NoCoverage] ConditionalExpression
src/services/team-service.ts:187:11
-         if (rows.length === 0) return undefined;

+         if (false) return undefined;


[NoCoverage] EqualityOperator
src/services/team-service.ts:187:11
-         if (rows.length === 0) return undefined;

+         if (rows.length !== 0) return undefined;


[NoCoverage] ObjectLiteral
src/services/team-service.ts:188:14
-         return { id: rows[0]!.id, name: rows[0]!.name, createdAt: rows[0]!.created_at };

+         return {};


[NoCoverage] BlockStatement
src/services/team-service.ts:189:19
-       } catch (err) {

-         throw new StorageError('Failed to get team', err);

-       } finally {

+       } catch (err) {} finally {


[NoCoverage] StringLiteral
src/services/team-service.ts:190:30
-         throw new StorageError('Failed to get team', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/services/team-service.ts:191:15
-       } finally {

-         await sql.end();

-       }

+       } finally {}


[Survived] StringLiteral
src/services/team-service.ts:25:69
-     async joinTeam(teamId: string, teamName: string, role: TeamRole = 'member'): Promise<{ team: Team; member: TeamMember }> {

+     async joinTeam(teamId: string, teamName: string, role: TeamRole = ""): Promise<{ team: Team; member: TeamMember }> {

Tests ran:
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error


[Survived] ConditionalExpression
src/services/team-service.ts:27:9
-       if (!config.railwayUrl) throw new StorageError('railwayUrl not configured - cannot join team');

+       if (true) throw new StorageError('railwayUrl not configured - cannot join team');

Tests ran:
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error


[Survived] ConditionalExpression
src/services/team-service.ts:66:9
-       if (!config.railwayUrl) return [];

+       if (true) return [];

Tests ran:
    TeamService (offline / no railwayUrl) listMembers returns an empty array


[Survived] BlockStatement
src/services/team-service.ts:92:70
-     async getMemberRole(teamId: string): Promise<TeamRole | undefined> {

-       const config = getConfig();

-       if (!config.railwayUrl) return undefined;

-   

-       const { default: postgres } = await import('postgres');

-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });

-       try {

-         const rows = await sql<{ role: string }[]>`

-           SELECT role FROM team_members WHERE user_id = ${config.userId} AND team_id = ${teamId}

-         `;

-         return rows[0]?.role as TeamRole | undefined;

-       } catch (err) {

-         throw new StorageError('Failed to get member role', err);

-       } finally {

-         await sql.end();

-       }

-     }

+     async getMemberRole(teamId: string): Promise<TeamRole | undefined> {}

Tests ran:
    TeamService (offline / no railwayUrl) getMemberRole returns undefined


[Survived] ConditionalExpression
src/services/team-service.ts:94:9
-       if (!config.railwayUrl) return undefined;

+       if (true) return undefined;

Tests ran:
    TeamService (offline / no railwayUrl) getMemberRole returns undefined


[Survived] ConditionalExpression
src/services/team-service.ts:121:9
-       if (!config.railwayUrl) throw new StorageError('railwayUrl not configured - cannot assign role');

+       if (true) throw new StorageError('railwayUrl not configured - cannot assign role');

Tests ran:
    TeamService (offline / no railwayUrl) assignRole throws a clear configuration error


[Survived] ConditionalExpression
src/services/team-service.ts:151:9
-       if (!config.railwayUrl) throw new StorageError('railwayUrl not configured - cannot mint token');

+       if (true) throw new StorageError('railwayUrl not configured - cannot mint token');

Tests ran:
    TeamService (offline / no railwayUrl) mintToken throws a clear configuration error


[Survived] BlockStatement
src/services/team-service.ts:177:60
-     async getTeam(teamId: string): Promise<Team | undefined> {

-       const config = getConfig();

-       if (!config.railwayUrl) return undefined;

-   

-       const { default: postgres } = await import('postgres');

-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 1 });

-       try {

-         const rows = await sql<{ id: string; name: string; created_at: string }[]>`

-           SELECT id, name, created_at FROM teams WHERE id = ${teamId}

-         `;

-         if (rows.length === 0) return undefined;

-         return { id: rows[0]!.id, name: rows[0]!.name, createdAt: rows[0]!.created_at };

-       } catch (err) {

-         throw new StorageError('Failed to get team', err);

-       } finally {

-         await sql.end();

-       }

-     }

+     async getTeam(teamId: string): Promise<Team | undefined> {}

Tests ran:
    TeamService (offline / no railwayUrl) getTeam returns undefined


[Survived] ConditionalExpression
src/services/team-service.ts:179:9
-       if (!config.railwayUrl) return undefined;

+       if (true) return undefined;

Tests ran:
    TeamService (offline / no railwayUrl) getTeam returns undefined


[NoCoverage] BlockStatement
src/services/team-sync-service.ts:54:70
-     async pushSharedMemory(memory: SharedMemoryInput): Promise<string> {
-       const config = getConfig();
-       if (!config.railwayUrl || !config.teamId) {
-         throw new StorageError('railwayUrl or teamId not configured');
-       }
-       const { default: postgres } = await import('postgres');
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 2 });
-       const now = new Date().toISOString();
-       try {
-         await sql`
-           INSERT INTO team_shared_memories
-             (id, user_id, team_id, project, content, memory_type, tags, category, shared_at, updated_at)
-           VALUES (
-             ${memory.id}, ${config.userId}, ${config.teamId},
-             ${memory.project ?? null}, ${memory.content}, ${memory.memoryType},
-             ${memory.tags}, ${memory.category ?? null}, ${now}, ${now}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             content    = EXCLUDED.content,
-             tags       = EXCLUDED.tags,
-             updated_at = NOW()
-         `;
-         // Mirror into local cache
-         this.teamRepo.upsertSharedCache({
-           id: memory.id, userId: config.userId, teamId: config.teamId,
-           project: memory.project, content: memory.content,
-           memoryType: memory.memoryType, tags: Object.freeze(memory.tags),
-           category: memory.category, sharedAt: now, updatedAt: now,
-         });
-         return memory.id;
-       } catch (err) {
-         throw new StorageError('Failed to push shared memory', err);
-       } finally {
-         await sql.end();
-       }
-     }
+     async pushSharedMemory(memory: SharedMemoryInput): Promise<string> {}

[NoCoverage] ConditionalExpression
src/services/team-sync-service.ts:56:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (true) {

[NoCoverage] ConditionalExpression
src/services/team-sync-service.ts:56:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (false) {

[NoCoverage] LogicalOperator
src/services/team-sync-service.ts:56:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (!config.railwayUrl && !config.teamId) {

[NoCoverage] BooleanLiteral
src/services/team-sync-service.ts:56:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (config.railwayUrl || !config.teamId) {

[NoCoverage] BooleanLiteral
src/services/team-sync-service.ts:56:31
-       if (!config.railwayUrl || !config.teamId) {
+       if (!config.railwayUrl || config.teamId) {

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:56:47
-       if (!config.railwayUrl || !config.teamId) {
-         throw new StorageError('railwayUrl or teamId not configured');
-       }
+       if (!config.railwayUrl || !config.teamId) {}

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:57:30
-         throw new StorageError('railwayUrl or teamId not configured');
+         throw new StorageError("");

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:60:48
-       const { default: postgres } = await import('postgres');
+       const { default: postgres } = await import("");

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:61:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 2 });
+       const sql = postgres(config.railwayUrl, {});

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:61:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 2 });
+       const sql = postgres(config.railwayUrl, { ssl: "", max: 2 });

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:63:9
-       try {
-         await sql`
-           INSERT INTO team_shared_memories
-             (id, user_id, team_id, project, content, memory_type, tags, category, shared_at, updated_at)
-           VALUES (
-             ${memory.id}, ${config.userId}, ${config.teamId},
-             ${memory.project ?? null}, ${memory.content}, ${memory.memoryType},
-             ${memory.tags}, ${memory.category ?? null}, ${now}, ${now}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             content    = EXCLUDED.content,
-             tags       = EXCLUDED.tags,
-             updated_at = NOW()
-         `;
-         // Mirror into local cache
-         this.teamRepo.upsertSharedCache({
-           id: memory.id, userId: config.userId, teamId: config.teamId,
-           project: memory.project, content: memory.content,
-           memoryType: memory.memoryType, tags: Object.freeze(memory.tags),
-           category: memory.category, sharedAt: now, updatedAt: now,
-         });
-         return memory.id;
-       } catch (err) {
+       try {} catch (err) {

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:64:16
-         await sql`
-           INSERT INTO team_shared_memories
-             (id, user_id, team_id, project, content, memory_type, tags, category, shared_at, updated_at)
-           VALUES (
-             ${memory.id}, ${config.userId}, ${config.teamId},
-             ${memory.project ?? null}, ${memory.content}, ${memory.memoryType},
-             ${memory.tags}, ${memory.category ?? null}, ${now}, ${now}
-           )
-           ON CONFLICT (id) DO UPDATE SET
-             content    = EXCLUDED.content,
-             tags       = EXCLUDED.tags,
-             updated_at = NOW()
-         `;
+         await sql``;

[NoCoverage] LogicalOperator
src/services/team-sync-service.ts:69:13
-             ${memory.project ?? null}, ${memory.content}, ${memory.memoryType},
+             ${memory.project && null}, ${memory.content}, ${memory.memoryType},

[NoCoverage] LogicalOperator
src/services/team-sync-service.ts:70:29
-             ${memory.tags}, ${memory.category ?? null}, ${now}, ${now}
+             ${memory.tags}, ${memory.category && null}, ${now}, ${now}

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:78:39
-         this.teamRepo.upsertSharedCache({
-           id: memory.id, userId: config.userId, teamId: config.teamId,
-           project: memory.project, content: memory.content,
-           memoryType: memory.memoryType, tags: Object.freeze(memory.tags),
-           category: memory.category, sharedAt: now, updatedAt: now,
-         });
+         this.teamRepo.upsertSharedCache({});

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:85:19
-       } catch (err) {
-         throw new StorageError('Failed to push shared memory', err);
-       } finally {
+       } catch (err) {} finally {

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:86:30
-         throw new StorageError('Failed to push shared memory', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:87:15
-       } finally {
-         await sql.end();
-       }
+       } finally {}

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:101:119
-     async pushInsight(input: { insightType: TeamInsightType; content: string; project?: string }): Promise<TeamInsight> {
-       const config = getConfig();
-       if (!config.railwayUrl || !config.teamId) {
-         throw new StorageError('railwayUrl or teamId not configured');
-       }
-       const { default: postgres } = await import('postgres');
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 2 });
-       const now = new Date().toISOString();
-       try {
-         // IS NOT DISTINCT FROM matches NULL project to NULL (plain = would not).
-         const existing = await sql<{ id: string; source_count: number; confidence: number; version: number }[]>`
-           SELECT id, source_count, confidence, version FROM team_insights
-           WHERE team_id = ${config.teamId} AND insight_type = ${input.insightType}
-             AND project IS NOT DISTINCT FROM ${input.project ?? null}
-         `;
-         let insight: TeamInsight;
-         if (existing.length > 0) {
-           const row = existing[0]!;
-           const confidence = Math.min(1, row.confidence + CONFIDENCE_STEP);
-           const version = row.version + 1;
-           await sql`
-             UPDATE team_insights
-             SET content = ${input.content}, confidence = ${confidence}, version = ${version}, updated_at = ${now}
-             WHERE id = ${row.id}
-           `;
-           insight = {
-             id: row.id, teamId: config.teamId, project: input.project, insightType: input.insightType,
-             content: input.content, confidence, sourceCount: row.source_count, version, updatedAt: now,
-           };
-         } else {
-           const id = randomUUID();
-           await sql`
-             INSERT INTO team_insights (id, team_id, project, insight_type, content, confidence, source_count, version, updated_at)
-             VALUES (${id}, ${config.teamId}, ${input.project ?? null}, ${input.insightType}, ${input.content}, ${CURATED_INSIGHT_BASE_CONFIDENCE}, 1, 1, ${now})
-           `;
-           insight = {
-             id, teamId: config.teamId, project: input.project, insightType: input.insightType,
-             content: input.content, confidence: CURATED_INSIGHT_BASE_CONFIDENCE, sourceCount: 1, version: 1, updatedAt: now,
-           };
-         }
-         this.teamRepo.upsertInsight(insight);
-         return insight;
-       } catch (err) {
-         throw new StorageError('Failed to push team insight', err);
-       } finally {
-         await sql.end();
-       }
-     }
+     async pushInsight(input: { insightType: TeamInsightType; content: string; project?: string }): Promise<TeamInsight> {}

[NoCoverage] ConditionalExpression
src/services/team-sync-service.ts:103:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (true) {

[NoCoverage] ConditionalExpression
src/services/team-sync-service.ts:103:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (false) {

[NoCoverage] LogicalOperator
src/services/team-sync-service.ts:103:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (!config.railwayUrl && !config.teamId) {

[NoCoverage] BooleanLiteral
src/services/team-sync-service.ts:103:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (config.railwayUrl || !config.teamId) {

[NoCoverage] BooleanLiteral
src/services/team-sync-service.ts:103:31
-       if (!config.railwayUrl || !config.teamId) {
+       if (!config.railwayUrl || config.teamId) {

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:103:47
-       if (!config.railwayUrl || !config.teamId) {
-         throw new StorageError('railwayUrl or teamId not configured');
-       }
+       if (!config.railwayUrl || !config.teamId) {}

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:104:30
-         throw new StorageError('railwayUrl or teamId not configured');
+         throw new StorageError("");

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:107:48
-       const { default: postgres } = await import('postgres');
+       const { default: postgres } = await import("");

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:108:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 2 });
+       const sql = postgres(config.railwayUrl, {});

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:108:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 2 });
+       const sql = postgres(config.railwayUrl, { ssl: "", max: 2 });

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:110:9
-       try {
-         // IS NOT DISTINCT FROM matches NULL project to NULL (plain = would not).
-         const existing = await sql<{ id: string; source_count: number; confidence: number; version: number }[]>`
-           SELECT id, source_count, confidence, version FROM team_insights
-           WHERE team_id = ${config.teamId} AND insight_type = ${input.insightType}
-             AND project IS NOT DISTINCT FROM ${input.project ?? null}
-         `;
-         let insight: TeamInsight;
-         if (existing.length > 0) {
-           const row = existing[0]!;
-           const confidence = Math.min(1, row.confidence + CONFIDENCE_STEP);
-           const version = row.version + 1;
-           await sql`
-             UPDATE team_insights
-             SET content = ${input.content}, confidence = ${confidence}, version = ${version}, updated_at = ${now}
-             WHERE id = ${row.id}
-           `;
-           insight = {
-             id: row.id, teamId: config.teamId, project: input.project, insightType: input.insightType,
-             content: input.content, confidence, sourceCount: row.source_count, version, updatedAt: now,
-           };
-         } else {
-           const id = randomUUID();
-           await sql`
-             INSERT INTO team_insights (id, team_id, project, insight_type, content, confidence, source_count, version, updated_at)
-             VALUES (${id}, ${config.teamId}, ${input.project ?? null}, ${input.insightType}, ${input.content}, ${CURATED_INSIGHT_BASE_CONFIDENCE}, 1, 1, ${now})
-           `;
-           insight = {
-             id, teamId: config.teamId, project: input.project, insightType: input.insightType,
-             content: input.content, confidence: CURATED_INSIGHT_BASE_CONFIDENCE, sourceCount: 1, version: 1, updatedAt: now,
-           };
-         }
-         this.teamRepo.upsertInsight(insight);
-         return insight;
-       } catch (err) {
+       try {} catch (err) {

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:112:110
-         const existing = await sql<{ id: string; source_count: number; confidence: number; version: number }[]>`
-           SELECT id, source_count, confidence, version FROM team_insights
-           WHERE team_id = ${config.teamId} AND insight_type = ${input.insightType}
-             AND project IS NOT DISTINCT FROM ${input.project ?? null}
-         `;
+         const existing = await sql<{ id: string; source_count: number; confidence: number; version: number }[]>``;

[NoCoverage] LogicalOperator
src/services/team-sync-service.ts:115:46
-             AND project IS NOT DISTINCT FROM ${input.project ?? null}
+             AND project IS NOT DISTINCT FROM ${input.project && null}

[NoCoverage] ConditionalExpression
src/services/team-sync-service.ts:119:11
-         if (existing.length > 0) {
+         if (true) {

[NoCoverage] ConditionalExpression
src/services/team-sync-service.ts:119:11
-         if (existing.length > 0) {
+         if (false) {

[NoCoverage] EqualityOperator
src/services/team-sync-service.ts:119:11
-         if (existing.length > 0) {
+         if (existing.length >= 0) {

[NoCoverage] EqualityOperator
src/services/team-sync-service.ts:119:11
-         if (existing.length > 0) {
+         if (existing.length <= 0) {

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:119:32
-         if (existing.length > 0) {
-           const row = existing[0]!;
-           const confidence = Math.min(1, row.confidence + CONFIDENCE_STEP);
-           const version = row.version + 1;
-           await sql`
-             UPDATE team_insights
-             SET content = ${input.content}, confidence = ${confidence}, version = ${version}, updated_at = ${now}
-             WHERE id = ${row.id}
-           `;
-           insight = {
-             id: row.id, teamId: config.teamId, project: input.project, insightType: input.insightType,
-             content: input.content, confidence, sourceCount: row.source_count, version, updatedAt: now,
-           };
-         } else {
+         if (existing.length > 0) {} else {

[NoCoverage] MethodExpression
src/services/team-sync-service.ts:121:28
-           const confidence = Math.min(1, row.confidence + CONFIDENCE_STEP);
+           const confidence = Math.max(1, row.confidence + CONFIDENCE_STEP);

[NoCoverage] ArithmeticOperator
src/services/team-sync-service.ts:121:40
-           const confidence = Math.min(1, row.confidence + CONFIDENCE_STEP);
+           const confidence = Math.min(1, row.confidence - CONFIDENCE_STEP);

[NoCoverage] ArithmeticOperator
src/services/team-sync-service.ts:122:25
-           const version = row.version + 1;
+           const version = row.version - 1;

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:123:18
-           await sql`
-             UPDATE team_insights
-             SET content = ${input.content}, confidence = ${confidence}, version = ${version}, updated_at = ${now}
-             WHERE id = ${row.id}
-           `;
+           await sql``;

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:128:19
-           insight = {
-             id: row.id, teamId: config.teamId, project: input.project, insightType: input.insightType,
-             content: input.content, confidence, sourceCount: row.source_count, version, updatedAt: now,
-           };
+           insight = {};

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:132:14
-         } else {
-           const id = randomUUID();
-           await sql`
-             INSERT INTO team_insights (id, team_id, project, insight_type, content, confidence, source_count, version, updated_at)
-             VALUES (${id}, ${config.teamId}, ${input.project ?? null}, ${input.insightType}, ${input.content}, ${CURATED_INSIGHT_BASE_CONFIDENCE}, 1, 1, ${now})
-           `;
-           insight = {
-             id, teamId: config.teamId, project: input.project, insightType: input.insightType,
-             content: input.content, confidence: CURATED_INSIGHT_BASE_CONFIDENCE, sourceCount: 1, version: 1, updatedAt: now,
-           };
-         }
+         } else {}

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:134:18
-           await sql`
-             INSERT INTO team_insights (id, team_id, project, insight_type, content, confidence, source_count, version, updated_at)
-             VALUES (${id}, ${config.teamId}, ${input.project ?? null}, ${input.insightType}, ${input.content}, ${CURATED_INSIGHT_BASE_CONFIDENCE}, 1, 1, ${now})
-           `;
+           await sql``;

[NoCoverage] LogicalOperator
src/services/team-sync-service.ts:136:46
-             VALUES (${id}, ${config.teamId}, ${input.project ?? null}, ${input.insightType}, ${input.content}, ${CURATED_INSIGHT_BASE_CONFIDENCE}, 1, 1, ${now})
+             VALUES (${id}, ${config.teamId}, ${input.project && null}, ${input.insightType}, ${input.content}, ${CURATED_INSIGHT_BASE_CONFIDENCE}, 1, 1, ${now})

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:138:19
-           insight = {
-             id, teamId: config.teamId, project: input.project, insightType: input.insightType,
-             content: input.content, confidence: CURATED_INSIGHT_BASE_CONFIDENCE, sourceCount: 1, version: 1, updatedAt: now,
-           };
+           insight = {};

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:145:19
-       } catch (err) {
-         throw new StorageError('Failed to push team insight', err);
-       } finally {
+       } catch (err) {} finally {

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:146:30
-         throw new StorageError('Failed to push team insight', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:147:15
-       } finally {
-         await sql.end();
-       }
+       } finally {}

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:157:41
-     async sync(): Promise<TeamSyncResult> {
-       const config = getConfig();
-       if (!config.railwayUrl || !config.teamId) {
-         return { sharedMemoriesPushed: 0, promptLogsPushed: 0, insightsPulled: 0, sharedMemoriesPulled: 0, patternsPulled: 0, skipped: true };
-       }
-       const { default: postgres } = await import('postgres');
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
-       try {
-         // Push prompt log buffer
-         const promptLogsPushed = await this.promptLogSvc.pushBuffer();
-         const since = this.teamRepo.getLastPullAt(config.userId, config.teamId) ?? '1970-01-01T00:00:00Z';
-         // Pull team insights updated since last pull
-         const remoteInsights = await sql<Record<string, unknown>[]>`
-           SELECT id, team_id, project, insight_type, content, confidence, source_count, version, updated_at
-           FROM team_insights
-           WHERE team_id = ${config.teamId} AND updated_at > ${since}
-           ORDER BY updated_at DESC
-         `;
-         for (const ri of remoteInsights) {
-           this.teamRepo.upsertInsight({
-             id: ri['id'] as string, teamId: ri['team_id'] as string,
-             project: ri['project'] as string | undefined,
-             insightType: ri['insight_type'] as TeamInsight['insightType'],
-             content: ri['content'] as string, confidence: ri['confidence'] as number,
-             sourceCount: ri['source_count'] as number, version: ri['version'] as number,
-             updatedAt: toIsoString(ri['updated_at']),
-           });
-         }
-         // Pull team-shared memories updated since last pull (excluding own)
-         const remoteShared = await sql<Record<string, unknown>[]>`
-           SELECT id, user_id, team_id, project, content, memory_type, tags, category, shared_at, updated_at
-           FROM team_shared_memories
-           WHERE team_id = ${config.teamId}
-             AND user_id != ${config.userId}
-             AND updated_at > ${since}
-           ORDER BY updated_at DESC
-           LIMIT 200
-         `;
-         for (const rs of remoteShared) {
-           // `text[]` arrives as a JS array and `timestamptz` as a JS Date; neither binds to
-           // SQLite. Normalising here is what fixed `team sync` against the real Postgres.
-           const tags = JSON.parse(toTagsJson(rs['tags'])) as string[];
-           this.teamRepo.upsertSharedCache({
-             id: rs['id'] as string, userId: rs['user_id'] as string,
-             teamId: rs['team_id'] as string, project: rs['project'] as string | undefined,
-             content: rs['content'] as string, memoryType: rs['memory_type'] as string,
-             tags: Object.freeze(tags), category: rs['category'] as string | undefined,
-             sharedAt: toIsoString(rs['shared_at']), updatedAt: toIsoString(rs['updated_at']),
-           });
-         }
-         // Pull team patterns updated since last pull
-         const remotePatterns = await sql<Record<string, unknown>[]>`
-           SELECT id, user_id, team_id, project, pattern_type, metric, value, period, computed_at
-           FROM team_patterns
-           WHERE team_id = ${config.teamId} AND computed_at > ${since}
-           ORDER BY computed_at DESC
-         `;
-         for (const rp of remotePatterns) {
-           this.teamRepo.upsertPattern({
-             id: rp['id'] as string, userId: rp['user_id'] as string,
-             teamId: rp['team_id'] as string, project: rp['project'] as string | undefined,
-             patternType: rp['pattern_type'] as TeamPattern['patternType'],
-             metric: rp['metric'] as string, value: rp['value'] as number,
-             period: rp['period'] as TeamPattern['period'],
-             computedAt: toIsoString(rp['computed_at']),
-           });
-         }
-         const now = new Date().toISOString();
-         this.teamRepo.setLastPullAt(config.userId, config.teamId, now);
-         return {
-           sharedMemoriesPushed: 0,
-           promptLogsPushed,
-           insightsPulled: remoteInsights.length,
-           sharedMemoriesPulled: remoteShared.length,
-           patternsPulled: remotePatterns.length,
-           skipped: false,
-         };
-       } catch (err) {
-         throw new StorageError('Team sync failed', err);
-       } finally {
-         await sql.end();
-       }
-     }
+     async sync(): Promise<TeamSyncResult> {}

[NoCoverage] ConditionalExpression
src/services/team-sync-service.ts:159:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (true) {

[NoCoverage] ConditionalExpression
src/services/team-sync-service.ts:159:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (false) {

[NoCoverage] LogicalOperator
src/services/team-sync-service.ts:159:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (!config.railwayUrl && !config.teamId) {

[NoCoverage] BooleanLiteral
src/services/team-sync-service.ts:159:9
-       if (!config.railwayUrl || !config.teamId) {
+       if (config.railwayUrl || !config.teamId) {

[NoCoverage] BooleanLiteral
src/services/team-sync-service.ts:159:31
-       if (!config.railwayUrl || !config.teamId) {
+       if (!config.railwayUrl || config.teamId) {

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:159:47
-       if (!config.railwayUrl || !config.teamId) {
-         return { sharedMemoriesPushed: 0, promptLogsPushed: 0, insightsPulled: 0, sharedMemoriesPulled: 0, patternsPulled: 0, skipped: true };
-       }
+       if (!config.railwayUrl || !config.teamId) {}

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:160:14
-         return { sharedMemoriesPushed: 0, promptLogsPushed: 0, insightsPulled: 0, sharedMemoriesPulled: 0, patternsPulled: 0, skipped: true };
+         return {};

[NoCoverage] BooleanLiteral
src/services/team-sync-service.ts:160:134
-         return { sharedMemoriesPushed: 0, promptLogsPushed: 0, insightsPulled: 0, sharedMemoriesPulled: 0, patternsPulled: 0, skipped: true };
+         return { sharedMemoriesPushed: 0, promptLogsPushed: 0, insightsPulled: 0, sharedMemoriesPulled: 0, patternsPulled: 0, skipped: false };

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:163:48
-       const { default: postgres } = await import('postgres');
+       const { default: postgres } = await import("");

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:164:45
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
+       const sql = postgres(config.railwayUrl, {});

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:164:52
-       const sql = postgres(config.railwayUrl, { ssl: 'require', max: 3 });
+       const sql = postgres(config.railwayUrl, { ssl: "", max: 3 });

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:165:9
-       try {
-         // Push prompt log buffer
-         const promptLogsPushed = await this.promptLogSvc.pushBuffer();
-         const since = this.teamRepo.getLastPullAt(config.userId, config.teamId) ?? '1970-01-01T00:00:00Z';
-         // Pull team insights updated since last pull
-         const remoteInsights = await sql<Record<string, unknown>[]>`
-           SELECT id, team_id, project, insight_type, content, confidence, source_count, version, updated_at
-           FROM team_insights
-           WHERE team_id = ${config.teamId} AND updated_at > ${since}
-           ORDER BY updated_at DESC
-         `;
-         for (const ri of remoteInsights) {
-           this.teamRepo.upsertInsight({
-             id: ri['id'] as string, teamId: ri['team_id'] as string,
-             project: ri['project'] as string | undefined,
-             insightType: ri['insight_type'] as TeamInsight['insightType'],
-             content: ri['content'] as string, confidence: ri['confidence'] as number,
-             sourceCount: ri['source_count'] as number, version: ri['version'] as number,
-             updatedAt: toIsoString(ri['updated_at']),
-           });
-         }
-         // Pull team-shared memories updated since last pull (excluding own)
-         const remoteShared = await sql<Record<string, unknown>[]>`
-           SELECT id, user_id, team_id, project, content, memory_type, tags, category, shared_at, updated_at
-           FROM team_shared_memories
-           WHERE team_id = ${config.teamId}
-             AND user_id != ${config.userId}
-             AND updated_at > ${since}
-           ORDER BY updated_at DESC
-           LIMIT 200
-         `;
-         for (const rs of remoteShared) {
-           // `text[]` arrives as a JS array and `timestamptz` as a JS Date; neither binds to
-           // SQLite. Normalising here is what fixed `team sync` against the real Postgres.
-           const tags = JSON.parse(toTagsJson(rs['tags'])) as string[];
-           this.teamRepo.upsertSharedCache({
-             id: rs['id'] as string, userId: rs['user_id'] as string,
-             teamId: rs['team_id'] as string, project: rs['project'] as string | undefined,
-             content: rs['content'] as string, memoryType: rs['memory_type'] as string,
-             tags: Object.freeze(tags), category: rs['category'] as string | undefined,
-             sharedAt: toIsoString(rs['shared_at']), updatedAt: toIsoString(rs['updated_at']),
-           });
-         }
-         // Pull team patterns updated since last pull
-         const remotePatterns = await sql<Record<string, unknown>[]>`
-           SELECT id, user_id, team_id, project, pattern_type, metric, value, period, computed_at
-           FROM team_patterns
-           WHERE team_id = ${config.teamId} AND computed_at > ${since}
-           ORDER BY computed_at DESC
-         `;
-         for (const rp of remotePatterns) {
-           this.teamRepo.upsertPattern({
-             id: rp['id'] as string, userId: rp['user_id'] as string,
-             teamId: rp['team_id'] as string, project: rp['project'] as string | undefined,
-             patternType: rp['pattern_type'] as TeamPattern['patternType'],
-             metric: rp['metric'] as string, value: rp['value'] as number,
-             period: rp['period'] as TeamPattern['period'],
-             computedAt: toIsoString(rp['computed_at']),
-           });
-         }
-         const now = new Date().toISOString();
-         this.teamRepo.setLastPullAt(config.userId, config.teamId, now);
-         return {
-           sharedMemoriesPushed: 0,
-           promptLogsPushed,
-           insightsPulled: remoteInsights.length,
-           sharedMemoriesPulled: remoteShared.length,
-           patternsPulled: remotePatterns.length,
-           skipped: false,
-         };
-       } catch (err) {
+       try {} catch (err) {

[NoCoverage] LogicalOperator
src/services/team-sync-service.ts:169:21
-         const since = this.teamRepo.getLastPullAt(config.userId, config.teamId) ?? '1970-01-01T00:00:00Z';
+         const since = this.teamRepo.getLastPullAt(config.userId, config.teamId) && '1970-01-01T00:00:00Z';

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:169:82
-         const since = this.teamRepo.getLastPullAt(config.userId, config.teamId) ?? '1970-01-01T00:00:00Z';
+         const since = this.teamRepo.getLastPullAt(config.userId, config.teamId) ?? "";

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:172:66
-         const remoteInsights = await sql<Record<string, unknown>[]>`
-           SELECT id, team_id, project, insight_type, content, confidence, source_count, version, updated_at
-           FROM team_insights
-           WHERE team_id = ${config.teamId} AND updated_at > ${since}
-           ORDER BY updated_at DESC
-         `;
+         const remoteInsights = await sql<Record<string, unknown>[]>``;

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:178:40
-         for (const ri of remoteInsights) {
-           this.teamRepo.upsertInsight({
-             id: ri['id'] as string, teamId: ri['team_id'] as string,
-             project: ri['project'] as string | undefined,
-             insightType: ri['insight_type'] as TeamInsight['insightType'],
-             content: ri['content'] as string, confidence: ri['confidence'] as number,
-             sourceCount: ri['source_count'] as number, version: ri['version'] as number,
-             updatedAt: toIsoString(ri['updated_at']),
-           });
-         }
+         for (const ri of remoteInsights) {}

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:179:37
-           this.teamRepo.upsertInsight({
-             id: ri['id'] as string, teamId: ri['team_id'] as string,
-             project: ri['project'] as string | undefined,
-             insightType: ri['insight_type'] as TeamInsight['insightType'],
-             content: ri['content'] as string, confidence: ri['confidence'] as number,
-             sourceCount: ri['source_count'] as number, version: ri['version'] as number,
-             updatedAt: toIsoString(ri['updated_at']),
-           });
+           this.teamRepo.upsertInsight({});

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:185:37
-             updatedAt: toIsoString(ri['updated_at']),
+             updatedAt: toIsoString(ri[""]),

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:190:64
-         const remoteShared = await sql<Record<string, unknown>[]>`
-           SELECT id, user_id, team_id, project, content, memory_type, tags, category, shared_at, updated_at
-           FROM team_shared_memories
-           WHERE team_id = ${config.teamId}
-             AND user_id != ${config.userId}
-             AND updated_at > ${since}
-           ORDER BY updated_at DESC
-           LIMIT 200
-         `;
+         const remoteShared = await sql<Record<string, unknown>[]>``;

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:199:38
-         for (const rs of remoteShared) {
-           // `text[]` arrives as a JS array and `timestamptz` as a JS Date; neither binds to
-           // SQLite. Normalising here is what fixed `team sync` against the real Postgres.
-           const tags = JSON.parse(toTagsJson(rs['tags'])) as string[];
-           this.teamRepo.upsertSharedCache({
-             id: rs['id'] as string, userId: rs['user_id'] as string,
-             teamId: rs['team_id'] as string, project: rs['project'] as string | undefined,
-             content: rs['content'] as string, memoryType: rs['memory_type'] as string,
-             tags: Object.freeze(tags), category: rs['category'] as string | undefined,
-             sharedAt: toIsoString(rs['shared_at']), updatedAt: toIsoString(rs['updated_at']),
-           });
-         }
+         for (const rs of remoteShared) {}

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:203:41
-           this.teamRepo.upsertSharedCache({
-             id: rs['id'] as string, userId: rs['user_id'] as string,
-             teamId: rs['team_id'] as string, project: rs['project'] as string | undefined,
-             content: rs['content'] as string, memoryType: rs['memory_type'] as string,
-             tags: Object.freeze(tags), category: rs['category'] as string | undefined,
-             sharedAt: toIsoString(rs['shared_at']), updatedAt: toIsoString(rs['updated_at']),
-           });
+           this.teamRepo.upsertSharedCache({});

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:208:36
-             sharedAt: toIsoString(rs['shared_at']), updatedAt: toIsoString(rs['updated_at']),
+             sharedAt: toIsoString(rs[""]), updatedAt: toIsoString(rs['updated_at']),

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:208:77
-             sharedAt: toIsoString(rs['shared_at']), updatedAt: toIsoString(rs['updated_at']),
+             sharedAt: toIsoString(rs['shared_at']), updatedAt: toIsoString(rs[""]),

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:213:66
-         const remotePatterns = await sql<Record<string, unknown>[]>`
-           SELECT id, user_id, team_id, project, pattern_type, metric, value, period, computed_at
-           FROM team_patterns
-           WHERE team_id = ${config.teamId} AND computed_at > ${since}
-           ORDER BY computed_at DESC
-         `;
+         const remotePatterns = await sql<Record<string, unknown>[]>``;

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:219:40
-         for (const rp of remotePatterns) {
-           this.teamRepo.upsertPattern({
-             id: rp['id'] as string, userId: rp['user_id'] as string,
-             teamId: rp['team_id'] as string, project: rp['project'] as string | undefined,
-             patternType: rp['pattern_type'] as TeamPattern['patternType'],
-             metric: rp['metric'] as string, value: rp['value'] as number,
-             period: rp['period'] as TeamPattern['period'],
-             computedAt: toIsoString(rp['computed_at']),
-           });
-         }
+         for (const rp of remotePatterns) {}

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:220:37
-           this.teamRepo.upsertPattern({
-             id: rp['id'] as string, userId: rp['user_id'] as string,
-             teamId: rp['team_id'] as string, project: rp['project'] as string | undefined,
-             patternType: rp['pattern_type'] as TeamPattern['patternType'],
-             metric: rp['metric'] as string, value: rp['value'] as number,
-             period: rp['period'] as TeamPattern['period'],
-             computedAt: toIsoString(rp['computed_at']),
-           });
+           this.teamRepo.upsertPattern({});

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:226:38
-             computedAt: toIsoString(rp['computed_at']),
+             computedAt: toIsoString(rp[""]),

[NoCoverage] ObjectLiteral
src/services/team-sync-service.ts:233:14
-         return {
-           sharedMemoriesPushed: 0,
-           promptLogsPushed,
-           insightsPulled: remoteInsights.length,
-           sharedMemoriesPulled: remoteShared.length,
-           patternsPulled: remotePatterns.length,
-           skipped: false,
-         };
+         return {};

[NoCoverage] BooleanLiteral
src/services/team-sync-service.ts:239:18
-           skipped: false,
+           skipped: true,

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:241:19
-       } catch (err) {
-         throw new StorageError('Team sync failed', err);
-       } finally {
+       } catch (err) {} finally {

[NoCoverage] StringLiteral
src/services/team-sync-service.ts:242:30
-         throw new StorageError('Team sync failed', err);
+         throw new StorageError("", err);

[NoCoverage] BlockStatement
src/services/team-sync-service.ts:243:15
-       } finally {
-         await sql.end();
-       }
+       } finally {}

[NoCoverage] BlockStatement
src/services/trigger-service.ts:41:14
-     }): string {

-       const id = `trig_${crypto.randomUUID()}`;

-       try {

-         this.db.prepare(`

-           INSERT INTO triggers (id, memory_id, action, content, severity, created_at, active)

-           VALUES (?, ?, ?, ?, ?, ?, 1)

-         `).run(

-           id,

-           input.memoryId ?? null,

-           input.action,

-           input.content,

-           input.severity ?? 'warning',

-           new Date().toISOString(),

-         );

-       } catch (err) {

-         throw new StorageError('Failed to set trigger', err);

-       }

-       return id;

-     }

+     }): string {}


[NoCoverage] StringLiteral
src/services/trigger-service.ts:42:16
-       const id = `trig_${crypto.randomUUID()}`;

+       const id = ``;


[NoCoverage] BlockStatement
src/services/trigger-service.ts:43:9
-       try {

-         this.db.prepare(`

-           INSERT INTO triggers (id, memory_id, action, content, severity, created_at, active)

-           VALUES (?, ?, ?, ?, ?, ?, 1)

-         `).run(

-           id,

-           input.memoryId ?? null,

-           input.action,

-           input.content,

-           input.severity ?? 'warning',

-           new Date().toISOString(),

-         );

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] StringLiteral
src/services/trigger-service.ts:44:23
-         this.db.prepare(`

-           INSERT INTO triggers (id, memory_id, action, content, severity, created_at, active)

-           VALUES (?, ?, ?, ?, ?, ?, 1)

-         `).run(

+         this.db.prepare(``).run(


[NoCoverage] LogicalOperator
src/services/trigger-service.ts:49:9
-           input.memoryId ?? null,

+           input.memoryId && null,


[NoCoverage] LogicalOperator
src/services/trigger-service.ts:52:9
-           input.severity ?? 'warning',

+           input.severity && 'warning',


[NoCoverage] StringLiteral
src/services/trigger-service.ts:52:27
-           input.severity ?? 'warning',

+           input.severity ?? "",


[NoCoverage] BlockStatement
src/services/trigger-service.ts:55:19
-       } catch (err) {

-         throw new StorageError('Failed to set trigger', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/services/trigger-service.ts:56:30
-         throw new StorageError('Failed to set trigger', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/services/trigger-service.ts:68:69
-     checkTriggers(action: string, _project?: string): TriggerResult[] {

-       try {

-         const rows = this.db.prepare(`

-           SELECT id, memory_id, action, content, severity FROM triggers

-           WHERE action = ? AND active = 1

-         `).all(action) as TriggerRow[];

-         return rows.map(row => ({

-           id: row.id,

-           action: row.action,

-           content: row.content,

-           severity: row.severity,

-           memoryId: row.memory_id ?? undefined,

-         }));

-       } catch (err) {

-         throw new StorageError('Failed to check triggers', err);

-       }

-     }

+     checkTriggers(action: string, _project?: string): TriggerResult[] {}


[NoCoverage] BlockStatement
src/services/trigger-service.ts:69:9
-       try {

-         const rows = this.db.prepare(`

-           SELECT id, memory_id, action, content, severity FROM triggers

-           WHERE action = ? AND active = 1

-         `).all(action) as TriggerRow[];

-         return rows.map(row => ({

-           id: row.id,

-           action: row.action,

-           content: row.content,

-           severity: row.severity,

-           memoryId: row.memory_id ?? undefined,

-         }));

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] ArrowFunction
src/services/trigger-service.ts:74:23
-         return rows.map(row => ({

-           id: row.id,

-           action: row.action,

-           content: row.content,

-           severity: row.severity,

-           memoryId: row.memory_id ?? undefined,

-         }));

+         return rows.map(() => undefined);


[NoCoverage] ObjectLiteral
src/services/trigger-service.ts:74:31
-         return rows.map(row => ({

-           id: row.id,

-           action: row.action,

-           content: row.content,

-           severity: row.severity,

-           memoryId: row.memory_id ?? undefined,

-         }));

+         return rows.map(row => ({}));


[NoCoverage] LogicalOperator
src/services/trigger-service.ts:79:19
-           memoryId: row.memory_id ?? undefined,

+           memoryId: row.memory_id && undefined,


[NoCoverage] BlockStatement
src/services/trigger-service.ts:81:19
-       } catch (err) {

-         throw new StorageError('Failed to check triggers', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/services/trigger-service.ts:82:30
-         throw new StorageError('Failed to check triggers', err);

+         throw new StorageError("", err);


[NoCoverage] BlockStatement
src/services/trigger-service.ts:91:35
-     removeTrigger(id: string): void {

-       try {

-         this.db.prepare('UPDATE triggers SET active = 0 WHERE id = ?').run(id);

-       } catch (err) {

-         throw new StorageError('Failed to remove trigger', err);

-       }

-     }

+     removeTrigger(id: string): void {}


[NoCoverage] BlockStatement
src/services/trigger-service.ts:92:9
-       try {

-         this.db.prepare('UPDATE triggers SET active = 0 WHERE id = ?').run(id);

-       } catch (err) {

+       try {} catch (err) {


[NoCoverage] StringLiteral
src/services/trigger-service.ts:93:23
-         this.db.prepare('UPDATE triggers SET active = 0 WHERE id = ?').run(id);

+         this.db.prepare("").run(id);


[NoCoverage] BlockStatement
src/services/trigger-service.ts:94:19
-       } catch (err) {

-         throw new StorageError('Failed to remove trigger', err);

-       }

+       } catch (err) {}


[NoCoverage] StringLiteral
src/services/trigger-service.ts:95:30
-         throw new StorageError('Failed to remove trigger', err);

+         throw new StorageError("", err);


[NoCoverage] StringLiteral
src/shared/config/index.ts:55:30
-     ? path.resolve(process.env['CHRONICLE_HOME'])
+     ? path.resolve(process.env[""])

[NoCoverage] BlockStatement
src/shared/config/index.ts:60:40
-   export function getConfigDir(): string {
-     return CONFIG_DIR;
-   }
+   export function getConfigDir(): string {}

[NoCoverage] BlockStatement
src/shared/config/index.ts:64:43
-   function randomHex(bytes: number): string {
-     return crypto.randomBytes(bytes).toString('hex');
-   }
+   function randomHex(bytes: number): string {}

[NoCoverage] StringLiteral
src/shared/config/index.ts:65:45
-     return crypto.randomBytes(bytes).toString('hex');
+     return crypto.randomBytes(bytes).toString("");

[NoCoverage] BlockStatement
src/shared/config/index.ts:68:29
-   function gitEmail(): string {
-     try {
-       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();
-     } catch {
-       return '';
-     }
-   }
+   function gitEmail(): string {}

[NoCoverage] BlockStatement
src/shared/config/index.ts:69:7
-     try {
-       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();
-     } catch {
+     try {} catch {

[NoCoverage] MethodExpression
src/shared/config/index.ts:70:12
-       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();
+       return execSync('git config --global user.email', {
+     encoding: 'utf-8',
+     stdio: ['ignore', 'pipe', 'ignore']
+   });

[NoCoverage] StringLiteral
src/shared/config/index.ts:70:21
-       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();
+       return execSync("", { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();

[NoCoverage] ObjectLiteral
src/shared/config/index.ts:70:55
-       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();
+       return execSync('git config --global user.email', {}).trim();

[NoCoverage] StringLiteral
src/shared/config/index.ts:70:67
-       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();
+       return execSync('git config --global user.email', { encoding: "", stdio: ['ignore', 'pipe', 'ignore'] }).trim();

[NoCoverage] ArrayDeclaration
src/shared/config/index.ts:70:83
-       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();
+       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: [] }).trim();

[NoCoverage] StringLiteral
src/shared/config/index.ts:70:84
-       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();
+       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ["", 'pipe', 'ignore'] }).trim();

[NoCoverage] StringLiteral
src/shared/config/index.ts:70:94
-       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();
+       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', "", 'ignore'] }).trim();

[NoCoverage] StringLiteral
src/shared/config/index.ts:70:102
-       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', 'ignore'] }).trim();
+       return execSync('git config --global user.email', { encoding: 'utf-8', stdio: ['ignore', 'pipe', ""] }).trim();

[NoCoverage] BlockStatement
src/shared/config/index.ts:71:11
-     } catch {
-       return '';
-     }
+     } catch {}

[NoCoverage] StringLiteral
src/shared/config/index.ts:72:12
-       return '';
+       return "Stryker was here!";

[NoCoverage] BlockStatement
src/shared/config/index.ts:83:47
-   export function loadConfig(): ChronicleConfig {
-     const email = gitEmail();
-     if (!fs.existsSync(CONFIG_FILE)) {
-       if (!email) {
-         throw new ConfigurationError(
-           `Chronicle config not found at ${CONFIG_FILE} and no git user.email detected. ` +
-           `Run: git config --global user.email "you@example.com" and restart, ` +
-           `or create ${CONFIG_FILE} manually with a userId field.`,
-         );
-       }
-       const defaults: ChronicleConfig = {
-         userId: email,
-         deviceId: `${os.hostname()}-${randomHex(4)}`,
-         dbPath: path.join(CONFIG_DIR, 'chronicle.db'),
-       };
-       fs.mkdirSync(CONFIG_DIR, { recursive: true });
-       fs.writeFileSync(CONFIG_FILE, JSON.stringify(defaults, null, 2), 'utf-8');
-       // A freshly-derived identity on a machine that is meant to join an existing team is the failure
-       // mode described on `userId` above. Say so on stderr, which an MCP host shows in its logs,
-       // rather than letting the machine quietly own nothing.
-       process.stderr.write(
-         `chronicle: created ${CONFIG_FILE} with a NEW identity "${defaults.userId}" derived from your ` +
-         `git email.
-   ` +
-         `  If you already sync or belong to a team under a different id, set "userId" to that id now — ` +
-         `it keys every synced row and your team membership.
-   `,
-       );
-       return defaults;
-     }
-     const raw = fs.readFileSync(CONFIG_FILE, 'utf-8');
-     const config = JSON.parse(raw) as ChronicleConfig;
-     // Backfill userId for existing installs that left it empty.
-     if (!config.userId) {
-       if (!email) {
-         throw new ConfigurationError(
-           `userId is empty in ${CONFIG_FILE} and no git user.email detected. ` +
-           `Set userId manually or run: git config --global user.email "you@example.com"`,
-         );
-       }
-       config.userId = email;
-       fs.writeFileSync(CONFIG_FILE, JSON.stringify(config, null, 2), 'utf-8');
-     }
-     return config;
-   }
+   export function loadConfig(): ChronicleConfig {}

[NoCoverage] BooleanLiteral
src/shared/config/index.ts:86:7
-     if (!fs.existsSync(CONFIG_FILE)) {
+     if (fs.existsSync(CONFIG_FILE)) {

[NoCoverage] ConditionalExpression
src/shared/config/index.ts:86:7
-     if (!fs.existsSync(CONFIG_FILE)) {
+     if (true) {

[NoCoverage] ConditionalExpression
src/shared/config/index.ts:86:7
-     if (!fs.existsSync(CONFIG_FILE)) {
+     if (false) {

[NoCoverage] BlockStatement
src/shared/config/index.ts:86:36
-     if (!fs.existsSync(CONFIG_FILE)) {
-       if (!email) {
-         throw new ConfigurationError(
-           `Chronicle config not found at ${CONFIG_FILE} and no git user.email detected. ` +
-           `Run: git config --global user.email "you@example.com" and restart, ` +
-           `or create ${CONFIG_FILE} manually with a userId field.`,
-         );
-       }
-       const defaults: ChronicleConfig = {
-         userId: email,
-         deviceId: `${os.hostname()}-${randomHex(4)}`,
-         dbPath: path.join(CONFIG_DIR, 'chronicle.db'),
-       };
-       fs.mkdirSync(CONFIG_DIR, { recursive: true });
-       fs.writeFileSync(CONFIG_FILE, JSON.stringify(defaults, null, 2), 'utf-8');
-       // A freshly-derived identity on a machine that is meant to join an existing team is the failure
-       // mode described on `userId` above. Say so on stderr, which an MCP host shows in its logs,
-       // rather than letting the machine quietly own nothing.
-       process.stderr.write(
-         `chronicle: created ${CONFIG_FILE} with a NEW identity "${defaults.userId}" derived from your ` +
-         `git email.
-   ` +
-         `  If you already sync or belong to a team under a different id, set "userId" to that id now — ` +
-         `it keys every synced row and your team membership.
-   `,
-       );
-       return defaults;
-     }
+     if (!fs.existsSync(CONFIG_FILE)) {}

[NoCoverage] BooleanLiteral
src/shared/config/index.ts:87:9
-       if (!email) {
+       if (email) {

[NoCoverage] ConditionalExpression
src/shared/config/index.ts:87:9
-       if (!email) {
+       if (true) {

[NoCoverage] ConditionalExpression
src/shared/config/index.ts:87:9
-       if (!email) {
+       if (false) {

[NoCoverage] BlockStatement
src/shared/config/index.ts:87:17
-       if (!email) {
-         throw new ConfigurationError(
-           `Chronicle config not found at ${CONFIG_FILE} and no git user.email detected. ` +
-           `Run: git config --global user.email "you@example.com" and restart, ` +
-           `or create ${CONFIG_FILE} manually with a userId field.`,
-         );
-       }
+       if (!email) {}

[NoCoverage] StringLiteral
src/shared/config/index.ts:89:9
-           `Chronicle config not found at ${CONFIG_FILE} and no git user.email detected. ` +
+           `` +

[NoCoverage] StringLiteral
src/shared/config/index.ts:90:9
-           `Run: git config --global user.email "you@example.com" and restart, ` +
+           `` +

[NoCoverage] StringLiteral
src/shared/config/index.ts:91:9
-           `or create ${CONFIG_FILE} manually with a userId field.`,
+           ``,

[NoCoverage] ObjectLiteral
src/shared/config/index.ts:94:39
-       const defaults: ChronicleConfig = {
-         userId: email,
-         deviceId: `${os.hostname()}-${randomHex(4)}`,
-         dbPath: path.join(CONFIG_DIR, 'chronicle.db'),
-       };
+       const defaults: ChronicleConfig = {};

[NoCoverage] StringLiteral
src/shared/config/index.ts:96:17
-         deviceId: `${os.hostname()}-${randomHex(4)}`,
+         deviceId: ``,

[NoCoverage] StringLiteral
src/shared/config/index.ts:97:37
-         dbPath: path.join(CONFIG_DIR, 'chronicle.db'),
+         dbPath: path.join(CONFIG_DIR, ""),

[NoCoverage] ObjectLiteral
src/shared/config/index.ts:99:30
-       fs.mkdirSync(CONFIG_DIR, { recursive: true });
+       fs.mkdirSync(CONFIG_DIR, {});

[NoCoverage] BooleanLiteral
src/shared/config/index.ts:99:43
-       fs.mkdirSync(CONFIG_DIR, { recursive: true });
+       fs.mkdirSync(CONFIG_DIR, { recursive: false });

[NoCoverage] StringLiteral
src/shared/config/index.ts:100:70
-       fs.writeFileSync(CONFIG_FILE, JSON.stringify(defaults, null, 2), 'utf-8');
+       fs.writeFileSync(CONFIG_FILE, JSON.stringify(defaults, null, 2), "");

[NoCoverage] StringLiteral
src/shared/config/index.ts:105:7
-         `chronicle: created ${CONFIG_FILE} with a NEW identity "${defaults.userId}" derived from your ` +
+         `` +

[NoCoverage] StringLiteral
src/shared/config/index.ts:106:7
-         `git email.
-   ` +
+         `` +

[NoCoverage] StringLiteral
src/shared/config/index.ts:108:7
-         `  If you already sync or belong to a team under a different id, set "userId" to that id now — ` +
+         `` +

[NoCoverage] StringLiteral
src/shared/config/index.ts:109:7
-         `it keys every synced row and your team membership.
-   `,
+         ``,

[NoCoverage] StringLiteral
src/shared/config/index.ts:115:44
-     const raw = fs.readFileSync(CONFIG_FILE, 'utf-8');
+     const raw = fs.readFileSync(CONFIG_FILE, "");

[NoCoverage] BooleanLiteral
src/shared/config/index.ts:119:7
-     if (!config.userId) {
+     if (config.userId) {

[NoCoverage] ConditionalExpression
src/shared/config/index.ts:119:7
-     if (!config.userId) {
+     if (true) {

[NoCoverage] ConditionalExpression
src/shared/config/index.ts:119:7
-     if (!config.userId) {
+     if (false) {

[NoCoverage] BlockStatement
src/shared/config/index.ts:119:23
-     if (!config.userId) {
-       if (!email) {
-         throw new ConfigurationError(
-           `userId is empty in ${CONFIG_FILE} and no git user.email detected. ` +
-           `Set userId manually or run: git config --global user.email "you@example.com"`,
-         );
-       }
-       config.userId = email;
-       fs.writeFileSync(CONFIG_FILE, JSON.stringify(config, null, 2), 'utf-8');
-     }
+     if (!config.userId) {}

[NoCoverage] BooleanLiteral
src/shared/config/index.ts:120:9
-       if (!email) {
+       if (email) {

[NoCoverage] ConditionalExpression
src/shared/config/index.ts:120:9
-       if (!email) {
+       if (true) {

[NoCoverage] ConditionalExpression
src/shared/config/index.ts:120:9
-       if (!email) {
+       if (false) {

[NoCoverage] BlockStatement
src/shared/config/index.ts:120:17
-       if (!email) {
-         throw new ConfigurationError(
-           `userId is empty in ${CONFIG_FILE} and no git user.email detected. ` +
-           `Set userId manually or run: git config --global user.email "you@example.com"`,
-         );
-       }
+       if (!email) {}

[NoCoverage] StringLiteral
src/shared/config/index.ts:122:9
-           `userId is empty in ${CONFIG_FILE} and no git user.email detected. ` +
+           `` +

[NoCoverage] StringLiteral
src/shared/config/index.ts:123:9
-           `Set userId manually or run: git config --global user.email "you@example.com"`,
+           ``,

[NoCoverage] StringLiteral
src/shared/config/index.ts:127:68
-       fs.writeFileSync(CONFIG_FILE, JSON.stringify(config, null, 2), 'utf-8');
+       fs.writeFileSync(CONFIG_FILE, JSON.stringify(config, null, 2), "");

[NoCoverage] BlockStatement
src/shared/config/index.ts:140:46
-   export function getConfig(): ChronicleConfig {
-     if (!_cachedConfig) {
-       _cachedConfig = loadConfig();
-     }
-     return _cachedConfig;
-   }
+   export function getConfig(): ChronicleConfig {}

[NoCoverage] BooleanLiteral
src/shared/config/index.ts:141:7
-     if (!_cachedConfig) {
+     if (_cachedConfig) {

[NoCoverage] ConditionalExpression
src/shared/config/index.ts:141:7
-     if (!_cachedConfig) {
+     if (true) {

[NoCoverage] ConditionalExpression
src/shared/config/index.ts:141:7
-     if (!_cachedConfig) {
+     if (false) {

[NoCoverage] BlockStatement
src/shared/config/index.ts:141:23
-     if (!_cachedConfig) {
-       _cachedConfig = loadConfig();
-     }
+     if (!_cachedConfig) {}

[NoCoverage] BlockStatement
src/shared/config/index.ts:154:42
-   export function resetConfigCache(): void {
-     _cachedConfig = null;
-   }
+   export function resetConfigCache(): void {}

[NoCoverage] BlockStatement
src/shared/exceptions/index.ts:25:48
-     constructor(message: string, field?: string) {
-       super(message, 'VALIDATION_ERROR', { field });
-       this.name = 'ValidationError';
-     }
+     constructor(message: string, field?: string) {}

[NoCoverage] StringLiteral
src/shared/exceptions/index.ts:26:20
-       super(message, 'VALIDATION_ERROR', { field });
+       super(message, "", { field });

[NoCoverage] ObjectLiteral
src/shared/exceptions/index.ts:26:40
-       super(message, 'VALIDATION_ERROR', { field });
+       super(message, 'VALIDATION_ERROR', {});

[NoCoverage] StringLiteral
src/shared/exceptions/index.ts:27:17
-       this.name = 'ValidationError';
+       this.name = "";

[NoCoverage] StringLiteral
src/shared/exceptions/index.ts:47:16
-         detail ? `${message}: ${detail}` : message,
+         detail ? `` : message,

[NoCoverage] BlockStatement
src/shared/exceptions/index.ts:57:32
-     constructor(message: string) {
-       super(message, 'EMBEDDING_ERROR');
-       this.name = 'EmbeddingError';
-     }
+     constructor(message: string) {}

[NoCoverage] StringLiteral
src/shared/exceptions/index.ts:58:20
-       super(message, 'EMBEDDING_ERROR');
+       super(message, "");

[NoCoverage] StringLiteral
src/shared/exceptions/index.ts:59:17
-       this.name = 'EmbeddingError';
+       this.name = "";

[NoCoverage] BlockStatement
src/shared/exceptions/index.ts:64:32
-     constructor(message: string) {
-       super(message, 'CONFIGURATION_ERROR');
-       this.name = 'ConfigurationError';
-     }
+     constructor(message: string) {}

[NoCoverage] StringLiteral
src/shared/exceptions/index.ts:65:20
-       super(message, 'CONFIGURATION_ERROR');
+       super(message, "");

[NoCoverage] StringLiteral
src/shared/exceptions/index.ts:66:17
-       this.name = 'ConfigurationError';
+       this.name = "";

[NoCoverage] BlockStatement
src/shared/exceptions/index.ts:71:49
-     constructor(message: string, cause?: unknown) {
-       super(message, 'SYNC_ERROR', { cause: String(cause) });
-       this.name = 'SyncError';
-     }
+     constructor(message: string, cause?: unknown) {}

[NoCoverage] StringLiteral
src/shared/exceptions/index.ts:72:20
-       super(message, 'SYNC_ERROR', { cause: String(cause) });
+       super(message, "", { cause: String(cause) });

[NoCoverage] ObjectLiteral
src/shared/exceptions/index.ts:72:34
-       super(message, 'SYNC_ERROR', { cause: String(cause) });
+       super(message, 'SYNC_ERROR', {});

[NoCoverage] StringLiteral
src/shared/exceptions/index.ts:73:17
-       this.name = 'SyncError';
+       this.name = "";

[Survived] StringLiteral
src/shared/exceptions/index.ts:19:11
-       super(`${resource} not found: ${id}`, 'NOT_FOUND', { resource, id });
+       super(``, 'NOT_FOUND', { resource, id });
Tests ran:
    SessionService throws NotFoundError rather than failing silently for an unknown id
    SessionService throws NotFoundError for an empty id — the shape the MCP handler used to pass


[Survived] StringLiteral
src/shared/exceptions/index.ts:19:43
-       super(`${resource} not found: ${id}`, 'NOT_FOUND', { resource, id });
+       super(`${resource} not found: ${id}`, "", { resource, id });
Tests ran:
    SessionService throws NotFoundError rather than failing silently for an unknown id
    SessionService throws NotFoundError for an empty id — the shape the MCP handler used to pass


[Survived] StringLiteral
src/shared/exceptions/index.ts:13:17
-       this.name = 'ChronicleError';
+       this.name = "";
Tests ran:
    SessionService throws NotFoundError rather than failing silently for an unknown id
    SessionService throws NotFoundError for an empty id — the shape the MCP handler used to pass
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error
  and 2 more tests!


[Survived] ObjectLiteral
src/shared/exceptions/index.ts:19:56
-       super(`${resource} not found: ${id}`, 'NOT_FOUND', { resource, id });
+       super(`${resource} not found: ${id}`, 'NOT_FOUND', {});
Tests ran:
    SessionService throws NotFoundError rather than failing silently for an unknown id
    SessionService throws NotFoundError for an empty id — the shape the MCP handler used to pass


[Survived] StringLiteral
src/shared/exceptions/index.ts:20:17
-       this.name = 'NotFoundError';
+       this.name = "";
Tests ran:
    SessionService throws NotFoundError rather than failing silently for an unknown id
    SessionService throws NotFoundError for an empty id — the shape the MCP handler used to pass


[Survived] ConditionalExpression
src/shared/exceptions/index.ts:45:61
-       const detail = cause instanceof Error ? cause.message : cause == null ? '' : String(cause);
+       const detail = cause instanceof Error ? cause.message : true ? '' : String(cause);
Tests ran:
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error
    TeamService (offline / no railwayUrl) assignRole throws a clear configuration error
    TeamService (offline / no railwayUrl) mintToken throws a clear configuration error


[Survived] ConditionalExpression
src/shared/exceptions/index.ts:45:61
-       const detail = cause instanceof Error ? cause.message : cause == null ? '' : String(cause);
+       const detail = cause instanceof Error ? cause.message : false ? '' : String(cause);
Tests ran:
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error
    TeamService (offline / no railwayUrl) assignRole throws a clear configuration error
    TeamService (offline / no railwayUrl) mintToken throws a clear configuration error


[Survived] EqualityOperator
src/shared/exceptions/index.ts:45:61
-       const detail = cause instanceof Error ? cause.message : cause == null ? '' : String(cause);
+       const detail = cause instanceof Error ? cause.message : cause != null ? '' : String(cause);
Tests ran:
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error
    TeamService (offline / no railwayUrl) assignRole throws a clear configuration error
    TeamService (offline / no railwayUrl) mintToken throws a clear configuration error


[Survived] StringLiteral
src/shared/exceptions/index.ts:45:77
-       const detail = cause instanceof Error ? cause.message : cause == null ? '' : String(cause);
+       const detail = cause instanceof Error ? cause.message : cause == null ? "Stryker was here!" : String(cause);
Tests ran:
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error
    TeamService (offline / no railwayUrl) assignRole throws a clear configuration error
    TeamService (offline / no railwayUrl) mintToken throws a clear configuration error


[Survived] StringLiteral
src/shared/exceptions/index.ts:48:7
-         'STORAGE_ERROR',
+         "",
Tests ran:
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error
    TeamService (offline / no railwayUrl) assignRole throws a clear configuration error
    TeamService (offline / no railwayUrl) mintToken throws a clear configuration error


[Survived] ObjectLiteral
src/shared/exceptions/index.ts:49:7
-         { cause: String(cause) },
+         {},
Tests ran:
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error
    TeamService (offline / no railwayUrl) assignRole throws a clear configuration error
    TeamService (offline / no railwayUrl) mintToken throws a clear configuration error


[Survived] ObjectLiteral
src/shared/exceptions/index.ts:50:7
-         { cause },
+         {},
Tests ran:
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error
    TeamService (offline / no railwayUrl) assignRole throws a clear configuration error
    TeamService (offline / no railwayUrl) mintToken throws a clear configuration error


[Survived] StringLiteral
src/shared/exceptions/index.ts:52:17
-       this.name = 'StorageError';
+       this.name = "";
Tests ran:
    TeamService (offline / no railwayUrl) joinTeam throws a clear configuration error
    TeamService (offline / no railwayUrl) assignRole throws a clear configuration error
    TeamService (offline / no railwayUrl) mintToken throws a clear configuration error


[NoCoverage] StringLiteral
src/shared/repo-identity.ts:79:39
-     if (s.split('/').length < 2) return '';
+     if (s.split('/').length < 2) return "Stryker was here!";

[NoCoverage] StringLiteral
src/shared/repo-identity.ts:113:90
-     const rootCommit = git(['rev-list', '--max-parents=0', 'HEAD'], cwd).split('\n')[0] ?? '';
+     const rootCommit = git(['rev-list', '--max-parents=0', 'HEAD'], cwd).split('\n')[0] ?? "Stryker was here!";

[Survived] Regex
src/shared/repo-identity.ts:56:14
-       .replace(/^[^@/]+@([^:/]+):/, '$1/')
+       .replace(/[^@/]+@([^:/]+):/, '$1/')
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 12 more tests!


[Survived] MethodExpression
src/shared/repo-identity.ts:51:19
-     const trimmed = remote.trim();
+     const trimmed = remote;
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 16 more tests!


[Survived] ConditionalExpression
src/shared/repo-identity.ts:52:7
-     if (!trimmed) return '';
+     if (false) return '';
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 16 more tests!


[Survived] Regex
src/shared/repo-identity.ts:58:14
-       .replace(/^[a-z+]+:\/\//i, '')
+       .replace(/[a-z+]+:\/\//i, '')
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 12 more tests!


[Survived] Regex
src/shared/repo-identity.ts:60:14
-       .replace(/^[^@/]*@/, '')
+       .replace(/[^@/]*@/, '')
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 12 more tests!


[Survived] Regex
src/shared/repo-identity.ts:61:14
-       .replace(/\.git$/i, '')
+       .replace(/\.git/i, '')
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 12 more tests!


[Survived] Regex
src/shared/repo-identity.ts:62:14
-       .replace(/\/+$/, '')
+       .replace(/\/$/, '')
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 12 more tests!


[Survived] ConditionalExpression
src/shared/repo-identity.ts:70:5
-       !s.includes('/') ||
-       /^[a-z]:[\\/]/i.test(trimmed) ||
+       false ||
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 12 more tests!


[Survived] LogicalOperator
src/shared/repo-identity.ts:70:5
-       !s.includes('/') ||
-       /^[a-z]:[\\/]/i.test(trimmed) ||
+       !s.includes('/') && /^[a-z]:[\\/]/i.test(trimmed) ||
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 12 more tests!


[Survived] StringLiteral
src/shared/repo-identity.ts:70:17
-       !s.includes('/') ||
+       !s.includes("") ||
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 12 more tests!


[Survived] Regex
src/shared/repo-identity.ts:71:5
-       /^[a-z]:[\\/]/i.test(trimmed) ||
+       /^[^a-z]:[\\/]/i.test(trimmed) ||
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 12 more tests!


[Survived] Regex
src/shared/repo-identity.ts:71:5
-       /^[a-z]:[\\/]/i.test(trimmed) ||
+       /^[a-z]:[^\\/]/i.test(trimmed) ||
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 12 more tests!


[Survived] MethodExpression
src/shared/repo-identity.ts:74:5
-       s.startsWith('/')
+       s.endsWith('/')
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 10 more tests!


[Survived] ConditionalExpression
src/shared/repo-identity.ts:79:7
-     if (s.split('/').length < 2) return '';
+     if (false) return '';
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 10 more tests!


[Survived] EqualityOperator
src/shared/repo-identity.ts:79:7
-     if (s.split('/').length < 2) return '';
+     if (s.split('/').length <= 2) return '';
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 10 more tests!


[Survived] StringLiteral
src/shared/repo-identity.ts:79:15
-     if (s.split('/').length < 2) return '';
+     if (s.split("").length < 2) return '';
Tests ran:
    normalizeRemote normalises the https form, dropping the .git suffix
    normalizeRemote normalises the scp-like ssh form to the same id as https
    normalizeRemote normalises the ssh:// form
  and 10 more tests!


[Survived] MethodExpression
src/shared/repo-identity.ts:86:12
-       return execFileSync('git', args as string[], {
-         cwd,
-         encoding: 'utf8',
-         stdio: ['ignore', 'pipe', 'ignore'],
-         timeout: 5_000,
-       }).trim();
+       return execFileSync('git', args as string[], {
+     cwd,
+     encoding: 'utf8',
+     stdio: ['ignore', 'pipe', 'ignore'],
+     timeout: 5_000
+   });
Tests ran:
    resolveProjectIdentity prefers the normalised remote
    resolveProjectIdentity falls back to the root commit when there is no remote
    resolveProjectIdentity falls back to the directory name outside a git repository
  and 7 more tests!


[Survived] ArrayDeclaration
src/shared/repo-identity.ts:89:14
-         stdio: ['ignore', 'pipe', 'ignore'],
+         stdio: [],
Tests ran:
    resolveProjectIdentity prefers the normalised remote
    resolveProjectIdentity falls back to the root commit when there is no remote
    resolveProjectIdentity falls back to the directory name outside a git repository
  and 7 more tests!


[Survived] StringLiteral
src/shared/repo-identity.ts:89:33
-         stdio: ['ignore', 'pipe', 'ignore'],
+         stdio: ['ignore', 'pipe', ""],
Tests ran:
    resolveProjectIdentity prefers the normalised remote
    resolveProjectIdentity falls back to the root commit when there is no remote
    resolveProjectIdentity falls back to the directory name outside a git repository
  and 7 more tests!


[Survived] BlockStatement
src/shared/repo-identity.ts:135:51
-   export function resetProjectIdentityCache(): void {
-     cached = null;
-   }
+   export function resetProjectIdentityCache(): void {}
Tests ran:
    resolveProjectIdentity prefers the normalised remote
    resolveProjectIdentity falls back to the root commit when there is no remote
    resolveProjectIdentity falls back to the directory name outside a git repository
  and 9 more tests!


[Survived] LogicalOperator
src/shared/time.ts:32:7
-     if (typeof value === 'number' && Number.isFinite(value)) {
+     if (typeof value === 'number' || Number.isFinite(value)) {
Tests ran:
    SqliteTeamRepository.searchSharedCache finds a memory by words that are not a contiguous phrase
    SqliteTeamRepository.searchSharedCache still finds a memory by an exact phrase
    SqliteTeamRepository.searchSharedCache matches on a single word
  and 16 more tests!


[Survived] ConditionalExpression
src/shared/time.ts:32:7
-     if (typeof value === 'number' && Number.isFinite(value)) {
+     if (true && Number.isFinite(value)) {
Tests ran:
    SqliteTeamRepository.searchSharedCache finds a memory by words that are not a contiguous phrase
    SqliteTeamRepository.searchSharedCache still finds a memory by an exact phrase
    SqliteTeamRepository.searchSharedCache matches on a single word
  and 16 more tests!


[Survived] ConditionalExpression
src/shared/time.ts:35:36
-     if (typeof value === 'string' && value.length > 0) {
+     if (typeof value === 'string' && true) {
Tests ran:
    SqliteTeamRepository.searchSharedCache finds a memory by words that are not a contiguous phrase
    SqliteTeamRepository.searchSharedCache still finds a memory by an exact phrase
    SqliteTeamRepository.searchSharedCache matches on a single word
  and 14 more tests!


[Survived] EqualityOperator
src/shared/time.ts:35:36
-     if (typeof value === 'string' && value.length > 0) {
+     if (typeof value === 'string' && value.length >= 0) {
Tests ran:
    SqliteTeamRepository.searchSharedCache finds a memory by words that are not a contiguous phrase
    SqliteTeamRepository.searchSharedCache still finds a memory by an exact phrase
    SqliteTeamRepository.searchSharedCache matches on a single word
  and 14 more tests!


[Survived] MethodExpression
src/shared/time.ts:55:36
-     if (typeof value === 'string' && value.trim().startsWith('[')) return value;
+     if (typeof value === 'string' && value.startsWith('[')) return value;
Tests ran:
    toTagsJson passes an already-serialised JSON array through unchanged
    toTagsJson returns an empty array for null, undefined and a non-array value
    toTagsJson always returns something SQLite can bind


Ran 1.25 tests per mutant on average.
-----------------------------------|------------------|----------|-----------|------------|----------|----------|
                                   | % Mutation score |          |           |            |          |          |
File                               |  total | covered | # killed | # timeout | # survived | # no cov | # errors |
-----------------------------------|--------|---------|----------|-----------|------------|----------|----------|
All files                          |  27.42 |   76.72 |      733 |         5 |        224 |     1729 |        0 |
 adapters                          |  41.13 |   72.51 |      153 |         0 |         58 |      161 |        0 |
  repositories                     |  41.13 |   72.51 |      153 |         0 |         58 |      161 |        0 |
   sqlite-memory-repository.ts     |  43.51 |   69.07 |       67 |         0 |         30 |       57 |        0 |
   sqlite-preference-repository.ts |   0.00 |    0.00 |        0 |         0 |          0 |       51 |        0 |
   sqlite-session-repository.ts    |  27.27 |   57.14 |       12 |         0 |          9 |       23 |        0 |
   sqlite-team-repository.ts       |  60.16 |   79.57 |       74 |         0 |         19 |       30 |        0 |
 domain                            |  62.79 |   91.53 |       54 |         0 |          5 |       27 |        0 |
  entities                         |  62.79 |   91.53 |       54 |         0 |          5 |       27 |        0 |
   decision.ts                     |   0.00 |    0.00 |        0 |         0 |          0 |        2 |        0 |
   memory.ts                       | 100.00 |  100.00 |       30 |         0 |          0 |        0 |        0 |
   preference.ts                   |   0.00 |    0.00 |        0 |         0 |          0 |        8 |        0 |
   prompt-log.ts                   |  54.55 |   54.55 |        6 |         0 |          5 |        0 |        0 |
   session.ts                      | 100.00 |  100.00 |       17 |         0 |          0 |        0 |        0 |
   solution.ts                     |   0.00 |    0.00 |        0 |         0 |          0 |        6 |        0 |
   team.ts                         |  16.67 |  100.00 |        1 |         0 |          0 |        5 |        0 |
   trigger.ts                      |   0.00 |    0.00 |        0 |         0 |          0 |        6 |        0 |
  types.ts                         |    n/a |     n/a |        0 |         0 |          0 |        0 |        0 |
 infrastructure                    |  46.15 |   94.12 |       47 |         1 |          3 |       53 |        0 |
  db                               |  62.50 |  100.00 |       20 |         0 |          0 |       12 |        0 |
   database.ts                     |  62.50 |  100.00 |       20 |         0 |          0 |       12 |        0 |
  gateways                         |  38.89 |   90.32 |       27 |         1 |          3 |       41 |        0 |
   fastembed-gateway.ts            |  44.83 |   92.86 |       25 |         1 |          2 |       30 |        0 |
   node-clock.ts                   |   0.00 |    0.00 |        0 |         0 |          1 |        7 |        0 |
   node-id-generator.ts            |  33.33 |  100.00 |        2 |         0 |          0 |        4 |        0 |
 mcp                               |   0.00 |    0.00 |        0 |         0 |          0 |      841 |        0 |
  server.ts                        |   0.00 |    0.00 |        0 |         0 |          0 |      609 |        0 |
  team-gate.ts                     |   0.00 |    0.00 |        0 |         0 |          0 |       22 |        0 |
  team-tools.ts                    |   0.00 |    0.00 |        0 |         0 |          0 |      210 |        0 |
 services                          |  34.59 |   75.36 |      363 |         4 |        120 |      574 |        0 |
  coordination-service.ts          |  50.61 |   76.26 |      163 |         4 |         52 |      111 |        0 |
  distill.ts                       |   0.00 |    0.00 |        0 |         0 |          0 |       46 |        0 |
  memory-service.ts                |  51.85 |   60.87 |       14 |         0 |          9 |        4 |        0 |
  pattern-service.ts               |  69.39 |   79.07 |       34 |         0 |          9 |        6 |        0 |
  preference-service.ts            |   0.00 |    0.00 |        0 |         0 |          0 |        8 |        0 |
  prompt-log-service.ts            |  39.29 |   91.67 |       11 |         0 |          1 |       16 |        0 |
  session-service.ts               |  90.00 |   90.00 |        9 |         0 |          1 |        0 |        0 |
  sync.ts                          |  10.71 |   60.00 |       27 |         0 |         18 |      207 |        0 |
  team-promotion-service.ts        |  74.56 |   80.19 |       85 |         0 |         21 |        8 |        0 |
  team-service.ts                  |  21.51 |   68.97 |       20 |         0 |          9 |       64 |        0 |
  team-sync-service.ts             |   0.00 |    0.00 |        0 |         0 |          0 |       83 |        0 |
  trigger-service.ts               |   0.00 |    0.00 |        0 |         0 |          0 |       21 |        0 |
 shared                            |  51.10 |   75.32 |      116 |         0 |         38 |       73 |        0 |
  config                           |   0.00 |    0.00 |        0 |         0 |          0 |       56 |        0 |
   index.ts                        |   0.00 |    0.00 |        0 |         0 |          0 |       56 |        0 |
  exceptions                       |   6.67 |   13.33 |        2 |         0 |         13 |       15 |        0 |
   index.ts                        |   6.67 |   13.33 |        2 |         0 |         13 |       15 |        0 |
  repo-identity.ts                 |  79.44 |   80.95 |       85 |         0 |         20 |        2 |        0 |
  time.ts                          |  85.29 |   85.29 |       29 |         0 |          5 |        0 |        0 |
-----------------------------------|--------|---------|----------|-----------|------------|----------|----------|
[32m16:50:31 (22604) INFO MutationTestReportHelper[39m Final mutation score of 27.42 is greater than or equal to break threshold 27
[32m16:50:31 (22604) INFO JsonReporter[39m Your report can be found at: file:///C:/workspace/PragmaWorks/mcp/chronicle/reports/mutation/mutation.json
[32m16:50:31 (22604) INFO HtmlReporter[39m Your report can be found at: file:///C:/workspace/PragmaWorks/mcp/chronicle/reports/mutation/index.html
[32m16:50:31 (22604) INFO MutationTestExecutor[39m Done in 1 minute 42 seconds.

