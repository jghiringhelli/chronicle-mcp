# chronicle
<!-- ForgeCraft sentinel | CNT root (ADR-000): stays ≤3 lines. The five GS categories live in the nodes this routes to and are gate-enforced by scripts/gs-cascade-check.mjs. -->
MCP server giving AI assistants persistent cross-project memory. Library + CLI. Must not become a monolith.

Read `.claude/index.md` before any task, then `.claude/core.md` and `.claude/ledger.md` — always, every task. Navigate to one branch only.
