# Specs — chronicle

Product specs for chronicle (individual AI memory MCP server). One file per major feature or capability.

`feat:` and behavior-affecting `refactor:` commits should touch a spec here.

Legacy single-file specs at `docs/PRD.md`, `docs/spec.md`, `docs/chronicle-spec.md` — see `docs/manifest.yaml`. New specs go here.
